# Parked Bugs

Bugs that keep a `BUG-XXX` identity but aren't active work. Each row
carries the reproduction / trigger that re-opens it. See
[README.md](README.md) for the lifecycle.

| # | Bug | Last seen | Re-open trigger |
|---|---|---|---|

*(Empty — no bugs are parked.)*

The table above previously carried a blank `| | | | |` row as a stub. It renders
as an empty row, so the file claimed one parked bug that did not exist. A header
with nothing under it already says "none"; a placeholder row says something
false.
