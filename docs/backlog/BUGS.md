# Parked Bugs

Bugs that keep a `BUG-XXX` identity but aren't active work. Each row
carries the reproduction / trigger that re-opens it. See
[README.md](README.md) for the lifecycle.

| # | Bug | Last seen | Re-open trigger |
|---|---|---|---|
| **BUG-032** | **A push runs the full gate, reports nothing wrong, and pushes nothing — `git push` dies of SIGPIPE after the gate succeeds.** Seven occurrences on 2026-08-19, each costing a ~361 s gate. **Signature, measured:** `git push` exits **141** (`128 + 13` = SIGPIPE), the gate prints `PASSED`, there is no `To github.com:` line, and `git ls-remote` shows the ref absent. **A re-run is NOT a workaround** — it appeared to be one for the first five, then failed twice consecutively on an unchanged branch, which is what made the mechanism testable. **Mechanism — CONFIRMED by experiment:** git contacts the remote for its ref state *before* running `pre-push` — that is how it fills the `<remote sha>` field the hook reads on stdin — then the gate holds the connection idle for ~361 s, then git writes to a socket the server has since closed. **The proof:** after two consecutive failures on an unchanged branch, the identical push with `GIT_SSH_COMMAND="ssh -o ServerAliveInterval=20"` landed first try, exit 0. Nothing else differed. The intermittency is the gate sitting on a ~5-minute idle boundary. **This is TASK-013's re-open trigger firing** — *"the gate crosses an interval that changes behaviour"* — just not the behaviour predicted (nobody started skipping the hook; the push started dying). The two items should be read together. **Half of the original description was measurement error and is corrected here:** the handover recorded this as *"a push can report success"*. It never reported success — the usual invocation is `git push … 2>&1 | tail -N`, and **a pipeline's exit status is the last command's**, so the `0` came from `tail` and was never evidence about git. The vanished push is real; `git ls-remote` is the only authority. **Not the fix:** `--no-verify` (denied by policy, and it defeats the gate), or retrying blindly (it works, but pays a full gate per attempt). **Candidate fixes, in order of preference:** get the gate under the idle boundary (TASK-013 — `bootstrap-gate` alone is 50.6%); set an ssh `ServerAliveInterval` so the connection is not idle; or run the gate out-of-band and leave a short hook on the push path. **Reproducer needed before any fix:** a hook that sleeps past the boundary, asserting the push still lands — without it there is no way to tell a fix from a lucky run. | S2 | KEEP | Raised 2026-08-19 after five occurrences in one session. Re-open immediately if a push is ever believed to have landed without `git ls-remote` confirming it — that is the failure this bug makes possible. |
| **BUG-030** | Running `tests/bootstrap-gate` **resets the live coordination baton** — `logs/state/signal.md` went to `Holder=Nobody / State=IDLE / "Bootstrapped from the blueprint"` while Andreas was mid-review of BUG-029 R3, 37 seconds after he claimed the mic. Recovered from `signal-history.log`; his verdict was never lost, but it could have been. **A test wrote live state** — the A-09 / BUG-014 fixture-isolation class, and this time the state it clobbered is the one the whole team coordinates through. Two things make it worse than a stray file: a poller that samples the baton between the reset and the reviewer's hand-back sees `IDLE` and can dispatch against finished work, and the reset text is the *bootstrap default*, which reads as a legitimate fresh checkout rather than as damage. **Suspect:** `bootstrap-gate` bootstraps a project and runs its gate, so either the derived project's state dir resolved back to the blueprint's own `logs/state/` (which is exactly what `scripts/lib/state-dir.sh` exists to prevent) or `new-project.sh` wrote the baton at the blueprint root. Note `tests/git-isolation` and `tests/state-dir` both exist and did not catch this. | 2026-08-19 | Found during BUG-029 R3 cross-provider review. Fix alongside or after BUG-029 — do NOT fold into it. |
| **BUG-026** | **`signal-set.sh` publishes any `State` string, so the protocol is documented and unenforced.** `AGENT_SIGNAL.md` §"The protocol" says *"`State` is `IDLE` / `ACTIVE` / `OVER_TO_<NAME>`"*; `signal-set.sh:83` checks only that it is non-empty. `--state DONE`, `--state over_to_codex` or `--state "OVER_TO CODEX"` all publish cleanly, and the dispatch watcher matches `OVER_TO_CODEX` exactly — so a typo does not fail, **it just never dispatches**. Found while reviewing Andreas's FEATURE-005 change, which whitelisted those three forms in the mic READER. That was overruled: a reader failing closed on a well-formed baton goes silently blind, which is the exact failure the feature exists to remove. Validity belongs on the WRITER, where it fails loudly at the point of the mistake instead of becoming silence three layers away. Fix: validate in `signal-set.sh` and refuse, naming the accepted forms. | 2026-08-17 | Fix when `signal-set.sh` is next touched. Until then a mistyped state publishes happily and the dispatch simply never happens — the same shape BUG-022 had to fix once already. |
| **BUG-025** | **The dispatch boilerplate hardcodes `Claude Code` as the handback `Holder`, which is not a persona.** `start-codex-signal-watch.sh:145` ends every dispatch with *"hand the mic back … with `--holder` set to `Claude Code`"*. `Holder` is documented as a **persona name from the roster** (`CLAUDE.md` §"Agent Coordination"), and no roster row is named `Claude Code` — the Orchestrator is a name the engineer chooses. So the standing instruction tells every dispatched agent to write an invalid Holder, and per-dispatch briefs that name the real persona **contradict it inside the same prompt**. Alexey hit exactly that on 2026-08-17, spotted it, and resolved it correctly by reading the roster — the next agent may not. **Alexis's ruling: a BUG, not a doc fix** — the faulty instruction is generated by executable machinery, and prose elsewhere cannot neutralise boilerplate delivered inside every run. **Same class as BUG-010**: role is the stable key, persona name is roster-owned data. Fix: resolve the `Orchestrator` role through `scripts/lib/roster.sh` — which BUG-010 built for this — and never emit a literal, neither `Claude Code` nor a project-specific name. | 2026-08-17 | Fix when the dispatcher is next touched. Until then every dispatch carries a self-contradicting instruction, survived so far only by the reviewer being careful. |
| **BUG-024** | **`codex-last-message.md` is not guaranteed written per run, and the wrapper asserts it is.** `start-codex-signal-watch.sh:144` passes `--output-last-message` to `codex exec`, and line 153 then prints *"see …/codex-last-message.md for the last message"* unconditionally. When a run does not write it, **the previous run's content stays in place** and that line vouches for it. Observed 2026-08-17: Alexey's dispatch finished at 17:40:46 and the file still held **2026-08-06**'s message — ten days old, a different item, a plausible format — and I read it as his verdict. The next dispatch (Alexis) *did* write it, so the file is overwritten on some runs and not others; **the cause is unresolved and the fix must not assume it.** The defect is that a stale file and a fresh one are indistinguishable to the reader, which is this repo's recurring shape (BUG-004 a hook that never ran, BUG-018 a refusal returning 0). Fix: stamp or truncate BEFORE the run, so a missing write leaves an empty or marked file rather than someone else's answer, and only vouch for it in the finished line if it changed. | 2026-08-17 | Fix when the dispatcher is next touched, or immediately — it is small and it misleads silently. Workaround, already in use: read the tail of `codex-runs.log` instead. |

**A promoted row leaves nothing behind.** The whole row moves; no stub, no
forwarding note. A row left behind after a promotion is the duplicate record
TASK-005 removed once already, and a forwarding note is the same thing one size
smaller — it goes stale the moment the item moves again.
