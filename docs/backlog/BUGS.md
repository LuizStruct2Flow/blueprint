# Parked Bugs

Bugs that keep a `BUG-XXX` identity but aren't active work. Each row
carries the reproduction / trigger that re-opens it. See
[README.md](README.md) for the lifecycle.

| # | Bug | Last seen | Re-open trigger |
|---|---|---|---|
| **BUG-073** | **TASK-018 phase 1's "no shipping suite is TypeScript" invariant now forces a wrong-shaped answer twice in one day, and `tests/manifest` #2b is RED on `main` because the third answer — reporting it — was chosen deliberately.** #2b refuses a TypeScript-only suite that ships, offering two exits: keep a shell runner, or `export-ignore` the suite. `tests/bug-numbers` (BUG-071, duplicate bug numbers across the four lifecycle `BUGS.md`) fits neither. **Derived projects have their own `BUGS.md` files and their own bug numbers, so they have this defect identically** — export-ignoring it would buy a quiet gate by removing the check from every project that needs it, which is a coverage decision made to fit a constraint rather than to match risk, the thing CLAUDE.md §"Pre-push tolerance" is entirely about. And a shell runner alongside means two implementations of one check, which is the copy-that-drifts this repo has now deleted twice (`INDEX.md`, `tests/SUITES.md`); `tests/proc-cwd` is living proof, carrying a full shell duplicate of its own spec. **The invariant's own logic names the resolution, and it is not a third exit:** #2b enforces *the TS toolchain ships IFF a shipping suite is TypeScript*, and this suite is a shipping suite that is TypeScript — so the consistent state is that the toolchain ships, i.e. **phase 2**, whose three steps `.gitattributes` already spells out (delete the export-ignore block, teach `tests/bootstrap-gate` to `npm ci` before the nested gate, land BUG-045). **This is the second instance today**, not the first: Elias hit the same wall with `tests/blueprint-relocation/` and took the `export-ignore` exit, flagging it in `.gitattributes` as a phase-1 consequence to revisit. Two suites in one day is evidence about the rule rather than about the suites. **The red gate is the finding, and it is deliberate** — nothing was pushed, and CLAUDE.md's own argument (BUG-031, BUG-045) is that a gate people route around protects nothing, so routing around this one to keep it green would have destroyed exactly what it is for. | S2 | KEEP | Found 2026-09-10 by Vitali (QA-1) landing BUG-071's suite, on instruction from the Orchestrator to report the tension rather than resolve it locally. **Re-open trigger: whichever comes first** — the next suite that wants to ship and be TypeScript (three would settle it), or the phase-2 decision, which this blocks on. Until then `tests/manifest` #2b names `bug-numbers` on every run, which is the reminder. |
| **BUG-067** | **`agent-activity.sh --daemon` can print `started (daemon)` and return 0 having started nothing.** After `setsid`, `cmd_daemon` polls `feed_is_running` to decide whether the launch worked — but that predicate tests whether the lock at `logs/.agent-activity.lock` is HELD, and **cannot distinguish its own new child from a predecessor that is still holding it**. So a launch that failed while an older supervisor was shutting down reads as a success, and the caller is told a feed is running when none is. **Why it matters beyond the lie:** `--daemon` is called on every wake by the Orchestrator (CLAUDE.md §"On wake"), and `arm_gate` rides along with it — so a silent no-op here also means the push gate may go unarmed, which is A-22 exactly. **Found 2026-09-10 by Philipp (Infrastructure-1) while fixing BUG-065**, and it is why he **deliberately did not widen** `wait_sup`'s 40-iteration and `reader_ready`'s 20-iteration bounds: those are counts rather than measurements and are genuinely TASK-018-RULES R4's class, but widening a bound to accommodate a launcher that lies about having launched papers over the defect underneath. **Fix the identity question first, then revisit the bounds.** Direction: the child must prove it is the holder — a token the supervisor writes and the launcher reads back, or a pid comparison against the lock holder — rather than the launcher inferring its own success from a predicate about anyone. Same family as **BUG-039**. **This is the repo's signature shape a fourth time** — BUG-004 (a hook that never ran), A-22 (a gate never armed), BUG-035 (a scanner reading a language the repo does not contain), BUG-066 (a gate passing over zero suites): *a thing that reports success having done nothing*. | S2 | KEEP | Raised 2026-09-10 by Philipp (Infrastructure-1), rowed by Eto (Orchestrator) so it is not lost inside BUG-065's row. **Re-open trigger: whichever comes first** — the next investigation that wants to widen `wait_sup` or `reader_ready` (it is blocked on this), or any report of a feed that was believed running and was not. Evidence is in `.scratch/instr{,2,3}/`, left in place deliberately. |
| **BUG-066** | **The pre-push gate can render `PASSED` having executed zero suites, and `tests/manifest` — the control built to prevent exactly this — agrees with it.** Three ingredients compose. (1) All 37 shell-suite invocations in `.githooks/pre-push-project` are guarded **cwd-relatively**: `if [ -f tests/<suite>/test.sh ]` … `else pipe_skip "<suite>" "suite absent"`. (2) `pipe_skip` (`scripts/lib/pipeline.sh:259`) records a skip and **does not fail** — by design, and correct downstream, where a project that has not yet pulled a new suite legitimately lacks it. (3) `tests/manifest` #4 proves invocation by grepping the hook's **text** for `bash tests/<suite>/…` (`tests/manifest/test.sh:729`), which cannot distinguish *invoked* from *named inside a guard that is false*. **Measured three times, ending end-to-end.** Christian (Senior Architect) and Alexey (Architect, Codex) each reached `rc=0` / `PASSED` / zero shell suites in separate probe trees. Matthias (Back-End-1) then ran it to completion: a `--no-hardlinks` clone, a local bare remote, `core.hooksPath=.githooks`, and a **real `git push` that was accepted** — `PASSED · 4 stages · 47 skipped · 8.1s`, against a `43 passed · 8 skipped · 180.0s` baseline in the same clone. The four survivors are gitleaks, semgrep, osv-scanner and the settings guard, all cwd-independent because they scan the whole tree or a fixed dotfile. **The blast radius is wider than the 37 shell suites**: the DoD checklist (`scripts/lib/dod-gate.sh`) and the **entire vitest batch — 8 TS suites** — sit behind the same guard shape and skip with it. **And the false green is not the first thing that happens.** The naive move fails LOUDLY twice first: `.githooks/commit-msg` refuses the commit (cannot read `scripts/lib/commit-subject.sh`) and `.githooks/pre-push:61` refuses the push (`scripts/lib/pipeline.sh is missing — the gate renderer is absent`). Both fail closed, correctly. The silent pass appears only after those two lines are repointed — which is a two-line `sed` and the first thing any restructurer does. **That is the trap: the loud failures teach you the move is fixed, and then the gate agrees with you.** **The restructure is the trigger, not the defect, and the fix does not wait for it.** The gate has two failure modes split by which file is missing, and the split is arbitrary: a missing `scripts/lib/pipeline.sh` exits 1 loudly (FEATURE-002 fail-closed), a missing `tests/<suite>/test.sh` skips silently and passes. The `if [ -f ]` guard exists so a derived project that has not pulled a suite does not block — correct downstream. **In the blueprint it is unconditionally wrong**: every one of those suites is always present, so the guard can only ever fire on a defect. Any cause fires it — a moved directory, a deleted suite, a mistyped `git mv`. Any cause of absence does this — a moved directory, a deleted suite, a bad `git mv` — and the gate reports success. **This is the repo's signature failure inside the control written to close it**: CLAUDE.md §"Pre-push tolerance" says *"a rule only a careful reviewer can check is not a control"*, and #4 is a text grep, which is a careful reviewer. Same class as BUG-004 (a hook that never ran), A-22 (a gate never armed), BUG-035 (a scanner examining a language the repo does not contain). **Fix direction, not settled:** derive the suite set from the filesystem and make a declared-but-absent suite **fail** in the blueprint while still skipping downstream, and give #4 a **runtime** assertion — a `pipe_stage` count compared against the declared count — instead of a grep over the hook's text. Alexey's review adds a constraint Christian's proposal does not meet: failing loudly tells you the suites are gone, it does not give the hook a code-root from which to run them. Both halves are needed. **Two corrections to the mechanism as first stated, both from Matthias's run.** (a) `tests/manifest` is not vacuity-blind: check #7 asserts the derived suite count is non-zero, and it is a real guard. It simply is not hit here — #7 derives from `$ROOT/tests`, and `$ROOT` follows the physical-script walk into `scaffolding/`, where all 43 suites still are. (b) Manifest *does* exit 1 in a moved tree, but for the wrong reason and with a useless diagnostic: `$ROOT` points `GATE`/`HOOK` at `scaffolding/.githooks/pre-push*`, which does not exist, so `GATE_CMDS` is empty and **every** suite reads as `shell runner never invoked` — including the ones the hook genuinely still invokes. It fails closed on an unreadable hook instead of naming the path mismatch. Moot in practice: its own invocation is one of the 37 guarded lines, so the gate never calls it. | S1 | KEEP | Found 2026-09-10 by Christian (Senior Architect) while sweeping Codex finding 2, confirmed by execution by Alexey (Architect) in an independent probe tree. **Re-open trigger: TASK-021 Stage A** — this must land BEFORE `scaffolding/` exists, because after the move the gate that would prove the move correct is the thing that stops running. Also re-open immediately if any gate run shows a non-zero `skipped` count for a suite that is present in `git ls-files`. |
| **BUG-065** | **Diagnosed 2026-09-10 by Philipp (Infrastructure-1). Two independent defects wear one signature, and the row's own framing — "two flaky suites" — is the part that was wrong.** (A) **`agent-activity-bound` restarts the feed four times and synchronises only one of them.** `wait_sup` proves a supervisor is RESIDENT (it holds the repo-wide `logs/.agent-activity.lock`); it does not prove the supervisor has run `seed_offset` and registered the run log, and those are not the same instant — the gap BUG-038 already documented and closed for #10/#18/#5f/#7 with the `reader_ready` handshake. #12, #9 and #2 were left on the far side of it. **Reproduced on the FIRST run under 96-way synthetic load** (32-core box, load avg ~110): `#12 MAX_FRAGMENT force-flush never fired` — Christian's observation verbatim — plus `#2 process count grew with transcripts (sup 0→0)`. Repeat-run harness, 8 runs at load ~110: **7 failed, with a DIFFERENT failure set each time** (#4, #9 ×5, #7, #13, #14, #18). **This is NOT R4's known weakness, and proving that is the most useful thing here:** with a 5 s delay injected in front of `seed_offset`, #12's payload was appended before the seed, folded into the baseline, and **never emitted in 184 s** — so raising the 8 s bound is not merely insufficient, it is irrelevant. No bound recovers a byte that was seeded away. The three unsynchronised restarts are FIXED (`reader_ready` on #12 and #9, `wait_sup 0` on #7's teardown, and `wait_sup 1 \|\| fail` on #2 so "the feed never started" stops being reported as an RC-2 process leak). (B) **`tests/harness`'s canary asserts the REAL `logs/state/signal.md` is byte-unchanged across every scenario, in a repo whose premise is concurrent agents.** Elias (Infrastructure-2) correlated three consecutive `bootstrap-gate` failures with Alexey's mic flips in `signal-history.log` (18:15:07 / 18:18:29 / 18:19:50 / 18:21:11 Z). The throw is in TEARDOWN — every gate assertion passed. **Do not delete the canary**: it exists because `bootstrap-gate` DID clobber the live baton (BUG-030). Proposed discriminator, cheap and sound: the guarded defect writes `signal.md` directly and therefore appends NOTHING to `signal-history.log`, while a real agent's flip always goes through `signal-set.sh` and always appends one line. So snapshot the bytes AND the history line count, and on a byte change with a matching history append, REPORT rather than fail. Residual, stated: a test that itself called `signal-set.sh` against the real baton would be waved through — small, because `AGENT_SIGNAL_FILE` is on the harness's forbidden list (R3). **Which observation is which.** Observation 2 is (A) and cannot be (B): its capture shows the DERIVED gate's own summary, `6 passed · 1 failed · 8 skipped · 40.2 s` — a nested stage genuinely failed, and a canary throw in the outer vitest teardown cannot produce that string. Christian's #12 observation is (A), reproduced. Observation 1 is UNRESOLVED: no clock time was recorded, so it cannot be correlated against `signal-history.log` either way. **The varying-failure-set argument does not distinguish the two halves** — the repeat harness runs `bash tests/agent-activity-bound/test.sh` directly, with no vitest, no scenario and no canary anywhere near it, and load alone produced a different failure set on every run. **Derived projects are exposed to (A) and not to (B):** `tests/agent-activity-bound/` is not `export-ignore`d and its `pipe_stage` sits in the blueprint-managed region of `.githooks/pre-push-project`, so it blocks every derived project's push on hardware nobody here has measured; `tests/bootstrap-gate/` and `tests/harness/` are both `export-ignore`d and stay behind. **What remains open, deliberately unfixed.** `wait_sup`'s bound is 40 iterations of `sleep 0.1` plus a full `ps -eo pid,args` scan per iteration — a count, not a deadline, and a guess rather than a measurement (0.02 s idle, 7.29 s at load ~110). It is the one thing here that IS R4's named weakness, and `reader_ready`'s 20-iteration bound is the same shape (it still timed out once at load ~98 after the fix). Both are left alone on purpose: the `could not start a supervisor` failures at load ~110 may be slow starts OR the `cmd_daemon` defect below, and widening a bound before that is settled papers over the second. **The measured boundary after the fix**, so the exposure is a number rather than an adjective: 6 of 6 green at load 27-58 on 32 cores (~1.8x oversubscription, which is past anything the gate plausibly produces), and 2 of 8 green at load ~105, where every remaining failure is `could not start a supervisor` and none is #12, #9 or #2. One of those runs is the fix reporting itself correctly: `#2 the feed never started` fired BEFORE the old `sup 0→0` line, which is the misdirection closing. **Product defect found in passing, not rowed:** `cmd_daemon` polls `feed_is_running` after `setsid`, which cannot distinguish its own child taking the lock from a predecessor still holding it — so it prints `started (daemon)` and returns 0 having started nothing. That is BUG-039's family and wants its own row. | S2 | KEEP | Raised 2026-09-10 by the founder (observation 1) and Christian (Senior Architect) (observation 2); rowed by Vitali (QA-1) without diagnosis, by instruction. **Diagnosed and half-fixed 2026-09-10 by Philipp (Infrastructure-1); half (B) diagnosed by Elias (Infrastructure-2).** Half (A) is fixed in `tests/agent-activity-bound/test.sh` and mutant-verified by an injected `seed_offset` delay. Half (B) is UNFIXED — the canary still fails any scenario a concurrent agent's mic flip overlaps. **Re-open trigger, narrowed: (i)** the canary decision, which is the remaining work and needs a founder call on which discriminator to take; **(ii)** any `agent-activity-bound` failure AFTER this fix, which would mean the handshake is not the whole of (A) — capture the FULL suite output, never a truncated tail, because observation 2 lost its FAIL line to truncation and cost a day of inference; **(iii)** any `could not start a supervisor` failure on a derived project, which promotes the `wait_sup` bound and the `cmd_daemon` defect from noted to rowed. Do not retry a red gate without recording the clock time — half of (B)'s evidence is a timestamp correlation, and observation 1 is unresolvable for want of one. |
| **BUG-057** | **A failing gate stage kills every stage after it, so only the FIRST failure in a run is ever visible.** `.githooks/pre-push` runs `set -e` and sources `.githooks/pre-push-project`; a stage function returning non-zero aborts the remaining sources rather than being tallied. Measured across this session's pushes: a run whose `§1b·1` failed printed 47 stages and stopped, where the same commit clean prints 54 — `§2`, `§7G`, `§D·F·H` and all four vitest stages never ran. **The cost is not the missing stages, it is the serialisation of diagnosis:** every push reveals exactly one problem, so N independent failures cost N full gate runs at ~380 s each. Getting one branch pushed took **eleven pushes**, most of them re-discovering that something else was also broken. **Same mechanism as BUG-055, one layer up** — there an unprotected command substitution killed a stage silently; here an unprotected stage kills the run. The renderer already has the right model (`pipe_finish` returns non-zero if ANY stage failed), so the tally is designed for multiple failures and cannot receive them. **Fix direction, not yet decided:** stages are already invoked through `pipe_stage`, which captures status — the abort appears to come from the sourcing and the bare stage blocks around it, so the shape is probably to make the region between stages `set -e`-safe rather than to change the renderer. Needs measurement before implementation: confirm the abort point, then decide. **Do not fix by removing `set -e`** — it is what makes an unhandled error in the gate fail closed. | S2 | KEEP | Found 2026-09-09 by Eto (Orchestrator) while pushing TASK-018 — inferred from stage counts across eleven runs, then confirmed by the pattern holding on every failing push. Re-open immediately; it taxes every multi-failure push in every project. |
| **BUG-034** | **`blueprint pull` can silently delete a project's own content — `marker_aware_merge` compares marker COUNTS and never their ORDER.** `scripts/blueprint:311` guards with `bp_begin -ne bp_end || proj_begin -ne proj_end || bp_begin -ne proj_begin`. A file carrying `BLUEPRINT:END` **before** `BLUEPRINT:BEGIN` has counts 1 and 1, passes every arm, and reaches the awk — which sets `capture` on BEGIN and clears it on END, so with them inverted the region tracking is wrong while `pull` reports the project's half preserved. **This is the core sync verb**, the one thing every derived project runs against this repo, and the failure is silent by construction. Was A-11, buried in the `AUDIT-TRIAGE` row of ~12 undispositioned findings — found by sampling that row at random during the 2026-08-20 grooming, which is the argument for triaging the remaining ~11 rather than for fixing this one. **Reproducer first:** a fixture file with inverted markers, asserting the project region survives — the fix is worthless without a test that fails on the parent commit. **Note the sibling still live in the same row: A-10**, the watcher replaying the current baton at startup, an implicit billable replay and a direct CLAUDE.md §Cost capability-4 violation. | S2 | KEEP | Raised 2026-08-20 by the grooming pass. Re-open immediately — this is the product's core verb and nothing currently guards marker order. |
| **BUG-035** | **The SAST gate scans a language this repo does not contain, and no shell linter runs at all.** `git ls-files` census: **99 `.md`, 79 `.sh`, zero JS/TS**. The semgrep stage runs `--config=p/owasp-top-ten` and CI adds `p/javascript` and `p/typescript`. **`shellcheck` is never invoked** — not in `.githooks/pre-push`, not in the workflows, not in the toolchain installer. It appears only as `# shellcheck disable=` / `source=` directives across 10+ files, so **the codebase is annotated for a linter that has never run**. **Proof this is live and not theoretical:** A-16 (`eval` injection, confirmed by execution) and A-17 (glob scope under dash, confirmed by execution) are both default shellcheck findings. Both were found by a human audit in July 2026. The gate has printed `SAST · semgrep p/owasp-top-ten ✓` on every push since. **This is the repo's own signature failure inside its security gate** — BUG-004 (a hook that never ran), A-22 (a gate never armed), BUG-005 (a suite covering nothing): *a gate that skips looks exactly like a gate that passed*. It is the most expensive instance because that green check reads as "the OWASP top-10 are covered" with maximum credibility. **Fix:** `shellcheck` in `scripts/install-toolchain.sh` (which replaced the `Brewfile` in TASK-017) plus one `pipe_stage` over `scripts/`, `.githooks/` and `tests/**/*.sh`, using the missing-tool → `pipe_skip` + CI-backstop idiom every other scanner stage already has, with a ratcheted baseline so the 79 existing files do not block day one. ~10 lines. **Not the fix:** adding a semgrep bash pack alone, which leaves the `disable=` directives still addressing nobody. | S2 | KEEP | Raised 2026-08-20 by the grooming pass (Markus, Security-1). Re-open immediately — every push since July has reported a security gate that examined none of the product's source. |
| **BUG-033** | **`tests/session-resume` #13 is load-sensitive and failed a real push gate — a flaky suite on the push path.** Observed once, 2026-08-19, during a docs-only push: `FAIL: #13 1 event(s) landed between a window close and the next open`, gate `FAILED · 18 passed · 1 failed`. The same suite **passes in isolation immediately afterwards**, and had passed on five gates earlier the same day; the failing push changed no code. It ran while the machine was loaded (a 2.4 GB dependency install plus overlapping gates). **#13 is a genuine race by construction** — it forks a loop calling `signal-set.sh` continuously while driving 30 `--mark` calls, then asserts no event landed between a window close and the next open. **Two candidate causes, and they are different bugs:** (a) the atomicity claim is conditional — the suite's own comment says the fix is *"one append rather than a lock: a single small `printf` to an O_APPEND file is one write()"*, which holds only while the payload stays inside one write syscall, so a longer marker or a loaded box could split it, and then the defect is real and BUG-023's class; or (b) the `awk` orphan-detector races the writer and reports a torn read, in which case the suite is wrong and the feature is fine. **Telling them apart needs a repeat-run harness under controlled load, not a re-run** — a single green re-run is exactly what a flaky test gives you and proves nothing. **Why this matters more than one failure suggests:** a gate that fails randomly is how `--admin` habits start, and the process half of BUG-031 is the worked example — five PRs merged over a red CI because the red had stopped meaning anything. | S2 | KEEP | Raised 2026-08-19 on a single observation, deliberately — the evidence is thin and recorded as thin. Re-open on the second occurrence, or immediately if anyone re-runs a failed gate without diagnosing it. **SECOND OCCURRENCE 2026-09-09** — #13 failed inside `bootstrap-gate`'s nested derived gate while pushing TASK-018 (`1 event(s) landed between a window close and the next open`) and passed on the immediate retry, on an otherwise green 54-stage run. **The re-open trigger this row states is therefore met.** Two things the new observation adds: it fired from inside the NESTED gate, so it costs ~150 s of bootstrap before it shows and is easy to misread as a bootstrap failure; and the retry that cleared it is exactly the `--admin` habit the row warns about — recorded here rather than left as an undocumented re-run. Note the row's stated mechanism is one of three the 2026-08-20 grooming flagged as KNOWN WRONG (both hypotheses refuted in 80 runs; the real defect is that #13 discards `--mark`'s exit code), so promote and amend together rather than investigating the text as written. |
| **BUG-030** | Running `tests/bootstrap-gate` **resets the live coordination baton** — `logs/state/signal.md` went to `Holder=Nobody / State=IDLE / "Bootstrapped from the blueprint"` while Andreas was mid-review of BUG-029 R3, 37 seconds after he claimed the mic. Recovered from `signal-history.log`; his verdict was never lost, but it could have been. **A test wrote live state** — the A-09 / BUG-014 fixture-isolation class, and this time the state it clobbered is the one the whole team coordinates through. Two things make it worse than a stray file: a poller that samples the baton between the reset and the reviewer's hand-back sees `IDLE` and can dispatch against finished work, and the reset text is the *bootstrap default*, which reads as a legitimate fresh checkout rather than as damage. **Suspect:** `bootstrap-gate` bootstraps a project and runs its gate, so either the derived project's state dir resolved back to the blueprint's own `logs/state/` (which is exactly what `scripts/lib/state-dir.sh` exists to prevent) or `new-project.sh` wrote the baton at the blueprint root. Note `tests/git-isolation` and `tests/state-dir` both exist and did not catch this. | 2026-08-19 | Found during BUG-029 R3 cross-provider review. Fix alongside or after BUG-029 — do NOT fold into it. |
| **BUG-026** | **`signal-set.sh` publishes any `State` string, so the protocol is documented and unenforced.** `AGENT_SIGNAL.md` §"The protocol" says *"`State` is `IDLE` / `ACTIVE` / `OVER_TO_<NAME>`"*; `signal-set.sh:83` checks only that it is non-empty. `--state DONE`, `--state over_to_codex` or `--state "OVER_TO CODEX"` all publish cleanly, and the dispatch watcher matches `OVER_TO_CODEX` exactly — so a typo does not fail, **it just never dispatches**. Found while reviewing Andreas's FEATURE-005 change, which whitelisted those three forms in the mic READER. That was overruled: a reader failing closed on a well-formed baton goes silently blind, which is the exact failure the feature exists to remove. Validity belongs on the WRITER, where it fails loudly at the point of the mistake instead of becoming silence three layers away. Fix: validate in `signal-set.sh` and refuse, naming the accepted forms. | 2026-08-17 | Fix when `signal-set.sh` is next touched. Until then a mistyped state publishes happily and the dispatch simply never happens — the same shape BUG-022 had to fix once already. |
| **BUG-025** | **The dispatch boilerplate hardcodes `Claude Code` as the handback `Holder`, which is not a persona.** `start-codex-signal-watch.sh:145` ends every dispatch with *"hand the mic back … with `--holder` set to `Claude Code`"*. `Holder` is documented as a **persona name from the roster** (`CLAUDE.md` §"Agent Coordination"), and no roster row is named `Claude Code` — the Orchestrator is a name the engineer chooses. So the standing instruction tells every dispatched agent to write an invalid Holder, and per-dispatch briefs that name the real persona **contradict it inside the same prompt**. Alexey hit exactly that on 2026-08-17, spotted it, and resolved it correctly by reading the roster — the next agent may not. **Alexis's ruling: a BUG, not a doc fix** — the faulty instruction is generated by executable machinery, and prose elsewhere cannot neutralise boilerplate delivered inside every run. **Same class as BUG-010**: role is the stable key, persona name is roster-owned data. Fix: resolve the `Orchestrator` role through `scripts/lib/roster.sh` — which BUG-010 built for this — and never emit a literal, neither `Claude Code` nor a project-specific name. | 2026-08-17 | Fix when the dispatcher is next touched. Until then every dispatch carries a self-contradicting instruction, survived so far only by the reviewer being careful. |
| **BUG-024** | **`codex-last-message.md` is not guaranteed written per run, and the wrapper asserts it is.** `start-codex-signal-watch.sh:144` passes `--output-last-message` to `codex exec`, and line 153 then prints *"see …/codex-last-message.md for the last message"* unconditionally. When a run does not write it, **the previous run's content stays in place** and that line vouches for it. Observed 2026-08-17: Alexey's dispatch finished at 17:40:46 and the file still held **2026-08-06**'s message — ten days old, a different item, a plausible format — and I read it as his verdict. The next dispatch (Alexis) *did* write it, so the file is overwritten on some runs and not others; **the cause is unresolved and the fix must not assume it.** The defect is that a stale file and a fresh one are indistinguishable to the reader, which is this repo's recurring shape (BUG-004 a hook that never ran, BUG-018 a refusal returning 0). Fix: stamp or truncate BEFORE the run, so a missing write leaves an empty or marked file rather than someone else's answer, and only vouch for it in the finished line if it changed. | 2026-08-17 | Fix when the dispatcher is next touched, or immediately — it is small and it misleads silently. Workaround, already in use: read the tail of `codex-runs.log` instead. |

**A promoted row leaves nothing behind.** The whole row moves; no stub, no
forwarding note. A row left behind after a promotion is the duplicate record
TASK-005 removed once already, and a forwarding note is the same thing one size
smaller — it goes stale the moment the item moves again.
