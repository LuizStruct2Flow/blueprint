# Backlog rows promoted into active work

Rows pulled from [`../backlog/BACKLOG.md`](../backlog/BACKLOG.md) and being
implemented now. They travel on to `waiting-acceptance/` when the work lands,
and their artefacts (plans, reviews) travel with them.

**This file did not exist until the first promotion** — see
[README.md](README.md). Its absence means nothing has been promoted; it is not
a missing file.

| # | Item | Sev | Category | Re-open trigger / next-step gate |
|---|---|---|---|---|
| **TASK-014** | **RUN 2026-08-19 — verdict: the socket is real, ruflo does not fit it. Full evidence in [`TASK-014-attachment-trial.md`](TASK-014-attachment-trial.md).** The seam holds under construction as well as deletion: a fresh bootstrap passes its own gate (44 stages, 170.9 s) and still passes with the full ruflo CLI installed alongside (175.9 s, ~5 s overhead, no failures). **Ruflo is not what fills it, and not for Part 2's reason** — the baton asks *who holds the mic, on what, and who do they hand to*; a ruflo swarm coordinates anonymous workers over a task queue, and **none of the three baton rows has a source** in either its files or its CLI. Sharpest datum: with a swarm initialised and a 7-worker daemon live (one worker already executed), `swarm-activity.json` still read `coordination_active: false` at its install-time timestamp and the `sessions` table was empty — a file-reading adapter reports IDLE while a swarm runs, which is worse than no adapter. **§7G needs neither of the two fixes anticipated:** it failed closed over a coordination model it could not describe, which is it working. **Two further findings raised for their own items:** `tests/state-dir` is a core guard (`both`) whose sample population IS the orchestration layer, so the strip makes it fail *vacuous* by design and it cannot be suppressed without defeating the control; and the install grants itself `Bash(node .claude/*)` over a gitignored directory it just wrote 110 files into. **Corrections to Part 2:** the `CLAUDE.md` collision does **not** reproduce (project file byte-identical; it writes machine-global `~/.claude/CLAUDE.md` instead), and `.claude/settings.json` is merged rather than clobbered with deny, ask and hooks untouched. **Not determined:** no work item was pushed through with ruflo actually orchestrating — that is a billable LLM path and the trial ran credential-less by design. ORIGINAL PROMOTION NOTE FOLLOWS. **PROMOTED 2026-08-19 — the founder authorised the trial on reopening TASK-012.** The two soft blockers below stand and are now understood as the trial's *first output* rather than its precondition: what §7G and `drift`/`pull` actually need is one of the things attaching an orchestrator is supposed to reveal, and specifying them ahead of the trial would repeat TASK-012's own mistake — deciding the shape of the seam before anything was plugged into it. **The attachment trial — can a foreign orchestrator actually plug into the socket?** TASK-012 measured **detach** and inferred **attach**; see Part 3 of [`TASK-012-strip-test.md`](TASK-012-strip-test.md) for why that step is invalid. Nothing has ever been plugged in, so there is no verdict on ruflo — the earlier DO NOT ADOPT answered a different topology. **Method mirrors the strip test, run in the other direction:** bootstrap a throwaway project, install the **full** ruflo CLI, mount the four-state lifecycle and the DoD gate **on top of it**, push one real work item through, and let the failures be the coupling map. Deletion proved the seam opens; only construction proves something fits it. **FOUR THINGS IT MUST ANSWER, none of which a detach-only run can see.** (1) **§7G** — with no orchestrator there is no baton and `pipe_skip` looks sufficient; with a foreign one there IS coordination state, just not ours. Can ruflo's state be **translated into the baton contract** (`Holder`/`State`/`Task`)? That is a mapping problem, and the answer decides whether §7G moves, skips, or dies. (2) **Does adoption CREATE the second-record defect it was rejected for?** `lib/feed.sh` and `log-activity.sh` are the observability lane and correctly stay; ruflo brings its own. Two writers to one activity log is exactly Q3's objection, manufactured by the fix rather than avoided by the rejection. (3) **`CLAUDE.md` collision** — the full CLI writes it and `blueprint pull` owns it. Real integration cost; price it, do not let it close the question the way it closed Part 2. (4) **Who invokes the gate**, and do the DoD stages still receive the inputs they assume when the orchestrator drives rather than a human. **TWO COSTS TO PRICE, NEITHER A BLOCKER:** Node + `npx` on every Bash and Edit call in a repo that is POSIX `sh`, `jq` and `awk` (their own note says `npx` can take 30+s cold); and the load-bearing half of the product moving onto a third party's upgrade cadence — 68k stars, mid-rename from claude-flow, with a self-declared `"_legacy_unaudited_shim": true` at its root whose `PreCompact` hook injects "54 available agents" and SPARC workflows that do not exist here. You would own and delete that file on install; it is evidence about hygiene, not an inherited defect. **NOT THE ANSWER:** re-running the lite path (an add-on by construction — it cannot fill a socket); deciding from source reading again (Part 2's own stated gap is that **nothing was ever installed**); or treating this as a migration. **Exit criterion is a recommendation with evidence, same as TASK-012 — the deliverable is a verdict, not an adoption.** **Blocked-by, soft:** the two follow-ups Part 1 named are the socket's other half and there is nothing to attach to until they exist — §7G out of the core DoD gate, and `drift`/`pull` learning the profile (today a profiled project is permanently drifted and one `blueprint pull --yes` away from having the layer back, which under attachment would collide with the attached orchestrator rather than merely reappear). | S2 | PROMOTED | Founder, 2026-08-19, on reopening TASK-012: *"the blueprint is not about the orchestration of agents itself"* — so a third-party orchestrator is a component swap to be tested, not a product replacement to be refused. Re-open immediately; it is the evidence TASK-012's revalidation is waiting on. Drop to DEFER only if the founder decides the orchestration layer stays in-house regardless of what a trial would show. |
| **TASK-012** | **REOPENED by the founder, 2026-08-19 — the conclusions were drawn from a wrong assumption.** The spike measured whether orchestration comes **off** and concluded something about whether another orchestrator can go **on**; only the first was tested, and nothing was ever attached. Part 1's measurements stand, its *socket* inference is downgraded to **detachable — attachment unmeasured**. Part 2's **DO NOT ADOPT is withdrawn**: it rested on Q1 alone, and Q1's finding (ruflo cannot observe a `codex exec` *we* launched) holds only while we keep our own dispatcher — an orchestrator that owns the dispatch sees it by construction. The brief's **"evaluate on the lite path only"** constraint is retired: filling the socket means *being* the orchestrator, which is the full CLI, so the lite path could never answer the question it was paired with. Revalidation in Part 3 of [`TASK-012-strip-test.md`](TASK-012-strip-test.md); the evidence it now needs is **TASK-014** (the attachment trial). **Everything below is the ORIGINAL brief, kept verbatim as the record of what was asked** — including the two assumptions Part 3 retires. Superseded findings that survive: **BUG-028** (a fresh bootstrap cannot pass its own gate), the ~14% measurement, and §7G living in the core gate. ORIGINAL BRIEF FOLLOWS. Part 1 (strip test): orchestration is **separable-with-work**, so the blueprint is a product with a plug-in orchestration socket. Part 2 (ruflo): **DO NOT ADOPT**, and Q1 decides it alone — ruflo's Codex support is a *dispatcher*, not an observer, so it cannot see a `codex exec` we launched, which is the half of the feed that produces our review findings. Full evidence in [`TASK-012-strip-test.md`](TASK-012-strip-test.md). **Should the blueprint adopt [ruflo](https://github.com/ruvnet/ruflo), and in which form?** Not *how to use it* — that presumes the answer. **Why now:** 2026-08-18 produced four commits of feed plumbing and seven cross-provider review findings, none of which is why anyone would use this blueprint. The differentiator is the four-state lifecycle, the DoD gates, the cross-provider review rule and the `a2bp` sync model; the feed is infrastructure, and infrastructure is what you buy rather than debug. Founder asked whether an off-the-shelf answer exists, which is ladder rung 1 — *does this need to exist at all* — and nobody had asked it about the feed. **THREE QUESTIONS DECIDE IT, and none needs a commitment to answer.** (1) Does its observability capture a **`codex exec` dispatch**, or only Claude agents? Ruflo advertises 5 providers with failover, but that is MODEL ROUTING — our reviews run a different *harness* (`codex exec`, own sandbox, own context), and that is where the findings come from. If it cannot see the Codex half, it replaces the half of our feed that already works. (2) Does the **lite plugin path** (`/plugin install ruflo-core@ruflo`, *"slash commands only, zero workspace files"*) stay out of `CLAUDE.md`? The full CLI writes to `CLAUDE.md`, `.claude/` and `.claude-flow/` — and `CLAUDE.md` is a `MANAGED_FILES` entry that `blueprint pull` owns, so the full path collides head-on with the sync model. (3) Does the dashboard **replace** `tail -f logs/agent-activity.log`, or sit beside it as a second record — the failure mode this repo has fixed three times (TASK-005, the lifecycle triggers, the forwarding notes)? **Evaluate on the lite path only**; the full one adds Node + TypeScript + Rust and a daemon to a repo that is POSIX `sh`, `jq` and `awk`. **Exit criterion is a recommendation with evidence, not a migration.** **Strip test RUN 2026-08-18 — verdict separable-with-work, findings in [`TASK-012-strip-test.md`](TASK-012-strip-test.md).** **Method and sequencing below** — a `MANAGED_FILES` profile rather than a thin fork, and a no-orchestration bootstrap run FIRST, because it answers whether the differentiator is separable at all and cannot be invalidated by whatever ruflo turns out to do. Compare against [disler/claude-code-hooks-multi-agent-observability](https://github.com/disler/claude-code-hooks-multi-agent-observability) as the narrower alternative — 12 hook types, but Claude-only and its subagent handling is the bookend approach BUG-027 just replaced. | S2 | REOPENED | Founder, 2026-08-18. **Blocks nothing, but should be answered before more feed hardening lands** — if the feed is going to be replaced, BUG-027's residue and FEATURE-004 are work we would throw away. |


## TASK-012 — how to run it, and why not a fork

**The method is a `MANAGED_FILES` profile, not a second repo.** The founder's
instinct was a thin blueprint with orchestration stripped out, as the spike's
vehicle. The separation is right and the fork is not: two blueprints is one fact
recorded twice, and it drifts — the same defect as `INDEX.md` (TASK-005), the
lifecycle triggers when the PR rule landed, and the forwarding notes removed on
2026-08-18. At repo scale it would be the worst instance yet, because nothing
would fail when they diverged.

`MANAGED_FILES` and `new-project.sh` already decide what reaches a derived
project, so "thin" is a profile in the one blueprint.

**THE DELIVERABLE IS FALSIFIABLE: bootstrap a project with orchestration OFF and
see whether the lifecycle, the gates and the sync still hold together alone.**
That is worth doing whatever ruflo turns out to be, because it answers the
question underneath this one — **is the differentiator separable at all?**

- If a no-orchestration bootstrap is coherent, the blueprint is a product with a
  plug-in orchestration socket, and ruflo is a candidate to fill it.
- If it is not, **the orchestration IS the product**, and adopting ruflo means
  adopting a different product rather than a component.

**Do this BEFORE evaluating ruflo.** It needs no third party, it cannot be
invalidated by what ruflo turns out to do, and stripping first would risk
deleting the half of the feed that works — the Codex half — on the strength of a
capability nobody has verified yet.

### The measurement, taken 2026-08-18

| | files | lines |
|---|---|---|
| orchestration — baton, watchers, feed, roster | 18 | 3,150 |
| core — DoD, gates, sync CLI, concern recipes | 18 | 5,892 |

Plus **9 of 37 test suites** are orchestration (3,463 lines), measured on a branch
missing `wait-mic` and `subagent-feed` — so the real figure is higher. Roughly
**40% of the repo**, and effectively all of 2026-08-18.

That number is the argument for asking the question, not for any particular
answer to it.

