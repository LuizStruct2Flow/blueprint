# Backlog rows promoted into active work

Rows pulled from [`../backlog/BACKLOG.md`](../backlog/BACKLOG.md) and being
implemented now. They travel on to `waiting-acceptance/` when the work lands,
and their artefacts (plans, reviews) travel with them.

**This file did not exist until the first promotion** — see
[README.md](README.md). Its absence means nothing has been promoted; it is not
a missing file.

| # | Item | Sev | Category | Re-open trigger / next-step gate |
|---|---|---|---|---|
| **TASK-022** | **Anchor the rules in mechanisms, and shrink what agents must remember.** Founder direction, 2026-09-10: *"I think we should rely in automation and not in agents having all rules in their context and being disciplinated on applying these rules… the md files will be used for documentation, and we will anchor the controls in the ci and development lifecycle."* Triggered by a cross-review of `docs/way-of-working.md` in which two reviewers independently found its strongest untold story was that **controls are required to be checkable and are repeatedly caught lying** — a framing the founder rejected as pitched, while accepting the evidence behind it. Artefacts: [`PLAN-TASK-022.md`](PLAN-TASK-022.md), the rule-enforcement audit as `TASK-022-rule-enforcement.{csv,xlsx}`, and the talk at [`../talk-enforcing-agentic-quality.md`](../talk-enforcing-agentic-quality.md). **Partly landed:** `34d5d00` fixed the trunk rule contradicting itself across CLAUDE.md and DoD §6 — TASK-019 made `main` the trunk and deleted the section two other rules still pointed at — and reframed the landing table on the axis that was wrong all along: **the contributor, not the repo.** The rest is not started. | S2 | Process | **Next-step gate:** the plan's inventory of which rules are prose-only versus mechanically enforced. This session produced eight instances of `docs/config/findings.md` F-002 — checks inferring a property from a proxy satisfiable without it — which is direct evidence for the plan's premise and should be folded in before the rest is sequenced. |
| **TASK-024** | **The procedure layer: named procedures move out of always-on prose into scripts, with per-provider pointers — and skills are recorded as ergonomics, never as controls.** Two problems were being conflated under "rely on automation": TASK-022 anchors *invariants* in the lifecycle, and this item fixes *context economy* — the founder's other half of the same sentence, "the md files will be used for documentation". **Measured today:** `CLAUDE.md` is 1214 lines / 10147 words and is in every session's context before the founder types anything, with `docs/DoD.md` (818) read on top by instruction, and the four recipe docs (`OBSERVABILITY` 309, `SECURITY` 304, `INFRASTRUCTURE` 377, `DOCUMENTATION` 292) carrying "choose one of three recipes" decisions a project makes ONCE at bootstrap. `.claude/skills/` does not exist. **The rule this item is built on, and it is the reason skills are not the deliverable:** a control inside one vendor's client binds one provider. Derived from the TASK-022 sheet — of 309 rules, **233 bind nothing, 67 bind every provider *only if* `core.hooksPath` is armed (A-22: a fresh clone is not), 8 bind Claude Code alone (`PreToolUse`), and exactly 1 binds unconditionally (CI)**. Jesko (Codex) proved the 8 empirically in the TASK-022 audit: the no-chain guard does not reach `exec_command`, and he complied *by discipline* while auditing whether rules are enforced by discipline. **So the shape is: the procedure is a SCRIPT** (`scripts/<name>.sh`, testable, one copy, runnable by any provider), **a Claude skill is a thin pointer at it**, a line in `AGENTS.md` is Codex's pointer, and **a CI job running the same script is the only layer that binds everyone**. A skill whose body is a prose checklist is `DoD.md` §1c with a new filename and must not be built. **Candidates, strongest first:** `lcm` — already triggered by the founder typing `lcm`, i.e. a slash command implemented as prose an agent must remember to find in DoD §1c; the wake protocol (four identical numbered steps, in the always-on file only because there was nowhere else); the Sonar triage workflow (five steps plus exact API queries); the major-bug consensus process; the `a2bp` playbook; backlog grooming. **Blocked on TASK-021**, deliberately: a `.claude/skills/` directory needs the same ships-vs-blueprint-only tier decision that the `scaffolding/` + `forge/` split is establishing, plus its `MANAGED_FILES` and `.gitattributes` entries, and deciding that twice is how BUG-051/053/061 each happened. | S2 | KEEP | Raised 2026-09-10 by the founder, asking whether skills could cover some of the TASK-022 requirements — the answer that produced this item is that they cover the *context* half and none of the *enforcement* half. **Promote when TASK-021 lands** and the tier axis exists. Re-open earlier if any procedure is proposed as a skill whose body is prose rather than a pointer at a script, or if a Claude-only mechanism is ever recorded in the enforcement sheet without its `BINDS` value. |
| **TASK-021** | **The `scaffolding/` + `forge/` restructure — TASK-018-TARGET §2, mapped end-to-end before implementation.** Map: [`PLAN-TASK-021-RESTRUCTURE.md`](PLAN-TASK-021-RESTRUCTURE.md) (read-only, against `e5e3600`). **Three stages.** **Stage A — `forge/` alone**, green and pushable on its own: `templates/`, `scripts/new-project.sh` and the six blueprint-only suites move, no path strip is introduced, ~15 file moves, and the terminology inversion (`blueprint`/`both` → `forge`/`scaffolding`, `bp_blueprint_only` → `bp_forge_only`, 11 load-bearing sites) flips here. **Stage A′ — the compatibility release, and it is NOT OPTIONAL:** a `scripts/blueprint` that probes `[ -d "$BLUEPRINT_ROOT/scaffolding" ]` and prefixes only if present, landed while the blueprint is still flat so it is byte-identical in behaviour, then pulled by every derived project *before* Stage B. Without it, `bp_expand_managed_dirs` (`scripts/blueprint:670`) runs `git archive HEAD tests` against a blueprint that no longer has a root `tests/`, `_bp_expand_die` fires fail-closed (BUG-029 R2-S1) inside `read_blueprint_source` — the one path every subcommand goes through — so **`linkedin-watcher-agent`, `storm2flow` and `struct2flow-www` each lose `drift`, `pull` AND `a2bp` at once, including `blueprint pull scripts/blueprint`, which is the recovery path.** Recovery would be a manual `cp` on three machines with no tool saying why: BUG-028 exactly, self-inflicted with the map in hand. `blueprint drift` is mandated at every wake, so this lands on the first session after the push. **Stage B — `scaffolding/`, and it is ATOMIC: ~185 file moves** plus 7 CLI path sites, the bootstrap archive+strip line, the `.gitattributes` split, 37 CI lines, 38 hook blocks, 13 shell-suite fixture fixes and 5 TS sites. Nothing in it is individually green — the moment `scripts/` moves, the blueprint's own gate cannot source `scripts/lib/pipeline.sh`. Iterate with a throwaway all-suites runner, not the ~390 s gate that reveals only the first failure (BUG-057). **Stage C — co-location (R2), incremental per component**, in TARGET §3.2's order. **Verified by probe, and it changes the shape of the work:** `git archive` honours a nested `scaffolding/.gitattributes`, patterns stay relative to the containing directory, and `--strip-components=1` lands them correctly — so the export-ignore lines move **unchanged, byte for byte**, `scripts/lib/suites.sh:96` keeps working, the derived project stops inheriting blueprint-only lines, and the ~46-line TASK-018-phase-1 block dissolves. **Two costs TARGET §2 did not anticipate, both blocking:** (1) `.github/workflows/security.yml` **cannot move** — GitHub Actions discovers workflows only at the repo root — and `CLAUDE.md` + `.claude/settings.json` are pinned the same way by Claude Code, so root-anchored *shipping* files are a third class the two-bucket rule has no cell for; (2) `scripts/blueprint` is sync + a2bp yet **must ship**, contradicting "`forge/` = bootstrap, sync, a2bp, templates". | S2 | OPEN | **BLOCKED on eight founder decisions** — see the map's closing section; the gating ones are §0 (`.github/workflows/`) and §8 (authorize Stage A′). Do not start Stage B until A′ has landed and all three derived projects have pulled it. Re-open if any derived project is found on a `scripts/blueprint` predating A′. |
| **TASK-039** | **The DoD bug-test stage finds a project's own regression tests, and a blueprint suite can no longer vouch for a project bug that shares its number.** From storm2flow, `FR-storm2flow-dod-test-roots` (branch `fr/storm2flow/2026-09-15`; text saved in `.scratch/fr-storm2flow-2026-09-15.diff`). **The defect:** `scripts/lib/dod-gate.sh:154` searches only `${BP_CODE_ROOT:-.}/tests/`. In a derived project, that directory holds the blueprint's shipped suites, which name about 109 BUG numbers, about 97 of which are also storm2flow bug numbers. So those storm2flow bugs pass with no test of their own, while storm2flow's real tests (`backend/src`, `frontend/src`, `frontend/e2e`, `infrastructure/test`) are never found. **Do:** a project-declared list of test roots (for example `BP_TEST_ROOTS` in `project_config_paths.md`, defaulting to today's `tests/`). Do NOT make `BP_CODE_ROOT` configurable, because `.githooks/pre-push:83-103` and `dod-gate.sh:187-189` resolve from it. Scope the match so a blueprint-shipped suite cannot satisfy a project bug. Also: `dod_items_in_push` (`dod-gate.sh:58-64`) parses subjects with its own `sed` pattern instead of `scripts/lib/commit-subject.sh`; use the shared parser. Update `docs/DoD.md` §2 and the templates' paths config. | S2 | DoD gate | **Promoted 2026-09-15 on founder decision** ("Start all now"). Done when a derived project's bug passes only on a test under its declared roots, a blueprint suite naming the same number does not count, tests pin both, and the branch is deleted with the request recorded in `docs/backlog/feature-requests.md`. |
| **TASK-043** | **`CLAUDE.md` `@`-imports the five project config files, so project rules are in every agent session.** From storm2flow, `FR-storm2flow-claude-md-imports`. `CLAUDE.md:3-5` says to read three `project_config_*.md` files alongside it, and `CLAUDE.md:909-911` names five, so the two lists disagree. Nothing loads them. Because `CLAUDE.md` is whole-file managed, a project has to move its rules out of it, and today they leave agent context when they do. **Founder decision 2026-09-15: import all five** (overview, paths, dod, security, infra), and fix the 3-vs-5 contradiction. **Do:** add the `@` imports; check how Claude Code treats an imported file that is missing (a project that has not created one yet); make sure `templates/` seeds all five; and confirm the blueprint's own root copies exist, since here they are this repo's config (BUG-009). Note the context cost against TASK-024. | S2 | Agent context | **Promoted 2026-09-15 on founder decision.** Done when a fresh session in a derived project has the five files' content loaded, a missing file does not break the session, and CLAUDE.md lists them consistently. |
| **TASK-044** | **A project declares its CI, so suites that assume GitHub Actions check the declared pipeline or skip with a visible notice.** From storm2flow's orchestrator (Sylvia) after rehearsing the pulled gate at `dbed972` (`FR-storm2flow-declared-ci`). Several suites require `.github/workflows/security.yml`: manifest #5/#6 (`tests/manifest/manifest.ts:510,706-713`), commit-subjects #5, sync-by-address #33/#33b/#39, and ts-bridge #3/#5/#7. storm2flow's CI is AWS CodePipeline on CodeCommit, where that file is inert, so for a non-GitHub project those suites certify CI that never runs. **Do:** add a CI declaration in `project_config_paths.md` (template, and the blueprint's root copy declaring `github-actions`). Each listed suite checks the GitHub workflow only when the declared CI is GitHub Actions, and otherwise skips with a visible reason naming the declared CI. A skip must be visible in the gate output (`pipe_skip`-style, never silent), because a suite omitted without a trace is the failure `tests/manifest` exists to refuse. An undeclared CI keeps today's behaviour. | S2 | Test suites / CI | **Filed and promoted 2026-09-15 on founder decision** ("Start now"). Done when a project declaring non-GitHub CI gets visible skips instead of passes on an inert workflow, the blueprint still runs every check, and tests pin both. |
| **TASK-046** | **`CLAUDE.md` imports a project-owned `claude.internal.md`, and whether that file is tracked is the project's own decision.** Founder direction, 2026-09-16: *"can we always link to claude.internal.md, and this file belongs to the derived project, and also imo is a local decision of the project if it'll gitignore it or not"*. **The problem it solves:** `.gitignore:73-82` excludes `/CLAUDE.md`, `/AGENTS.md`, `/AGENT_SIGNAL.md`, `docs/DoD.md`, `docs/PUBLISHING.md` and `docs/doing/HANDOVER.md` from every derived project as "multi-AI methodology (PRIVATE)", so managed docs link files a clone does not have — the limitation BUG-125 had to accept. Splitting the generic from the private removes the reason to exclude the generic half. **The shape** is the one already proven twice, by `.githooks/pre-push-project` and by TASK-042's `.claude/settings.project.json`: the blueprint owns the generic file, the project owns an extension, and pull never touches the extension. **Do:** add `@claude.internal.md` to the imports in `CLAUDE.md` and the template; seed nothing, since Claude Code skips a missing import silently (verified in its loader for TASK-043); say in `CLAUDE.md` that the file is the project's, that it is where project-private agent notes go, and that tracking it is the project's call; and revisit the `.gitignore` methodology block, which is the half that actually decides whether a link resolves in a clone. | S2 | Agent context / privacy | **Filed and promoted 2026-09-16 on founder direction.** Done when a derived project can keep private notes in `claude.internal.md` with the generic docs tracked, a missing file still yields a working session, and BUG-125's limitation no longer applies to those links. |
| **TASK-047** | **Every test is a spec-driven TypeScript test: one extension, `*.spec.ts`, no exceptions.** Founder decision 2026-09-16: *"migrate the tests to be spec driven ts tests, we don't need exceptions"*, answering why the gate accepts five runner extensions while only `*.spec.ts` executes (TASK-039 finding 6, Alexey). **The facts behind it:** under `tests/` there are 55 TypeScript test files, one real shell runner (`tests/ts-bridge/test.sh`) and one shell helper (`tests/helpers/proc-cwd.sh`, invoked from a spec, not a test itself). `.spec.js` / `.test.js` were accepted only in case a derived project used JavaScript; no such file exists. **Do:** port the shell runner to a spec; collapse the accepted set to `*.spec.ts` in `scripts/lib/dod-gate.sh`, `scripts/lib/suites.sh` discovery, `tests/vitest.config.ts` and the three checks in `scripts/run-ts-suites.sh`, so what the gate counts and what actually runs are one set; simplify TASK-039's guard #14 accordingly. **Then the convention docs, which is where this reaches further than `tests/`:** CLAUDE.md §"Test Layers" prescribes `*.test.{js,ts}` for co-located unit tests in `src/` and `*.snap.test.{js,ts}` for snapshots, and §"Test directory layout" repeats it. Report the full ripple before rewriting those rows — whether co-located unit tests in a derived project's `src/` also become `*.spec.ts` is a convention change for every project and goes back to the founder if it is not obviously covered. | S2 | Test suites | **Filed and promoted 2026-09-16 on founder decision.** Done when one extension is accepted, discovered and executed, the shell runner is a spec, no suite is declared that the runner cannot report, and the convention docs match. |
| **TASK-048** | **A derived project tracks all six methodology files: the `.gitignore` privacy block goes, with no exceptions.** Founder decision 2026-09-16, "All six, no exceptions", following TASK-046: private project notes now have a home in `claude.internal.md`, so excluding the framework's own docs protects nothing and costs every doc link into them (the limitation BUG-125 had to accept). **The six:** `/CLAUDE.md`, `/AGENTS.md`, `/AGENT_SIGNAL.md`, `docs/DoD.md`, `docs/PUBLISHING.md`, `docs/doing/HANDOVER.md`. Five are blueprint-managed or generic protocol and identical in every project; the sixth is the project's live handover, and **the founder accepted publishing it**. **Evidence that shapes the work (Christian):** `.gitignore` is NOT in `MANAGED_FILES` but IS in the bootstrap archive, so it is seeded once and project-owned — **a blueprint edit reaches NEW projects only**, and every existing project must edit its own copy. **Do:** (a) remove the six from the blueprint's `.gitignore` and **replace** the "multi-AI methodology (PRIVATE)" comment rather than delete it, since it records a deliberate decision — put the replacement wording in front of the founder; (b) same commit, update `docs/PUBLISHING.md`, which encodes the exclusions in §0, in §1b's preflight (it greps the index and prints `PRIVATE FILES STILL TRACKED — DO NOT PUSH`) and in §3a/§3b's allowlist and untrack commands — otherwise the runbook fails a project for tracking its own docs; (c) write the one-line instruction existing projects run, and say where it goes so storm2flow and the others actually get it; (d) check `tests/bootstrap-contents` (`:96-107`) and `tests/bootstrap-gate`, which force-add these files so the fixture mirrors today's repo. **Leave `project_config_*.md` ignored** — A-27 put the threat model and infra account IDs there deliberately. **Absorbs A-13**, cancelled 2026-09-16 with a pointer at `docs/config/findings.md` F-004: it recorded the same fact from the other end — `.gitignore` is not managed, so the privacy block's "they'd come back on next sync" instruction described a mechanism that does not exist, and a derived project's block silently never updated. That is (c) here, the instruction existing projects run themselves. Two live records of one fact drift, which is why A-13 does not stay open alongside this. | S2 | Privacy / sync | **Filed and promoted 2026-09-16 on founder decision.** Done when a fresh project tracks all six, the publishing runbook agrees, existing projects have their instruction, and the replacement comment has been shown to the founder. |


## TASK-012 — how to run it, and why not a fork

**The method is a `MANAGED_FILES` profile, not a second repo.** The founder's
instinct was a thin blueprint with orchestration stripped out, as the spike's
vehicle. The separation is right and the fork is not: two blueprints is one fact
recorded twice, and it drifts — the same defect as `INDEX.md` (TASK-005), the
lifecycle triggers when the PR rule landed, and the forwarding notes removed on
2026-08-18. At repo scale it would be the worst instance yet, because nothing
would fail when they diverged.

`MANAGED_FILES` and `new-project.sh` already decide what reaches a derived
project, so "thin" is a profile in the one blueprint.

**THE DELIVERABLE IS FALSIFIABLE: bootstrap a project with orchestration OFF and
see whether the lifecycle, the gates and the sync still hold together alone.**
That is worth doing whatever ruflo turns out to be, because it answers the
question underneath this one — **is the differentiator separable at all?**

- If a no-orchestration bootstrap is coherent, the blueprint is a product with a
  plug-in orchestration socket, and ruflo is a candidate to fill it.
- If it is not, **the orchestration IS the product**, and adopting ruflo means
  adopting a different product rather than a component.

**Do this BEFORE evaluating ruflo.** It needs no third party, it cannot be
invalidated by what ruflo turns out to do, and stripping first would risk
deleting the half of the feed that works — the Codex half — on the strength of a
capability nobody has verified yet.

### The measurement, taken 2026-08-18

| | files | lines |
|---|---|---|
| orchestration — baton, watchers, feed, roster | 18 | 3,150 |
| core — DoD, gates, sync CLI, concern recipes | 18 | 5,892 |

Plus **9 of 37 test suites** are orchestration (3,463 lines), measured on a branch
missing `wait-mic` and `subagent-feed` — so the real figure is higher. Roughly
**40% of the repo**, and effectively all of 2026-08-18.

That number is the argument for asking the question, not for any particular
answer to it.

