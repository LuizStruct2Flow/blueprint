# Bugs — active (being implemented)

Bugs currently being worked. Pushed bugs move to
`docs/waiting-acceptance/BUGS.md`; founder-accepted bugs move to
`docs/done/BUGS.md`. See [README.md](README.md) for the lifecycle.

**Keep rows to one line.** Link out for the detail. A row that grows into a
paragraph belongs in a `PLAN-*.md` or a work-item folder — a table cell
holding half a page is unreadable, which is how this file stopped being
useful once already.

## Two namespaces, and only one of them is a work item

**`BUG-XXX` / `FEATURE-XXX` / `TASK-XXX` are the lifecycle IDs.** They are what the
commit convention, the regression-test naming rule and these lifecycle folders
key off (CLAUDE.md §"Bug Management", §"Team Workflow").

**`A-NN` is not a work item.** Those are findings from one audit — the
2026-07-23 contamination sweep in
[BLUEPRINT-AUDIT-2026-07-23.md](../config/BLUEPRINT-AUDIT-2026-07-23.md) — in the same
category as a Codex finding ID. A finding is a *claim that something is wrong*;
it becomes work when it gets a `BUG-`/`FEATURE-` number and a row here.

They were being used as though they were work items — folder names, rows in this
table, gate comments — and then extended with new numbers (A-38, A-39) for
findings that had nothing to do with that audit. Live items were renumbered on
2026-07-30. The audit document and everything in `done/` keep their `A-NN` IDs as
historical provenance, because the Codex review documents argue about findings by
those names and renaming them would break the trail they exist to be.

**Rule going forward:** an `A-NN` reference is a citation of history. If you are
about to work on something, give it a `BUG-`/`FEATURE-` number first.

| # | Bug | Severity | Status | Detail |
|---|---|---|---|---|
| **BUG-150** | **The stranded-mic recovery matches on Holder AND Task, but an agent rewrites the Task when it claims ACTIVE — so the recovery never fires for the common failure.** | S2 | OPEN | **Observed live 2026-09-23 09:54Z**, the first failure after BUG-144 was accepted. Thomas (Kimi) was dispatched BUG-148, claimed the mic with `State=ACTIVE` and `Task=Fixing BUG-148 (CI red...)` — his own summary, replacing the dispatch text — and `kimi -p` then died on the 5-hour quota. `recoverStrandedMic` compared the baton against the Task it had DISPATCHED, saw a different one, concluded this was no longer its dispatch, and returned silently: the watcher log shows `kimi FAILED (exit 1)` and then nothing at all. The mic stayed at `Holder=Thomas State=ACTIVE` until the Orchestrator took it back by hand. **The wrong assumption is stated in the code**: `scripts/signal-watch.mts`'s docblock says "a well-behaved agent claims ACTIVE without touching Task". It does not. Every dispatch brief tells the agent to claim ACTIVE, and agents write their own summary when they do — Andreas (Codex) did the same on 2026-09-22 (`Task=TASK-070 round 2: extend backlog marker enforcement...`). So the Task match, added in BUG-144 round 3 to stop a genuine race (a NEW dispatch to the same Holder in the same State being clobbered), defeats the ACTIVE recovery it was meant to keep. **Why this is a new bug and not BUG-144 reopened:** BUG-144's causes were the roster path and the shell. This is the identity rule itself — Holder+Task cannot identify a dispatch whose Task the holder is expected to rewrite, and Holder+State alone cannot tell a new round from a stranded one. Needs a dispatch identity that neither party rewrites: `logs/state/signal-history.log` records every flip, so "has anything been dispatched since mine" is answerable without guessing from the Task text. Design it before writing it. **No regression test:** until the fix names its mechanism; then a case where the stub claims ACTIVE with a CHANGED Task and dies, which today passes wrongly. **Re-open if** a stranded mic is left unrecovered because the holder edited the Task. |
| **BUG-148** | **`tests/mic-recovery` reads the operator's gitignored `AGENT_ROSTER.md`, so the suite cannot run in CI or in a fresh clone.** | S1 | OPEN | **CI red on `1ee7b3d` (run 35841815567), and flaky before that** (`1d45861` failed, then passed on a rerun). The suite's `beforeAll` resolves the Orchestrator through the REAL repo-root roster: `[roster] no AGENT_ROSTER.md in '/home/runner/work/blueprint/blueprint' — only the shipped AGENT_ROSTER.example.md`, and the whole file fails before its first case. `AGENT_ROSTER.md` is per-engineer and gitignored by design (CLAUDE.md §"Agent Coordination"), so CI never has one and neither does a fresh clone. **Introduced by BUG-144 round 3** while fixing the opposite defect: the old fixture fabricated a roster beside the fixture baton, which production never does, so the correction reached for the real one. Both are wrong in the same way — the suite must build a repo-SHAPED tree of its own (roster at that tree's root, baton under its `logs/state`) and resolve through it, depending on no file outside the fixture. **No regression test:** the failing suite is itself the reproducer; the fix is proven by that suite running green in CI, where no roster exists. **Re-open if** any suite depends on a gitignored, per-machine file. |
| **BUG-149** | **The founder cannot run the test suite: the obvious commands fail with 969 errors in an ordinary terminal.** | S2 | OPEN | **Reported 2026-09-23 by the founder**, reproduced immediately: a VS Code terminal exports `GIT_ASKPASS`, and `tests/harness/env.ts`'s `assertProcessEnvClean` refuses to run any scenario while a forbidden ambient variable is present, so `npm test` in `tests/` (its own declared script) and a plain `vitest run` both fail in nearly every case. The guard is right — BUG-046/BUG-047 are real — but it fires on the ENTRY POINT the project itself documents, and the only working invocation is an `env -u GIT_ASKPASS -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA …` incantation that lives in the handover rather than in the tooling. **The person who must run acceptance tests is the one person for whom the documented command does not work.** **Fix direction:** make the obvious entry points scrub for themselves (`tests/package.json`'s `test` script, and whatever the docs tell a human to run), so the guard protects specs that spawn directly while the founder's command just works. Then say in one place how to run the suite, and make the acceptance rows point at it. **No regression test:** until the fix names its entry point; then a case that runs it with `GIT_ASKPASS` set and expects green. **Re-open if** running the documented command in a normal terminal fails. |
| **BUG-147** | **The DoD gate counts only `*.spec.ts`/`*.spec.tsx` as a bug's regression test, on every searched root, so a JavaScript project can never satisfy it.** | S2 | OPEN | **From storm2flow, 2026-09-22:** its BUG-216 fix landed with a real, running regression test, `frontend/src/configLoader.bug216.spec.js`, whose `it()` title names the bug. `dod_stage_bugtests` (`scripts/lib/dod-gate.sh`) still refused the push with "No test under the searched roots names: BUG-216", because its extension filter applies to a project's own declared `BP_TEST_ROOTS` too. Those roots are run by the project's own runner, never by the blueprint's `tests/vitest.config.ts`. **A reproducer and a fix already exist,** written by storm2flow's Matthias directly in this checkout, and are kept on local branch `storm2flow/bug147` (`58ff781` reproducer, `35d902d` fix). They were taken off `main` because they collided with the Orchestrator's unpushed work, and because the fix edits `scripts/lib/dod-gate.sh` in place, which TASK-067's gate refuses. **Founder, 2026-09-22: "port it."** No exception; `scripts/lib/dod-gate.sh` is ported whole under TASK-067 before the fix lands. **The design question that must be settled first:** the file is SOURCED, by `.githooks/pre-push-project` and by `.github/workflows/security.yml`, so a two-line shim cannot serve it — a shim execs, and an exec cannot hand shell functions back to the caller that sourced it. The port needs a calling boundary (a CLI its shell callers invoke), designed and reviewed before any translation starts. **More evidence from storm2flow (Sylvia, 2026-09-22):** its frontend vitest includes only `src/**/*.spec.js(x)`, so TypeScript was never an option there, and two further bugs tested only in `.spec.js` are latent blocks of the same shape (BUG-204 and BUG-137, both in `frontend/src/build.spec.js`). **A workaround is live there and must be undone when this lands:** that one spec was renamed `.spec.ts` and `frontend/vite.config.mjs` also includes `src/**/*.spec.ts`, both commented as BUG-147. storm2flow holds a row to revert it, so tell Sylvia when the fix ships. **No regression test:** not on `main` yet. Its reproducer exists, `tests/dod-gate` #11c on branch `storm2flow/bug147`, and lands with the fix once the founder rules. **Re-open if** a declared project root's JavaScript spec whose title names a bug is refused. |
| **BUG-146** | **`tests/sync-by-address` #20d (BUG-120) intermittently hangs about 320 s in CI, turning `main` red on pushes that changed nothing it tests.** | S2 | OPEN | **Observed twice:** CI run 35596083384 on `61cfe01` (2026-09-21, a docs-only push, unnoticed then because the Orchestrator did not wait for its CI) and run 35733083948 on `374a8d9` (2026-09-22, also docs-only). Both times "#20d BUG-120: a TERM that reaches the held refresh child with its release is not lost — no fetch, no wait for the budget" ran 320019-320024 ms and failed. That takes `bootstrap-gate` #2/#3 down with it, because a fresh bootstrap runs the same suite in its own gate. A rerun of the failed job passed with no change. The test exists to prove a TERM is NEVER lost, and a hang is exactly what a lost TERM looks like. So either the test's synchronisation races on a slow runner, or BUG-120's fix loses the signal in a window the test sometimes hits. **The first job is to decide which, from evidence:** reproduce under CI-like load (`taskset -c 0-3`, see HANDOVER §0 item 3) in a loop until it hangs, then read what the held refresh child and its parent were doing. A flaky gate test is a defect whichever side it is on: it trains everyone to rerun until green, which is the BUG-031 failure. **Diagnosis attempted 2026-09-22 (Philipp), inconclusive:** 162 of 162 local runs were clean, 150 of #20d alone under `taskset -c 0-3` plus pinned busy-loops and 12 of the full suite (which nests bootstrap-gate). The hang happened at both levels in CI (outer on `61cfe01`, nested on `374a8d9`) and at neither here. A local full suite takes about 157 s, where CI takes over 340 s for the same work, so `taskset` does not reproduce this runner. No defect was found in BUG-120's token gate (`scripts/blueprint:672-704`) or in the test, and nothing was changed. **Next step:** capture evidence ON the next CI occurrence. When a scenario's wait times out, the harness dumps the process tree and wait channels as a CI artefact. **No regression test:** the test that catches this bug already exists, `tests/sync-by-address` #20d itself, and it does not reproduce locally. A BUG-146-titled copy would prove nothing more. **Founder, 2026-09-22: leave it.** The harness-side capture is not built; the row stays open and unattended until the next hang. **Re-open if** #20d hangs again on any run. |

**Do not narrate status here.** Which items are where is answered by the
folders: `doing/` is what is being implemented, `waiting-acceptance/` is what is
pushed and untested, `done/` is what the founder accepted. Prose repeating that
becomes a second record of one fact, and on 2026-08-03 every such line in this
file had gone false — including one telling the next session that a decision was
still pending on an item accepted that morning. Resume context belongs in
[HANDOVER.md](HANDOVER.md), which is rewritten to match reality on every wake.
