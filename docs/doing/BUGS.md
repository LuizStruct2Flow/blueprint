# Bugs — active (being implemented)

Bugs currently being worked. Pushed bugs move to
`docs/waiting-acceptance/BUGS.md`; founder-accepted bugs move to
`docs/done/BUGS.md`. See [README.md](README.md) for the lifecycle.

**Keep rows to one line.** Link out for the detail. A row that grows into a
paragraph belongs in a `PLAN-*.md` or a work-item folder — a table cell
holding half a page is unreadable, which is how this file stopped being
useful once already.

## Two namespaces, and only one of them is a work item

**`BUG-XXX` / `FEATURE-XXX` / `TASK-XXX` are the lifecycle IDs.** They are what the
commit convention, the regression-test naming rule and these lifecycle folders
key off (CLAUDE.md §"Bug Management", §"Team Workflow").

**`A-NN` is not a work item.** Those are findings from one audit — the
2026-07-23 contamination sweep in
[BLUEPRINT-AUDIT-2026-07-23.md](../config/BLUEPRINT-AUDIT-2026-07-23.md) — in the same
category as a Codex finding ID. A finding is a *claim that something is wrong*;
it becomes work when it gets a `BUG-`/`FEATURE-` number and a row here.

They were being used as though they were work items — folder names, rows in this
table, gate comments — and then extended with new numbers (A-38, A-39) for
findings that had nothing to do with that audit. Live items were renumbered on
2026-07-30. The audit document and everything in `done/` keep their `A-NN` IDs as
historical provenance, because the Codex review documents argue about findings by
those names and renaming them would break the trail they exist to be.

**Rule going forward:** an `A-NN` reference is a citation of history. If you are
about to work on something, give it a `BUG-`/`FEATURE-` number first.

| # | Bug | Severity | Status | Detail |
|---|---|---|---|---|
| **BUG-153** | **Reproducer: required.** **`tests/subagent-feed` #16 goes red on CI about one run in N: two callers reclaim one stale feed slot.** | S2 | OPEN | **Observed 2026-09-24 on `1d41617`**, a documentation-only commit — so nothing in the push could have caused it. The assertion is the one Codex's 2026-09-16 review added: *"two callers own one slot — the stale sweep deleted the other's fresh reservation and took the name for itself: expected 2 to be less than or equal to 1"*, naming `scripts/log-activity.sh`. The SAME commit went green on a re-run of the same job, with nothing else changed, so this is a race that manifests under CI's 4-core contention and not on this 32-core host — the BUG-133 shape, and `taskset -c 0-3` is the documented way to reproduce that class locally. **Two candidates, and the row must not pretend to know which:** (a) the 2026-09-16 fix narrowed the select-then-remove window without closing it, so the production cap really is undone under load; or (b) the test's own two-caller orchestration is timing-sensitive and the cap holds. (a) is a real defect in the feed's cap; (b) is a red `main` that teaches everyone to re-run, which is the BUG-031 failure. Diagnose before fixing. **Cost to be aware of:** `scripts/log-activity.sh` is legacy shell, so a production-side fix is a whole-file `.mts` port behind a shim (TASK-067) — which is a reason to diagnose carefully, never a reason to call a green re-run the answer. **No regression test:** the failing case IS the reproducer; what is missing is a way to run it under contention deterministically, which is the first thing the fix must deliver. **Re-open if** `main` goes red on a suite that passes on a re-run with nothing changed. |
| **BUG-152** | **Reproducer: required.** **Launching a worktree-isolated agent rewrites `core.hooksPath` to an absolute path, and the wake report then says the pre-push gate is NOT active.** | S3 | OPEN | **Observed twice on 2026-09-23**, both times immediately after a Claude Code agent was launched with `isolation: "worktree"`: `git config --local core.hooksPath` came back `/home/luiz/dev/struct2flow/blueprint/.githooks` instead of `.githooks`, and `scripts/session-start.sh` reported *"the struct2flow pre-push gate is NOT active"*. **The hooks still fire** — the absolute path resolves to the same directory, and the gate ran 67 stages on the pushes that followed — so today this is a false alarm. It is worth fixing for two reasons: a wake report that cries wolf is one nobody reads (the BUG-031 argument about a red CI everyone merges over), and the absolute path is wrong for any other worktree or clone, where `.githooks` would resolve locally and the absolute one names a tree that may not exist. **Cause:** the worktree feature must make the path work from a linked worktree, and the local config is shared across them, so it absolutises. `arm_gate` (`scripts/lib/gate.sh`) compares the value literally and, finding something other than `.githooks`, leaves it alone and declares the gate inactive. **Fix:** accept an absolute path that resolves to this repository's own `.githooks`, and keep refusing anything else. **Cost:** `scripts/lib/gate.sh` is legacy shell, so TASK-067 makes this a whole-file `.mts` port behind a shim before the one-line change — which is why it is filed rather than patched. **Workaround meanwhile:** `git config --local core.hooksPath .githooks` after a worktree agent runs; read the wake report and never push past that line without checking. **No regression test:** until the port that carries the fix names its entry point; then a case that arms the gate from an absolute path resolving to this repo's own `.githooks` and expects ARMED, plus one from a foreign absolute path that must still be refused. **Re-open if** a wake report calls the gate inactive while the hooks are in fact running. |
| **BUG-151** | **Reproducer: required.** **A roster tier resolves to a model the account cannot use, and every Codex dispatch dies before it starts.** | S2 | OPEN | **Observed live 2026-09-23 10:14Z.** Elias was dispatched a review; `codex exec` refused in 3 seconds: `The 'gpt-6-luna' model is not supported when using Codex with ...`, and the launcher logged `FAILED (exit 1)`. His roster cell is unchanged (`frontier-2:medium`), and the SAME cell resolved to `gpt-5.6-terra` yesterday, which worked all day. **Cause:** `bp_roster_model_for_name` maps a tier onto the provider's advertised model list, which is dynamic — Codex's list changed today, so `frontier-2` now names a model this subscription cannot run. The roster says WHICH RANK to use, the provider says what exists, and nothing checks that the rank the roster lands on is usable here. A cached list makes it worse: two resolutions minutes apart gave different answers (`gpt-5.6-terra`, then `gpt-6-luna`) as the cache refreshed. **Consequence:** every Codex persona is undispatchable while this holds, and it is silent until a dispatch dies. With Kimi at its 5-hour quota and Gemini at its daily free-tier cap, this left NO cross-provider reviewer available, which is the collision AGENTS.md §"Who does the work" says to hold on rather than work around. **Fix direction, to be designed:** either verify usability at resolution time (ask the CLI what it will actually accept, and fall to the next rank when it refuses), or let a roster cell name a model outright rather than a rank, or record the refusal the way TASK-065(c) plans for quota, so the provider leaves the rotation with a stated reason instead of failing every dispatch. **Measured 2026-09-23, and worse than "the list moved overnight": the mapping is unstable WITHIN a session.** Three readings minutes apart: `frontier-2` was `gpt-6-luna`, then `gpt-6-sol`, then `gpt-5.6-sol`; `frontier-5` was `gpt-5.6-terra`, then `gpt-5.5`. Probing the account directly (one tiny `codex exec` per slug) also refuted the guess that the whole `gpt-6` family is gated: `gpt-6-astra` **usable**, `gpt-6-sol` and `gpt-6-luna` **REFUSED**, `gpt-5.6-sol`/`-terra`/`-luna` **usable**. So specific models are gated for this account, and the roster points at whichever one holds a rank at that instant. After the probes, both refused slugs vanished from the CLI's advertised list altogether. **Stopgap applied 2026-09-23 (founder's call), in his gitignored AGENT_ROSTER.md only:** every Codex persona moved off the tiers that were resolving to refused models. He chose "the tier that means gpt-5.6-terra"; by the time it was applied that tier meant something else, which is the bug restated. The personas sit on `frontier-3`, and a live roll-call confirmed Elias dispatching on `gpt-5.6-luna` and handing the mic back. **A rank pin is not a fix and cannot be one.** **Queued behind BUG-147** (founder, 2026-09-23): the real fix lands in `scripts/lib/roster.sh`, a sourced legacy library of the same shape, so it follows the adapter pattern BUG-147's port proves first. Design: [PLAN-BUG-151-model-resolution.md](PLAN-BUG-151-model-resolution.md). **No regression test:** until the fix names its mechanism. **Re-open if** a dispatch dies because the roster resolved a model the provider will not run. |
| **BUG-150** | **Reproducer: required.** **The stranded-mic recovery matches on Holder AND Task, but an agent rewrites the Task when it claims ACTIVE — so the recovery never fires for the common failure.** | S2 | OPEN | **Observed live 2026-09-23 09:54Z**, the first failure after BUG-144 was accepted. Thomas (Kimi) was dispatched BUG-148, claimed the mic with `State=ACTIVE` and `Task=Fixing BUG-148 (CI red...)` — his own summary, replacing the dispatch text — and `kimi -p` then died on the 5-hour quota. `recoverStrandedMic` compared the baton against the Task it had DISPATCHED, saw a different one, concluded this was no longer its dispatch, and returned silently: the watcher log shows `kimi FAILED (exit 1)` and then nothing at all. The mic stayed at `Holder=Thomas State=ACTIVE` until the Orchestrator took it back by hand. **The wrong assumption is stated in the code**: `scripts/signal-watch.mts`'s docblock says "a well-behaved agent claims ACTIVE without touching Task". It does not. Every dispatch brief tells the agent to claim ACTIVE, and agents write their own summary when they do — Andreas (Codex) did the same on 2026-09-22 (`Task=TASK-070 round 2: extend backlog marker enforcement...`). So the Task match, added in BUG-144 round 3 to stop a genuine race (a NEW dispatch to the same Holder in the same State being clobbered), defeats the ACTIVE recovery it was meant to keep. **Why this is a new bug and not BUG-144 reopened:** BUG-144's causes were the roster path and the shell. This is the identity rule itself — Holder+Task cannot identify a dispatch whose Task the holder is expected to rewrite, and Holder+State alone cannot tell a new round from a stranded one. Needs a dispatch identity that neither party rewrites: `logs/state/signal-history.log` records every flip, so "has anything been dispatched since mine" is answerable without guessing from the Task text. Design it before writing it. **No regression test:** until the fix names its mechanism; then a case where the stub claims ACTIVE with a CHANGED Task and dies, which today passes wrongly. **Re-open if** a stranded mic is left unrecovered because the holder edited the Task. |
| **BUG-147** | **Reproducer: required.** **The DoD gate counts only `*.spec.ts`/`*.spec.tsx` as a bug's regression test, on every searched root, so a JavaScript project can never satisfy it.** | S2 | OPEN | **From storm2flow, 2026-09-22:** its BUG-216 fix landed with a real, running regression test, `frontend/src/configLoader.bug216.spec.js`, whose `it()` title names the bug. `dod_stage_bugtests` (`scripts/lib/dod-gate.sh`) still refused the push with "No test under the searched roots names: BUG-216", because its extension filter applies to a project's own declared `BP_TEST_ROOTS` too. Those roots are run by the project's own runner, never by the blueprint's `tests/vitest.config.ts`. **A reproducer and a fix already exist,** written by storm2flow's Matthias directly in this checkout, and are kept on local branch `storm2flow/bug147` (`58ff781` reproducer, `35d902d` fix). They were taken off `main` because they collided with the Orchestrator's unpushed work, and because the fix edits `scripts/lib/dod-gate.sh` in place, which TASK-067's gate refuses. **Founder, 2026-09-22: "port it."** No exception; `scripts/lib/dod-gate.sh` is ported whole under TASK-067 before the fix lands. **The design question that must be settled first:** the file is SOURCED, by `.githooks/pre-push-project` and by `.github/workflows/security.yml`, so a two-line shim cannot serve it — a shim execs, and an exec cannot hand shell functions back to the caller that sourced it. The port needs a calling boundary (a CLI its shell callers invoke), designed and reviewed before any translation starts. **More evidence from storm2flow (Sylvia, 2026-09-22):** its frontend vitest includes only `src/**/*.spec.js(x)`, so TypeScript was never an option there, and two further bugs tested only in `.spec.js` are latent blocks of the same shape (BUG-204 and BUG-137, both in `frontend/src/build.spec.js`). **A workaround is live there and must be undone when this lands:** that one spec was renamed `.spec.ts` and `frontend/vite.config.mjs` also includes `src/**/*.spec.ts`, both commented as BUG-147. storm2flow holds a row to revert it, so tell Sylvia when the fix ships. **No regression test:** not on `main` yet. Its reproducer exists, `tests/dod-gate` #11c on branch `storm2flow/bug147`, and lands with the fix once the founder rules. **Re-open if** a declared project root's JavaScript spec whose title names a bug is refused. |
| **BUG-146** | **Reproducer: required.** **`tests/sync-by-address` #20d (BUG-120) intermittently hangs about 320 s in CI, turning `main` red on pushes that changed nothing it tests.** | S2 | OPEN | **Observed twice:** CI run 35596083384 on `61cfe01` (2026-09-21, a docs-only push, unnoticed then because the Orchestrator did not wait for its CI) and run 35733083948 on `374a8d9` (2026-09-22, also docs-only). Both times "#20d BUG-120: a TERM that reaches the held refresh child with its release is not lost — no fetch, no wait for the budget" ran 320019-320024 ms and failed. That takes `bootstrap-gate` #2/#3 down with it, because a fresh bootstrap runs the same suite in its own gate. A rerun of the failed job passed with no change. The test exists to prove a TERM is NEVER lost, and a hang is exactly what a lost TERM looks like. So either the test's synchronisation races on a slow runner, or BUG-120's fix loses the signal in a window the test sometimes hits. **The first job is to decide which, from evidence:** reproduce under CI-like load (`taskset -c 0-3`, see HANDOVER §0 item 3) in a loop until it hangs, then read what the held refresh child and its parent were doing. A flaky gate test is a defect whichever side it is on: it trains everyone to rerun until green, which is the BUG-031 failure. **Diagnosis attempted 2026-09-22 (Philipp), inconclusive:** 162 of 162 local runs were clean, 150 of #20d alone under `taskset -c 0-3` plus pinned busy-loops and 12 of the full suite (which nests bootstrap-gate). The hang happened at both levels in CI (outer on `61cfe01`, nested on `374a8d9`) and at neither here. A local full suite takes about 157 s, where CI takes over 340 s for the same work, so `taskset` does not reproduce this runner. No defect was found in BUG-120's token gate (`scripts/blueprint:672-704`) or in the test, and nothing was changed. **Next step:** capture evidence ON the next CI occurrence. When a scenario's wait times out, the harness dumps the process tree and wait channels as a CI artefact. **No regression test:** the test that catches this bug already exists, `tests/sync-by-address` #20d itself, and it does not reproduce locally. A BUG-146-titled copy would prove nothing more. **Founder, 2026-09-22: leave it.** The harness-side capture is not built; the row stays open and unattended until the next hang. **Seen again 2026-09-24 on `c7c47f6`**, a documentation-only push: #20d timed out at 320 s inside the nested `bootstrap-gate` run, failing `#2 and #3 a freshly bootstrapped project passes its own pre-push gate`; a re-run of the same job on the same commit went green. That is the THIRD CI occurrence (`61cfe01` outer, `374a8d9` nested, now `c7c47f6` nested) against zero local reproductions, and it means the row's own trigger has fired while the evidence capture it asks for is still unbuilt — so this occurrence produced no more information than the first two. The next decision is the founder's: build the harness-side process-tree dump, or accept that each occurrence costs a re-run and teaches nothing. **Re-open if** #20d hangs again on any run. |

**Do not narrate status here.** Which items are where is answered by the
folders: `doing/` is what is being implemented, `waiting-acceptance/` is what is
pushed and untested, `done/` is what the founder accepted. Prose repeating that
becomes a second record of one fact, and on 2026-08-03 every such line in this
file had gone false — including one telling the next session that a decision was
still pending on an item accepted that morning. Resume context belongs in
[HANDOVER.md](HANDOVER.md), which is rewritten to match reality on every wake.
