# Bugs — active (being implemented)

Bugs currently being worked. Pushed bugs move to
`docs/waiting-acceptance/BUGS.md`; founder-accepted bugs move to
`docs/done/BUGS.md`. See [README.md](README.md) for the lifecycle.

**Keep rows to one line.** Link out for the detail. A row that grows into a
paragraph belongs in a `PLAN-*.md` or a work-item folder — a table cell
holding half a page is unreadable, which is how this file stopped being
useful once already.

## Two namespaces, and only one of them is a work item

**`BUG-XXX` / `FEATURE-XXX` are the only lifecycle IDs.** They are what the
commit convention, the regression-test naming rule and these lifecycle folders
key off (CLAUDE.md §"Bug Management", §"Team Workflow").

**`A-NN` is not a work item.** Those are findings from one audit — the
2026-07-23 contamination sweep in
[BLUEPRINT-AUDIT-2026-07-23.md](BLUEPRINT-AUDIT-2026-07-23.md) — in the same
category as a Codex finding ID. A finding is a *claim that something is wrong*;
it becomes work when it gets a `BUG-`/`FEATURE-` number and a row here.

They were being used as though they were work items — folder names, rows in this
table, gate comments — and then extended with new numbers (A-38, A-39) for
findings that had nothing to do with that audit. Live items were renumbered on
2026-07-30. The audit document and everything in `done/` keep their `A-NN` IDs as
historical provenance, because the Codex review documents argue about findings by
those names and renaming them would break the trail they exist to be.

**Rule going forward:** an `A-NN` reference is a citation of history. If you are
about to work on something, give it a `BUG-`/`FEATURE-` number first.

| # | Bug | Severity | Status | Detail |
|---|---|---|---|---|
| **BUG-014** | A test suite writes commits and config into the repository under test, disarming the gate | S1 | **FIXED 2026-08-02** — reported by linkedin-watcher-agent ([PR #4](https://github.com/LuizStruct2Flow/blueprint/pull/4)), reproduced and fixed here | Git exports `GIT_DIR` to every hook. The pre-push gate runs the suites, so all of them inherit it — and thirteen build fixtures with `( cd "$WORK" && git init && git add . && git commit )`. **`cd` protects nothing**: with `GIT_DIR` set git ignores the working directory entirely and operates on the directory that variable names, so the fixture's commit lands in the REAL repository. Reproduced in seconds: `GIT_DIR=/tmp/victim/.git bash tests/marker-merge/test.sh` moved the victim's HEAD `43c2949 → 54a4d6f`. **This closes the open question on BUG-004** — that row records `.git/config` rewritten at 12:40:15 with `core.bare=true` and `core.hooksPath` wiped by "an unrelated process it could not identify". It was these suites. The suite that exists to prove the gate arms is what disarmed it, and the ungated push of `fdae0e2` follows directly. **Fix, two layers:** `.githooks/pre-push` strips `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE`/`GIT_OBJECT_DIRECTORY` once at the source (safe — git runs hooks from the repo top level, so discovery from cwd finds the same repository), and all 13 fixture-building suites strip them individually so a suite is safe run from any other hook context. Regression: [`tests/git-isolation/`](../../tests/git-isolation/test.sh), in the gate and CI. **A near-miss worth recording:** my first reproducer exported `GIT_DIR` *and* `GIT_WORK_TREE` and passed — setting both changes git's behaviour and the fixture stops landing in the victim, so the test would have gone green against unfixed code. Git hands a pre-push hook `GIT_DIR` alone. **Their original report, kept because it is evidence I could not have produced:** the defect surfaced when pushing a branch from a `git worktree` — `tests/marker-merge/test.sh:92` landed its fixture commit *on the branch being pushed*, reflog-confirmed six seconds after theirs, tree replaced, the push then failing with `path '.githooks/pre-push' exists on disk, but not in 'HEAD'`. They flagged the `GIT_DIR` attribution as inferred rather than proven; the reproduction here proves it. **This is also why [PR #3](https://github.com/LuizStruct2Flow/blueprint/pull/3) flagged its own worktree recipe as unsafe — that gap is this bug, and it is now closed.** |
| **BUG-004** | A fresh clone is ungated: `core.hooksPath` is repo-local, so cloning and pushing never invokes the gate | S1 | **HALF A DONE 2026-07-30. HALF B still needs a founder decision** | Reopened 2026-07-29 after QA-2 rejected it with a live reproduction: a fresh clone, a real high-entropy token committed, `origin` redirected to a throwaway bare repo, and a real push executed without ever running the feed or drift — `real_ungated_push_rc=0`, `secret_commit_reached_destination=yes`. `arm_gate` works; the acceptance boundary was never "the arming paths work" but "a human cannot clone and push without invoking them". **Not closable by another local hook** — a pre-push hook is repo-local, absent on a clone, and defeated by `--no-verify`. **SPLIT IN TWO on 2026-07-30 after a founder correction** (I had written this as one all-or-nothing trade against trunk-based development, which was wrong). **Half A — DONE 2026-07-30:** branch protection on `main` with `enforce_admins: false`, four required checks, and a PR required (0 approvals) for non-admins. Verified by probe: admin direct-push to `main` still works, so trunk-based is untouched. **Its real value is narrower than I first said, and the narrower claim is the honest one** — the repo is public and user-owned, so outsiders already could not push (no write access; GitHub forces fork+PR). What this actually buys: (1) a PR **cannot merge red**, which is what puts FEATURE-001's contamination scan server-side on a2bp request PRs where a requester cannot skip it, and (2) any future write grant — collaborator, derived-project token — is constrained. **Half B — the owner's own fresh clone** is BUG-004 as reproduced, and Half A deliberately does not close it; levers are accept-explicitly, detect-via-CI (already runs on push to `main`; adequate for tests, **weak for secrets: the repo is PUBLIC, so a pushed secret is world-readable before gitleaks starts — CI's answer is "rotate", never "that did not happen"**), or drop the bypass (the only option that actually costs trunk-based). **New evidence 2026-08-02 — arming is REVERSIBLE, and silently.** Something on this host rewrote `.git/config` at 12:40:15, setting `core.bare = true` and wiping `core.hooksPath`. Git then refused every operation (`fatal: this operation must be run in a work tree`) until `core.bare` was unset — but the wiped `hooksPath` was NOT noticed, because nothing reports it at push time. The push of `fdae0e2` at ~13:02 therefore went out with the gate **not running**; the next `agent-activity.sh --daemon` re-armed it and printed `(was unset)`, which is the only reason it surfaced at all. Damage bounded: it was docs-only and the 4 required CI checks passed on that SHA. This is Half B's argument from a new direction — the local gate is not merely absent on a fresh clone, it can be *disarmed underneath a working checkout* by an unrelated process, and the push that follows looks identical to a gated one. Whatever wrote that config is unidentified; a concurrent session was active in this repo. Folder: [BUG-004-gate-arming/](BUG-004-gate-arming/). Was `A-22`; the server-side option was `A-37`. |
| **BUG-008** | `blueprint pull` strips the executable bit, so a pulled hook is armed but never runs | S1 | **REOPENED 2026-07-31 — the fix does not hold. Reproduced end-to-end at HEAD `8abb406`** | The marker-aware merge writes to a `mktemp` (mode 600) and `mv`s it into place; `mv` carries the temp's mode onto the destination. **Silent and severe:** `core.hooksPath` is set, so the gate looks armed, but git skips a non-executable hook without a word — BUG-004's failure mode through a different door. The 2026-07-30 fix added `_bp_sync_exec_bit` to all four write paths — **but every one of them calls `substitute_placeholders` on the very next line, and `bp_substitute_in_place` ([lib/placeholders.sh:190-199](../../scripts/lib/placeholders.sh#L190-L199)) does its OWN `mktemp`(600) + `mv`.** The chmod is undone by the next statement, so the fix repaired the caller and left the primitive. Affects any managed file that is both executable and placeholder-bearing, minus the `_should_substitute` exemptions (`scripts/blueprint`, `scripts/new-project.sh`): **`.githooks/pre-push`** — the gate itself — plus `scripts/start-codex-signal-watch.sh` and `scripts/start-gemini-signal-watch.sh`, all three verified 775 + placeholder-bearing. **Reproducer:** a temp project with a 755 `.githooks/pre-push`, then `blueprint pull --yes .githooks/pre-push` → **600, not executable**. Surfaced by the linkedin-watcher-agent session, which verified the primitive and call order but explicitly flagged the end-to-end path as unconfirmed; confirmed here. Mitigation that masked it: `gate.sh` refuses to arm on a non-executable hook, and `cp` onto an existing file preserves that file's mode, so a project already at 755 via a different path looks healthy. Fix direction: preserve the destination mode inside `bp_substitute_in_place` (`chmod --reference` before the `mv`) — repair the primitive, not the four call sites, since `mktemp`+`mv` mode loss resurfaces wherever else the pattern is used. **The missing regression test is why this shipped as "fixed".** |
| **BUG-009** | `project_config_*.md` are simultaneously this repo's own config AND the seed template, so blueprint-specific content propagates to every new project | S2 | Mitigated 2026-07-30, structure unfixed | A hard-coded wake-time Monitor row for the agent-exchange board sat in `project_config_paths.md` — including a rationale describing an incident that happened in *this* stream — and was seeded verbatim into linkedin-watcher-agent. Same class as BUG-002: one project's specifics in a file that travels. **Mitigation:** the Monitors table now ships empty with guidance, and this stream's own row moved to `HANDOVER.md` (project-owned, never synced). **Structural fix not done:** the template and this repo's own config are still the same file, so the next concrete thing written there propagates again. Real fix is a separate template source (e.g. `templates/project_config_*.md`) that `new-project.sh` seeds from, letting this repo keep its own live config. |
| **BUG-006** | `LWA_FEED_*` env-var namespace in `scripts/log-activity.sh` — BUG-002's contamination in env-var form | S3 | NEXT UP, not started | Same class as BUG-002: a name specific to one project baked into a generic script. Detail in [BLUEPRINT-AUDIT-2026-07-23.md](BLUEPRINT-AUDIT-2026-07-23.md). Was `A-08`. |
| **BUG-011** | `blueprint a2bp` pushes the branch, opens no PR, prints a green ✓ and exits 3 ("filed") | S1 | Found 2026-07-31 from linkedin-watcher-agent, root-caused + reproduced here, not fixed | `bp_file_existing_pr` ([lib/request-file.sh:115-120](../../scripts/lib/request-file.sh#L115-L120)) runs `gh pr list … --jq '.[0] \| "\(.state)\t\(.url)"'`. On an empty list `.[0]` is `null`, and jq interpolates that as the **literal string** `null`, so the function prints `null\tnull` instead of nothing — verified: `echo '[]' \| jq -r '.[0] \| "\(.state)\t\(.url)"'` → `null^Inull`. The caller ([scripts/blueprint:1116](../../scripts/blueprint#L1116)) guards with `[ -n "$existing" ] && [ "$existing" != "<tab>" ]` — it anticipated empty fields but not the `null` literal — so a non-existent PR passes as an existing one, falls to the `*)` branch, prints `✓ request already open: null` and returns `BP_RC_PENDING` (3). **Worse than a silent failure: the exit code asserts filed while nothing was filed**, which is precisely what CLAUDE.md forbids ("no script may read 'PR opened' as 'the blueprint has this'"). It breaks the only sanctioned path for improvements to reach the blueprint. Hit both of that session's requests; the branches and commits were correct, only the PR step failed, and both PRs were opened by hand ([#1](https://github.com/LuizStruct2Flow/blueprint/pull/1), [#2](https://github.com/LuizStruct2Flow/blueprint/pull/2)). Fix direction: treat `null`/empty as "no PR"; only return 3 once a PR URL is actually in hand; branch-pushed-but-no-PR is exit 5 (operational failure), never 3. **Folded in 2026-08-02 (founder decision) — the trust half of the same defect.** Filing a request needs push access to the blueprint remote, and in the same-owner setup every agent authenticates as the owner: verified `enforce_admins: false`, `restrictions: null`, effective permission **admin**, so branch protection's PR requirement does not apply to the identity the agents present. A derived project's agent that runs plain `git push` reaches `main` directly — evidenced by every push this session printing `remote: Bypassed rule violations for refs/heads/main`. So "a2bp cannot write into the blueprint" was an overclaim: the discipline is a convention `a2bp` implements, not a boundary the repository enforces. This bug is what makes it bite today — the branch is pushed, the PR never opens, and exit 3 reports "filed", so the review step is skipped while success is asserted. **Not fixable with repository settings**: no ruleset can distinguish a derived project's agent from the owner while both present the same credential; it needs a separate narrower credential (or a fork), which is independent of `enforce_admins` and so does NOT cost trunk-based development. **Founder decision 2026-08-02: accept the model, document it honestly** — done in CLAUDE.md, README.md, docs/A2BP_PLAYBOOK.md and project_config_paths.md §"Back-propagation trust boundary". The code fix for the `null` PR remains open. |
| **BUG-012** | `new-project.sh` writes an absolute `blueprint_source`, so a derived project breaks the moment either checkout moves | S3 | Found + fixed 2026-08-02, reproducer first | [`scripts/new-project.sh:189`](../../scripts/new-project.sh#L189) wrote `blueprint_source = $BLUEPRINT_ROOT`, an absolute host path — the one field in `.blueprint-source` that cannot be right on two machines at once. **storm2flow is the observed instance:** it carried a `/Users/…` path onto a Linux box and every blueprint command died on it ([HANDOVER.md:89](HANDOVER.md#L89)). BUG-007 answered this with a three-step *resolution ladder* (`$BLUEPRINT_ROOT` → file → the CLI's own repo) rather than fixing what bootstrap writes; the ladder's step 3 is itself unsound (see the derived-project misdetection). Fix is to write a path relative to the project root, which costs nothing because `read_blueprint_source` already requires cwd to be the project root (it greps `./.blueprint-source`). Relative pins the LAYOUT — blueprint beside project — which is what bootstrap actually knows. Pure-bash relativiser, not `realpath --relative-to`: that flag is GNU-only and macOS is exactly the platform the storm2flow incident came from. Pinned by `tests/bootstrap-contents/` §8. Raised from linkedin-watcher-agent by the founder. |
| **BUG-013** | `blueprint drift` tells every derived project "This IS the blueprint" and reports zero drift | S1 | Found 2026-08-02 from linkedin-watcher-agent, reproduced there and here, not fixed | `_bp_is_blueprint_itself` ([scripts/blueprint:646-658](../../scripts/blueprint#L646-L658)) identifies the blueprint by "the CLI being executed lives in the repo you are standing in", verified with `CLAUDE.md` + `docs/DoD.md` + `scripts/blueprint`. **Every one of those is true in every derived project** — `scripts/blueprint` is itself in `MANAGED_FILES`, so each project ships a copy at exactly that path. `bash scripts/blueprint drift` — the invocation CLAUDE.md documents as the fallback when `blueprint` is not on PATH, and the one a fresh clone and every spawned persona use — therefore short-circuits at [:665](../../scripts/blueprint#L665), prints `✓ This IS the blueprint`, and **exits 0 having compared nothing**. The wake-mandated drift check becomes a silent no-op. **Not theoretical:** linkedin-watcher-agent reported clean all morning while **18 commits and 4 required new files behind**, including `scripts/lib/pipeline.sh`, without which the pulled `.githooks/pre-push` fails closed. It surfaced only when the blueprint's own copy of the CLI was invoked instead. **Second site, same root cause:** `_bp_resolve_blueprint_root` step 3 ([:435](../../scripts/blueprint#L435)) validates its fallback with the same always-true test, so a project whose `blueprint_source` fails to resolve silently adopts **itself** as the comparison base and reports clean forever — the exact outcome the comment above it says it is avoiding ("a silently-substituted comparison base would make every drift report untrustworthy"). Fix direction: the signal that actually separates them is `.blueprint-source`, which the blueprint does not have and every registered project does. The existing comment rejects it as a *sole* signal, correctly — but as an additional conjunct it is sound, because a config-less derived project should fall through to the existing "unregistered struct2flow project" path, which is the right thing to tell it. `tests/drift-in-blueprint/` exists and passes; it only ever exercises the blueprint side, which is why nothing caught this. |
| **BUG-015** | Six libs the CLI requires are absent from `MANAGED_FILES`, so `a2bp` cannot run in any derived project | S2 | Found 2026-07-31 from linkedin-watcher-agent, still present at HEAD `344656b` | `scripts/lib/staleness.sh` and `request.sh` / `request-build.sh` / `request-config.sh` / `request-file.sh` / `request-inputs.sh` are sourced by the CLI ([:545](../../scripts/blueprint#L545), [:888](../../scripts/blueprint#L888), [:1176](../../scripts/blueprint#L1176)) but appear nowhere in `MANAGED_FILES`, so `blueprint pull` never delivers them. Consequences in every derived project: `blueprint a2bp` dies at `scripts/lib/request-config.sh is missing`, and `drift` reports `remote staleness: unknown`, losing the one check that tells you the comparison base is itself behind. Verified in linkedin-watcher-agent both before and **after** a full sync to HEAD `344656b` — a complete pull does not fix it, because the files are not managed. **This is the same fail-closed reasoning already written into the list for `contamination.sh`** ("a derived project missing this file cannot back-propagate at all — which is the intended fail-closed behaviour, but only useful if the file travels"): the intent is stated and the file does not travel. It also compounds BUG-011 — the sanctioned upstream path is doubly unavailable from a derived project, once because the libs are missing and once because filing reports success without opening a PR. Fix: add the six to `MANAGED_FILES` with the same MUST-ship rationale comments the neighbouring entries carry. |
| **BUG-016** | Pulling ONE file bumps `bootstrap_sha` to blueprint HEAD, so drift then reports the project as fully synced | S3 | Found 2026-08-02 from linkedin-watcher-agent, not fixed | `blueprint pull scripts/blueprint --yes` — a deliberate single-file pull — ended with `Updated .blueprint-source bootstrap_sha → 3d09ba1…`, the blueprint's HEAD. `bootstrap_sha` is what `drift` measures "commits since sync" from, so after any single-file pull the commit-log half of the report reads **empty** while every other managed file is still behind. The file-by-file half still works, which is the only reason the gap stayed visible at all. Observed in a session that pulled one file and was afterwards told it was level with a HEAD it had adopted almost nothing from. Fix direction: only advance `bootstrap_sha` when the pull covered every drifted file (or record it per-file), and say which of the two happened in the output. |
| **BUG-017** | `pull`'s confirmation diff is rendered BEFORE substitution, so the diff you approve is not the content that lands | S3 | Found 2026-08-02 from linkedin-watcher-agent, not fixed | The per-file preview diffs the project's file against the raw blueprint temp, but `substitute_placeholders` runs *after* the write. So the operator is shown `{{PROJECT_NAME}}` being written **into** their files — e.g. `-JSON lines to ~/.linkedin-watcher-agent/logs/…` / `+JSON lines to ~/.{{PROJECT_NAME}}/logs/…` — and approves a change that is the reverse of what happens. Verified benign in outcome (0 literal placeholders after the pull) but not benign in effect: it trains operators to approve diffs they have read as wrong, and it is indistinguishable from the genuine de-substitution failure it imitates. In this session it directly caused a false alarm and a detour to disprove it. Fix: substitute into the temp before rendering the preview, so preview and result are the same bytes. |
| **BUG-018** | Interactive `pull` dies on `/dev/tty` in a non-interactive session, while `drift` handles the same case gracefully | S3 | Found 2026-08-02 from linkedin-watcher-agent, not fixed | `blueprint pull <file>` without `--yes` reaches its prompt and aborts with `scripts/blueprint:820: /dev/tty: No such device or address` — after printing the diff, so the operator sees a proposal and then a crash. Any agent session, CI job, or `nohup` run hits this. `drift` already solves exactly this: `tests/staleness/drift-integration.sh` case #1 pins "no TTY: warns, offers a copy-pasteable command, never prompts". `pull` should degrade the same way — detect the absent TTY, print the `--yes` invocation, and exit non-zero without having half-run. Same class as BUG-011's exit-code contract: a command must not report progress it did not make. |


BUG-002 moved to `waiting-acceptance/` on 2026-07-27; its blocker (audit finding
A-09) is fixed and pushed.

**BUG-004 still needs a decision before it needs code.** Do not attempt another
local mechanism — that is exactly what the rejection ruled out. (BUG-005 was
decided on 2026-08-02 and is in `waiting-acceptance/`.)

## Waiting acceptance

**FEATURE-001** — back-propagation becomes a request, not a write — was pushed on
2026-07-30 and sits in
[`../waiting-acceptance/FEATURE-001-a2bp-pr/`](../waiting-acceptance/FEATURE-001-a2bp-pr/)
with its plan and review trail. BUG-005 came out of building it.

Everything else delivered on 2026-07-29 is ACCEPTED and in
[`../done/`](../done/) — eight items, verdicts and evidence in
[`../done/ACCEPTANCE-JESKO-2026-07-29.md`](../done/ACCEPTANCE-JESKO-2026-07-29.md).
