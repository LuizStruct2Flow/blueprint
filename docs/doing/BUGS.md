# Bugs — active (being implemented)

Bugs currently being worked. Pushed bugs move to
`docs/waiting-acceptance/BUGS.md`; founder-accepted bugs move to
`docs/done/BUGS.md`. See [README.md](README.md) for the lifecycle.

**Keep rows to one line.** Link out for the detail. A row that grows into a
paragraph belongs in a `PLAN-*.md` or a work-item folder — a table cell
holding half a page is unreadable, which is how this file stopped being
useful once already.

## Two namespaces, and only one of them is a work item

**`BUG-XXX` / `FEATURE-XXX` / `TASK-XXX` are the lifecycle IDs.** They are what the
commit convention, the regression-test naming rule and these lifecycle folders
key off (CLAUDE.md §"Bug Management", §"Team Workflow").

**`A-NN` is not a work item.** Those are findings from one audit — the
2026-07-23 contamination sweep in
[BLUEPRINT-AUDIT-2026-07-23.md](../config/BLUEPRINT-AUDIT-2026-07-23.md) — in the same
category as a Codex finding ID. A finding is a *claim that something is wrong*;
it becomes work when it gets a `BUG-`/`FEATURE-` number and a row here.

They were being used as though they were work items — folder names, rows in this
table, gate comments — and then extended with new numbers (A-38, A-39) for
findings that had nothing to do with that audit. Live items were renumbered on
2026-07-30. The audit document and everything in `done/` keep their `A-NN` IDs as
historical provenance, because the Codex review documents argue about findings by
those names and renaming them would break the trail they exist to be.

**Rule going forward:** an `A-NN` reference is a citation of history. If you are
about to work on something, give it a `BUG-`/`FEATURE-` number first.

| # | Bug | Severity | Status | Detail |
|---|---|---|---|---|
| **BUG-152** | **Reproducer: required.** **Launching a worktree-isolated agent rewrites `core.hooksPath` to an absolute path, and the wake report then says the pre-push gate is NOT active.** | S3 | OPEN | **Observed twice on 2026-09-23**, both times immediately after a Claude Code agent was launched with `isolation: "worktree"`: `git config --local core.hooksPath` came back `/home/luiz/dev/struct2flow/blueprint/.githooks` instead of `.githooks`, and `scripts/session-start.sh` reported *"the struct2flow pre-push gate is NOT active"*. **The hooks still fire** — the absolute path resolves to the same directory, and the gate ran 67 stages on the pushes that followed — so today this is a false alarm. It is worth fixing for two reasons: a wake report that cries wolf is one nobody reads (the BUG-031 argument about a red CI everyone merges over), and the absolute path is wrong for any other worktree or clone, where `.githooks` would resolve locally and the absolute one names a tree that may not exist. **Cause:** the worktree feature must make the path work from a linked worktree, and the local config is shared across them, so it absolutises. `arm_gate` (`scripts/lib/gate.sh`) compares the value literally and, finding something other than `.githooks`, leaves it alone and declares the gate inactive. **Fix:** accept an absolute path that resolves to this repository's own `.githooks`, and keep refusing anything else. **Cost:** `scripts/lib/gate.sh` is legacy shell, so TASK-067 makes this a whole-file `.mts` port behind a shim before the one-line change — which is why it is filed rather than patched. **Workaround meanwhile:** `git config --local core.hooksPath .githooks` after a worktree agent runs; read the wake report and never push past that line without checking. **No regression test:** until the port that carries the fix names its entry point; then a case that arms the gate from an absolute path resolving to this repo's own `.githooks` and expects ARMED, plus one from a foreign absolute path that must still be refused. **Re-open if** a wake report calls the gate inactive while the hooks are in fact running. |
| **BUG-155** | **Reproducer: required.** **The contamination scanner reads `${VAR:-$HOME/.codex}` as the dotdir `codex}` and blocks a directory it already knows is benign.** | S3 | OPEN | **Observed 2026-09-25** in CI on `4a2b7e2`: the `contamination scan (pushed diff)` job blocked `scripts/start-codex-signal-watch.mts:206` (`$HOME/.codex}`) as a "literal per-project state dir", although `codex` is on `_CONTAMINATION_KNOWN_DOTDIRS` (`scripts/lib/contamination.sh:119`). **Cause:** the extracting regex at `scripts/lib/contamination.sh:451` allows `{` and `}` in the name, so the shell default-value form `${CODEX_HOME:-$HOME/.codex}` yields `codex}`, which misses the known list. `main` went red and was unblocked by marking the lines `a2bp-allow` (`88840f9`), which is a workaround for the scanner's misreading, not a justified exception. The same launchers name `~/.kimi-code`, which is not on the known list at all, so it would still need a decision after the fix. **Scoped 2026-09-25 (Markus):** `contamination.sh` is legacy (477 lines) and SOURCED by two callers, `scripts/blueprint`'s `cmd_a2bp` and `scripts/contamination-push-scan.mts`. **Queued behind TASK-081**: once `scripts/blueprint` is `.mts`, both callers import a `contamination.mts` natively and the old path becomes the ordinary exec shim — no sourced adapter, no `shell-inventory-check.mts` extension (porting first would cost a full BUG-147). The port must not spell `{{PROJECT_NAME}}` literally (`placeholders.sh:90` exempts only the `.sh`; same answer as PLAN-TASK-081's Option 1). **Fix, after a behaviour-identical port:** (1) the regex allows braces only as a whole placeholder, `(\$HOME|~)/\.(\{\{[A-Za-z0-9_]+\}\}|[A-Za-z0-9_][A-Za-z0-9_.-]*)`, keeping `~/.{{PROJECT_NAME}}` visible (the A-09 defect; `tests/a2bp-contamination` #10/#11 pin both sides); (2) `kimi-code` joins `_CONTAMINATION_KNOWN_DOTDIRS` — the Kimi CLI's own home, the same class as `codex`/`gemini`, missing only because Kimi joined after the list was written; (3) the `88840f9` `a2bp-allow` markers are removed, since "the scanner misread" is not a threat-model entry. Latent today: 23 sites (`roster.sh`, `codex-session.sh`, `scripts/blueprint:840`, the launchers, `AGENTS.md:446`) go red the next time their line is touched; mark them the same way meanwhile. **Reproducer:** a line `X="${CODEX_HOME:-$HOME/.codex}"` in a shipped file must not BLOCK, and `~/.kimi-code` must not BLOCK. **No regression test:** until the reproducer lands. **Re-open if** a known dotdir inside `${…}` blocks a push. |
| **BUG-156** | **Reproducer: required.** **[SEC] The shipped `.gitleaks.toml` declares an allowlist and no rules, so every secret scan that loads it — the pre-push gate and CI, here and in every derived project — detects nothing.** | S1 | OPEN | **Reported 2026-09-18 by storm2flow as [PR #76](https://github.com/LuizStruct2Flow/blueprint/pull/76); confirmed here 2026-09-25.** gitleaks replaces its whole rule set with the config it loads, and it auto-discovers `.gitleaks.toml` in the scanned root, so no `-c` is needed for this to bite. `.githooks/pre-push`'s `_st_gitleaks` runs `gitleaks detect --log-opts=<range>` from the root, and CI's `gitleaks/gitleaks-action` does the same. **Measured:** a planted synthetic `ghp_` token in `.scratch/` → `-c .gitleaks.toml` says *"no leaks found"*, default rules say *"leaks found: 1"*. The file's header also promised a `gitleaks protect --staged` safety net that `.githooks/pre-push:108` records was removed. **Exposure check, blueprint:** a full-history scan (1328 commits) with explicit default rules found 2 hits, both false positives — a git blob sha in `scripts/shell-inventory.json` (`generic-api-key`) and `curl -u "${SONAR_TOKEN}:"` in `scripts/sonar-api.sh` (`curl-auth-user`). **Nothing leaked here; nothing needs rotating.** **Fix:** `[extend] useDefault = true` (PR #76's change), plus allowlisting what the default rules now false-positive on in this tree — at least the blob shas in `scripts/shell-inventory.json`, which every legacy-shell change rewrites — so the restored gate is not immediately red. **Also decided with this row (founder accepted PR #76 including its open question, 2026-09-25):** the `(^|/)\.env\..*$` path allowlist also hides the TRACKED `.env.example`, where a pasted real key would be invisible; narrow it so `.env.example` is scanned. **Downstream:** after `blueprint pull`, each derived project runs one full-history scan with default rules, since its scan was vacuous since bootstrap. **Reproducer:** a test that runs the gate's gitleaks step with the REPO'S OWN `.gitleaks.toml` over a planted token and requires it to be found, and a planted token in a tracked `.env.example` found too. **No regression test:** until the reproducer lands. **Re-open if** a planted secret passes the gate's scan with the shipped config. |
| **BUG-157** | **Reproducer: required.** **`tests/reproducer-order` misreads every bug numbered below 100 and asserts the blueprint's own history size, so a derived project's suite goes red on its first bug fix.** | S2 | OPEN | **Reported 2026-09-25 by linkedin-watcher-agent as [PR #79](https://github.com/LuizStruct2Flow/blueprint/pull/79), superseded by [PR #82](https://github.com/LuizStruct2Flow/blueprint/pull/82).** Two defects in the suite TASK-077 shipped. **(1) Id padding:** commit subjects are unpadded (`BUG#37:`, CLAUDE.md's convention) and rows are padded (`**BUG-037**`); `classifySubject` builds `BUG-37` and `parseDeclarations` keys `BUG-037`, so the fix commit finds no row and lands in `unjudged`. Latent in the blueprint only because every bug fixed since TASK-077 is ≥100 (history has `BUG#83:` beside `**BUG-083**`); in a young project every bug is two-digit. **(2) Non-vacuity floors:** ≥50 `Reproducer: required.` rows and ≥30 reproducer commits are the blueprint's own counts, so a fresh project fails them by construction. **Fix (PR #82's):** one `bugId()` that pads both sides; the floors asserted only where `.blueprint-root` exists, with a visible skip note elsewhere — the order check itself still runs everywhere. **Reproducer:** `BUG#37:` reproducer + fix commits against a `**BUG-037**` row are checked, not unjudged; and the real-tree case run without `.blueprint-root` does not fail on the floors. **No regression test:** until the reproducer lands. **Re-open if** a derived project's first correct bug fix turns `reproducer-order` red. |
| **BUG-158** | **Reproducer: required.** **`tests/tsconfig.json` typechecks every `.ts` under `tests/`, including a derived project's own tests, whose root dependencies CI's ts-tests job never installs — so a derived project's CI is red on every push.** | S2 | OPEN | **Reported 2026-09-25 by linkedin-watcher-agent as [PR #81](https://github.com/LuizStruct2Flow/blueprint/pull/81).** `include: ["**/*.ts"]` reaches a project-owned `*.test.ts` in `tests/`, which imports `src/` and root packages. CI runs `npm ci` in `tests/` only, so tsc fails TS2307 there, while a host with root `node_modules` passes the local gate — green locally, red in CI. **Fix (PR #81's):** include `**/*.spec.ts`, `**/*.spec.tsx` and `vitest.config.ts`, reaching harness and helper modules by import; the project's own tests are the project's root tsconfig's job. Verify the PR's claim that the blueprint still compiles all ~100 files it did before, and name any harness module that drops out (unimported = dead). **Reproducer:** `tests/ts-bridge` runs the REAL `tests/tsconfig.json` in a fixture without root `node_modules`: a project-owned `foo.test.ts` importing an uninstalled package passes, and a type error in a spec-imported helper still fails. **No regression test:** until the reproducer lands. **Re-open if** a derived project's CI typecheck fails on a file the harness does not run. |
| **BUG-146** | **Reproducer: not applicable — this row tracks a CI-only hang that has never reproduced locally (162+ runs, including under `taskset -c 0-3`), so no reproducer commit can be written; the code this row's commits land is evidence-capture instrumentation, not a behavioural fix for BUG-120.** **`tests/sync-by-address` #20d (BUG-120) intermittently hangs about 320 s in CI, turning `main` red on pushes that changed nothing it tests.** | S2 | OPEN | **Observed twice:** CI run 35596083384 on `61cfe01` (2026-09-21, a docs-only push, unnoticed then because the Orchestrator did not wait for its CI) and run 35733083948 on `374a8d9` (2026-09-22, also docs-only). Both times "#20d BUG-120: a TERM that reaches the held refresh child with its release is not lost — no fetch, no wait for the budget" ran 320019-320024 ms and failed. That takes `bootstrap-gate` #2/#3 down with it, because a fresh bootstrap runs the same suite in its own gate. A rerun of the failed job passed with no change. The test exists to prove a TERM is NEVER lost, and a hang is exactly what a lost TERM looks like. So either the test's synchronisation races on a slow runner, or BUG-120's fix loses the signal in a window the test sometimes hits. **The first job is to decide which, from evidence:** reproduce under CI-like load (`taskset -c 0-3`, see HANDOVER §0 item 3) in a loop until it hangs, then read what the held refresh child and its parent were doing. A flaky gate test is a defect whichever side it is on: it trains everyone to rerun until green, which is the BUG-031 failure. **Diagnosis attempted 2026-09-22 (Philipp), inconclusive:** 162 of 162 local runs were clean, 150 of #20d alone under `taskset -c 0-3` plus pinned busy-loops and 12 of the full suite (which nests bootstrap-gate). The hang happened at both levels in CI (outer on `61cfe01`, nested on `374a8d9`) and at neither here. A local full suite takes about 157 s, where CI takes over 340 s for the same work, so `taskset` does not reproduce this runner. No defect was found in BUG-120's token gate (`scripts/blueprint:672-704`) or in the test, and nothing was changed. **Next step:** capture evidence ON the next CI occurrence. When a scenario's wait times out, the harness dumps the process tree and wait channels as a CI artefact. **No regression test:** the test that catches this bug already exists, `tests/sync-by-address` #20d itself, and it does not reproduce locally. A BUG-146-titled copy would prove nothing more. **Founder, 2026-09-22: leave it.** The harness-side capture is not built; the row stays open and unattended until the next hang. **Seen again 2026-09-24 on `c7c47f6`**, a documentation-only push: #20d timed out at 320 s inside the nested `bootstrap-gate` run, failing `#2 and #3 a freshly bootstrapped project passes its own pre-push gate`; a re-run of the same job on the same commit went green. That is the THIRD CI occurrence (`61cfe01` outer, `374a8d9` nested, now `c7c47f6` nested) against zero local reproductions, and it means the row's own trigger has fired while the evidence capture it asks for is still unbuilt — so this occurrence produced no more information than the first two. The next decision is the founder's: build the harness-side process-tree dump, or accept that each occurrence costs a re-run and teaches nothing. **Re-open if** #20d hangs again on any run. **Capture built 2026-09-24 (Philipp).** `tests/harness/dump.ts` adds `waitOrDump`, a generic Scenario method any wait can use: it races a promise against its own short bound and, on ANY rejection (not only a timeout), writes this scenario's whole tracked process tree — `ps` pid/ppid/stat/wchan/args, `/proc/<pid>/wchan`, `/proc/<pid>/stack`, open fds — to a file before the error propagates. #20d's two real waits (the refresh-child probe and the unbounded `await done` that the 320s hangs actually sat inside) now go through it, at 65s and 30s respectively — both far under the nested suite's 320s `testTimeout`. **What the 320s told us:** it is not a wait budget anything in the test chose; `tests/vitest.config.ts`'s `testTimeout` is exactly `320_000`, and the row's own measured "320019-320024ms" is that ceiling firing on an unbounded `await done` (the run's own `close` event, no timeout of its own) — so all three occurrences are consistent with a genuinely stuck child process, not with a slow machine or a mistimed assertion. **On the next occurrence:** the suite fails within ~30-65s instead of ~320s, and a `tests/.timeout-dumps/*.txt` file exists with the process tree at that moment. CI uploads it as the `harness-timeout-dumps` artifact (`if: failure()`, `.github/workflows/security.yml`). **Round 2 (2026-09-24, Philipp) — the NESTED case was still broken.** A Codex four-eyes review of the round-1 capture found, and Philipp confirmed, that round 1's fix for the nested case (hit twice: `374a8d9`, `c7c47f6`) never worked: setting `BP_HARNESS_DUMP_DIR` on the derived gate's env looked right, but `scripts/run-ts-suites.sh`'s `ts_scrubbed` — the ONE scrub, BUG-117 — unsets every `BP_*` name before the derived project's own nested vitest starts, for the same reason BUG-046/047/066 built the scrub in the first place: ambient `BP_*` must never leak into a fixture. So the env var never reached the nested process, and it always fell back to its own workspace-local default — exactly the directory `dispose()` deletes on teardown. Two of the three CI hangs this row tracks (`374a8d9`, `c7c47f6`) are nested, so round 1 captured nothing for the likely case; only the non-nested `61cfe01` occurrence would ever have produced a surviving dump. **Fixed without touching the scrub:** `tests/harness/dump.ts` adds `collectDumps(fromDir, toDir)` — a plain `readdir`+`cp`, no env var, no scrub interaction. `tests/bootstrap-gate/bootstrap-gate.release.spec.ts`'s gate `s.run` now runs `collectDumps` in a `.finally()`, reading whatever the nested run actually wrote to `<derived-proj>/tests/.timeout-dumps` off disk — while the outer scenario's workspace is still alive, before its own teardown removes it — and copying it into the outer repo's own `tests/.timeout-dumps`, the directory CI already uploads. Runs whether the gate passed, failed, or the outer `s.run` itself timed out (via `finally`), so a nested hang is captured regardless of which level notices it first. `BP_HARNESS_DUMP_DIR` plumbing on the nested process's env is removed as dead code; the env var itself stays for the in-process override `tests/harness/harness.spec.ts`'s existing BUG-146 cases use directly on `process.env`, which never crosses the scrub. Proven by `tests/harness/harness.spec.ts` "BUG-146: a nested run's dump survives into the outer dump dir via collectDumps, not an env var" — red before this fix (`collectDumps` did not exist), green after. No fix to BUG-120 attempted — this is capture only, per founder direction. **Fourth occurrence, 2026-09-24, run 36010475442 on `28c8a91` (the push that landed the capture), and the first to teach something:** the outer #20d r2 wait "waiting for the run to exit" timed out at 30 s after SIGINT, where the test requires the run to end within 2 s. A slow runner does not turn 2 s into more than 30 s, so this is a child that genuinely did not exit, which is what a lost TERM looks like, not a timing flake. The dump was written on the runner (`tests/.timeout-dumps/-20d-r2--waiting-for-the-run-to-exit-*.txt`) but NOT uploaded: `actions/upload-artifact` >= v4.4 skips hidden paths by default and `if-no-files-found: ignore` hid it. Fixed in the workflow (`include-hidden-files: true`, `warn`), so the next occurrence uploads its dump. **Fifth occurrence, 2026-09-25, run 36129880176 on `52e8e32`** (a push that touched none of this): #20d r1 "waiting for the run to exit" timed out at 30 s, outer level. **The dump was uploaded this time**: artifact `harness-timeout-dumps` of that run, file `-20d-r1--waiting-for-the-run-to-exit-2738-1790335996377.txt` (`gh run download 36129880176`; a local copy is in `.scratch/ci-dump-36129880176/`). **Next step: read it** — which process in #20d's tree is alive, its state and wait channel, and whether the held refresh child ever received the TERM. The failed job was re-run. |

**Do not narrate status here.** Which items are where is answered by the
folders: `doing/` is what is being implemented, `waiting-acceptance/` is what is
pushed and untested, `done/` is what the founder accepted. Prose repeating that
becomes a second record of one fact, and on 2026-08-03 every such line in this
file had gone false — including one telling the next session that a decision was
still pending on an item accepted that morning. Resume context belongs in
[HANDOVER.md](HANDOVER.md), which is rewritten to match reality on every wake.
