# Bugs — active (being implemented)

Bugs currently being worked. Pushed bugs move to
`docs/waiting-acceptance/BUGS.md`; founder-accepted bugs move to
`docs/done/BUGS.md`. See [README.md](README.md) for the lifecycle.

**Keep rows to one line.** Link out for the detail. A row that grows into a
paragraph belongs in a `PLAN-*.md` or a work-item folder — a table cell
holding half a page is unreadable, which is how this file stopped being
useful once already.

## Two namespaces, and only one of them is a work item

**`BUG-XXX` / `FEATURE-XXX` / `TASK-XXX` are the lifecycle IDs.** They are what the
commit convention, the regression-test naming rule and these lifecycle folders
key off (CLAUDE.md §"Bug Management", §"Team Workflow").

**`A-NN` is not a work item.** Those are findings from one audit — the
2026-07-23 contamination sweep in
[BLUEPRINT-AUDIT-2026-07-23.md](../config/BLUEPRINT-AUDIT-2026-07-23.md) — in the same
category as a Codex finding ID. A finding is a *claim that something is wrong*;
it becomes work when it gets a `BUG-`/`FEATURE-` number and a row here.

They were being used as though they were work items — folder names, rows in this
table, gate comments — and then extended with new numbers (A-38, A-39) for
findings that had nothing to do with that audit. Live items were renumbered on
2026-07-30. The audit document and everything in `done/` keep their `A-NN` IDs as
historical provenance, because the Codex review documents argue about findings by
those names and renaming them would break the trail they exist to be.

**Rule going forward:** an `A-NN` reference is a citation of history. If you are
about to work on something, give it a `BUG-`/`FEATURE-` number first.

| # | Bug | Severity | Status | Detail |
|---|---|---|---|---|
| **BUG-070** | **Three of the four DoD stages take their range from `push_log_opts`, which returns nothing on a deletion-only or hand-run push — and then print a reason that is false.** `.githooks/pre-push-project:837` does `_dod_ranges="$(push_log_opts)"` with no fallback. `push_log_opts` (`.githooks/pre-push:120-130`) emits one range per non-deleting ref line on stdin, so it is EMPTY when every ref is a deletion and when the hook is run by hand, by CI, or by a fixture rehearsing the gate — none of which supply ref lines. With an empty list, `dod_items_in_push`'s `for _dg_range in $1` iterates zero times, so §1b·1 (every item has a backlog row) and §2 (every BUG has a regression test) both pass over nothing, as does the item extraction the DoD self-audit reads. **Proven by execution, not by reading** (`.scratch/prove-dod-empty.sh`): `dod_stage_rows ""` → exit 0 printing `no item-bearing commits in this push (merge/revert/root only)`, `dod_stage_bugtests ""` → exit 0 printing `no BUG items in this push`, while the same functions over `HEAD~3..HEAD` correctly extract `TASK-18 TASK-21`. **The printed reason is the sharp end:** the message asserts *merge/revert/root only*, which is a real and legitimate absence case, when the actual cause is that the stage received no range at all — so the one line an operator would read to notice the hole actively explains it away. That is the BUG-041/042 misdirection class inside the Definition-of-Done gate. **The asymmetry names the fix:** the gitleaks stage calls the same function and has exactly this fallback at `.githooks/pre-push:169-187` — `@{u}..HEAD` when an upstream exists, `--all` when none does, with `echo "no ref info on stdin — scanning $_gl_ranges"` — under a comment reading *"Scanning nothing here would turn 'run the gate manually' into a hole, so … Never silently skip."* The DoD block needs the same three branches, and the two absence cases must print DIFFERENT text: deletion-only is genuinely nothing to check, no-ref-info is a fallback that says what it scanned instead. | S1 | OPEN | Found 2026-09-10 by Vitali (QA-1) auditing docs/DoD.md for TASK-022 and verified by execution by the Orchestrator the same day. Re-open if any gate stage derives a commit range from `push_log_opts` without handling the empty result, or if an absence branch reports a cause it has not established. |
| **BUG-069** | **The DoD gate's baton check validates three rows while its own comment says four, so `Last update` has never been checked.** `dod_stage_signal` (`scripts/lib/dod-gate.sh:173-192`) loops `for _dg_row in Holder State Task` — and the comment four lines above it, at `:170`, states the check is that the baton *"exists and carries its four rows"*. The comment is false about the code directly below it. `AGENT_SIGNAL.md` defines the baton as four rows and `scripts/signal-set.sh:118,203` does write and refresh `Last update`, so the row exists and the gate simply never looks at it: a baton missing or carrying a stale `Last update` passes §7G, and the stage then prints `baton well-formed`. **Why it matters beyond the missing row:** `Last update` is the only row that says whether the baton is LIVE, so it is the row a stale-dispatch check would need, and DoD §7G is the stage that gates a handoff. The stage is also weaker than the protocol in three further ways worth deciding on together rather than one at a time — it does not check that `State` holds an allowed value, that `Task` is non-empty, or that `Holder` is a name on the roster, all of which `scripts/lib/roster.sh` can already resolve. **Found by cross-provider diff, which is the part worth keeping:** Vitali (Claude) rowed §7G as enforced and quoted the rule as *"Holder / State / Task / Last update rows are all updated"*, Christian (Claude) rowed the sibling rule as enforced with the anchor UNSURE, and Jesko (Codex), auditing independently and forbidden from reading either, reported that the stage checks three rows and that *"well-formed" is weaker than the prose protocol*. Two agents reading the same mechanism credited it with what its comment claimed; the third read the loop. No agent reported the false comment — the diff between them did. | S2 | OPEN | Found 2026-09-10 by Jesko (QA-2, Codex) in the TASK-022 independent enforcement audit, verified against `scripts/lib/dod-gate.sh:185` and `scripts/signal-set.sh:118,203` by the Orchestrator. Regression test must fail on the parent: a baton with `Holder`/`State`/`Task` and no `Last update` currently passes `dod_stage_signal`. Re-open if a control's comment is found describing a stronger check than its code performs. |
| **BUG-053** | **A `blueprint`-tier row was treated as an unmet obligation in derived projects, where the suite cannot exist — and underneath it, an assertion that had been passing vacuously for years.** The suite directories for `drift-in-blueprint`, `pull-exec-bit` and `harness` are blueprint-tier and correctly do not ship (`git archive HEAD \| tar -t \| grep -c` → **0**), but `tests/SUITES.md` **does** ship (→ **1**). So a derived project receives rows describing suites whose files are absent, and `tests/manifest` `#4`/`#4b`/`#5` demanded invocation for them. Caught by `tests/bootstrap-gate` #6 — the only suite that speaks for downstream projects — after `TASK-018` retired two shell runners. **The older defect is the more interesting half:** before retirement, `#4`/`#5` downstream fell through to asking whether the gate text contained `bash tests/drift-in-blueprint/test.sh`, which it did — inside an `if [ -f … ]` that always skips there. **The assertion was about a string, describing a file the project does not have, guarding a stage that never runs.** It passed vacuously for years, and retiring the runners is the only reason anyone found out. **Fix:** a `blueprint`-tier row is *not applicable* outside a blueprint, keyed on **`.blueprint-root`** — the same positive marker `drift` uses — and **never on the directory being absent**, because absence-keying would let a project silently drop a suite it should run by deleting a directory, which is BUG-005 with an extra step. Two guards on top: if every blocking row is skipped the assertions **fail** as vacuous rather than passing over nothing, and re-tiering a shipping suite to `blueprint` to buy the skip is refused by `#2b`. **Same defect, second instance:** `tests/git-isolation` names four anchors it proves *by execution* drive git, one of which (`bootstrap-identity`) is blueprint-only — fixed the same way, with a floor of three so the anchor set cannot collapse. Proven on a real derived layout (`git archive HEAD` extracted, manifest run there): 7 rows skipped, **36 still enforced**, rc=0. **Not fixed by stripping the rows on pull** — that would make the shipped file differ from its source and turn `blueprint pull` from a copy into a transformation. | S2 | OPEN | Found 2026-09-09 by `bootstrap-gate` #6 while landing TASK-018. Fixed by Vitali (QA-1) and Sylvia the same session. Re-open if any control keys applicability on a file being missing rather than on `.blueprint-root`. |
| **BUG-072** | **A spawned agent whose dispatch text does not name a roster persona is labelled by AGENT TYPE, silently — 1510 of 2489 feed lines in one session read `[general-purpose - Claude Code]`.** The mechanism is not broken: `log-activity.sh:141` passes the dispatch `description` to `bp_roster_name_in_text`, and it resolves correctly — verified, `"Vitali isolation violation audit"` → `Vitali`, `"QA-1 isolation violation audit"` → **empty**, `"Fix live isolation defects"` → **empty**. The convention is that the description must contain the persona **NAME**; a ROLE does not match, and neither does a task-shaped description. **The failure is that forgetting it costs nothing at dispatch time and everything afterwards:** the run proceeds, the work is correct, and the feed — the one durable record of who did what — attributes three different personas to one anonymous string. `AGENT_ROSTER.md` exists so identity resolves by role (BUG-010), and this defeats it at the last hop. **CLAUDE.md points the reader at `AGENT_PERSONA`**, which is an environment variable a `Agent`-tool dispatch cannot set — so the documented override is not reachable on the path a Claude subagent actually takes, and the reachable one (the description) is documented only in a comment inside `log-activity.sh`. **Fix has two halves and the second is the one that matters:** document the description convention where a dispatcher will read it (CLAUDE.md §"On wake" / AGENTS.md §Dispatching), and make the fallback **visible** — when the lookup yields nothing, the bookend should say so once per dispatch rather than quietly writing the agent type into `.subagent-map`, which is what the streamed lines then inherit. A label that is merely *unspecific* is survivable; one that is *confidently wrong about identity* is the same class as a green gate that checked nothing. **Found by the founder**, reading the feed, after three implementation agents in one session all logged as the same string. **Filed and pushed as BUG-052, renumbered to BUG-072 by BUG-071's duplicate check:** BUG-052 was already the marker-aware-merge defect and is anchored there in code (`tests/manifest` #7b), while this row's number appeared in no test, no commit and no comment — so it is the one that could move without breaking a reference. Cite BUG-072 for this defect. | S2 | OPEN | Found 2026-09-09. Re-open immediately — every multi-agent session since BUG-027 has been recording anonymous attribution, and the cost lands on whoever later asks "which persona wrote this?". |
| **BUG-046** | **BUG-030's root cause found and REPRODUCED — it is the whole bootstrap family, and the mechanism is env inheritance, not path derivation.** `scripts/new-project.sh:253-257` seeds a baton on every bootstrap via `signal-set.sh` **with no `--file`**. `signal-set.sh:56` → `state-dir.sh:87` honours `$AGENT_SIGNAL_FILE`, and `:56` honours `$AGENT_STATE_HOME`. `scripts/codex-signal-watch.sh:250` **exports `AGENT_SIGNAL_FILE` into every dispatched wake command**. Four suites drive `new-project.sh` and **none of them unsets those vars**: `template-source:26`, `bootstrap-contents:38`, `bootstrap-gate:47`, and `bootstrap-identity` (which unsets nothing at all). **Reproduced by execution:** running `template-source` and `bootstrap-contents` with a decoy baton and `AGENT_SIGNAL_FILE` set overwrote it with `Holder=Nobody / State=IDLE / Task="Bootstrapped from the blueprint. Claim the mic to begin."`, appended a journal row, **and exited 0** — byte-for-byte the damage BUG-030 records. **BUG-030 named `bootstrap-gate` as the suspect and guessed the cause was state-dir resolution; both are wrong.** Any of four suites does it, and only when run from a context that exports the baton env — which is exactly what a dispatched Codex wake does, and why it struck mid-review. **`.githooks/pre-push:46` unsets only `GIT_*`; there is no gate-level scrub for this class at all.** **Fix:** `--file` on the seed at `new-project.sh:253`, plus one `unset AGENT_SIGNAL_FILE AGENT_STATE_HOME AGENT_FEED_LOG` at the `pipe_stage` boundary, then delete the per-suite unsets that duplicate it (`dod-gate:21` is the pattern). Net deletion, retires a class with four prior instances. | S1 | OPEN | Found 2026-09-09 by Vitali (QA-1) auditing isolation for TASK-018. Supersedes BUG-030's suspect list — amend that row when this lands. |
| **BUG-047** | **Two suites mutate the REAL repository through inherited `GIT_DIR`, and the guard that exists to catch exactly this cannot see them because its membership set is decided by COMMENTS.** Reproduced against a victim repo the way `git-isolation:80` does: `tests/commit-subjects` (rc=0) **rewrote the victim's config — adding `core.hooksPath=.githooks` and overwriting `user.email`/`user.name`.** Setting `core.hooksPath` on a real repo *is* the A-22/BUG-004 failure, produced by a test. `tests/bootstrap-identity` (rc=1) **left a commit in the victim repo** (`chore(bootstrap): initialize test-noident …`). Baseline: with `GIT_DIR` set, `git -C "$fixture" init` returns 0, creates **no** `.git` in the fixture, and the later commit lands in `GIT_DIR`. Both are protected today **only** by `.githooks/pre-push:46`, so either suite run directly — as an agent debugging one naturally does — hits the real repo. **The guard's own defect is the more interesting half:** `git-isolation:118` discovers fixture-builders with `grep -q 'git init'`, but `commit-subjects:195` is `git -C "$T6" init -q`, which contains no such substring. Meanwhile suites it *does* find match on **comment prose** — `branch-guard:22` says "this suite runs `git init`" while its code at `:52` is `git -C "$1" init -q -b main`. **The control's population is determined by what comments say, not by what code does**, and the non-vacuity check at `:121` passes at 20 members and masks it. Same shape as BUG-035 (a codebase annotated for a linter that never ran). | S1 | OPEN | Found 2026-09-09 by Vitali (QA-1), reproduced by execution. Fix the two suites AND the discovery predicate — fixing only the suites leaves the guard still blind. |
| **BUG-048** | **`tests/a2bp-contamination`'s headline assertion can never fail the suite.** `fail` is called inside `run_a2bp_in` (`:201`), which every caller invokes as `rc=$(run_a2bp …)` — a command substitution, i.e. a subshell. `FAILED=1` is set in the child and lost on return. The assertion so disarmed is **"a2bp MOVED THE BLUEPRINT'S MAIN BRANCH — a request must never land"**, which is the single most important thing this suite exists to check: A-07 and the whole request-not-delivery model rest on it. The suite would print its normal PASS while a2bp had pushed to `main`. Two sibling defects in the same family: `a2bp-build:258-259` (the `GIT_INDEX_FILE=` prefix is stripped by `request.sh:141`, so #8 passes for the wrong reason) and `a2bp-e2e:232` (a degenerate grep pattern makes #5 half-vacuous). **This is the repo's signature failure inside the guard on the door BUG-002 and A-09 came through.** | S1 | OPEN | Found 2026-09-09 by Vitali (QA-1) while auditing isolation. Fix by returning a status and failing in the caller's shell, then add a mutant proving the assertion can fail. |
| **BUG-049** | **`tests/marker-merge` leaks a full blueprint archive on every run — 23 of them, 133 MB, measured.** `marker-merge:89` is `BP_CLONE="$(mktemp -d)/bp-clone"`; the parent `mktemp -d` is never captured, and the suite's trap covers only `$WORK`. Two leak per push rather than one, because `git-isolation:80` executes `marker-merge` a second time. Smaller instances of the same omission: `a2bp-contamination:68` (`$GH_SHIM`) and `state-dir:281` — and `tests/state-dir` has **no `trap` at all**. Relevant to TASK-018 beyond tidiness: an isolation contract that does not assert "a suite leaves nothing behind" would not have caught 133 MB of debris accumulating in `$TMPDIR`, and `a2bp-e2e:309` already scans that same directory for leaked dirs, so the debris is a live cross-suite hazard rather than only waste. | S3 | OPEN | Found 2026-09-09 by Vitali (QA-1), measured. Fold into the isolation contract's teardown assertion (TASK-018 §4.3). |
| **BUG-043** | **`signal-set.sh` trimmed the baton's Task differently depending on the publisher's `LANG`, breaking its own documented contract.** The trim is `sed 's/^[[:space:]]*//; s/[[:space:]]*$//'`, and `[[:space:]]` is locale-dependent by definition. In a UTF-8 locale on BSD/macOS it also matches **U+00A0 NBSP**, so the trim removed characters the file's own header explicitly promises to leave alone: *"NOT SUPPORTED → Unicode whitespace (U+00A0 NBSP and friends) is NOT recognised, so it survives at the boundary."* The comment even said "in the current locale", which is the defect written down as if it were a specification. Caught by `tests/signal-set` #3h (*"the code now exceeds its documented `[[:space:]]` contract"*), pre-existing at `c4dc95b`, confirmed in a clean worktree. **Why it matters beyond the character:** this is the sole writer of the baton every agent on the team parses, so identical input published from a Mac and from Linux produced different bytes — a coordination primitive whose output depends on who published it. **Fix:** pin `LC_ALL=C` on that one `sed`, making `[[:space:]]` ASCII-only and identical on every platform. Same idiom the feed already uses for byte-exact length (`LC_ALL=C awk`). | S2 | OPEN | Found 2026-09-09 by the gate while landing TASK-017 on a Mac. Fixed the same session. Re-open if any shipped script matches a POSIX class without pinning the locale. |
| **BUG-042** | **`blueprint a2bp` does not work on a stock Mac, and its suite reports that as 19 assertion failures blaming the contamination guard rather than as the missing tool it is.** a2bp stages a request with `diff --unchanged-line-format/--old-line-format/--new-line-format`, which is GNU diffutils. macOS ships Apple's FreeBSD diff, which has none of those flags, so staging fails before any scanning happens and every file is refused with exit 4 and **no finding reported**. `tests/a2bp-contamination` then fails 19 cases with text like *"a2bp exited 4 on a legitimately generic file"* and *"the output does not quote the offending line"* — an accusation against the guard, for a tool that was never installed. **a2bp itself behaves correctly**: it fails closed and prints `'diff' must support --unchanged-line-format ... Install GNU diffutils and re-run.` The suite simply never surfaces that. **Fixed** by `scripts/install-toolchain.sh` (TASK-017) installing `diffutils` on macOS, checked by **capability rather than presence** — `have diff` is true on every Mac and tells you nothing, the same trap as `have coreutils` versus an actual `timeout` provider. **Still worth doing, not done here:** the suite should assert its precondition and `pipe_skip` with a stated reason (or block naming the tool), because 19 misdirected failures is strictly worse than one accurate one — this repo's whole objection to controls that misreport. **Also note the product verb was simply unavailable on macOS**, so back-propagation — the mechanism every derived project uses to send improvements upstream — has never worked from a Mac. | S2 | OPEN | Found 2026-09-09 by the gate while landing TASK-017. Tool gap fixed the same session. Re-open for the suite-precondition half. |
| **BUG-041** | **Bare `sed -i` in test fixtures is GNU-only, so on macOS the fixture edit silently never happens and the suite fails describing a product defect that does not exist.** BSD `sed` requires an argument to `-i` (the backup suffix), so `sed -i 's/x/y/' FILE` consumes the SCRIPT as the suffix and then treats the FILE PATH as the script, failing with `sed: 1: "/var/folders/...": invalid command code f` — where the "command code" is merely the first letter of the path. Four call sites: `tests/session-resume/test.sh:143,340,347` and `tests/roster/test.sh:267`. **The misdirection is the expensive part:** session-resume #11 reported *"a known PAST id was reported as an unbacked claim"*, an accusation against `scripts/session-resume.sh`, when the truth was that the fixture's marker had never been rewritten. A test that cannot edit its own fixture does not report "I could not set up" — it reports a failure of the thing it was pointed at. **The shipped scripts were already correct** (`scripts/blueprint:1088` and `scripts/new-project.sh:254` both use `sed -i.bak`), which is the recurring shape of this whole macOS sequence: the product is portable and the tests that gate it are not. **Fix:** `tests/helpers/sed-inplace.sh` providing `bp_sed_i`, using a temp file rather than `-i.bak` because a stray `FILE.bak` is its own hazard in a repo whose suites glob and count files, and writing back with `cat` rather than `mv` so the file keeps its inode (`agent-activity-bound` #8 watches inodes to detect rotation). | S2 | OPEN | Found 2026-09-09 by the gate while landing TASK-017 on a Mac. Fixed the same session. Re-open if a suite reintroduces bare `sed -i`. |
| **BUG-038** | **The feed suites synchronise on "a supervisor is resident", but the property they need is "the supervisor has registered this run log" — and on a slow host those are not the same instant.** `wait_sup` returns as soon as a process matching `--supervise` exists. The feed deliberately does not replay pre-existing content (case #16), so a payload appended between `wait_sup` returning and registration is folded into the baseline and never emitted. The suite then reports "the reader lost the record", which is **indistinguishable from the regression the case exists to catch**. Isolated by single-variable experiment: inserting one `sleep 2` after `wait_sup 1` — the only change — turned #7 from fail to pass. **Fixed** by a `reader_ready` handshake that writes a unique sentinel to the very file the case will use and waits for it to come out the other end, proving liveness AND that this file is registered. Applied to #7/#10/#18/#5f, and to `tests/subagent-feed` #1/#2 which had the same shape. **Both suites now pass on macOS — `agent-activity-bound` for the first time.** Two refinements the first attempt got wrong: the sentinel must be RE-WRITTEN rather than written once, because `seed_offset` skips anything behind the offset PERMANENTLY, so a longer timeout cannot help (proved by raising the bound to 45s against a 32.6s latency, which changed no outcome and cost 2m41s); and #5f's `sleep 1.5` had to become a poll, because by that point case #2 has created 80 subagent transcripts and the effective tick is ~10s rather than the configured 0.25s. A first version of the helper returned a status no caller checked, so a failed handshake silently degraded to the unsynchronised behaviour it exists to prevent — a guard whose failure mode is invisible is not a guard, and every call site now fails loudly. | S2 | OPEN | Found 2026-09-09 while fixing BUG-036. Fixed for #10/#18. Re-open if any new case appends to a run log without a handshake first. |
| **BUG-039** | **`--daemon` returns while the supervisor is resident but not yet pumping, and the first record appended in that window waits 32.6 seconds.** Measured twice on macOS at **32.60s and 32.59s** — a deterministic constant, not load — while every subsequent record lands in 0.25-0.47s, i.e. the tick. Foreground mode has no such delay (timestamped `bash -x` trace shows the payload emitted 3.09s after start, appended at 3s). **This is an operator-facing defect, not only a test problem:** start the feed, do something, and watch half a minute of silence before the first line appears, which reads as a broken feed. The cause is that `cmd_daemon`'s readiness oracle is `feed_is_running`, i.e. the lock — BUG-037 made it poll that oracle correctly, but the oracle answers "resident", not "pumping". **Likely fix:** have the supervisor signal readiness once it has completed its initial scan, and have `--daemon` wait for THAT. It would also let `tests/agent-activity-bound`'s handshake bound drop back to a second or two. **RESOLVED under BUG-038 — and a speculation recorded here was wrong.** This row previously said #7 might be a genuine word-splitting defect on a state dir containing spaces. It is not: a single-variable experiment on a known-good probe (change the state dir to one containing spaces, change nothing else) measured **32.61s then 0.25-0.59s, identical to the plain dir**. Spaces are handled correctly, and Codex — which said so from isolated probes — was right where this row was wrong. #7 and #5f were both the seed-window race, not this latency. What remains true here, and unfixed, is the 32.6s itself. | S2 | OPEN | Found 2026-09-09 by measuring append-to-emit latency while fixing BUG-036. Blocks the macOS gate together with the #7 and #5f residue. |
| **BUG-037** | **`agent-activity.sh --daemon` reports "failed to start" and exits 1 while the supervisor it just launched is alive and healthy — and a caller that believes it spawns a second one.** `cmd_daemon` launched the supervisor, slept a fixed `0.4`s, checked `feed_is_running` **once**, and gave up. Measured on macOS the supervisor acquires its `flock` after **~0.6s**, so the check ran before the thing it was checking for. On a faster host startup fits inside 0.4s, which is why a fixed sleep looked correct for months. **This is not a cosmetic exit code.** `cmd_daemon`'s own idempotency guard is `feed_is_running`, so a caller that trusts the failure and retries gets a SECOND supervisor — the unbounded-spawn shape BUG-001 rode to load 175 for 2.7 days, reachable here by an honest retry rather than by a broken guard. It also made `tests/agent-activity-bound` report "could not start a supervisor" for cases #10/#13/#14/#18. **Observed live in this session's very first command:** the wake-time `--daemon` printed `failed to start` while `--status` immediately after reported it running. **Fix:** poll for the lock on the same 5s bound and 0.1s tick `cmd_stop` already uses, rather than guessing a single interval — a timing assumption removed beats a magic number enlarged. | S1 | OPEN | Found 2026-09-09 while fixing BUG-036. Fixed in the same session. Re-open if any launcher regains a fixed-sleep verification. |
| **BUG-036** | **The pre-push gate cannot pass on macOS: three test helpers identify a process's cwd through `/proc`, which does not exist there, so the supervisor count is always 0 and `agent-activity-bound` fails closed.** `tests/agent-activity-bound/test.sh:159` and `tests/subagent-feed/test.sh:49,125` run `readlink -f /proc/$p/cwd`; on macOS that is always empty, the `case` never matches, and cases #1/#6/#7/#13/#14/#15 fail with "0 supervisors" while the feed is in fact running. Verified pre-existing at `c4dc95b` (tip of `main`) in a clean worktree, with the live feed stopped — **`main` is red on any Mac, and no push can be made from one.** The product itself is portable: `scripts/agent-activity.sh:226` branches on `/proc/<pid>/stat` vs BSD `ps -o lstart=`. Only the tests assume Linux, so this is the Brewfile's defect (TASK-017) recurring one layer down — the thing that decides whether work can ship is Linux-only while the thing it tests is not. Second defect in the same file: `test.sh:420` interpolates `$n1→$n2`, and under the `LC_ALL=C` the suite sets, bash absorbs the multibyte arrow into the identifier (`n1\xe2: unbound variable`) — needs `${n1}`. Third: the suite reports "no UTF-8 locale available" and SKIPs #19 on a host where `locale` is `en_US.UTF-8` and `locale -a` lists it, so its own detection is wrong. **Not the fix:** skipping the suite on Darwin — that is a suite silently covering nothing (BUG-005). Port the cwd lookup to a shared helper that uses procfs where present and `lsof -a -p <pid> -d cwd -Fn` on BSD. | S1 | OPEN | Found 2026-09-09 while landing TASK-017, by the gate refusing that push. Blocks every push from macOS. |
| **BUG-050** | **`tests/pipeline` #19's real-feed escape canary is wedged RED in this checkout, and the mechanism means any agent that merely *writes about* the suite disarms it permanently.** The case asserts two things about `logs/agent-activity.log` after running a fixture stage: the `[GATE]` line count is unchanged, and the fixture's canary label does not appear. The label is a **fixed literal**, and `logs/agent-activity.log` is the feed **every agent's prose lands in** — so on 2026-09-09 at 17:38:34 a `general-purpose` agent quoting this very case in its TASK-018 isolation audit wrote the literal into the live feed, and case #19 has failed on every run since. Verified by execution: the count half passes (299 → 299), the fixture appends nothing (1 occurrence before, 1 after, and the fixture writes to a pinned `AGENT_FEED_LOG`), and the sole match is that agent's sentence. **The failure text accuses the suite of polluting the production log, which is exactly backwards** — the same misdirection class as BUG-041/BUG-042, and it blocks every push from this checkout until someone rotates an untracked log for reasons the message does not explain (`logs/` is gitignored, so a fresh clone is clean and this reads as "works on my machine"). The repo already carries this false-positive class knowingly — CLAUDE.md notes `Bash(* --no-verify*)` blocks a task string that merely *discusses* `--no-verify`. **Fix:** make the token unique per run (`canary-$$-$(date +%s)`) so quoting the suite cannot match a future fixture, and scope the search to lines the fixture's own run could have produced rather than the whole file. Vitali's S2.1 finding (PLAN-TASK-018 §4.1) reached the opposite conclusion — *"the token check is unique per run and survives anything — keep it"* — which is true of the **fixture instance** but not of the **literal**, and that gap is the defect. | S2 | OPEN | Found 2026-09-09 by Philipp (Infrastructure-1) while regression-testing `scripts/lib/pipeline.sh` for TASK-018. Pre-existing and unrelated to that change, proven by execution. Fold into TASK-018 §4.3's isolation contract — a canary keyed on a literal shared with the audit trail is not isolated. |
| **BUG-051** | **`tests/manifest` was anchored on `*.sh` in every assertion, so the TypeScript migration it is supposed to govern was invisible to it: migrate a suite, delete its `.sh`, delete its `tests/SUITES.md` row, and NOTHING fails.** No unclassified-suite error, because no shell file remained for `#1` to discover (`find "$ROOT/tests" -name '*.sh'`); no missing-invocation error, because no row remained for `#4`/`#5` to check. That is BUG-005 itself — a silent coverage cut behind a green gate — re-entering through the one door the control could not see (PLAN-TASK-018 §7.1). **Reproduced by execution:** with `tests/zz-fake-probe/zz-fake-probe.spec.ts` on disk and no row, the pre-change file at `c4dc95b` printed `ok — #1 every suite directory containing shell files is classified`. **A second, older weakness surfaced while fixing it:** `#2` tested `[ -f "$ROOT/tests/$s/test.sh" ]` — the literal filename `#1` had already stopped trusting, one assertion further down the same file — so a suite whose only runner is named anything else (`tests/staleness/drift-integration.sh` is one, and it passes only because a `test.sh` happens to sit beside it) was reported a GHOST while being perfectly real. **Fixed** by making a runner mean `*.sh` OR `*.spec.ts` in `#1`, `#2`, `#2b`, `#4` and `#5`. **`#2b` then needed a second correction, because "shipped" and "runnable" stopped being the same word:** with TASK-018 phase 1 holding the TS toolchain out of derived projects, `tests/proc-cwd` legitimately ships its `test.sh` and withholds its `.spec.ts`, which the old rule reported as `proc-cwd(1/2 runners)` — a correct boundary called broken, which is how a control gets switched off. The invariant that holds in both phases is *ships at least one runner the recipient can execute, and none it cannot*, with `TS_SHIPS` derived from the archive rather than declared so phase 2 re-reads it with nothing to remember. **New `#2c` makes the phase transition all-or-nothing**: the two propagation paths disagree by construction — bootstrap ships the whole archive, `pull` ships `MANAGED_FILES` and `tests/` is managed while the root files are not — so a half-done move gives an *existing* project specs with no runner while a project bootstrapped the same day is fine. `#2c` requires the toolchain to ship and be managed together, or neither, and asserts its own `MANAGED_FILES` parse is non-vacuous first (a stale parse would have passed phase 1 silently). **`#4` then had to follow a sourced bridge**, because the gate does not run `vitest` itself — it sources `scripts/run-ts-suites.sh` and calls `ts_suites_stage`. The hop is discovered, not named: a sourced file counts only when it exists AND the hook calls a function it defines, so sourcing a file that merely *declares* one proves nothing, and an absent bridge (`pipe_skip`ped at runtime) fails a blocking suite rather than passing it. **That generic bridge discovery immediately found a live propagation gap** — `scripts/run-ts-suites.sh` ships but is not in `MANAGED_FILES`, so a bootstrapped project freezes it forever while an existing project pulling the managed hook never receives it at all and `pipe_skip`s that stage on every push, permanently, with a remedy message (`blueprint pull scripts/run-ts-suites.sh`) that cannot work on an unmanaged file. `#2c` now checks every file the managed hooks source, both directions. **Finally, `#4b`** — the founder's policy that a migrated suite keeps its `.sh` but stops being run needed the decision to be DECLARED, because "we deliberately stopped running it" and "it fell out of the gate" are otherwise the same observation. A `RETIRED-SHELL-RUNNERS` table carries the mutation recipe PLAN-TASK-018 §5 requires, checked for content (names a concrete existing path, names the case it turned red, and may not argue from the clock — the saving is a consequence of retirement, never its justification), and `#4` accepts a retirement only when the suite has a spec that the chain proves is running. A spec's invocation is proven as a chain — the spec exists, the gate runs `vitest` with no path filter, and `vitest.config.ts`'s include actually covers it — so that breaking any single link fails the push, and "a spec exists somewhere" is never accepted. **`#9` asserts by execution that this control still needs no Node**, re-running the whole file with `node`/`npm`/`npx`/`tsc`/`vitest` replaced by recording shims that exit 127: a project part-way through the migration must not get a wrong answer out of its own coverage control. | S1 | OPEN | Found 2026-09-09 by Philipp (Infrastructure-1) reading the manifest for TASK-018 §7.1; closed by Vitali (QA-1) on `task-018-ts-test-harness`, with 15 negative controls in `.scratch/rig.py` proving each new assertion fails when it should. **Not green yet:** `#4`/`#5` now correctly report that `tests/harness` and `tests/proc-cwd` ship specs the gate and the workflow invoke nowhere — the vitest stage in `.githooks/pre-push-project` and `.github/workflows/security.yml` is the missing piece, and it is owned by the pipeline agent. |
| **BUG-052** | **The marker-aware merge has NEVER run on either of the two managed files that need it, because both describe their own markers in prose — so `blueprint pull` has been falling back to a whole-file copy and destroying every derived project's own content on every pull.** `marker_aware_merge` (`scripts/blueprint:311-323`) counts `BLUEPRINT:BEGIN` and `BLUEPRINT:END` as **substrings** and returns 1 unless the counts match; `pull_file` then warns, writes a `.bp-bak`, and copies the whole file over. Measured: `tests/SUITES.md` **1 BEGIN / 4 END**, `.githooks/pre-push-project` **1 BEGIN / 3 END** — in both cases because a comment or a sentence *explains* where the end marker is (`# BLUEPRINT:BEGIN — blueprint-managed. Put YOUR guards after BLUEPRINT:END.`, and `everything after BLUEPRINT:END is yours and is preserved`). The two things CLAUDE.md promises are "preserved byte-for-byte" — a project's own suite rows and its own gate guards after the managed region — are exactly the two things being replaced, and the only symptom is a yellow warning line inside a command that prints many. **`tests/marker-merge` cannot catch it:** it drives the MECHANISM against fixture files that satisfy the precondition and never asks whether the real managed files do — a control that tests the machine and not the instance, the same gap as `git-isolation` choosing its population from comments (BUG-047). **Fixed for `tests/SUITES.md`** (prose now says "the managed region"); `.githooks/pre-push-project:2,5` still carries it and is a two-line reword. **Guarded** by `tests/manifest` #7b, which asserts BEGIN/END balance for every marker vocabulary in `tests/SUITES.md`, `.githooks/pre-push` and `.githooks/pre-push-project` — so the next marker anyone adds cannot be poisoned by the sentence that documents it. | S1 | OPEN | Found 2026-09-09 by Vitali (QA-1) while adding a marker region to `tests/SUITES.md` for TASK-018 — reading the merge's precondition to check the new marker was safe is what revealed the old ones never had been. Re-open if any managed file's markers stop balancing. |
| **BUG-078** | **`tests/staleness` was green over three separate real defects in the probe it guards — including the one WRITE path into another operator's repository, whose fast-forward-only constraint has never been enforced.** Found by running both implementations of the suite over ten perturbed trees during the TASK-018 port and diffing verdicts mechanically; each is a case passing for a reason other than its subject, which is the TASK-018-RULES R6 class. **(1) `#9` passes with `--ff-only` DELETED from `bp_staleness_fast_forward`.** The fixture rewrote the same `f.txt` on both sides, so a non-fast-forward merge CONFLICTS and git refuses it anyway — the case's "the merge was refused" was satisfied by the conflict, not by the constraint. The real hazard is the opposite shape: a cleanly-mergeable divergence, where dropping `--ff-only` silently creates a merge commit in a repository the operator is not in. That is the bound `scripts/lib/staleness.sh`'s own header calls "the contract, not implementation taste". **(2) `#8` cannot see a defect in the lib's own top-level assignments.** The suite sources the lib once and calls `BP_STALENESS_TIMEOUT=2 bp_staleness_assess …`; an assignment prefix on a FUNCTION CALL wins, because `BP_STALENESS_TIMEOUT="${BP_STALENESS_TIMEOUT:-8}"` already ran at source time. A real caller is a separate process that sources the lib AFTER the environment is set, so there the lib wins — so hardcoding the budget to 30s leaves `#8` green while a fresh-process caller measures 30.2s against the 2s it asked for. The suite was testing an invocation shape that cannot occur in production. **(3) `drift-integration` `#3` never consulted `BP_NO_PROMPT` at all.** It ran with stdin closed, so the `[ ! -t 0 ]` arm suppressed the prompt and the flag's own arm was dead code: deleting `[ "${BP_NO_PROMPT:-0}" = "1" ] \|\|` leaves the case green. Exactly BUG-054's lesson one suite over — a two-armed guard needs the other arm held false, or the case cannot say which fired. **All three are closed by `tests/staleness/staleness.spec.ts`**: `#9` advances with uniquely-named files so the divergence merges cleanly, `lib()` spawns a fresh shell per call, and `D#3` supplies a pty and asserts an unsuppressed control prompts FIRST. Verified by execution — mutants S6, S7 and S10 are PASS/FAIL splits with the shell suite green and the spec red. **One product finding came with them, not a defect:** the `&& [ "$blocker" = "none" ]` conjunct at `scripts/lib/staleness.sh:147` is DEAD — at that line `blocker` is `none` or `not-behind`, and `not-behind` implies `status != behind` — so no mutation of it is observable. Delete it rather than guard it. | S2 | OPEN | Found 2026-09-11 by Elias (Infrastructure-2) porting the sync suites for TASK-018, by mechanical verdict-diffing rather than by reading. Fixed in the spec in the same pass; the shell runners still carry all three and stay in the gate until the central retirement pass. Re-open if any case asserts a refusal whose fixture could be refused for a second reason. |
| **BUG-079** | **Codex R8's fix never worked: `scripts/session-resume.sh --mark` rolls the replay window in TWO `write()` calls, not one, so a concurrent mic flip can still land in a gap that belongs to no replay window — and `tests/session-resume` #13, the case written to catch exactly this, was GREEN against it.** The fix reads `printf '%s\|n' "$roll"` where `$roll` holds two lines, with a comment asserting "a single small `printf` to a file opened O_APPEND is one write(), and no other appender can interleave inside it". The premise is false: **bash's `printf` builtin flushes at every newline**, so strace shows `write(1, "[ts] </aaa>\|n", 12)` followed by `write(1, "[ts] <bbb> head=x\|n", 18)`. `echo`, `printf '%s\|n%s\|n'` and even coreutils `/usr/bin/printf` split identically; only a heredoc fed to `cat` is one write, because bash writes the body to a temp file before `cat` runs (`printf … \| cat` gets it right only by luck — `cat` may read the first line before the second arrives). Measured with a competing appender at ~170 kHz over 60 `--mark` calls: **36 events landed between a close marker and the next open in the UNMODIFIED script, 0 after the fix** (`.scratch/probe-r8.sh`). **Why nothing noticed for a month is the more useful half.** #13 drove the race with a full `bash signal-set.sh` per flip — about 33 Hz against a gap of microseconds, so the chance of landing inside it over 30 marks was under one percent. The case asserted a race it could not provoke, which is BUG-005's shape, and the review that accepted the fix read the same false premise the code states. **Fixed in `scripts/session-resume.sh`** (heredoc to `cat`, with the measurement in the comment so the claim is checkable) **and in `tests/session-resume/session-resume.spec.ts` #13**, whose appender is now the one `printf >>` that `signal-set.sh` actually contributes, issued directly at ~170 kHz over 60 marks — provably able to fail, verified against the reverting mutant. The shell runner's #13 is left as-is and is therefore the one check where the TypeScript port is deliberately STRICTER; it retires with the rest. | S2 | OPEN | Found 2026-09-11 by Matthias (Back-End-1) porting the signal/mic suites for TASK-018. Surfaced by the equivalence method rather than by reading: the mutant that reverts the fix left BOTH implementations green, which is the "neither covers this" verdict, and chasing it found the product defect underneath. Re-open if any journal writer ever emits a multi-line record through a shell builtin again — the general rule is that only ONE `write()` is indivisible, and no shell builtin promises one per statement. |
| **BUG-084** | **`tests/baton-durability` #5 (Codex F4) samples for 6 MILLISECONDS against a publication that takes 32, so all 400 of its samples see NO FILE AT ALL and it has been asserting "the seed was never visible" over an empty directory.** The case exists to prove `signal-set.sh`'s first creation is atomic — that a poller can never read the default `Holder=Nobody / State=IDLE` seed at the canonical path. Its sampler is `for _ in $(seq 1 400)`, and every iteration short-circuits on `[ -f "$abaton" ]` while the file does not exist yet, so the loop is **bounded by an iteration count rather than by the subject** and runs to completion before `signal-set.sh` has finished sourcing `state-dir.sh`. Measured (`.scratch/probe-f4-sampler.sh`, with the defect injected — the seed written straight to `$SIGNAL`): `sampler: ran 0.006s, 0 of 400 iterations saw a file at all` / `publisher: took 0.032s` / `MISSED the seed`. **The case would pass with the seed sitting at the canonical path for a full second**, which is the entire property it claims to guard. Same family as BUG-074 and Elias's BUG-078: a green that cannot go red, believed because it is green. **Closed by `tests/baton-durability/baton-durability.spec.ts` #5**, whose sampler loops `while (!publisher.exited)` — bounded by the SUBJECT, so it cannot finish early and cannot be vacuous; it also keeps the final assertion that the requested baton did arrive, so an empty sample set can no longer read as a pass. Verified by execution: mutant `b3-seed-written-to-the-canonical-path` is a PASS/FAIL split, shell green and spec red. The shell runner keeps its 6 ms sampler and retires with the rest. | S2 | OPEN | Found 2026-09-11 by Matthias (Back-End-1) porting the signal/mic suites for TASK-018. It is a PORT-ONLY-RED row in the equivalence table rather than a disagreement to fix, which is the verdict this method exists to distinguish. Re-open if any sampling or racing fixture bounds itself by a count, a duration or a retry limit instead of by the subject's own completion. |
| **BUG-083** | **The pre-push gate's colour codes lost their escape byte, so every push from a terminal prints `[2m`, `[32m` and `[K` as literal text — and `tests/pipeline` #9/#17 cannot see it.** `scripts/lib/pipeline.sh:91-92` sets `_C_DIM='[2m'`, `_C_OK='[32m'`, `_C_BAD='[31m'`, `_C_WARN='[33m'`, `_C_OFF='[0m'` and `_PIPE_CLR='[K'` with the `ESC` (0x1b) byte ABSENT: `grep -c $'\033' scripts/lib/pipeline.sh` is **0**. Verified on a real pty (`script -qec`): the TTY branch emits zero ANSI bytes and renders `[2m╭─ gate [0m`, `[32m✓[0m`, `a…\r[K[2m…` as visible garbage. Two consequences. (1) Cosmetic, but on the founder's screen on every push. (2) **`#9` ("non-TTY output is free of ANSI escapes") and `#17` ("feed lines are plain text") are VACUOUS with respect to the colour path** — there is no escape anywhere to leak, so a mutant forcing `[ -t 1 ]` true survives BOTH the retiring shell suite and the port. Only a mutant that restores real escape bytes AND forces the TTY branch turns `#9` red, which is how this was found. `tests/pipeline/pipeline.spec.ts` `#9b` PINS the defect and goes RED when it is fixed, at which point `#9`/`#17` become live for the first time and `#9b` should be replaced by a pty-driven assertion. `scripts/lib/pipeline.sh` is the Pipeline agent's file under TASK-018-CONVENTIONS, so this row reports it and does not fix it. | S3 | OPEN | Found 2026-09-11 by Vitali (QA-1) porting the gate/hooks suites for TASK-018, by mutation sweep rather than by reading. Re-open if the escapes are restored without making `#9`/`#17` pty-driven. |
| **BUG-080** | **Three assertions in the gate/hooks suites could not fail on the defect they name, all for one reason: the evidence they look for is satisfiable without the property.** Found by running both implementations over 58 perturbed trees and diffing verdicts; each is the TASK-018-RULES R6 class. **(1) `tests/pipeline` `#3` ("a failed stage halts the pipeline")** greps stdout for a line echoed by a following stage — but with the halt removed that stage RUNS AND PASSES, so `pipe_stage`'s buffer swallows exactly the evidence, and the case stays green. Ported to assert a filesystem side effect, which the buffer cannot hide. **(2) `tests/ts-bridge` `#1b`/`#2c`** grep the gate output for the suite name `demo` — which `pipe_batch_end`'s refusal message `declared but never reported: demo` CONTAINS, so deleting `pipe_stage_report` from the bridge leaves both green while saying, in the matched text, that no suite was reported. Ported to require a rendered result marker (`✓ demo`). **(3) `tests/pipeline` `#19`** compares `[GATE]` counts in the real activity feed across a fixture run, over a path nothing binds to that feed: pointed at a non-existent path the counts are `0 -> 0` and the guard reports clean. Its `[ -f ]` disarm switch was deleted earlier the same day, which was necessary and not sufficient — the vacuity survived one branch later. Ported to ASK `scripts/lib/feed.sh` for the path (`#19a` pins the rule, `#19b` proves the detector fires, `#19c` asserts the run neither wrote to nor created it). All three verified by execution: each perturbation is a PASS/FAIL split, shell green and spec red. | S2 | OPEN | Found 2026-09-11 by Vitali (QA-1) during the TASK-018 port. Fixed in the specs in the same pass; the shell runners still carry all three and stay in the gate until the central retirement pass. Re-open if any assertion's evidence could be produced by the failure it is meant to detect. |
| **BUG-081** | **Two isolation/attribution defects in the retiring gate suites: `tests/pipeline` `#12` decides its verdict from the whole machine, and `tests/no-chain-guard` `#4` lets one fail-closed branch impersonate another.** **(1) `#12` ("no temp directories leak")** counts `/tmp/tmp.*` MACHINE-WIDE before and after five runs, so any unrelated process creating a temp dir in that window fails it. Observed twice in one equivalence sweep, under two mutants that touch no temp-dir code at all, and not at all on the re-run — a global read, which TASK-018-RULES R5 forbids and which is why the suite could not be made parallel. Ported to count inside the scenario's own pinned `TMPDIR`: same property, deterministic, parallel-safe. **(2) `#4`'s four fail-closed cases** assert only the exit code, and `jq` on empty input exits 0 with no output — so with the dedicated empty-payload branch DELETED the guard still blocks two branches later at `payload has no string .tool_name`, with the same code, and the case stays green. The retiring suite's own header states the rule it was breaking ("assert the CAUSE, because otherwise one fail-closed branch impersonates another") and applied it to the missing-jq case alone. Ported so each fail-closed branch asserts its own message. Both verified by execution as PASS/FAIL splits. | S2 | OPEN | Found 2026-09-11 by Vitali (QA-1) during the TASK-018 port. Fixed in the specs; shell runners unchanged until the central retirement pass. Re-open if a suite's verdict can be changed by a process it does not own, or if two refusals share one assertion. |
| **BUG-082** | **Three assertions in the retiring a2bp shell suites cannot fail for the reason they name — found by running both implementations over one mutated tree, not by reading them.** All three were caught by the TASK-018 equivalence method (build a mutant that injects the defect one assertion exists to catch; run the shell suite and the TypeScript port over the same perturbed copy; diff the verdicts). **(1) `tests/a2bp-request` cannot witness the loss of length framing.** `#3`'s header calls itself *"the entire reason the key is framed rather than delimited"*, and with the byte count removed from BOTH framing primitives (`bp_request_frame`, `bp_request_frame_file`) the whole suite stays GREEN — `#1`, `#2`, `#3`, `#3b` and `#4` all pass. Every fixture shifts a byte into a field whose next neighbour is CONSTANT, so plain concatenation still differs; a witness needs the shifted byte to CROSS a boundary. Verified both ways with `.scratch/andreas/probe-framing.sh`: project `ab` + path `x` versus project `a` + path `bx` COLLIDES on the mutant and is distinct on the real library. **(2) `tests/a2bp-pr-filing` `#5` is fail-open, the BUG-074 shape.** It selects the caller's null-rejecting guard with `sed -n '/if \[ -n "$existing" \]/,/; then/p'` — but `; then` sits on the very line the clause under test occupies, so deleting the clause also deletes the range's TERMINATOR and the range runs ~90 lines on, through comment prose containing the word `null`. Verified: with the whole `&& [ "$existing" != "null\tnull" ] && …` continuation deleted, `#5` stays green. **(3) `tests/a2bp-inputs` `#2b` passes on inherited state.** It asserts an absent `blueprint_branch` defaults to `main`, reading `${BP_CFG_BRANCH:-}` — but `#2`, earlier in the SAME shell, already `eval`'d `BP_CFG_BRANCH=main`. With the default deleted, `bp_config_load` refuses and emits nothing, the `eval` is a no-op, and the stale value satisfies the assertion. R5 isolation is what exposes it. **Fixed in the TypeScript ports, each with a named regression case that goes red on its mutant and green on the real tree:** `a2bp-request` `#3c` (new: a byte shifted across a component boundary), `a2bp-pr-filing` `#5` (the condition is bounded by its own continuation backslashes, not by a `; then` pattern), `a2bp-inputs` `#2b` (one scenario per case, so nothing is inherited). **The shell suites are NOT changed by this row** — TASK-018 retires them in one consolidated pass, and both implementations running is how two of these three were found. | S2 | OPEN | Found 2026-09-11 by Andreas (Back-End-2) porting the six a2bp suites to TypeScript under TASK-018, by mechanical verdict diffing over 78 perturbed trees. Number taken after checking all four `docs/*/BUGS.md`; 078-081 were already claimed by concurrent agents, so renumber if 082 collided. Re-open if any assertion's range or fixture is found unable to distinguish the defect its own comment names. |
| **BUG-086** | **A bug number with COMMITS and no row is invisible to every control in the repo.** `tests/bug-numbers` guards the opposite direction — one number must not carry TWO rows — and nothing guarded it carrying NONE. BUG-073's row was dropped by a whole-file write (`5dd158e` rebuilt the table from `c59a2e3`, which predates the row that added it), and it stayed dropped through two further sessions while its fix sat on `main`; the 2026-09-11 `lcm` pass found it by reading the folders. Fourth instance of the stale-buffer clobber that day and the only one no test could see. The naive check is a false-positive factory — "every `BUG-NNN` mentioned anywhere has a row" reports twelve on a healthy tree, because `BUG-099`/`BUG-123`/`BUG-999` are SYNTHETIC numbers inside fixtures — which is why nobody built it. Asking git instead ("which numbers have a `BUG#N:` commit subject?") has no synthetic population at all, since a fixture never commits here: zero false positives on the real tree, and red at `2bde427^` where BUG-073 had two commits and no row. | S2 | OPEN | Found 2026-09-11 by Timon (Front-End-1) during the TASK-018 port, from the gap the same day's `lcm` pass had to close by hand. Closed by `tests/lifecycle-docs` #6 (`bugsWithoutRows`), with the mention-is-not-a-row distinction asserted so a row's own commit message cannot satisfy it. Re-open if a rowless number can reach `main`. |
| **BUG-087** | **The `new-project.sh` arm of `tests/git-isolation` #3's discovery predicate cannot match any realistic call site, and never has.** BUG-047 widened the predicate to `(git\|new-project\.sh)` specifically to catch the `bootstrap-identity` class — a suite that drives git WITHOUT typing `git`, because `scripts/new-project.sh` does it for them, and which left `chore(bootstrap): initialize …` as a commit in a victim repository. The prefix guard excludes `[[:alnum:]_./-]` before the token and `/` is in that set, so `…/scripts/new-project.sh` is rejected before the name is considered. Every suite in this repo invokes it by path; only a bare `new-project.sh …` matches, which nothing writes. Probed against the shell regex: three realistic spellings, none matched. It has also never caught anything — at `d24b472`, the commit that added the arm, `bootstrap-identity` was in the member set via a `git config` line on `:54`, i.e. the OTHER arm — so the control passed while the half added for the motivating case was inert. Same shape as BUG-074: a guard that appears to cover something it does not. | S3 | OPEN | Found 2026-09-11 by Timon (Front-End-1) during the TASK-018 port. PINNED, NOT FIXED: widening a predicate under a migration changes a verdict, which is the one thing a port must not do silently. `tests/git-isolation` #3 carries the case asserting both the shared blindness and that the arm is present rather than deleted. Severity is S3 because the population it would protect is currently EMPTY — all four bootstrap suites are already specs — and shrinking. Fix by matching the basename regardless of its path, with a reproducer that is a path-qualified invocation. |
| **BUG-088** | **`tests/git-isolation`'s two hardcoded population floors fail a HEALTHY tree as the TASK-018 retirement wave proceeds, and the control goes red on the FIRST anchor retirement — through NEITHER floor.** `[ "$found" -lt 15 ]` sits at 20 git-driving shell suites today (`git-isolation`'s own runner among them); the isolation/lifecycle group retires three members (`commit-msg-gate`, `commit-subjects`, `state-dir`), leaving 17, so three FURTHER removals from any other group take the gate red while nothing is broken. What goes red on the FIRST anchor retirement is `#1`'s `[ ! -f "$s" ]` and `#3`'s `missing_anchor` branch — not the anchor floor, which cannot fire at all (see the mechanism correction in Notes). Both floors were correct when written and are now booby traps inside the migration they sit in — a test failing for the opposite of a defect, which is the shape CLAUDE.md rejects in §"The blueprint's `main` is its trunk" (a gate someone must route around to do their job protects nothing). Measured: the ported spec and the shell runner agree on ten of eleven perturbed trees, and the eleventh is exactly this — six fully-guarded suites including every anchor, shell red, port green. | S1 | OPEN | Found 2026-09-11 by Timon (Front-End-1) during the TASK-018 port. FIXED in `tests/git-isolation/git-isolation.spec.ts`: the floors are replaced by the half that would actually have caught BUG-047 — every anchor still ON DISK must be found by the predicate — plus an explicit assertion when the population empties, so the intended end state is reached visibly rather than silently. **That end-state assertion was itself RED at the end state and is fixed under BUG-109** — it asserted no `*.sh` remained under `tests/`, and `scanGitIsolation` considers `tests/helpers/proc-cwd.sh`, which is alive; it now asserts every anchor became a spec. **So "the port is retirement-safe" was not true when this row claimed it**, and was verified rather than inherited only on 2026-09-11. The anchor list is now INTERSECTED with disk rather than asserted against it. **The central retirement pass must land this port before retiring any git-driving shell suite**, or the gate goes red on the retirement itself. Re-open if a healthy tree can fail this control. **ARITHMETIC CORRECTED 2026-09-11 by Alexey (Architect, Codex) in cross-provider review; conclusion unchanged.** The count floor is NOT one retirement from red. The population is 20, and only **three** of the reviewed runners are members of it — `state-dir`, `commit-msg-gate` and `commit-subjects` — so retiring them leaves **17**, and `[ "$found" -lt 15 ]` fails only after three FURTHER removals. **MECHANISM CORRECTED 2026-09-11 by Christian (Architect); the ordering conclusion is unchanged and correct.** This row twice named `[ "$_gi_n" -ge 3 ]` as the floor that "breaks FIRST" / "fails **immediately**". **It cannot fail on any tree.** It word-counts the hardcoded literal `GI_ANCHORS="marker-merge gate-arming commit-subjects"` (test.sh:101,107-109), which is never derived from disk, so `_gi_n` is 3 over every possible tree including an empty one — deleting an anchor *runner* does not shorten the *literal*. MEASURED by deleting `tests/commit-subjects/test.sh` from a scratch copy and running the real runner: baseline PASS (`#3 all 20 git-driving suites unset GIT_DIR themselves`), then `FAIL: #1 tests/commit-subjects/test.sh not found` and `FAIL: #3 discovery MISSED suites #1 proves drive git: commit-subjects`. **`git-isolation`'s port must still land before any git-driving shell suite is retired** — an anchor retirement IS immediately red, just two lines elsewhere. A reader sent to the anchor floor finds a line that can never fail, concludes the constraint is imaginary, and retires an anchor. Recorded rather than quietly fixed because this row is a SEQUENCING CONSTRAINT for the retirement pass, and a wrong number in a constraint is worse than no number: it invites the next reader to recompute the margin and act on it. |
| **BUG-089** | **`tests/agent-activity-bound` and `tests/subagent-feed` treat an empty `bp_proc_cwd` result as "does not match", which the helper's own contract forbids, and neither calls `bp_proc_cwd_available`.** `tests/helpers/proc-cwd.sh` states it explicitly — *"Callers MUST treat empty as unknown, never as does not match"* — and both consumers do exactly the forbidden thing with `case "$(bp_proc_cwd "$p")" in "$WORK"*)`. On a host with no `/proc` and no `lsof`, `supervisors()` is therefore always 0 and every assertion of the form `-eq 0` passes over zero information: `agent-activity-bound` #5b, *"--stop leaves zero residue"*, reports clean while a real supervisor leaks. Measured rather than reasoned — with one live process whose cwd IS under `$WORK`, the real helper counts 1 and the assertion correctly fails; a mechanism-less helper counts 0 and the assertion PASSES. That suite's own #6 message says "0 is the wedged/no-feed outcome and is not a pass", and #5b is the case where 0 is accepted as one. Latent on Linux, live on macOS — the BUG-036 host. | S2 | OPEN | Found 2026-09-11 by Timon (Front-End-1) while establishing whether `tests/proc-cwd/test.sh` was retirable. NOT FIXED — both suites belong to other porting groups. The vacuity guard itself is now safe: `bp_proc_cwd_available` had exactly one caller anywhere (the shell runner being retired), and `tests/proc-cwd/proc-cwd.spec.ts` now carries it plus #2-#5 by DRIVING the shipped helper, so the property survives retirement. Fix by calling `bp_proc_cwd_available` once at the top of each consumer and failing loudly, or by porting both suites. Re-open if a count of owned processes can be 0 for want of a mechanism. |
| **BUG-090** | **Every feed-driving shell suite pins `AGENT_STATE_HOME` and not `AGENT_SIGNAL_FILE`, so one exported variable points a fixture supervisor at the operator's LIVE baton.** `agent_signal_file` (`scripts/lib/state-dir.sh:211`) honours `$AGENT_SIGNAL_FILE` BEFORE it consults the state dir, so moving the state dir does NOT move the baton. `tests/watcher-liveness`, `tests/roster` #12, `tests/subagent-feed` and `tests/agent-activity-bound` all pass only `AGENT_STATE_HOME`, and are correct purely because nothing in their environment happens to set the other one. **Proven by execution:** `AGENT_SIGNAL_FILE=<live baton> bash tests/watcher-liveness/test.sh` → `FAIL: #6 the feed emitted no warning` and `FAIL: #6 the warning appeared 0 times`, because the fixture feed was reading the repository's real baton instead of its own. This is the BUG-046/BUG-047 class exactly — ONE missing line in a test, surviving months of review — and the same shape as the `unset AGENT_SIGNAL_FILE` BUG-046 was. It is worse than a red test: a suite in this state READS live coordination state, and `tests/watcher-liveness` #6 drives a supervisor that would then compare the live mic against a fixture lock. | S2 | OPEN | Found 2026-09-11 by Philipp (Infrastructure-1) porting the feed/persona suites for TASK-018 — the harness sets `AGENT_SIGNAL_FILE` for every scenario, so the omission failed loudly instead of silently. FIXED in the ports: `tests/helpers/feed-fixture.ts` pins BOTH, with the reason stated where the pinning happens. The shell runners are NOT changed — TASK-018 retires them centrally, and both implementations running is what surfaced this. Re-open if a fixture can reach the live baton by inheriting one variable. |
| **BUG-091** | **A running feed cannot see a same-size roster edit made within the same second, because `roster_token` is `file:size:mtime` and `f_mtime` is whole seconds.** `scripts/agent-activity.sh:132` builds the roster's change token from `f_size` and `f_mtime`, and `f_mtime` is `stat -c %Y` / `stat -f %m` (`:149`, `:153`) — SECONDS. A rename that preserves the cell width, which is what a padded roster table and every in-place `sed` produce, yields an identical token whenever it lands inside the same second as the previous poll's stat, and the supervisor never re-resolves identity. This is precisely the I-3 defect `tests/agent-activity-bound` #9b pins for the BATON — where it was fixed by making `signal_token` a `cksum` of the CONTENT (`:478`) — left unfixed one function away for the ROSTER, with nothing pinning it. **Measured, not reasoned:** `tests/roster` #12 renames `Alisa`→`Dara` inside fixed padding (identical byte length) and passes only because it sleeps 1 s before the edit and 2 s after; ported to a condition-based wait (R4) the same edit failed intermittently, roughly one run in three, and became deterministic as soon as the new name changed the file's size. The operator-visible symptom is BUG-010's own: the displayed persona and the roster disagree, and nothing says so. | S3 | OPEN | Found 2026-09-11 by Philipp (Infrastructure-1) during the TASK-018 port, from a flake that turned out to be a real product property. NOT FIXED — `scripts/agent-activity.sh` belongs to the agent-activity slice, which TASK-018-TARGET §3.2 sequences LAST because that daemon holds a `flock` and BUG-001 was a 2.7-day fork bomb. Fix by giving `roster_token` the same content `cksum` the baton already uses, with a reproducer that rewrites the roster to the same byte length inside one second. `tests/roster/roster.spec.ts` #12 documents the property at the point the change was made. Re-open if a roster rename can be invisible to a running feed. |
| **BUG-092** | **Five vacuity and coverage defects in the retiring feed/persona suites, every one found by running both implementations over a perturbed tree rather than by reading them.** **(1) NOTHING RENDERS THE CODEX LAUNCHER'S LABEL, and two probes prove it.** `tests/codex-persona-label` #3/#4 are source greps for `bp_roster_label` and `AGENT_SIGNAL_HOLDER` in `scripts/start-codex-signal-watch.sh`, so a launcher that resolves the roster from a path holding no roster (`bp_roster_label "/nonexistent" …`), and one that still READS `AGENT_SIGNAL_HOLDER` while labelling with a constant, BOTH pass shell and port. Each reproduces BUG-021's measured symptom — every Codex line under one name — with the shared function and the per-dispatch read syntactically intact. Closing it needs a rendered assertion, which needs a Codex; `tests/subagent-feed` covers the FEED's label end to end and nothing covers the launcher's. **(2) `tests/codex-persona-label` #4 is a FALSE POSITIVE on a commented-out pump.** It greps the raw file, so adding `# pump "$state/codex-runs.log" jsonl "CODEX"` as an explanatory comment to `scripts/agent-activity.sh` turns the suite red — forcing the explanation to be deleted to stay green, which is the opposite of what the check is for. The port strips comments first. **(3) `tests/watcher-liveness` #2's lock-path assertion has NO PASS BRANCH.** `case "$lock" in "$T"/*) : ;; *) fail …` can only ever print a failure, so on a healthy tree it emits nothing at all and its absence from the output is indistinguishable from the assertion having been deleted — while the property it guards (derive the lock from the baton's directory, never from a repo root) is the whole of #7. Ported as a named case. **(4) `tests/roster` #8 checks 5 of the 15 shipped example persona names.** The list is hardcoded (`Sylvia|Kathrin|Slava|Yannik|Jesko`), so a literal of any of the other ten ships to every derived project unchecked, and the list needs hand-editing whenever the example roster changes — the second-copy-that-drifts shape the suite itself is about. Verified: `BP_DEFAULT_REVIEWER="Matthias"` in executable code, shell GREEN, port RED. The port derives the list from `AGENT_ROSTER.example.md`. **(5) TWO BLIND SPOTS BOTH IMPLEMENTATIONS SHARE, recorded rather than fixed.** `tests/watcher-liveness` #5's source greps are defeated by any rename that keeps the identifier as a substring (`bp_watch_liveness` → `bp_watch_liveness_disabled` still matches), which is harmless only because #6 covers the same property behaviourally; and an A-09-class SPLIT DERIVATION between the feed's lock path and the watcher's is invisible to both, because in every fixture the baton directory and the state directory are the same directory — pointing the feed at `$state_dir` instead of `dirname "$signal_file"` changes no verdict anywhere. | S2 | OPEN | Found 2026-09-11 by Philipp (Infrastructure-1) porting `agent-activity-bound`, `subagent-feed`, `codex-persona-label`, `roster` and `watcher-liveness` for TASK-018, by mechanical verdict diffing over 60+ perturbed trees. Parts 2, 3 and 4 are FIXED in the specs; parts 1 and 5 are PINNED in the spec headers and are coverage gaps mutation cannot close, since each needs a NEW assertion rather than a stricter one. Shell runners unchanged until the central retirement pass. Re-open if a source check can be satisfied by a rename, or if an assertion has no observable success. |
| **BUG-094** | **Four `tests/agent-activity-bound` assertions cannot fail on the defect they are named for, and both the shell suite and its TypeScript port share all four — so mutation cannot close them.** Found by building one mutant per shipped mechanism and running both implementations over each of 19 perturbed trees. **(1) `#8b` ("rotation handled — inode change resets offset")** passes with the inode-reset branch DELETED, because its rotated-in file (`AFTER-ROTATE\n`, 13 bytes) is SMALLER than the offset the reader had reached, so the truncation branch `[ "$size" -lt "$off" ] && off=0` resets it anyway. One branch masks the other and the case cannot tell them apart; a witness needs a rotated-in file LARGER than the old offset. **(2) `#18` ("short sink emits nothing and consumes nothing")** passes with the short-capture guard `[ "${got:-0}" -ne $((size - off)) ]` replaced by `false`. Dropping one byte from a newline-terminated payload yields a capture with no trailing newline, which the fragment path already withholds — so nothing is emitted with the guard or without it. A witness needs a MULTI-RECORD payload where the short capture still ends in a newline, so a complete record is available to escape. **(3) `#15f` ("foreground spawns no tee")** passes with `emit` piping every line through `tee`, because the child is transient and a `ps` snapshot essentially never catches it; the assertion can only see a long-lived `tee`. **(4) `#13` ("stale state after SIGKILL reports not running")** passes with `feed_is_running` made to trust the state file, because `resolve_supervisor` then rejects the dead pid and `--status` still exits non-zero — one fail-closed branch impersonating another, the shape BUG-081 names. The defect IS caught, by `#13b` and `#4`, so this one is a mis-attribution rather than a hole. **Also measured, and the reason a first sweep had to be discarded: `#15` and `#2` count `tail -n0 -F` MACHINE-WIDE**, and the RC-2 mutant tree left 89 orphaned followers behind (a follower outlives the supervisor that forked it), so ONE tree's debris produced spurious `#15,#2` reds on six later trees whose mutations touch no process code at all. Those reds vanished on a clean re-run with follower sweeping in place. | S2 | OPEN | Found 2026-09-11 by Philipp (Infrastructure-1) porting the suite for TASK-018, by mechanical verdict diffing over 19 mutants plus controls. PINNED, NOT FIXED: all four need a NEW assertion or a changed fixture, which is a verdict change, and a port must not make one silently. The machine-wide counting IS fixed in the port (`ownedProcesses()` scopes every count to the scenario's workspace, and `tests/agent-activity-bound/agent-activity-bound.spec.ts` records the measurement). Fix #8b with a larger rotated-in file, #18 with a two-record payload, #15f by asserting on the process TREE over a window rather than a snapshot, #13 by asserting the reason `--status` gives. Re-open if an assertion's own fixture cannot distinguish the branch it names. |
| **BUG-095** | **`tests/agent-activity-bound` #15 and #2 counted `tail -n0 -F` MACHINE-WIDE, so both cases were unsatisfiable on any host running something whose argv merely CONTAINS that string — and the pre-push gate could not pass.** The supervisor half of each assertion was already scoped to the fixture by cwd (BUG-036); the follower half was not, and `ps -eo args | grep -c "[t]ail -n0 -F"` counts an agent's shell wrapper, and counts the `pgrep -fa 'tail -F'` you run to DIAGNOSE it. Measured on this host: **zero real followers resident, and the predicate answered two.** Same class as the note already in BUG-094 — the RC-2 mutant tree's 89 orphaned followers deciding six later trees' verdicts — but this instance needs no mutant at all, only a developer host with a second project's feed up (`linkedin-watcher-agent`'s supervisor, pid 9699, running legitimately from its own checkout). **Fixed by extracting the loop `supervisors()` already had into one `owned <argv-pattern>` predicate and deriving both `supervisors()` and a new `tails()` from it** — the counting predicate changed, no case added, nothing weakened: a follower our supervisor forks inherits the fixture cwd, so *zero followers rooted at OUR tree* still reds the RC-2 leak. **Non-vacuity measured rather than reasoned** (`.scratch/owned-check.sh`): with a real `tail -n0 -F` resident outside the fixture the old predicate answers 1 and the new answers 0, and with one resident INSIDE the fixture the new predicate answers 1. | S1 | OPEN | Found 2026-09-11 by Markus (Security-1); the founder authorised it explicitly as a fix to an EXISTING shell suite, which the all-TypeScript ruling permits where new shell test coverage would not — the suite is unretirable until reviewed and was unpassable until scoped. Full suite re-run GREEN with the other project's feed up and a decoy follower resident. The port needed no change: `ownedProcesses()` (`tests/helpers/feed-fixture.ts`) already scopes every count to its scenario's workspace, which is the measurement `agent-activity-bound.spec.ts`'s docblock records. Re-open if any assertion counts a process it does not own. |
| **BUG-096** | **Every TASK-018 mutation harness builds its tree with a FRESH `git init` + `git add -A`, and a fresh git applies `.gitignore` in full — so the sixteen files the real repo TRACKS while `.gitignore` also names them are absent from the baseline, and BOTH questions that git is asked come back wrong.** Tracked beats ignored in the real repo; in a fresh init nothing is tracked yet, so `.gitignore` wins. **(1) The CHANGED-NOTHING guard goes blind.** `git status --porcelain` cannot see a mutation to a file it never added, so the guard Christian added *precisely because* a hand-written file list had silently encoded the answer now answers `MUTANT CHANGED NOTHING` for a mutant that rewrote its whole target. **Observed, not reasoned:** a mutant renaming every `bp_roster_label` in `scripts/start-codex-signal-watch.sh` — which is `tests/codex-persona-label`'s entire subject — was refused a verdict on exactly these grounds. **(2) `git archive HEAD` inside the copy loses TEN SHIPPING FILES**, and `build_tree`'s own comment says the fixture builders and `suite-sync` #1d depend on that query answering inside the tree. Measured (`.scratch/markus-archive-gap2.sh`): real=184 paths, copy=174; absent are `CLAUDE.md`, `docs/DoD.md`, `AGENTS.md`, `AGENT_SIGNAL.md`, `.claude/settings.json`, `docs/PUBLISHING.md`, `docs/way-of-working.pdf`, `scripts/new-project.sh`, `scripts/codex-signal-watch.sh` and `scripts/start-codex-signal-watch.sh`. So every mutant tree tests a tree that differs from the real one in a specific, silent way, and `git show HEAD:<file>` — which `gate-arming`'s fixture relies on — reports *does not exist in HEAD* for any of the sixteen. **Present in all three harnesses:** `.scratch/elias-equiv.sh` (sync group, the pass that certified it), `.scratch/vitali-equiv.py:build_tree` (gate group) and `.scratch/equiv.sh`. **A measuring apparatus carrying the defect it measures for, which is the fourth instance this week** — and the only reason it cost minutes rather than a run is that the guard fails LOUDLY (INVALID, no verdict) instead of reporting the fabricated *neither implementation covers this* a missing guard would have produced. | S2 | OPEN | Found 2026-09-11 by Markus (Security-1) closing the feed group's R6 gap, from a mutant that was refused a verdict. **Fix is `git add -A -f`** — the guard's question is *did the mutant change any byte of THIS TREE*, so every byte `rsync` copied belongs in the baseline; `rsync` already excludes `.git/`, `logs/`, `coverage/`, `.scratch/` and `tests/node_modules/`, which is what makes `-f` safe here. Applied in `.scratch/markus-feed-r6.sh` and verified: the same mutant then reported `changed=1` and reddened exactly its named assertion in both implementations. **NOT yet applied to the other three harnesses** — they are scratch, they have already produced their tables, and changing a measuring instrument after the measurement without re-running it is worse than leaving it. Regenerate the list with `git ls-files -i -c --exclude-standard` rather than copying it; it has grown twice. **BLAST RADIUS MEASURED, and it is smaller than the defect** (`.scratch/markus-suitesync-probe.sh`): `tests/suite-sync` is the one suite whose oracle asks `git archive HEAD` inside the tree, and its healthy control is GREEN in both implementations either way — 175 paths plain, 190 forced — so the sync group's certification is not invalidated. The a2bp group is untouched for a different reason: `cmd_a2bp` writes its managed list from the `MANAGED_FILES` array verbatim and works against the fetched REMOTE base, so it has no local HEAD to expand from. **Neither measurement is a mutant-by-mutant re-run**, and the honest statement is that no mutant in either published population targets a file in the blind set — which is luck, not design. **CORROBORATED FROM A SECOND HARNESS the same day:** `.scratch/vitali-equiv.py`'s `r6-guard-not-wired` mutant rewrites `.claude/settings.json` — also one of the sixteen — and aborted with the identical `MUTANT CHANGED NOTHING` while the file had in fact been rewritten. **The two halves have DIFFERENT blast radii and the distinction is what a reader needs:** a suite is affected only if it MUTATES one of the sixteen (half 1) or READS one through `git archive`/`git show HEAD:` in the tree (half 2). `gate-arming` does the latter but on `scripts/lib/gate.sh`, `scripts/agent-activity.sh` and `scripts/blueprint`, none of them in the set, so its verdicts were identical before and after. **And `-f` alone is not the whole guard.** It repairs every file `rsync` copied, which is the stated hole, but the stronger form ORs it with a byte comparison against what the mutator saved — a check derived from the mutant DEFINITION rather than from any list, so git catches side effects outside the declared paths and the byte compare catches a path git cannot see for any reason at all. Adopted in the gate group's harness. Re-open if a mutant tree's git can disagree with the tree on disk. |
| **BUG-100** | **The mic catalogue's own runner resolved vitest through `npx`, which is the silent-no-op hazard the founder's standing instruction names — and it clobbered its own evidence file on every invocation.** `run.py` invoked `npx vitest`, which resolved the PINNED binary only because the cwd happened to be the mutant tree's `tests/`; a miss fetches an unpinned vitest that runs no files and exits 0, and a zero exit with an empty report is indistinguishable from a green suite — so a whole equivalence run would read as *both implementations agree, all green* while nothing executed. This is the R6 measuring-apparatus class again (BUG-096 is the same week's other instance) and it is worse here than in scratch, because this catalogue is TRACKED deliberately: PLAN-TASK-018 §5 requires the mutant catalogue be a tracked artefact rather than oral history, so the defect shipped with the evidence it produces. **Second half:** `outputs/results-last-run.json` was written per invocation with no target, so each run overwrote the last — the recorded red sets for four of the five suites survived only as `outputs/*.log`, and the JSON that reads as the canonical record held one suite. | S2 | OPEN | Found 2026-09-11 by Matthias (Back-End-1) while closing the mic group's R6 gap. **FIXED in the same pass:** `tests/node_modules/.bin/vitest` with an explicit `--root`, plus a `#no-tests-ran` verdict so an empty report is a distinct outcome rather than a green one, and `--out NAME` with `suite:mutant` targets so a run names its own artefact. Re-open if an evidence artefact can be produced by a runner that executed nothing, or overwritten by the next run. |
| **BUG-101** | **Four R6 findings across the eight isolation/lifecycle ports, the first of which is an assertion whose own `expect` has never been observed failing — because a STRONGER guard aborts the scenario first.** **(1) `tests/proc-cwd` block-1 `#1`, `#3` and `#5` — the three asserting `pwd -P` equals the workspace path — have no ISOLABLE negative proof on Linux.** The mutant that reproduces the macOS shape (drop the workspace realpath, point `TMPDIR` at a symlink) turns all TEN cases red with the same thrown error, because `assertOverrideAllowed` realpaths both sides of its containment test and a non-physical root aborts every scenario **before any assertion runs**. The defect IS caught, and caught earlier, which is the right architecture — but these three cases have no evidence of their own, and a guard that moves would take their coverage with it silently. Same shape as BUG-094(4), one layer up. **(2) Three `commit-msg-gate` `#3` cases are guarded THREE TIMES OVER** (missing argument, unreadable message file, empty message), so no single-edit mutant isolates any of them — measured: two separate mutants each red only `#3 … defended TWICE`. The `suite-sync` `#7-bare` shape: correct design, untestable cases. **(3) Three assertions are about git and the kernel rather than about this repo** (`#4 a repo WITHOUT it is ungated`, `#10c` ELOOP, `#6b` decoy hostility) and are given proof by FIXTURE perturbation instead of code mutation — the M6/Q15 shape, recorded so nobody reads them as code-covered. **(4) Two mutants nothing kills:** loosening `git-isolation`'s `UNSETS_GIT_DIR` predicate to a bare `/GIT_DIR/` is invisible because comment-stripping already removes what it would admit — so a CODE line merely mentioning the variable, and the documented refusal of `unset -v GIT_DIR`, are untested; and `.githooks/commit-msg`'s missing-lib `exit 1` is unobservable inside its own suite, seen only by `commit-subjects` `#6`. | S3 | OPEN | Found 2026-09-11 by Timon (Front-End-1) closing the isolation group's R6 gap for TASK-018. **41 of 136 cases had no observed red before this pass; 40 new mutants over 88 trees take it to 136/136 with no row lacking a verdict** — matrix and per-case attribution in `docs/doing/TASK-018-R6-isolation/`. **The denominator came from `vitest --reporter=verbose`, not a grep**, because a grep for `it(` cannot see an `it.each` table and 21 of the 136 live in one — including the `git-isolation` anchor table, which is where the only per-anchor evidence exists. PINNED, NOT FIXED: each needs a new assertion or a changed guard, which is a verdict change a port must not make silently. Re-open if an assertion's evidence belongs to a different assertion. |
| **BUG-102** | **`sort -u` is not a set operation under a UTF-8 locale, and an R6 harness derived BOTH its denominator and its observed red sets through one.** glibc collation ignores `#` at the primary level, so `#2 names no item: BUG: no number` and `#2 names no item: BUG#: no number` COLLAPSE into a single entry — the case inventory read 22 where vitest ran 23, and `uniq -d` found nothing because it compares bytes and the two lines differ. A mutation matrix built this way undercounts the cases it must cover AND loses reds it did observe, in the same operation, and reports a fuller house than it measured. Same class as BUG-043. `LC_ALL=C` on every sort in a set position. **Second, and the reason this is one row: A ONE-COMMIT BASELINE IS NOT NEUTRAL EITHER.** `tests/lifecycle-docs`' REAL TREE case reads `git log --format=%s` in the tree it runs in and asserts a >50-subject floor as its OWN non-vacuity control — so over a fresh-init mutant tree it goes red on a PRISTINE copy, which reads exactly like a finding and is a harness artefact. Both are the same failure as BUG-096: the apparatus answering a question about itself and being read as answering one about the subject. | S2 | OPEN | Found 2026-09-11 by Timon (Front-End-1) during the isolation group's R6 pass, both by being contradicted — a count that disagreed with vitest's, and a red on a tree with no mutation in it. **FIXED in `docs/doing/TASK-018-R6-isolation/code/iso-equiv.sh`:** `LC_ALL=C` throughout, and `seed_history` replays the real commit subjects as empty commits rather than copying `$SRC/.git` (five agents commit into that repo, so a copy can catch a torn index) — with the CHANGED-NOTHING guard's baseline count taken AFTER seeding instead of hardcoded to 1. **NOT applied to the other harnesses**, which have published their tables; changing a measuring instrument after the measurement without re-running it is worse than leaving it and saying so. Re-open if a harness derives a case list or a red set through a locale-sensitive sort, or if a suite that reads its own git history is run over a synthetic baseline. |
| **BUG-103** | **A mutant can apply to a COMMENT, and the result is a both-green verdict indistinguishable from a real finding — *applied* is not *applied to code*.** The literal-substitution guard every R6 harness here relies on (`sub` exits non-zero when its target text is absent, because a mutant that matches nothing reads exactly like "neither implementation covers this") is one level too shallow. Measured: a mutant targeting `  local TL='{{PROJECT_NAME}}' TU='{{PROJECT_NAME_UPPER}}'` in `scripts/lib/placeholders.sh` matched the file's own HEADER, which quotes the line it documents — the code line's two-space indent is a substring of the comment's three. It applied, changed a byte, satisfied the CHANGED-NOTHING guard, and changed nothing that runs. **Any harness doing literal substitution against a SELF-DOCUMENTING file has this**, and this repo's files are self-documenting by policy — the same property `tests/codex-persona-label`'s port strips comments for, and the one BUG-092(2) is about from the other side. The properly-anchored replacement reds `a2bp-contamination` `#1 #1b #25 #25b`, so the assertion was never the problem. | S2 | OPEN | Found 2026-09-11 by Jesko (QA-2) closing the a2bp group's R6 gap for TASK-018. **Control shipped, not just a note:** `docs/doing/TASK-018-EQUIVALENCE-a2bp/code/diff-audit.sh` prints what each mutant actually changed, and every both-green mutant in that catalogue was audited through it before being recorded as a finding. Fourth member of the family BUG-096 and BUG-102 name — the applied-check consulting something that encodes its own answer instead of the executed bytes. Re-open if a mutant's application is established by anything weaker than "the changed bytes are on a line that runs". |
| **BUG-104** | **Five a2bp assertions cannot be falsified by the defect they NAME. None is dead — each has a red mutant — so the gap is between an assertion's TITLE and its witness, which is the version of this that no red-set count can show.** **(1) `a2bp-pr-filing` `#2` and `#3`** name `--json state,url` and `--state all`, and the `gh` shim reproduces gh's `--jq` handling while ignoring every other flag — so mutating either query flag reaches nothing, and only a mutation of the jq FILTER reds them. The cases pass whether or not `a2bp` asks `gh` for the right fields. **(2) `a2bp-request` `#3` ("content boundaries are framed") and `#3b` ("framed, not newline-delimited")** are reddened by no unframing mutant at all, because in both fixtures the shifted byte's next neighbour is constant — the same measurement `#3c`'s own comment reports from the other side. `#3c` IS the witness, and it is TS-only, so the shell runner has no witness for this property anywhere. **(3) `a2bp-contamination` `#21` ("a diff capability/runtime failure fails closed")** stays GREEN with the `diff_rc > 1` guard it names removed, because the residual-project-name scan blocks the same bytes; both have to go before anything reds. **This class is why a coverage number is not an answer.** All 109 assertions in this group now have an observed red — `build 17/17 · contamination 36/36 · e2e 13/13 · inputs 21/21 · pr-filing 7/7 · request 15/15` over 98 trees — and these five are inside that 109. | S2 | OPEN | Found 2026-09-11 by Jesko (QA-2) closing the a2bp group's R6 gap for TASK-018, from a starting gap of ALL 109: five of the six docblocks pointed at a migration report that carries no per-assertion table, and `a2bp-contamination` had no equivalence record at all. **FIXED 2026-09-11 by Matthias (Back-End-1)**, after Andreas (Back-End-2, Codex) refused certification of the six ports over exactly these five. Every fix is in a FIXTURE, not in an assertion, so no verdict moved: `ghShim` now implements gh's `--state` filtering and `--json` field selection instead of consuming only `--jq` (so `F2` reds `#2` and `F3` reds `#3`); `#3` and `#3b` each carry a second key pair whose shifted bytes BECOME the next record's header, or forge a newline boundary, instead of landing in front of a constant neighbour (so `K3` reds `#3` and `K4` reds `#3b`); `#21`'s carrier line carries an `a2bp-allow` marker — the product's own sanctioned override, which suppresses the residual-name scan on that line — so the fail-closed `diff` guard is the only thing left and `C15` alone reds it. All five observed, not predicted: one perturbed tree per mutant, both implementations run over it. **`K3` and `K4` are port-stricter results**: the retiring shell suites stay entirely GREEN under both. Catalogue, per-mutant red sets and the caveat per suite in `docs/doing/TASK-018-EQUIVALENCE-a2bp/`, with each spec's docblock naming its own. **Fix `#2`/`#3` by asserting on what the shim was ASKED rather than on what it returned; `#3`/`#3b` with a fixture whose shifted byte has a varying neighbour; `#21` by removing the residual scan's cover for it.** Re-open if an assertion's title names a defect that cannot red it. |
| **BUG-105** | **Two `a2bp` product behaviours that NO assertion watches, in either implementation — both-green over the injected defect, re-confirmed serially.** These are the class mutation testing structurally cannot find by watching what goes red; they are found by watching what does not. **(1) `bp_file_base_content` can align at the ROOT coordinate instead of `bp_base_path`'s, and nothing reds.** No case in the six suites drives `a2bp` end to end against a base moved under `scaffolding/` — which is precisely the restructure TASK-018-TARGET §2 puts next, and the hazard that section names is path mapping. The function's own header states the consequence: an empty base for a file that EXISTS, so every line reads as new, the staged restore has nothing to align to, and A-07's placeholder restore degrades to nothing on exactly the lines it exists for. **(2) `cmd_a2bp`'s required-libs guard can become a `continue` and nothing reds.** Its comment cites BUG-003 — *"a guard that cannot run is not a guard that passed"* — and no assertion holds it to that. A2bp with a lib missing then proceeds with the contamination scan absent, which is the door BUG-002 and A-09 came through. | S2 | OPEN | Found 2026-09-11 by Jesko (QA-2) closing the a2bp group's R6 gap for TASK-018, over 98 mutant trees. **NOT a port regression** — the shell runner is equally blind, so this is missing coverage in both and needs a NEW case rather than a stricter one. **FIXED 2026-09-11 by Matthias (Back-End-1)**, both with a new case in `tests/a2bp-contamination/a2bp-contamination.spec.ts`: `#29` drives a2bp end to end against a base whose copy of the carrier lives under `scaffolding/` (with a root file of the same name left in place, which is the state DURING the move and the wrong answer a root-coordinate alignment picks up) and requires the request to be FILED with the placeholder restored — `B15` reds it. `#30` runs a copied CLI with `scripts/lib/contamination.sh` deleted, over a `tests/*` path, which is managed and NOT substituted: with the refusal downgraded to `continue`, staging is a plain `cp`, `contamination_scan` is simply not a command, `findings` comes back empty, and the request is filed with the scan having never run — `E7` reds it, and a control run with the complete copy proves the CLI reaches the scan at all. **THE COMPLETENESS CLAIM IN THIS TITLE WAS FALSE: there were three, not two** — the missing third is BUG-108. **(1) will become reachable, and red, the moment the `scaffolding/`+`forge/` restructure lands**, so the case is cheapest to write now while the pre-move behaviour is still the baseline to compare against. Re-open if a guard whose own comment cites a bug is unasserted, or if `a2bp` can resolve an empty base for a file that exists. |
| **BUG-106** | **`tests/a2bp-e2e` #10 scans the SHARED `$TMPDIR` for `a2bp.*`, so any concurrent process decides its verdict — measured red for ELEVEN mutants that touch no scratch code at all.** Under 2-way parallelism it failed for eleven unrelated trees and was clean on serial re-runs of the same two; the port scans the scenario's own pinned TMPDIR and did not move on any of them. **This is BUG-049's cross-suite hazard measured rather than argued**, and it is the third instance of one shape found today: BUG-095 is the same defect in `tests/agent-activity-bound` #15/#2 (`tail -n0 -F` counted machine-wide), and BUG-094 records its earlier cost — one mutant tree's 89 orphaned followers deciding six later trees' verdicts. **An assertion that reads something it does not own is not a flaky test; it is a test of the machine.** | S3 | OPEN | Found 2026-09-11 by Jesko (QA-2) during the a2bp group's R6 pass, from a red set that made no sense for the mutants that produced it. **NOT FIXED, and deliberately: this is a reason to retire the shell runner, not to repair it.** The port already scopes the scan, so the property is covered — repairing the runner would change a verdict while the port is being proven against it, which is the one thing a port must not do silently. Contrast BUG-095, where the founder authorised the same repair in `agent-activity-bound` for the opposite reason: that suite's assertion was UNSATISFIABLE on a developer host and blocked every push, so leaving it was not an option. Re-open if any retiring runner's verdict depends on a process it did not start. |
| **BUG-107** | **Three claims in the TASK-018 ports that were true-enough rather than measured — the reporting half of the proxy-assertion defect F-002 names.** **(1) Ceilings were WIDENED while the port report said none was:** `expectLine` 20 s against the shell's 8 s, `readerReady` 30 s against 20 s, supervisor discovery 10 s against `wait_sup`'s 4 s — up to 3.75x, and a widened ceiling is the cheapest way to make a flaky port look green. **(2) `scripts/session-resume.sh`'s BUG-079 comment stated single-`write(2)` `cat` as a GUARANTEE**, which POSIX does not give; it is a property of this implementation at this payload size, and the next person to grow the payload would have reasoned from the stronger claim. **(3) The `sleep` PATH shim records its marker BEFORE the sleep**, so N markers prove N comparisons but only N−1 completed polls — the elapsed-time claim was overstated by one poll. | S3 | OPEN | Raised 2026-09-11 by two cross-provider reviewers independently, each by re-MEASURING rather than re-reading. **FIXED by Nicole (UX) in the same pass:** all three ceilings restored to the shell's own numbers with the measurement beside them (4 full runs of the four feed suites: `expectLine` worst 3.31 s of 8 s, `readerReady` worst 3.26 s of 20 s, `expectSupervisors` worst 120 ms of 4 s), the one caller that genuinely needs more (#10 `RACE-C`, deliberately slowed read, 4.60 s) carrying the same explicit 30 s the shell gave it; the `cat` claim rewritten with its strace and its falsifier (the same binary splits a 1 MiB body into 65 writes); and the shim arithmetic corrected by raising the constants rather than weakening the claim (`wait-mic` 6→7, `signal-dispatch` +2→+3), so the ported bound is again at least the shell's wall-clock bound AND a comparison count the sleep could not promise. **No separate regression test:** these are the tests, and each correction is witnessed by the suites it lives in — a ceiling that cannot come down fails them. BUG-067 was checked as a possible floor and is not one here: nothing measured needed a `--daemon` ceiling above the shell's. Re-open if a ported bound is wider than the one it replaces without a measurement beside it. |
| **BUG-108** | **`a2bp`'s immediate pre-push base re-check can be skipped entirely and nothing notices — the request is then filed against a base that has already been superseded.** `bp_file_remote_tip` (`scripts/lib/request-file.sh:51-53`) pipes `git ls-remote` into `awk`, so the transport's exit status is lost and an unreachable, unauthenticated or slow remote prints NOTHING; the caller (`scripts/blueprint:1470-1494`) guards with `[ -n "$tip" ] && [ "$tip" != "$base" ]`, so an empty answer reads as *the blueprint did not move* and the push proceeds. The block's own comment calls itself the step that "closes the wide window and leaves one of round-trip size" — so the one guard whose purpose is to distrust the base it already holds treats *no answer* as *the reassuring answer*. That is the BUG-003 shape (a guard that could not run is not a guard that passed) on the last action before bytes reach the blueprint's remote. **Proven both-green before the fix**: with the function body replaced by `return 0`, all six retiring shell suites and every TypeScript case stayed green. | S2 | OPEN | Found 2026-09-11 by Andreas (Back-End-2, Codex) in cross-provider review of the six a2bp ports; it makes BUG-105's "two, not three" completeness claim false. **WITNESSED, NOT YET FIXED — the coverage half is closed, the product half is not.** `tests/a2bp-e2e/a2bp-e2e.spec.ts` `#12` drives the real race rather than simulating it: a `git` wrapper in front of the scenario's gh-free PATH advances the local bare remote's `main` immediately after the FIRST fetch returns, and the case requires the CLI to report the move and to file a request whose PARENT is the new tip. Mutant `E9` (catalogue `docs/doing/TASK-018-EQUIVALENCE-a2bp/`) reds it; the shell suites stay green, so this is also a port-stricter result. **The move is hung off the fetch, not off the re-check's own `ls-remote`, and that is what makes it a witness** — a defect that never asks for the tip still faces a moved blueprint and is caught building on the stale one. The product fix is to distinguish "the remote says it has not moved" from "the remote could not be read": the second must refuse, not push. Re-open if `bp_file_remote_tip` can return an empty tip that its caller reads as anything other than a failure.
| **BUG-109** | **Six controls that are green today go RED the moment the TASK-018 shell runners retire — every one of them pinned to a runner FILE rather than to the property it guards.** MEASURED in a scratchpad copy of the finished tree (36 runners deleted, 36 gate blocks and the CI step removed, `sed-inplace.sh` gone), not reasoned: 6 failed / 80 passed across the six. **(1) `tests/manifest` #1b** reports `tests/helpers/proc-cwd.sh` as an orphan; its `walk()` collected only `*.sh`, and after the wave the helper's consumers are TypeScript (`tests/helpers/feed-fixture.ts:301`, BUG-089's fail-open guard; `tests/proc-cwd/proc-cwd.spec.ts`). A live helper killed by a scan that cannot see its references. **(2) `tests/git-isolation` #3's terminal branch** asserts no `*.sh` remains under `tests/` — but `scanGitIsolation` walks `tests/<dir>/*.sh` and `tests/helpers/` is such a directory, so `proc-cwd.sh` is CONSIDERED, is not a MEMBER, population is zero and the end-state assertion fires on a correct file. **This is the one that matters: BUG-088's row claimed the port was already retirement-safe, and it was not.** **(3) `tests/live-state-canary` #4** `readFile`s three `test.sh` the wave deletes → ENOENT. **(4) `tests/bootstrap-contents` #3b** probes downstream for `tests/marker-merge/test.sh`. **(5) `tests/state-root` #F**'s four non-vacuity anchors are all shell fixtures, so `inScope` empties and the guard fires — correctly; it exists to refuse a scan that reaches nothing. Found ONLY by running the simulation. **(6) `tests/doc-links`** — eight markdown hrefs in `docs/DoD.md` and `docs/done/BUGS.md` name runners that will not exist; five sit in user-accepted history nobody expected the wave to touch, and no amount of reading the test tree would have found them. | S1 | OPEN | Found 2026-09-11 by Klaus (PO) via the scratch-copy simulation in [PLAN-TASK-018-RETIREMENT.md](PLAN-TASK-018-RETIREMENT.md) §1; fixed and independently re-measured the same day by Christian (Architect). **Fixed WITHOUT performing the retirement** — each fix is green on today's tree AND on a rebuilt end-state copy (84/84 both sides), which is the acceptance test. (1) `#1b`'s walk now reads `*.ts` as well as `*.sh`, and enters `tests/helpers/` for `.ts` only — the `.sh` exclusion exists so a helper's own header cannot vouch for itself, an argument about the file being judged and not about its neighbours. **That widening alone is green for the WRONG REASON, found by mutating it rather than by reading it, and both halves matter.** It made the check vouch for its OWN subject: the comment explaining the fix names `proc-cwd.sh`, and `manifest.ts` is a `.ts` file under `tests/`, so with both real consumers mutated away `#1b` stayed GREEN — the BUG-035 shape (membership decided by what comments SAY) reintroduced one level up from the exclusion that exists to prevent it. Comments are stripped first. And the match is on the FILENAME rather than on `helpers/<name>`, because the consumer the plan cites — `tests/helpers/feed-fixture.ts`, which sources the helper via `join(REPO_ROOT, 'tests', 'helpers', 'proc-cwd.sh')` — contains no `helpers/proc-cwd.sh` substring at all, so under the prefix match that live consumer was invisible and only its comment made it look otherwise. Non-vacuity re-measured after both: deleting the two CODE references reports `#1b shared helpers that no suite sources: proc-cwd.sh`, and `sed-inplace.sh` is still correctly orphaned if the wave forgets to delete it — no laundering path was opened. (2) the terminal branch states the end state POSITIVELY — every `DECLARED_ANCHOR` now owns a `*.spec.ts` — so it says the migration COMPLETED rather than that the directory is empty, and it survives both a live shell helper and a deliberately-retained runner. (3) and (5) DISSOLVE with their subjects, recorded in each spec's header per the `drift-in-blueprint` precedent; neither was replaced by an iteration over "whatever `.sh` still exist", which is a vacuous pass wearing the old name. (4) re-points at `marker-merge.spec.ts`. (6) every href now names the suite DIRECTORY, which is what its visible label already said, so no future port can break it again. **No runner, gate stage or CI line was deleted** — the retirement is a separate pass with a forced order (plan §3). Re-open if any of the six can go red on a healthy tree, before or after the wave. |

**Do not narrate status here.** Which items are where is answered by the
folders: `doing/` is what is being implemented, `waiting-acceptance/` is what is
pushed and untested, `done/` is what the founder accepted. Prose repeating that
becomes a second record of one fact, and on 2026-08-03 every such line in this
file had gone false — including one telling the next session that a decision was
still pending on an item accepted that morning. Resume context belongs in
[HANDOVER.md](HANDOVER.md), which is rewritten to match reality on every wake.
