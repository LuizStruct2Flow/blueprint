# Founder-accepted rows

Rows the founder has explicitly accepted. This is the source of truth for
*what has been delivered* — not what has been merged. See
[README.md](README.md) for the lifecycle.

**This file appears with the first acceptance** — absence means none yet, not a
missing file.

| # | Item | Accepted | Sev | Category |
|---|---|---|---|---|
| **FEATURE-001** | **Back-propagation becomes a request, not a write.** Artefacts: [`FEATURE-001-a2bp-pr/`](FEATURE-001-a2bp-pr/). `blueprint a2bp` used to copy a project's file straight into the blueprint's working tree; it now pushes a branch, opens a pull request, and lands nothing itself. **Corrected 2026-08-02, and the correction is the point:** filing a request needs push access to the blueprint remote, and in the same-owner setup every agent authenticates as the owner, so an agent running plain `git push` still reaches `main`. What `a2bp` removed is the unreviewed automatic write, which is real; what it did not create is a boundary. Folded into BUG-011 and stated in `project_config_paths.md` §"Back-propagation trust boundary". **Row reconstructed 2026-09-17** from `docs/waiting-acceptance/INDEX.md` at `344656b` and the folder's own README: this item's only record was that INDEX, which TASK-005 deleted as a second description that had drifted, so the item kept its artefacts and lost its row. Pushed 2026-07-30, accepted by the founder 2026-08-02 in `344656b` ("founder accepted everything in waiting-acceptance"). Found by BUG-138, the first check able to see a FEATURE folder at all. | 2026-08-02 | S2 | Sync |
| **TASK-022** | **Anchor the rules in mechanisms, and shrink what agents must remember.** Founder direction, 2026-09-10: *"I think we should rely in automation and not in agents having all rules in their context and being disciplinated on applying these rules… the md files will be used for documentation, and we will anchor the controls in the ci and development lifecycle."* Triggered by a cross-review of `docs/way-of-working.md` in which two reviewers independently found its strongest untold story was that **controls are required to be checkable and are repeatedly caught lying** — a framing the founder rejected as pitched, while accepting the evidence behind it. Artefacts: [`TASK-022-anchor-rules/PLAN-TASK-022.md`](TASK-022-anchor-rules/PLAN-TASK-022.md), the rule-enforcement audit in the same folder, refreshed 2026-09-17 at the founder's request: `TASK-022-rule-enforcement.{csv,xlsx}` holds all 373 rules, `-removed-from-md` the 27 rules no longer in any .md, `-summary` the counts old vs now (the 2026-09-10 version was deleted with stage 1 and is in history at `dc225c9`), and the talk at [`../talk-enforcing-agentic-quality.md`](../talk-enforcing-agentic-quality.md). **Partly landed:** `34d5d00` fixed the trunk rule contradicting itself across CLAUDE.md and DoD §6 — TASK-019 made `main` the trunk and deleted the section two other rules still pointed at — and reframed the landing table on the axis that was wrong all along: **the contributor, not the repo.** Stage 1 of the revised plan (the deletions) was committed 2026-09-17; stages 2 (CI anchors), 3 (SessionStart hook) and 4 (tell derived projects) are open. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-17**, CI green on `8f56ba6` (run 35209754799). **What to test:** `wc -l CLAUDE.md docs/DoD.md` reads about 344 and 253 (were 1,318 and 916), and no rule you still want is gone. A push to main whose earlier commit breaks a subject, row or BUG-test rule turns CI red. Starting Claude Code, including from a subdirectory, shows a session-start report with the feed and a drift verdict (`drift: UNKNOWN` when offline). **Reviews:** Alexey (Codex) on the plan; Jesko (Codex) on stages 2 and 3, each real finding fixed (force-push refusal, hook paths relative to the cwd). **Known limits:** a declared test root under tests/ fails the BUG-test check; CI after a history rewrite checks the tip only. **Next-step gate:** the plan's inventory of which rules are prose-only versus mechanically enforced. This session produced eight instances of `docs/config/findings.md` F-002 — checks inferring a property from a proxy satisfiable without it — which is direct evidence for the plan's premise and should be folded in before the rest is sequenced.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Process |
| **TASK-059** | **Each persona runs on a model tier from the roster, never a hardcoded version.** Founder-approved 2026-09-17. The roster gains a `Model` cell, `<tier>:<effort>`: `frontier` is the provider's best model, `frontier-N` is N places down its ranked list. Codex ranks from `~/.codex/models_cache.json` (visibility `list`, by priority); Claude from one roster line, `Claude models, best first: fable, opus, sonnet, haiku`, whose aliases Claude Code resolves to the newest version. `bp_roster_model_for_name` in `scripts/lib/roster.sh` resolves it; an invalid tier, an unsupported effort or a missing list is an error naming the persona. The Orchestrator's cell is `session-based` (founder, 2026-09-17), because the founder picks that session's model at start; on any other row that value is an error. Codex dispatch passes `-m` and `model_reasoning_effort`, and refuses a cell that does not resolve. `scripts/claude-agents.sh`, run from `scripts/session-start.sh`, writes `.claude/agents/<name>.md` with `model:` and `effort:` for every Claude persona but the Orchestrator. Feed lines read `[Name - model - effort]`, with the model a Claude subagent actually ran on once its transcript records one. **Re-open trigger / next-step gate at acceptance:** **Pushed again 2026-09-17**, CI green on `9ba218f` (run 35220731328, after one re-run of a transient osv-scanner exit 127 in bootstrap-gate). **Rejected earlier that day by the founder:** *"I cannot see in the logs that the correct model was selected."* In `logs/agent-activity.log`, the subagents this session dispatched as `matthias` and `vitali` log `[matthias - Claude Code]`. That is the agent-type fallback at `scripts/log-activity.sh:198`, with no model and no effort, so `bp_roster_subagent_label` resolved nothing for a real dispatch. `roster-models` #4 passes only on its own metadata fixture. **Fixed in `819db88`:** the lookup matches the meta file's `agentType` against each roster name's slug; #4b uses the real meta shape and failed before the fix. **Live evidence, 2026-09-17 14:21** (`logs/agent-activity.log`, feed daemon restarted first): `[Klaus - sonnet - medium] → dispatched`, then `[Klaus - claude-sonnet-5 - medium] ← finished`; `[Christian - opus - high] → dispatched`, then `[Christian - claude-opus-5 - high] ← finished`. The roster alias shows until the transcript records the model it ran on. **Known limit:** a feed daemon started before a change to `scripts/lib/roster.sh` keeps the old code. The session-start hook leaves a running daemon alone, so the fix reached the feed only after `agent-activity.sh --stop` and `--daemon`. **What to test:** dispatch a Claude persona: its live feed lines read `[Name - model - effort]`; dispatch a Codex persona: the run log shows `-m <slug>` and its feed line carries the model. **Known limit:** not yet proven that the Codex service accepts the model and effort flags at runtime. **Re-open if** a persona runs on a model its `Model` cell does not name, a bad cell is used without an error, or the feed does not show the model.  **ACCEPTED by the founder 2026-09-17**, on the live feed evidence. **Re-open trigger / next-step gate at acceptance:** **Fixed again and pushed 2026-09-17** (`3768542`), CI green on `3768542` (run 35255514920). **Live evidence, 19:52** (`logs/agent-activity.log`, after the feed restarted itself through BUG-135): `[Klaus - sonnet - medium] → dispatched`, then every later Klaus line `[Klaus - claude-sonnet-5 - medium]`; his helper `[Klaus › general-purpose - claude-sonnet-5]`. **Known limits:** a `dispatched` line is written before the transcript records a model, so it shows the roster alias (or `- Claude Code` for a helper); a helper that is not on the roster shows no effort, because nothing records one. A separate defect found in the same probe, the feed dropping a new subagent's first lines, is BUG-137. **REOPENED 2026-09-17 by its own re-open trigger ("the feed does not show the model"), raised by the founder.** Evidence from `logs/agent-activity.log`, Christian's TASK-022 audit, 19:38: (1) the subagent Christian spawned logged 404 lines as `[Christian › general-purpose - Claude Code]`, with no model and no effort; (2) Christian's own lines read `[Christian - opus - high]` 18 times but `[Christian - claude-opus-5 - high]` on the 2 hook bookends, and `Report delivered.` after `← finished` still read `opus`. The streamed lines never use the transcript model the bookends use, which contradicts the known limit written at acceptance ("the roster alias shows until the transcript records the model"). **What to test:** dispatch a persona that spawns its own subagent; every feed line of the persona names one model consistently, the nested subagent's lines name the model it ran on, and none reads `- Claude Code` without a model. **Re-open if** any subagent feed line lacks a model, or one agent's lines name two different models after its transcript records one. Previously accepted 2026-09-17.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Agent team |
| **TASK-021** | **What ships, decided by one boundary (was: the `scaffolding/` + `forge/` restructure, TASK-018-TARGET §2).** Plan: [`PLAN-TASK-021-RESTRUCTURE.md`](PLAN-TASK-021-RESTRUCTURE.md). **The physical move was dropped by the founder on 2026-09-16:** it cost ~180 path moves plus permanent path translation in sync and a2bp, and once the managed set is derived from the export archive, the audience is split by `.gitattributes` directory rules and unshipped files are retired downstream, it adds legibility, not consistency. Stages 0, 1 and 2 are on `main`, and the `scaffolding/` resolver branches are removed. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16/17**, CI green on `c567534` (run 35172596805). **What to test:** run `blueprint files` in the blueprint: no deck, talk, brand, requirements or playbook, and `AGENT_SIGNAL.md` is listed. Bootstrap a fresh project: no `new-project.sh`, `LICENSE` or `CLAUDE.blueprint.md`, and its root `CLAUDE.md` and `README.md` links resolve. In a derived project, `blueprint pull` offers to delete unedited copies of files that no longer ship (default No) and keeps edited ones as "yours now". The first pull with an old CLI may print one error after "Pulled N file(s)"; a second run is clean. **Reviews:** Alexey (Codex) twice, all S2 findings fixed; Jesko (Codex) once, fixed. Do not reimplement the move. Re-open if a shipped file is found that the export archive does not decide.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Sync |
| **TASK-058** | **Lifecycle pass: `doing/` holds only work in flight.** Founder request 2026-09-16 ("we have too many things in doing"). 46 bug rows from the TASK-018 port (BUG-036..109) sat in `doing/` with a stale `OPEN` status. A read-only classification (Vitali) checked each against `origin/main`: 35 fixed rows moved straight to `done/` on the founder's bulk acceptance ("yes, straight to done"), each carrying its fix commit. The 11 still-open bugs and TASK-021/022/024 go to `backlog/` or are cancelled per the founder's call on the review rule. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16/17.** **What to test:** `docs/doing/` holds only work in flight. The 35 fixed bugs are in `docs/done/BUGS.md`, BUG-039 and BUG-072 are in backlog, and eight bugs plus TASK-024 are cancelled into `docs/config/findings.md` F-005. Raised 2026-09-16 by the founder. **Re-open if** `doing/` again holds rows nobody is working on.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S3 | Lifecycle |
| **TASK-057** | **Git permissions allow only the everyday commands; everything else prompts.** Founder request 2026-09-16. `Bash(git *)` allowed every git command, including `reset --hard`, `clean -fdx`, `branch -D` and `config`. Now allowed: push, add, commit, pull, fetch, checkout, switch, stash, and the read-only status, log, diff, show, rev-parse, ls-files, check-ignore, blame, grep and branch listing, each also as `git -C <dir> …`. Ask: `checkout -- *`, `checkout .`, `stash drop`, `stash clear` (they discard work) plus the existing force-push rules. Every other git command falls to the default prompt. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** an agent running `git reset --hard` or `git clean -fd` is prompted; `git status` and `git commit` are not. **Known limit:** a git invocation with a global option other than `-C` (e.g. `git -c core.quotePath=false ls-files`) prompts. **Re-open if** a destructive git command runs unprompted.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Permissions |
| **TASK-056** | **kill, pkill, curl -s and brew install ask before running.** Founder request 2026-09-16: moved from `permissions.allow` to `permissions.ask` in the managed `.claude/settings.json`, so every project that pulls it is prompted before an agent kills a process, fetches a URL silently or installs a package. **Known limit:** `Bash(curl -sS *)` is still allowed; the founder named only `curl -s`. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** in a session, an agent running `kill`, `pkill`, `curl -s` or `brew install` is prompted. **Re-open if** any of the four runs unprompted.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S3 | Permissions |
| **TASK-055** | **The TypeScript suites run in parallel.** `fileParallelism` goes from `false` to `true` in `tests/vitest.config.ts`, in the gate and in CI alike. The config comment claimed `staleness` #8 and `pre-push-secrets` #10 passed only because the run was serial; measured, both bound the subject's own timeout and take the same 2.08 s and 3.08 s either way, and no suite needed a fix. **Measured 2026-09-16 on this host (32 cores), the 50 non-release files:** 197.5 s serial, then 20.0 s, 20.3 s and 20.1 s in three parallel runs, all 877 tests green. **Known limit:** BUG-111 (the harness counting a process it just killed) is load-sensitive and may show up more often. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder with TASK-054. **What to test:** push anything and read the vitest stage time in the gate summary: about 20 s on this host. **Re-open if** a suite fails only when run beside others.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Tooling |
| **TASK-054** | **A release tier: the five most expensive suites run in CI only.** Founder decision 2026-09-16: the ~940 s pre-push gate was blocking. `bootstrap-gate` (460 s), `subagent-feed` (127 s), `a2bp-e2e` (56 s), `agent-activity-bound` (50 s) and `signal-dispatch` (34 s) are renamed `*.release.spec.ts`. The gate's vitest run excludes `**/*.release.spec.*` and prints a skip line naming each; CI runs everything, and `released` moves only on green CI, so CI gates what derived projects pull. `tests/manifest` now requires CI to run every suite and the gate every non-release suite; any other exclude still counts as a narrowed run. This reverses BUG-005's "never CI-only"; CLAUDE.md §"Pre-push tolerance", DoD §3.7 and the deck say why. **Known limit:** a regression in a release suite now shows as a red `main` in CI instead of a refused push. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** push anything: the gate lists five `release tier` skips and no longer runs those suites; the CI `ts-tests` job still runs them. **Re-open if** a release suite stops running in CI, or a suite leaves the gate without the `.release.` name.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Tooling |
| **TASK-053** | **A push that changes only `.md` files skips the code stages.** Founder decision 2026-09-16, root files such as `CLAUDE.md` included. `.githooks/pre-push` lists the files in the push range (`git log -m --no-renames --name-only` over `push_log_opts`); if every one ends in `.md` it runs gitleaks, the host-path guard, the four DoD checklist stages and one `docs` stage running `doc-links`, `lifecycle-docs` and `bug-numbers`. semgrep, osv-scanner, backend, frontend, IaC, ShellCheck, the typecheck and the vitest batch skip with a `text-only push:` reason. An unknown range, an empty list or any other file gives the full gate; `--no-renames` keeps a script renamed to `.md` from passing as text. **Known limit:** a new branch is scanned over its whole history, so its first push almost always takes the full gate. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** push a commit that changes only a `.md` file: the gate shows the `text-only push` skips and a `docs · TASK-053` stage, in a couple of seconds. Push one that also changes a script: the full gate. **Re-open if** a push containing a non-`.md` file skips a code stage.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Tooling |
| **TASK-052** | **A review finding becomes work only if it is real and practical.** Founder decision 2026-09-16: "I want real and practical findings, we are generating 0 value". `docs/DoD.md` §1b rule 4 and the deck's rule 4 now say a finding is acted on only if it has happened, or sits on a core path with a realistic trigger; anything else is one "known limit" line, with no reproducer, mutant or re-review, and the implementer judges each finding instead of relaying the verdict. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`3940d57`, CI green on `3940d57`). Raised 2026-09-16 by the founder after BUG-126 (16 commits for a case `tsc` already catches). **What to test:** the next cross-provider review — hypothetical findings end as known-limit lines, not commits. **Re-open if** a hypothetical finding produces a reproducer or a rework round.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Process |
| **TASK-051** | **Every project's SonarQube scan covers its shell scripts.** Founder decision 2026-09-16: "every project should get it and scan it". SonarQube has no shell analyser, so `scripts/sonar.sh` runs ShellCheck over the files the gate lints (`sh_lint_files`) and imports the findings as external issues; `.scannerwork/` is ignored. SC2317 is left out (301 false positives on functions called by name). **Known limit:** the scanner never indexes files under a dot-directory, so `.githooks/` findings stay with the gate's ShellCheck stage. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`aeeada4`, CI green on `3940d57`). Raised 2026-09-16 by the founder. **What to test:** run `bash scripts/sonar.sh` in any project with shell scripts: it prints `→ ShellCheck: N shell files` and the dashboard shows `external_shellcheck:SC…` issues. Verified on the blueprint: 30 issues across 13 files. **Re-open if** shell findings do not reach the dashboard.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S3 | Tooling |
| **TASK-050** | **The seeded `sonar-project.properties` still recognises tests as `*.test.ts`, so SonarQube in a new project counts its specs as source.** TASK-047 made every test `*.spec.ts` / `*.spec.tsx` but missed this template: `sonar.test.inclusions` and `sonar.exclusions` still name `*.test.ts`, `*.snap.test.ts` and `*.integration.test.ts`. Found 2026-09-16 while adding the blueprint itself to the local SonarQube. **Do:** name `**/*.spec.ts` and `**/*.spec.tsx` in both keys. Existing projects own their copy (template file, not pulled) and edit it themselves. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`ed46dd3`, CI green on `3940d57`). Raised 2026-09-16 by Eto. **Re-open if** a seeded Sonar config names a test pattern the runner does not run.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S3 | Tooling |
| **TASK-061** | **A Codex persona hands the mic back to the Orchestrator's persona, resolved from the roster.** Founder decision 2026-09-17: "to the orchestrator, it can vary from environment to environment and project to project." Jesko set `Holder = Claude Code`, because the Codex run preamble in `scripts/start-codex-signal-watch.sh` and `AGENTS.md` both name "Claude Code", while `Holder` is a persona name. The Orchestrator's name comes from the roster's `Orchestrator` row (`agent-activity.sh --whoami`), never a hardcoded value. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-17** (`32dcfc4`), CI green on `e03fb6b` (run 35245006380). **Live evidence, 16:08Z:** Jesko's real dispatch handed back with `Holder = Eto`, `State = OVER_TO_CLAUDE`. **What to test:** dispatch any Codex persona; the mic comes back with `Holder` = your roster's Orchestrator name. **Re-open if** a Codex hand-back names anything but the roster's Orchestrator.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S3 | Agent team |
| **TASK-060** | **A Codex persona's feed label shows the model Codex actually ran, not the roster's resolution.** Founder decision 2026-09-17 ("the model actually running"). On Jesko's real dispatch the feed read `[Jesko - gpt-5.6-terra - medium]`, but that label is computed from the roster cell. The only record of what ran is Codex's own session file, `~/.codex/sessions/…/rollout-*.jsonl` (`"model":"gpt-5.6-terra"`, `"reasoning_effort":"medium"`), and `logs/state/codex-runs.log` records no model at all. A Claude subagent's label already reads the model from its transcript. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-17** (`ecdc63c`, type fix `e03fb6b`), CI green on `e03fb6b` (run 35245006380). **Live evidence, 16:08Z, Jesko:** `codex-runs.log` shows `[roster] requested model=gpt-5.6-terra effort=medium`, then `[roster] actual model=gpt-5.6-terra effort=medium (session …/rollout-2026-09-17T18-08-25-01a0b020-5907-79f3-b279-4fcbc54f7e77.jsonl)`, and the feed reads `[Jesko - gpt-5.6-terra - medium] finished`. The session file is found by the `thread_id` Codex announces on its `--json` stream and confirmed against its `session_meta` id, never by newest-file guessing. **What to test:** dispatch any Codex persona and read the two `[roster]` lines in `logs/state/codex-runs.log`. The actual model must match the `turn_context` in the named session file. **Known limit:** only the `finished` feed line carries the actual model, because the id is known once the run ends. Earlier lines of that run show the roster resolution. **Re-open if** a Codex label names a model that run's session did not record, or the log guesses instead of saying `unknown`.  **ACCEPTED by the founder 2026-09-17**, on the live Codex dispatch evidence. | 2026-09-17 | S2 | Agent team |
| **TASK-048** | **A derived project tracks all six methodology files: the `.gitignore` privacy block goes, with no exceptions.** Founder decision 2026-09-16, "All six, no exceptions", following TASK-046: private project notes now have a home in `claude.internal.md`, so excluding the framework's own docs protects nothing and costs every doc link into them (the limitation BUG-125 had to accept). **The six:** `/CLAUDE.md`, `/AGENTS.md`, `/AGENT_SIGNAL.md`, `docs/DoD.md`, `docs/PUBLISHING.md`, `docs/doing/HANDOVER.md`. Five are blueprint-managed or generic protocol and identical in every project; the sixth is the project's live handover, and **the founder accepted publishing it**. **Evidence that shapes the work (Christian):** `.gitignore` is NOT in `MANAGED_FILES` but IS in the bootstrap archive, so it is seeded once and project-owned — **a blueprint edit reaches NEW projects only**, and every existing project must edit its own copy. **Do:** (a) remove the six from the blueprint's `.gitignore` and **replace** the "multi-AI methodology (PRIVATE)" comment rather than delete it, since it records a deliberate decision — put the replacement wording in front of the founder; (b) same commit, update `docs/PUBLISHING.md`, which encodes the exclusions in §0, in §1b's preflight (it greps the index and prints `PRIVATE FILES STILL TRACKED — DO NOT PUSH`) and in §3a/§3b's allowlist and untrack commands — otherwise the runbook fails a project for tracking its own docs; (c) write the one-line instruction existing projects run, and say where it goes so storm2flow and the others actually get it; (d) check `tests/bootstrap-contents` (`:96-107`) and `tests/bootstrap-gate`, which force-add these files so the fixture mirrors today's repo. **Leave `project_config_*.md` ignored** — A-27 put the threat model and infra account IDs there deliberately. **Absorbs A-13**, cancelled 2026-09-16 with a pointer at `docs/config/findings.md` F-004: it recorded the same fact from the other end — `.gitignore` is not managed, so the privacy block's "they'd come back on next sync" instruction described a mechanism that does not exist, and a derived project's block silently never updated. That is (c) here, the instruction existing projects run themselves. Two live records of one fact drift, which is why A-13 does not stay open alongside this. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`9414ccd`, A-13 absorbed `9b1e409`, doc-links `066e0e8`, publishing fixes `6937540`, witness `a2123b6`). **What to test:** bootstrap a fresh project and run `git ls-files` in it — `CLAUDE.md`, `AGENTS.md`, `AGENT_SIGNAL.md`, `docs/DoD.md`, `docs/PUBLISHING.md` and `docs/doing/HANDOVER.md` must all be tracked, and the five `project_config_*.md` must NOT be (A-27 put the threat model and infra account IDs there). `bootstrap-contents` #5c asserts exactly that, because `git add -A` skips an ignored path in silence — "tracked" and "looks tracked" were otherwise indistinguishable. **Existing projects are not reached:** `.gitignore` is seeded at bootstrap and never pulled, so storm2flow and the others run the instruction in CLAUDE.md §"Blueprint sync" themselves. **Tracking is not publishing:** `docs/PUBLISHING.md` §3b now redacts the live handover before a public push and says so explicitly, rather than pointing at a scrub in §3a that §3b never executes; §3a's allowlist gained the three root documents it was missing. **A parked follow-up, TASK-049:** prose is not enforcement, and a guard that refuses to publish a live handover needs your ruling on managed-versus-project placement. **Re-open if** a fresh project excludes any of the six, or a published repo carries a live handover. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S2 | Privacy / sync |
| **TASK-047** | **Every test is a spec-driven TypeScript test: one extension, `*.spec.ts`, no exceptions.** Founder decision 2026-09-16: *"migrate the tests to be spec driven ts tests, we don't need exceptions"*, answering why the gate accepts five runner extensions while only `*.spec.ts` executes (TASK-039 finding 6, Alexey). **The facts behind it:** under `tests/` there are 55 TypeScript test files, one real shell runner (`tests/ts-bridge/test.sh`) and one shell helper (`tests/helpers/proc-cwd.sh`, invoked from a spec, not a test itself). `.spec.js` / `.test.js` were accepted only in case a derived project used JavaScript; no such file exists. **Do:** port the shell runner to a spec; collapse the accepted set to `*.spec.ts` in `scripts/lib/dod-gate.sh`, `scripts/lib/suites.sh` discovery, `tests/vitest.config.ts` and the three checks in `scripts/run-ts-suites.sh`, so what the gate counts and what actually runs are one set; simplify TASK-039's guard #14 accordingly. **Then the convention docs, which is where this reaches further than `tests/`:** CLAUDE.md §"Test Layers" prescribes `*.test.{js,ts}` for co-located unit tests in `src/` and `*.snap.test.{js,ts}` for snapshots, and §"Test directory layout" repeats it. Report the full ripple before rewriting those rows — whether co-located unit tests in a derived project's `src/` also become `*.spec.ts` is a convention change for every project and goes back to the founder if it is not obviously covered. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`5e09d62`..`0a9ddb8`, retirement `da73f36`/`da352b6`, convention `ae7e634`, guards `8c609e2`/`a305e8d`/`7c54cb4`). **What to test:** every test in the blueprint and in a derived project is now `*.spec.ts`, or `*.spec.tsx` where it contains JSX — one convention, no exceptions, with the layer in the infix (`.integration.`, `.snap.`). Check the gate cannot drift from the runner: `tests/dod-gate` **#17** compares what the gate COUNTS as evidence, what discovery FINDS, and what vitest RUNS, and fails if any one side changes; `#17b` witnesses discovery behaviourally. Both were mutated on each side to prove it. **A migration note for existing projects is in CLAUDE.md beside the rule** — it arrives with the pull that creates the need. Its warning is worth reading: **check the test count, not the colour**, because a glob that stops matching reports zero failures and looks exactly like success. **What this cost, on the record:** `tests/ts-bridge/test.sh` is retired and TASK-023 closed as an accepted loss (`F-003`) — it was the only assertion about the vitest bridge not executed by the vitest it asserts about, so a silently dead bridge now produces no red case. **Re-open if** a test file that the runner never executes counts as evidence, or the three sides disagree. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S2 | Test suites |
| **TASK-046** | **`CLAUDE.md` imports a project-owned `claude.internal.md`, and whether that file is tracked is the project's own decision.** Founder direction, 2026-09-16: *"can we always link to claude.internal.md, and this file belongs to the derived project, and also imo is a local decision of the project if it'll gitignore it or not"*. **The problem it solves:** `.gitignore:73-82` excludes `/CLAUDE.md`, `/AGENTS.md`, `/AGENT_SIGNAL.md`, `docs/DoD.md`, `docs/PUBLISHING.md` and `docs/doing/HANDOVER.md` from every derived project as "multi-AI methodology (PRIVATE)", so managed docs link files a clone does not have — the limitation BUG-125 had to accept. Splitting the generic from the private removes the reason to exclude the generic half. **The shape** is the one already proven twice, by `.githooks/pre-push-project` and by TASK-042's `.claude/settings.project.json`: the blueprint owns the generic file, the project owns an extension, and pull never touches the extension. **Do:** add `@claude.internal.md` to the imports in `CLAUDE.md` and the template; seed nothing, since Claude Code skips a missing import silently (verified in its loader for TASK-043); say in `CLAUDE.md` that the file is the project's, that it is where project-private agent notes go, and that tracking it is the project's call; and revisit the `.gitignore` methodology block, which is the half that actually decides whether a link resolves in a clone. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`86f3226`, `a5bd05a`, witness corrected in `71388b9`). **What to test:** in a derived project, create `claude.internal.md` with a note only that project should know, and confirm a fresh Claude Code session has it. Then delete the file: the session must still work, because a missing import is skipped silently. The file is yours — never seeded at bootstrap, never written by `blueprint pull`, and **whether you track it is the project's decision**: commit it to share it with the team, ignore it to keep it local. Nothing in the framework reads it. **Why it exists:** it is what made TASK-048 possible — once project-private notes had a home of their own, excluding the framework's own documents from every derived project protected nothing and only broke the links into them. **The import list had no test at all before this**; `bootstrap-contents` #3c now pins both halves against a real bootstrap — the import arrives, the file does not. From the founder's own proposal: *"can we always link to claude.internal.md… it's a local decision of the project if it'll gitignore it or not"*. **Re-open if** a project's internal file is overwritten by a pull, or a missing one breaks a session. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S2 | Agent context / privacy |
| **TASK-044** | **A project declares its CI, so suites that assume GitHub Actions check the declared pipeline or skip with a visible notice.** From storm2flow's orchestrator (Sylvia) after rehearsing the pulled gate at `dbed972` (`FR-storm2flow-declared-ci`). Several suites require `.github/workflows/security.yml`: manifest #5/#6 (`tests/manifest/manifest.ts:510,706-713`), commit-subjects #5, sync-by-address #33/#33b/#39, and ts-bridge #3/#5/#7. storm2flow's CI is AWS CodePipeline on CodeCommit, where that file is inert, so for a non-GitHub project those suites certify CI that never runs. **Do:** add a CI declaration in `project_config_paths.md` (template, and the blueprint's root copy declaring `github-actions`). Each listed suite checks the GitHub workflow only when the declared CI is GitHub Actions, and otherwise skips with a visible reason naming the declared CI. A skip must be visible in the gate output (`pipe_skip`-style, never silent), because a suite omitted without a trace is the failure `tests/manifest` exists to refuse. An undeclared CI keeps today's behaviour. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`d3b9a25`, `f69247c`, uncapped notices `fb09a60`/`910c6b4`, one-line notices `f8a8503`/`e700cf5`, docs `88ebe90`). **What to test:** in a project whose CI is not GitHub Actions, declare it — `- BP_CI: \`aws-codepipeline\`` in `project_config_paths.md` — and run the gate. The checks that read `.github/workflows/security.yml` must **skip with a visible `SKIP-NOTE:` naming the declared CI**, not pass. Before this they certified a workflow file that never runs there. The blueprint declares `github-actions` and still runs every check; an undeclared project behaves as before. **Two review findings fixed:** the gate capped notices at twenty, so a skip reason could vanish behind canary notes (now uncapped, with a fixture proving one survives 25 preceding notes); and a multiline title or reason stranded the reason on an unmarked second line (now collapsed at emission, so a notice is always one line). From storm2flow's `FR-storm2flow-declared-ci`. **Re-open if** a check passes on an inert workflow, or a skip reaches the gate without its reason. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S2 | Test suites / CI |
| **TASK-043** | **`CLAUDE.md` `@`-imports the five project config files, so project rules are in every agent session.** From storm2flow, `FR-storm2flow-claude-md-imports`. `CLAUDE.md:3-5` says to read three `project_config_*.md` files alongside it, and `CLAUDE.md:909-911` names five, so the two lists disagree. Nothing loads them. Because `CLAUDE.md` is whole-file managed, a project has to move its rules out of it, and today they leave agent context when they do. **Founder decision 2026-09-15: import all five** (overview, paths, dod, security, infra), and fix the 3-vs-5 contradiction. **Do:** add the `@` imports; check how Claude Code treats an imported file that is missing (a project that has not created one yet); make sure `templates/` seeds all five; and confirm the blueprint's own root copies exist, since here they are this repo's config (BUG-009). Note the context cost against TASK-024. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`981f045`, `9a1d0ae`, docs `0fcfe40`, scope `8f6926a`, witness `71388b9`). **What to test:** open a fresh Claude Code session in a derived project and ask it something answerable only from `project_config_overview.md` — it must know without being told to read the file. `CLAUDE.md` imports all five project configs plus `claude.internal.md`; a project that has not created the internal file still gets a working session, because Claude Code skips a missing import silently. **Scope, corrected after review:** this is Claude Code's import mechanism, so agents on other providers read those files by instruction, not automatically — the README and deck say so rather than promising "every agent session". **Context cost, measured:** the five configs are 997 lines here (989 from the templates) on top of CLAUDE.md's 1319, so a blueprint session starts at ~2316 lines. That number is what TASK-024 has to argue with. **Cross-provider review:** Alex (Codex) — the import syntax and missing-file handling were verified against the installed Claude Code loader, not assumed. **Re-open if** a project's config content is absent from a fresh session, or a missing internal file breaks one. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S2 | Agent context |
| **TASK-039** | **The DoD bug-test stage finds a project's own regression tests, and a blueprint suite can no longer vouch for a project bug that shares its number.** From storm2flow, `FR-storm2flow-dod-test-roots` (branch `fr/storm2flow/2026-09-15`; text saved in `.scratch/fr-storm2flow-2026-09-15.diff`). **The defect:** `scripts/lib/dod-gate.sh:154` searches only `${BP_CODE_ROOT:-.}/tests/`. In a derived project, that directory holds the blueprint's shipped suites, which name about 109 BUG numbers, about 97 of which are also storm2flow bug numbers. So those storm2flow bugs pass with no test of their own, while storm2flow's real tests (`backend/src`, `frontend/src`, `frontend/e2e`, `infrastructure/test`) are never found. **Do:** a project-declared list of test roots (for example `BP_TEST_ROOTS` in `project_config_paths.md`, defaulting to today's `tests/`). Do NOT make `BP_CODE_ROOT` configurable, because `.githooks/pre-push:83-103` and `dod-gate.sh:187-189` resolve from it. Scope the match so a blueprint-shipped suite cannot satisfy a project bug. Also: `dod_items_in_push` (`dod-gate.sh:58-64`) parses subjects with its own `sed` pattern instead of `scripts/lib/commit-subject.sh`; use the shared parser. Update `docs/DoD.md` §2 and the templates' paths config. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`e7e1d68`..`8473a3d`, guards `4a926aa`/`f10eff3`, rework `47d6ce7`/`998b6cb`/`ef3ec3d`/`9e076e1`). **What to test:** in a derived project, declare your test folders in `project_config_paths.md` as `- BP_TEST_ROOTS: \`backend/src frontend/e2e\``, then push a commit naming a bug whose test lives there — it must pass. Then check the gate cannot be fooled: a bug whose number appears only in a README, a CHANGELOG or a blueprint fixture under `tests/` must be reported UNTESTED. `tests/e2e` does not count in a derived project; a test file sitting directly in `tests/` does. A malformed declaration (indented, a table row, `BP_TEST_ROOTS =`, a glob, or two lines) is refused rather than silently defaulting. **Cross-provider review, three rounds:** the rule was enforced only at the `tests/` top level — every declared root was a recursive text search over ANY file, so a README satisfied the gate while a passing case hid it. Also fixed: containment is symmetric, roots must resolve inside the repo, and `#18` now pins the symlink and file-type halves of the filter. From storm2flow's `FR-storm2flow-dod-test-roots`. **Re-open if** any non-spec file counts as a bug's regression test, or a declared root reaches outside the project. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S2 | DoD gate |
| **TASK-045** | **doc-links resolves site-absolute links through a declared web root or allowlist, instead of reporting served pages as broken.** From storm2flow's orchestrator (Sylvia), `FR-storm2flow-doc-links-web-root`. The doc-links suite reports `/security.html`, `/privacy.html`, `/terms/v1.html` and `/impressum.html` (13 links in storm2flow's `docs/RELEASE-NOTES.md` and `docs/config/FEATURES.md`) as unresolved. They are served web pages, not repo files. **Do:** a project declares a web root (a repo directory that site-absolute links resolve against) and/or an allowlist of site-absolute paths, in `project_config_paths.md`. Without a declaration, a site-absolute link is still reported, so a typo is not silently accepted. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`a9267bf`, `e515d0f`, containment hardened with BUG-125 in `8429e95`/`02e1f31`). **What to test:** in a project whose docs link served pages, declare `- BP_WEB_ROOT: \`site\`` in `project_config_paths.md` and a link to `/security.html` resolves when `site/security.html` exists; `- BP_WEB_PATHS: \`/a.html /b.html\`` lets exactly those pass with no file behind them; with neither declared, the link is reported and the message names both lines. Matching is exact, so `/ok` does not allow `/okay`. A web root outside the repository, or a link escaping the served directory, is refused. **Cross-provider review:** Jesko found `stat` following symlinks straight through containment; after the fix Alexey retried all four escapes plus a relative outside root and a dangling target, all reported — **push as is**. From storm2flow's `FR-storm2flow-doc-links-web-root`. **Re-open if** a served-page link resolves without a declaration, or an escape passes. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S3 | Test suites / docs |
| **TASK-042** | **A project keeps its own Claude Code permission rules in a tracked file that `blueprint pull` never touches, merged with the blueprint's rules.** From storm2flow, `FR-storm2flow-settings-project-layer`. `.claude/settings.json` is whole-file managed (`scripts/blueprint:275`) and JSON cannot carry markers, so every pull replaces it. The only other home, `.claude/settings.local.json`, is gitignored and per-machine. storm2flow's tracked file has 89 entries that are not the blueprint's, 71 of them AWS CLI reads its diagnosis runbook depends on, and a pull deletes them. **Founder decision 2026-09-15: a project file merged in.** A tracked project-owned file (e.g. `.claude/settings.project.json`) is never pulled. Pull writes `settings.json` as the blueprint's rules plus the project's, and **the blueprint's `ask` and `deny` always win over a project `allow`**, so a rule the blueprint tightens (BUG-118 moved `put-approval-result` to `ask`) cannot be undone by a project entry. Chosen over merging arrays on pull, which cannot tell a project rule from a blueprint rule the blueprint deliberately removed. **Do:** the merge in pull, with drift comparing the merged result; `tests/permission-policy` extended; a migration note for projects whose tracked `settings.json` already carries their own rules. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`29aaa0f`, `1e62d68`, review fixes `49ef420`/`dc9c496`/`1c8ce26`; docs `0df1fb8`, `e4c32be`, `e747511`). **What to test:** in a derived project whose `.claude/settings.json` carries rules the blueprint does not ship — storm2flow's 89 AWS entries, for instance — run `blueprint pull`. It must **refuse** `settings.json`, print those rules as a ready `.claude/settings.project.json`, and leave `bootstrap_sha` unchanged (BUG-122). Save that file, `git add -f` it (`.claude/` is gitignored on purpose), and pull again: the merged `settings.json` must carry the blueprint's rules plus yours, with any project `allow` dropped that the blueprint names in `ask` or `deny` — check `put-approval-result` is still `ask`. `blueprint drift` must then be clean, not permanently drifted. **Cross-provider review:** Alexey (Codex) twice — **push as is** on the second pass; `_bp_one_object` refuses JSON streams, non-objects and `null` on all three inputs before anything merges. **Stated limits, not regressions:** duplicate JSON keys follow jq's last-wins, and there is no document-size ceiling. **Re-open if** a pull deletes a project's own rules, or a project `allow` re-enables something the blueprint asks or denies. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S2 | Sync / permissions |
| **TASK-041** | **One dependency-vulnerability threshold everywhere: MEDIUM+ (CVSS >= 4.0) blocks.** From storm2flow, `FR-storm2flow-osv-threshold`. The pre-push hook (`.githooks/pre-push:448-517`) and CI (`.github/workflows/security.yml:98-124`) already block MEDIUM+. `docs/DoD.md` §6.2 (`:486-487`, and `:510`) says only zero `HIGH`+ CVEs is required. **Founder decision 2026-09-15: MEDIUM+**, keeping what the two enforcing layers already do. Nothing gets looser. **Do:** correct DoD §6.2 and every other doc that states the SCA threshold (grep SECURITY.md, the deck, the templates' security config) to MEDIUM+ blocking, with lower severities reported. Leave the `trivy config` HIGH+ line alone unless its enforcement also differs. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`d6fa8ca`, root configs `bf8d37c`). **What to test:** `git grep -n "HIGH+" -- docs/DoD.md docs/SECURITY.md project_config_security.md project_config_dod.md templates` must show no osv-scanner or SCA row — every live statement says MEDIUM+ (CVSS >= 4.0), which is what `.githooks/pre-push` and `.github/workflows/security.yml` already enforce. Trivy's HIGH+ lines stay: nothing runs `trivy config` (`git grep -i trivy -- .githooks .github scripts` is empty), so they describe a recipe, not an enforced threshold. **Cross-provider review:** Alex (Codex) — **push as is**; he re-searched the tree and found no conflicting live threshold. From storm2flow's `FR-storm2flow-osv-threshold`; founder decision 2026-09-15: MEDIUM+. **Re-open if** any doc, hook or workflow states a different SCA threshold than the other two. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S3 | Security / docs |
| **TASK-040** | **The managed `.githooks/pre-push` stops telling projects to edit it, and finds IaC under `infrastructure/` as well as `infra/`.** From storm2flow, `FR-storm2flow-prepush-layout`. `.githooks/pre-push:528-529` says to "edit this block during bootstrap", but the hook has no marker lines, so a pull replaces the whole file and those edits are lost. TASK-026's skip messages (`:542-547`) already point at the pull-safe home below the end marker of `.githooks/pre-push-project`. The IaC stages detect only `infra/` (`:598-641`), so storm2flow's CDK app in `infrastructure/` is skipped. **Do:** replace the instruction with a pointer to `pre-push-project`, and detect `infrastructure/` alongside `infra/` for the CDK, Terraform and Helm recipes, with the skip message naming both. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-16** (`9acffae`, `3682b53`, `6ae87e0`, `2285f22`). **What to test:** in a project whose IaC lives in `infrastructure/`, the pre-push gate must run the CDK, Terraform and Helm stages — even with an empty `infra/` sitting beside it, which used to silence all three. Each stage names the directory it ran in (`IaC synth · cdk · infrastructure`). With neither directory present, the skip says "no infra/ or infrastructure/ directory". The hook no longer tells you to edit it at bootstrap; project stages go after the end marker in `.githooks/pre-push-project`. **Cross-provider review:** Alex (Codex) found the mixed layout silently unchecked; after the fix Alexey ran the real IaC block with recording tools and confirmed a failing second tree still fails the whole stage — **push as is**. From storm2flow's `FR-storm2flow-prepush-layout`. **Re-open if** one directory can hide the other, or a stage runs in the wrong directory. **ACCEPTED by the founder 2026-09-16.** | 2026-09-16 | S3 | Gate |
| **TASK-038** | **Remove `CHANGES.md` from the lifecycle: `BACKLOG.md` and `BUGS.md` are the only record files in each state.** No `CHANGES.md` has ever existed in any lifecycle folder, and every task and feature row has travelled through `BACKLOG.md` (TASK-025..033 are in `waiting-acceptance/BACKLOG.md` today). But the rules still send non-defect changes there, and they contradict themselves: `docs/DoD.md` §7C line 661 moves backlog rows to `waiting-acceptance/BACKLOG.md`, and lines 669-673 send forward features to `waiting-acceptance/CHANGES.md`; `docs/waiting-acceptance/README.md` says it is created on first use (12-15) and that only BACKLOG.md and BUGS.md belong (17-20). An agent following the text files features where nobody looks. **Stated in:** `CLAUDE.md:288`, `docs/DoD.md:34-35` and `:663-673`, `docs/waiting-acceptance/README.md:12-15`, `docs/done/README.md:7-8`; read by `scripts/lib/dod-gate.sh:76` and `tests/lifecycle-docs/lifecycle-docs.ts:62` (with `.spec.ts` cases around 207-220 and a `tests/dod-gate` comment at 114). **Do:** drop CHANGES.md everywhere live; keep the rule that a defect is a `BUG-XXX` row, reworded as "never a backlog row"; update the deck lifecycle slide if it names it. Dated records (`docs/done/TASK-018-*`, `docs/doing/TASK-022-rule-enforcement.csv`) stay as history. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-15** (`a006606`, `1622afe`). **What to test:** `git grep -n -i changes.md` must hit only dated records, the handover and this row. `docs/DoD.md` §7C must send tasks, features and behaviour changes to `BACKLOG.md`, and defects to `BUGS.md`, never to a backlog row. **The founder waived the cross-provider review for this item.** **Re-open if** a rule, gate or test sends work to a `CHANGES.md` again. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S3 | Lifecycle / docs |
| **TASK-037** | **`blueprint a2bp` can propose files the blueprint does not ship, and feature requests from derived projects have one visible inbox.** Requested by struct2flow-www in PR #72 (`FR-www-a2bp-feature-requests`): `blueprint a2bp docs/backlog/BUGS.md` exits 4 ("not a blueprint-managed file"), so that PR had to be opened by hand with plain `git`/`gh`, skipping the contamination guard. **Founder decision 2026-09-15:** a2bp should also carry files that are not in the blueprint's managed set; "when we analyse a PR, we can always reject it, if it brings any kind of contamination." The founder also asked for a place to see feature requests. **Do:** (a) let a2bp stage an unmanaged path (a new file or a blueprint-only file) on its request branch, still running the contamination scan and still filing a request, never landing; say in its output that the file is not shipped; keep the refusals that protect the repo (paths outside the project, `.git`, secrets). (b) Add `docs/backlog/feature-requests.md` from PR #72 as the intake inbox (code, from, filed, request, status), recording the QA-2 request as PROMOTED TASK-034 and this one as PROMOTED TASK-037. (c) Update CLAUDE.md §"Back-propagating", README and the sync deck slide where they say a2bp carries only managed files. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-15** (`0c47b5b`..`f23e7cb`; inbox `38455ae`). **What to test**, in a derived project with `blueprint a2bp --dry-run <path>`: a new file, or one the blueprint does not ship, is accepted and labelled "not shipped". Each of these must exit 4 before any fetch: a `.env` or `*.key` file (even force-tracked), a file holding a private key (with gitleaks installed), any path under `.git`, `PROJECT_CONFIG_DOD.MD`, a gitignored file, and a path under a symlinked directory. `docs/backlog/feature-requests.md` shows both of PR #72's requests as PROMOTED. **Cross-provider review:** Alexey (Codex) found 2 P1 and 3 P2 issues, all fixed with failing tests first: secrets were proposable, the config refusal was case-sensitive, case-only base collisions, `scaffolding/` relocation, and a non-independent test. Vitali's first secret test held a literal fake key, so its five commits were rebuilt with the founder's OK. **Alexey's re-check of the fixes is still to run** (Codex quota, after 2026-09-16 02:10). PR #72 closed pointing here. **Re-open if** a2bp files a secret, a `.git` path or a project config, or places a new file anywhere but where it was named. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Sync / a2bp |
| **TASK-036** | **Three stale pointers in `docs/DoD.md`.** Requested by storm2flow in a2bp PR #74. **Founder decision 2026-09-15:** "seems ok." (1) the storm2flow citation carries no `a2bp-allow` annotation, so the contamination guard flags it on every request touching DoD.md; (2) the major-bug pointer should name `project_config_overview.md`, where the core USP paths are declared; (3) §G and the wake side still describe the tracked `AGENT_SIGNAL.md` as the baton, while the live baton is `logs/state/signal.md`, written only by `scripts/signal-set.sh`. **Do:** land those three changes in the blueprint, checking each against current text. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-15** (`8ae253a`, `d6faf8d` DoD; `83492f3` deck; `3c13c6a` roster example and publishing guide). **What to test:** in a derived project, `blueprint a2bp --dry-run docs/DoD.md` must no longer have the contamination guard flag the storm2flow citation (line 58). `docs/DoD.md` §G and "The wake side" must name `logs/state/signal.md` as the live baton, and the major-bug pointer must name `project_config_overview.md`. `AGENT_ROSTER.example.md` must put the persona name in the live baton's Holder field. **Cross-provider review:** Alex (Codex) found two more stale baton references, now fixed. PR #74 closed pointing here. **Re-open if** a managed doc still presents `AGENT_SIGNAL.md` as live state. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S3 | Docs / DoD |
| **TASK-035** | **Every work item named to the founder carries a link to the line that defines it and one plain sentence saying what it does.** Requested by storm2flow in a2bp PR #73 (AGENTS.md). **Founder decision 2026-09-15:** "I want it." The need showed the same day in this repo: the Orchestrator referred the founder back to "your answer about #71–#74" without restating the question or linking the items. **Do:** land #73's AGENTS.md wording in the blueprint, adapted if needed, with the deck slide for the persona-team concern if it describes how agents report. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-15** (`e135324`, deck `5c4c5ba`). **What to test:** `AGENTS.md` line 113 says every work item named to you carries a link to the line that defines it and one plain sentence saying what it does. The persona-team slide in `docs/way-of-working.md` carries the same rule. In practice: the next time an agent names a bug, task or PR to you, it links it and says what it is. **Cross-provider review:** Alex (Codex) asked for the deck bullet, which was added. PR #73 closed pointing here. **Re-open if** an agent names an item to you without a link and a plain line. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S3 | Agent protocol |
| **TASK-034** | **Acceptance is the founder's, and so is any delegation of it: remove the standing "acceptance delegated to QA-2" rule everywhere it is stated.** Requested by struct2flow-www in a2bp PR #71 (one location) and in PR #72's `FR-www-qa2-standing-delegation` (all of them). **Founder decision 2026-09-15:** "an agent may not delegate acceptance to another agent without consulting me. I'm responsible for the acceptance or its delegation." The 2026-07-29 decision covered specific items; the text generalised it into a class of work. **Stated today in:** `templates/project_config_dod.md:181` (seeded into every new project, naming this repo's personas), `project_config_dod.md:162`, `docs/done/BUGS.md:6`, `docs/waiting-acceptance/README.md:28-33`. **Do:** replace each with the founder's rule (final acceptance is the founder's; an agent may propose a cross-provider QA pass, but its verdict becomes acceptance only when the founder explicitly delegates that item); keep the 2026-07-29 decision only as a history note naming the items it covered. Dated audit records stay as history. **Re-open trigger / next-step gate at acceptance:** **Pushed 2026-09-15** (`dfd3cd9`, review note `cd4c373`). **What to test:** `git grep -n "delegated to QA-2"` must hit only this row and the dated `docs/config/BLUEPRINT-AUDIT-2026-07-23.md`. Read `templates/project_config_dod.md` §"Acceptance authority": it must say final acceptance and any delegation of it are yours, item by item, with no persona names. `docs/waiting-acceptance/README.md` §"How acceptance works" must say the same. **Cross-provider review:** Alex (Codex) found no remaining standing grant. His finding about incident history in the shipped waiting-acceptance README was declined: those lines explain why the rules exist, as shipped docs do throughout. PR #71 closed pointing here. **Re-open if** any live rule lets an agent accept, or delegate acceptance, without you. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Lifecycle / acceptance |
| **TASK-033** | **ShellCheck is installed on every machine and run by both the pre-push gate and CI.** Today the shipped shell scripts carry `# shellcheck` directives as if they were linted, but nothing installs ShellCheck (`scripts/install-toolchain.sh` has no entry on macOS or Linux) and nothing runs it: no gate stage, no CI job, no DoD requirement. So the changed scripts of TASK-025, BUG-120 and TASK-031 went out unlinted. Founder decision 2026-09-15: it is needed on the Mac as well as Linux. **Shape, mirroring TASK-031:** the installer installs it on both OSes (brew / apt) and `check` reports it; one shared function lints the shipped shell scripts through the gate's scrub path, called by a managed gate stage and by CI; a parity control fails the push if either stops calling it; existing findings are fixed or justified with a reasoned disable, never by loosening the severity threshold. **Re-open trigger / next-step gate at acceptance:** **Landed on main 2026-09-15; reviewed by Jesko (Codex), his one finding fixed.** What to test: (1) `bash scripts/install-toolchain.sh check` lists ShellCheck as present, or `bash scripts/install-toolchain.sh` installs it (brew on the Mac, the pinned release into `~/.local/bin` on Linux, no sudo); (2) add an unused variable to any script under `scripts/` and run `git push` — the gate stops at the ShellCheck stage and names the warning; revert it and the stage passes; (3) `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" ts-bridge install-toolchain` passes. Re-open if a ShellCheck warning in a shipped shell script can pass either the gate or CI, or if a machine without ShellCheck pushes without being stopped. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Tooling |
| **TASK-031** | **No pre-push stage and no CI job runs the TypeScript typecheck, so type errors in `tests/` land silently.** Two `tsc` errors in `tests/code-root/code-root.spec.ts` (TS2353, TS2339 on `childRoot`) sat on main unnoticed until the TASK-028 port fixed them as BUG-119, and `tests/tsconfig.json` now enables `exactOptionalPropertyTypes` — a setting nothing enforces. vitest transpiles without typechecking, so a green suite says nothing about types. Found by Philipp (Infrastructure-1) during TASK-028. **Shape of the fix:** one stage that runs `tsc --noEmit -p tests` in the gate and the same command in CI through the gate's own path (BUG-117's lesson: CI and the gate must not diverge), a `tests/manifest` assertion that both invoke it, and a reproducer showing a planted type error fails the push. **Re-open trigger / next-step gate at acceptance:** **Landed on main 2026-09-15; reviewed by Jesko (Codex), all three findings answered (`835fb3f`, `f8f4102`).** What to test: plant a type error in any file under `tests/` and run `git push` — the gate stops at "typecheck · TASK-031" within about a second, showing the compiler error; revert it and the stage passes. Then `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" ts-bridge manifest` passes. The same check runs in CI as "Blueprint TypeScript typecheck". Re-open if a type error in `tests/` can pass either the gate or CI. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Test suites |
| **TASK-030** | **a2bp request PR #70 from linkedin-watcher-agent: watcher-lock isolation test stops reading the real checkout's lock file.** `tests/watcher-liveness` #7 decides by whether the REAL lock file exists, but a correct dispatcher leaves that file in place for its whole life and after a clean stop, so the test can reject correct state; it passes only while no real lock exists. The request replaces it with a self-contained fixture over the lock's three real states (none / dead / alive) and adds the comment explaining why lock files must persist. Production behaviour unchanged. **Recommendation: accept as-is** — applies cleanly to current main, 20/20 on the branch. Review: `.scratch/JESKO-pr67-70-review.md`. **Re-open trigger / next-step gate at acceptance:** **Landed on main 2026-09-15 as `b87491a`; PR #70 closed with a comment linking it, branch deleted.** What to test: run `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA tests/node_modules/.bin/vitest run --root "$PWD/tests" watcher-liveness` — case #7 reports one run per starting state of the real lock (none, dead, alive), and all pass whether or not a real watcher is running in this checkout. Re-open if #7 reads or writes the real checkout's lock file again. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S3 | Test suites |
| **TASK-029** | **a2bp request PR #69 from linkedin-watcher-agent: deliver three managed scripts, and make a symlinked CLI load its helpers from its physical path.** **U1 is already done** by TASK-026 (BUG-114, with the stronger derived `tests/managed-references`). **U7 is still real** — helpers load beside an out-of-tree symlink — but the patch no longer applies to the TASK-025-rewritten CLI, and TASK-025 commit 4 replaces the symlinked command with an installer-owned one. **Recommendation: implement differently, decline the PR as a unit** — close U1 as done; carry U7's two assertions into TASK-025 commit 4 (installed command behaves like direct project-local invocation; an incomplete layout fails non-zero with gate status, never a clean drift report), citing #69 as provenance. Review: `.scratch/JESKO-pr67-70-review.md`. **Re-open trigger / next-step gate at acceptance:** **Landed on main 2026-09-15 inside TASK-025 commit 4's installer work; PR #69 closed with a comment linking the commits, branch deleted.** What to test: `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" install-toolchain` passes #U7a (the installed command reports exactly what the project CLI reports) and #U7b (a project CLI missing `lib/` or only `lib/gate.sh` exits non-zero, prints `gate: … NOT armed` and no report). Re-open if drift prints a report while it could not arm the gate. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Sync |
| **TASK-028** | **a2bp request PR #68 from linkedin-watcher-agent: bound fixture root discovery and tighten harness process semantics (10 files).** Adds a test-only `BP_STATE_ROOT_CEILING`, refuses to create a workspace beneath an existing `.git` / `.blueprint-root` / `.blueprint-source`, fixes the `code-root` `childRoot` return type, preserves caller options in `runScript` / `startWatcher`, and waits for a timed-out process group to be gone. **Reproduced on current main** by Jesko (QA-2, Codex): with `/tmp/.git` present, `bp_state_root` returned `/tmp` and `state-root` #A6 went red; the timeout path returns before `waitGroupGone`. It no longer applies (H5 changed `env.ts` / `index.ts`). **Recommendation: accept with changes** — port onto the current H5 harness, reproducer-first for the marker-above-workspace inversion, the timeout returning early, and the option-loss / type mismatch; document that the workspace preflight makes the whole harness refuse to run while a stray `/tmp/.git` exists (BUG-110, BUG-111). Review: `.scratch/JESKO-pr67-70-review.md`. **Re-open trigger / next-step gate at acceptance:** **Landed on main 2026-09-15; reviewed by Jesko (Codex), both findings resolved; PR #68 closed with a comment linking the commits, branch deleted.** What to test: with `TMPDIR` pointing below a directory holding `.git`, `.blueprint-root` or `.blueprint-source`, any suite refuses at workspace creation and names the marker and the remedy; `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" harness state-root state-dir code-root` passes; `tests/node_modules/.bin/tsc --noEmit -p tests` is clean. Carries BUG-110, BUG-111 and BUG-119. Re-open if a scenario resolves a state root outside its workspace, or a timed-out run returns while its killed group still exists. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Test harness |
| **TASK-027** | **a2bp request PR #67 from linkedin-watcher-agent: the toolchain installer reads the harness's real Node range instead of a copied `18+`.** `scripts/install-toolchain.sh` checks `NODE_MIN_MAJOR=18` while `tests/package.json` requires `^20.19.0 or >=22.12.0`. **Reproduced on current main** by Jesko (QA-2, Codex): with a Node 20.0.0 shim, `install-toolchain.sh check` prints `All present`. **Recommendation: accept with changes, folded into TASK-025 commit 4** (which rewrites the same installer): keep the single source of truth and its four cases; fix or fail closed on the custom evaluator's `^0.x` caret semantics; record OBSERVED mutant red sets (the request's header admits they were derived). Review: `.scratch/JESKO-pr67-70-review.md`. **Re-open trigger / next-step gate at acceptance:** **Landed on main 2026-09-15 inside TASK-025's installer work; PR #67 closed with a comment linking the commits, branch deleted.** What to test: `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" install-toolchain` passes, including #1-#6: a Node below `tests/package.json`'s `engines.node` range is rejected, an uninterpretable range is UNVERIFIED, `^0.x` follows npm, and a leading `*` does not waive later comparators. Re-open if `install-toolchain.sh check` accepts a Node version the manifest's range excludes. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Toolchain |
| **TASK-026** | **Implement the a2bp request PR #66 from struct2flow-www — all six changes, in the blueprint, with the review's corrections.** Founder decision, 2026-09-14, after a cross-provider review by Jesko (QA-2, Codex) that tested each change separately. **(1)** add `scripts/no-chain-guard.sh`, `scripts/session-resume.sh` and `scripts/wait-mic.sh` to `MANAGED_FILES` — the PR's stated reason is wrong: fresh bootstraps already receive them; the damage is to EXISTING projects updated by `pull`, which copies only managed files. Needs a regression over an existing project, and a check that DERIVES the set of referenced scripts rather than hand-listing three. **(2)** `CLAUDE.md` prose must not carry the literal marker tokens, because `pull` detects marker files by substring — reproduced: a prose mention triggered marker handling, a backup and a whole-file replace. Needs a `pull`-level regression. **(3)** gate skip reasons point a differently-laid-out project at where its own stages belong — as-is. **(4)** `drift` compares marker files on managed regions only — but as proposed, `drift` and `pull` disagree about the same file (drift clean; pull still selects it and previews the project footer as deleted). Build ONE comparison shared by drift, pull's selection and pull's preview, and have it refuse inverted markers, which closes the BUG-034 case where pull deletes a footer while reporting it preserved. **(5)** the historical-path annotation — as-is. **(6)** `agent-exchange` leaves the shipped `.claude/settings.json` and moves to the untracked `.claude/settings.local.json` in the same step. **Ordered before TASK-025**, whose `drift` rewrite builds on (4). Close PR #66 with a comment linking the commits. **Re-open trigger / next-step gate at acceptance:** **All six changes landed and pushed 2026-09-14; PR #66 closed with a comment linking the commits.** What to test: in a derived project, `blueprint pull` delivers `scripts/no-chain-guard.sh`, `scripts/session-resume.sh` and `scripts/wait-mic.sh`; `blueprint drift` does not list `.githooks/pre-push-project` when only the project's own guards after the end marker differ; `.claude/settings.json` no longer names `agent-exchange`. Bugs BUG-034, BUG-112, BUG-113 and BUG-114 carry their own tests. Re-open if drift, pull's selection, preview or write disagree about a marker file. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Sync |
| **TASK-025** | **`blueprint drift` and `blueprint pull` read the blueprint from its repository address, not from a folder on this disk.** Founder decision, 2026-09-14: *"what if we keep track of the repository address of the blueprint? It doesn't matter where it is physically."* Today both commands resolve a local checkout — `blueprint_source = ../blueprint` in `.blueprint-source`, else `$BLUEPRINT_ROOT`, else the checkout the CLI itself lives in — and `_bp_resolve_blueprint_root` already warns that the field *"cannot be right on two machines at once"*. Meanwhile every project ALREADY records `blueprint_remote` and `blueprint_branch` (config version 2), and `a2bp` already reads the blueprint through that remote with a throwaway clone (`scripts/lib/request-file.sh`). `drift` and `pull` should do the same. **Why before Stage B:** `~/.local/bin/blueprint` is a per-machine wrapper that `exec`s the hard-coded path to `scripts/blueprint`, which Stage B moves — so the move would break `blueprint` for every project on the machine, and no commit can fix a file outside git. Reading by address makes projects indifferent to where the checkout is and to the layout inside it. **Costs to design for, not discover:** the network becomes a dependency of `drift`/`pull` (as it already is of `a2bp`); an offline wake must say *"could not reach the blueprint"* rather than report clean — a silent clean is F-002's shape; and the fetched tree needs a cache so every wake does not re-clone. **Founder decisions, 2026-09-14, on the plan's four open questions** (plan §R3): **(1) T** — `drift` means *"matches the newest blueprint on its branch"*; the pinned reading (P) was considered and not chosen. **(2) Adopt a `released` branch** — CI fast-forwards it to the tested `$GITHUB_SHA` after every other job passes, on push events only, never with force, and only in the blueprint's own repository (`security.yml` ships downstream). Derived projects fetch it through a new optional field `blueprint_release_branch = released`; `blueprint_branch` stays `main` because a2bp uses it as the pull request base. Migration checks each project's `bootstrap_sha` is an ancestor of `released` before switching it. **(3) A leftover `blueprint_source` field warns on every run**, with no cut-off date. **(4) `scripts/install-toolchain.sh` writes the per-machine `blueprint` command** to `~/.local/bin/blueprint`: it names no checkout, so TASK-021 Stage B cannot break it, and it never overwrites a file it did not write (today's hand-written wrapper is replaced by hand, in migration). **Re-open trigger / next-step gate at acceptance:** **Landed on main 2026-09-15: all four plan commits and every review fix; reviewed by Alexey (Codex) through six plan revisions and three implementation reviews, final verdict "push as is".** What to test: (1) in a derived project, `blueprint drift` names the remote, `(released)` and a full fetched SHA, warns once about a leftover `blueprint_source`, and exits 5 offline (`BLUEPRINT_ROOT=<checkout>` is the local override); (2) `bash scripts/install-toolchain.sh check` reports the current hand-written `~/.local/bin/blueprint` with a warning and leaves it alone; (3) the suite runs green: `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" sync-by-address install-toolchain managed-references`. Migration of the three derived projects (plan §7.2, nine steps) is NOT done by this item and needs the founder. Re-open if drift or pull answers from a local checkout without `BLUEPRINT_ROOT`, or from a stale cache after a failed refresh. **ACCEPTED by the founder 2026-09-15.** | 2026-09-15 | S2 | Sync |
| **TASK-020** | **The suite set is derived from the filesystem and `tests/SUITES.md` is deleted, and the TypeScript harness manifest moves under `tests/`. Two founder decisions, 2026-09-10, landed as one change because both rewrite what `tests/manifest` believes about tier and shipping.** **(1) R1 wins over the tier table.** TASK-018-RULES R1 forbids a catalogue of tests, and the one that existed drifted twice in one afternoon while it was being built. `scripts/lib/suites.sh` now derives the suite set from the `*.sh` / `*.spec.ts` files under `tests/` and the tier from the `tests/<suite>/  export-ignore` lines in `.gitattributes`. Compared against every row of the deleted table before deleting it: 43 suites, **zero disagreements**. The residual risk `scripts/lib/pipeline.sh` names — the batch API's per-suite reports are only trustworthy if the expected set comes from a source the runner cannot edit at run time — is answered by the filesystem rather than weakened: `find` consults no vitest config, include glob or reporter. Five manifest cases policing the table are deleted (#2 ghosts, #3 legal tier, #4b retirement declarations, #6 tier rationale, #8 parallelism class), and with #8 goes the taxonomy R5 retires. The RETIRED-SHELL-RUNNERS table and the six dead shell runners it covered are deleted together — #4b required the `.sh` to exist for a row to be valid — and each mutation recipe is carried into its spec's docblock, which is where R6 requires it recorded and R1 says a test's description belongs. The rule is now: **every runner on disk is invoked**, no exemptions. **(2) `package.json` under `tests/`, because managing it at the root is a data-loss bug.** Verified: `pull_file` (`scripts/blueprint`) falls to the legacy whole-file `cp` for a file with no marker vocabulary, and writes no `.bp-bak` on that branch; JSON cannot carry markers and every derived project has its own root `package.json` (STACK_DEFAULTS.md), so listing ours in MANAGED_FILES would silently replace a project's real dependency manifest on the command every wake runs. `tests/` is already a managed DIRECTORY expanded through `git archive HEAD tests`, which is the same query bootstrap answers — so under it, ships and managed are one fact, collision is impossible rather than avoided, and phase 2 becomes one edit instead of two that must agree. `#2c`'s whole managed/shipped split for the toolchain is replaced by a structural check that no toolchain file has wandered back out from under `tests/`. **Phase 1's invariant is unchanged and still asserted:** the TS toolchain ships if and only if a shipping suite is TypeScript. Nothing was migrated and nothing new ships. **Re-open trigger / next-step gate at acceptance:** Raised and done 2026-09-10 by founder decision. Re-open if a derived project is ever found running a suite the blueprint dropped, or if the export boundary and `.gitattributes` are found disagreeing — both are what `tests/manifest` #2b and #4 exist to refuse. | 2026-09-14 | S2 | OPEN |
| **TASK-019** | **The blueprint is trunk-based, like every project derived from it — the main-branch guard is removed.** It was enforced in three places: `.githooks/pre-commit` refused the commit, `.githooks/pre-push` refused the push, and `tests/branch-guard` (TASK-003) pinned both, with `scripts/lib/branch-guard.sh` as the shared implementation. **Founder decision, 2026-09-10: pull requests exist for EXTERNAL collaboration — which is what `blueprint a2bp` files — not as a review step the owner performs on themselves.** The rule was not silly and what it protected is real: `main` here is the trunk every derived project pulls from, so anything landing fans out on their next `blueprint pull`. What it got wrong is who was being gated — on this repo the author, reviewer and owner are one person, so the PR was a ceremony performed alone, and `git push` would not let them skip it. **A gate someone must disable to do their ordinary work protects nothing**, which is the argument BUG-031 makes about a red CI everyone merges over and BUG-045 about a local scan harsher than CI. **Deleted rather than disabled:** `pre-commit` existed only for this guard, so a neutered guard would have left a hook, a lib, a suite, a `SUITES.md` row and two invocations all describing a rule nobody enforced — worse than either extreme, and the exact drift this repo keeps cataloguing. **What still protects `main`, unchanged:** the pre-push gate runs on every push, `a2bp` remains a request rather than a delivery, and a back-propagation still needs a human to merge it. Nothing about the REACH of `main` changed — only who is asked for permission to use it. Docs corrected in the same change: CLAUDE.md §"Never push to the blueprint's `main`" (replaced, with the reasoning kept rather than deleted), `docs/way-of-working.md` (three passages — the deck is the pitch surface and drift there is treated as a defect), and README.md's file map. **The "two cross-references" this row claimed were also corrected were NOT**, and both then contradicted the section that replaced them: `CLAUDE.md`'s trunk-based bullet still declared the blueprint exempt and still pointed at the deleted section name, while `docs/DoD.md` §1b·6 still split its landing table by **repo** and stated the blueprint may "**never**" take a direct push — three lines from `CLAUDE.md`'s own "no pull request is required". Both corrected 2026-09-11 onto the axis the founder named: **the contributor, not the repo** — a maintainer pushes the trunk of any repo, and an external contribution arrives as a PR, which is what `a2bp` files. **The lesson is this row:** a completed-work record naming the doc sites it fixed is a *claim*, and nothing checked it — the same shape as every control in the TASK-022 audit that printed PASSED over nothing. **Re-open trigger / next-step gate at acceptance:** Raised and done 2026-09-10 by founder decision. Re-open if the blueprint ever gains a second committer, which is the condition that made the guard make sense in the first place. | 2026-09-14 | — | OPEN |
| **TASK-017** | **The blueprint's only documented toolchain install path was a macOS-only `Brewfile`, which made a reduced pre-push gate the default outcome on every other OS.** Founder directive, 2026-09-09, citing the redcare blueprint where the same removal already happened (its `install-toolchain.sh` header records it as BUG-016). **Why this is a gate defect and not packaging taste:** the hook `pipe_skip`s a scanner it cannot find, so a machine that could not run `brew bundle` got a gate that printed PASSED having checked less — the repo's signature failure (BUG-004 a hook that never ran, A-22 a gate never armed, BUG-005 a suite covering nothing, BUG-035 a scanner aimed at absent languages). CI is `ubuntu-latest` throughout and never used the Brewfile, so the asymmetry was invisible from CI. The requirement is also already on the record: `scripts/wait-mic.sh:31` cites *"the founder then required the system be OS-agnostic or Linux-based"* as the reason an inotify watcher was rejected. **Delivered:** `Brewfile` deleted; `scripts/install-toolchain.sh` added and added to `MANAGED_FILES` — one tool list, per-OS mechanism (Homebrew on macOS, pinned release binaries into `~/.local/bin` with no sudo on Linux, `semgrep` via pipx), plus `check` and `--infra` modes and a `scripts/install-toolchain-project.sh` extension hook mirroring `.githooks/pre-push-project`. **Versions are pinned, unlike the redcare reference**, which resolves `latest` from the GitHub API at install time — `.github/workflows/security.yml` SHA-pins its actions and pins `OSV_SCANNER_VERSION`, and the installer holds that same posture so a bump is a reviewable diff. **Stated gap, deliberately not papered over:** the Linux path does NOT verify checksums, and the script's header says so rather than implying integrity it does not provide. Closing it needs a pinned SHA256 per tool per architecture. **Two records corrected as a consequence:** BUG-035's stated fix said "shellcheck in the `Brewfile`" and now names the installer, and `doing/HANDOVER.md` told a fresh checkout to run `brew bundle` (and named `trivy`, which neither the hook nor CI has ever invoked). **Re-open trigger / next-step gate at acceptance:** Founder-directed 2026-09-09. Re-open if a third OS is targeted, or when the checksum gap is closed. | 2026-09-14 | S2 | KEEP |
| **TASK-018** | **Migrate the blueprint regression suites from shell runners to TypeScript BDD-style tests, with deterministic parallel execution as the payoff rather than the premise.** Founder request, 2026-09-09: the current tests are slow and unpleasant to maintain in shell; the desired end state is BDD tests in TypeScript, parallel where isolation makes that honest. **Current shape:** 41 `tests/*/test.sh` suites, ~13.6k lines, wired one by one through `.githooks/pre-push-project` and duplicated in `.github/workflows/security.yml`; recent local gate runs are ~10 minutes, with `env-namespace` ~104-116s, `agent-activity-bound` ~98-106s, `a2bp-contamination` ~50s, `signal-dispatch`/`baton-durability` ~35-38s, and `a2bp-e2e` ~26-30s. **Decision to make explicit before implementation:** TypeScript improves fixture reuse, assertions, diagnostics, portability and parallel scheduling, but it will not by itself remove real elapsed time from negative timing tests, daemon settle windows, `timeout` cases, or real git/bootstrap end-to-end checks. **Plan:** add a root TS harness (`vitest` is the likely first fit; reserve full `@cucumber/cucumber`/Gherkin for acceptance-level flows if its ceremony earns its keep), typed helpers for hermetic repos/processes/signals/output assertions, and a BDD vocabulary (`given*`, `when*`, `then*`) while still spawning the real shell scripts/hooks as the product under test. Run TS serially at first and keep shell suites beside it until each translated suite proves equivalent; then replace the gate/CI invocation for that suite and delete the old runner. **Migration order:** start with mostly static/CLI suites (`doc-links`, `lifecycle-docs`, `manifest`, `commit-subjects`, `commit-msg-gate`, `env-namespace`, `no-chain-guard`, `proc-cwd`, `signal-set`), then git/bootstrap/a2bp suites, then daemon/timing suites last (`agent-activity-bound`, `signal-dispatch`, `baton-durability`, `wait-mic`, `watcher-liveness`, `subagent-feed`, `roster`). **Parallelism gate:** mark tests parallel-safe only after every scenario owns unique temp dirs, `HOME`, `AGENT_STATE_HOME`, `AGENT_SIGNAL_FILE`, logs, remotes and lock files; unsets inherited git pointers; kills only fixture-owned processes; and never relies on mutable global state. **Toolchain consequence:** this adds a root Node/TypeScript dependency to a blueprint that currently has no root `package.json`, so `scripts/install-toolchain.sh`, `tests/SUITES.md`, `.githooks/pre-push-project`, CI, bootstrap export boundaries and derived-project expectations must change together. **Success measure:** gate output still lists every suite/stage (no silent coverage cut), deterministic suites run safely in parallel, slowest-stage reporting remains honest, and any intentionally serial/timing test is named as such rather than hidden. **Re-open trigger / next-step gate at acceptance:** Re-open when test-infrastructure work is prioritised, when the gate duration starts causing skipped hooks, or when a new suite would be significantly easier to write in TS than shell. | 2026-09-11 | S2 | KEEP |
| **TASK-012** | **ANSWERED 2026-08-20 — adopt nothing, keep the socket.** The question was *should the blueprint adopt ruflo, and in which form?* The answer is **no form**, established by TASK-014's delegation test rather than by this spike's own reasoning: ruflo does not execute work, it allocates agent slots and defers to Claude Code, which this blueprint already has. **What this spike is worth keeping for** is the other half — orchestration is separable, measured both ways (it detaches, and a foreign layer sits beside it for ~5 s of gate overhead) — and the couplings are now named (§7G, and TASK-015). **Read the reopen history, not just the verdict:** this row was answered, reopened, answered again and corrected four times, each time because a premise had gone unexamined rather than because a measurement was wrong. That record is the most reusable thing here. **For acceptance:** nothing to run — the artefacts are [`TASK-012-strip-test.md`](TASK-012-strip-test.md) and [`TASK-014-attachment-trial.md`](TASK-014-attachment-trial.md), both in three parts. Judge them as decision records. ORIGINAL ROW FOLLOWS. **REOPENED by the founder, 2026-08-19 — the conclusions were drawn from a wrong assumption.** The spike measured whether orchestration comes **off** and concluded something about whether another orchestrator can go **on**; only the first was tested, and nothing was ever attached. Part 1's measurements stand, its *socket* inference is downgraded to **detachable — attachment unmeasured**. Part 2's **DO NOT ADOPT is withdrawn**: it rested on Q1 alone, and Q1's finding (ruflo cannot observe a `codex exec` *we* launched) holds only while we keep our own dispatcher — an orchestrator that owns the dispatch sees it by construction. The brief's **"evaluate on the lite path only"** constraint is retired: filling the socket means *being* the orchestrator, which is the full CLI, so the lite path could never answer the question it was paired with. Revalidation in Part 3 of [`TASK-012-strip-test.md`](TASK-012-strip-test.md); the evidence it now needs is **TASK-014** (the attachment trial). **Everything below is the ORIGINAL brief, kept verbatim as the record of what was asked** — including the two assumptions Part 3 retires. Superseded findings that survive: **BUG-028** (a fresh bootstrap cannot pass its own gate), the ~14% measurement, and §7G living in the core gate. ORIGINAL BRIEF FOLLOWS. Part 1 (strip test): orchestration is **separable-with-work**, so the blueprint is a product with a plug-in orchestration socket. Part 2 (ruflo): **DO NOT ADOPT**, and Q1 decides it alone — ruflo's Codex support is a *dispatcher*, not an observer, so it cannot see a `codex exec` we launched, which is the half of the feed that produces our review findings. Full evidence in [`TASK-012-strip-test.md`](TASK-012-strip-test.md). **Should the blueprint adopt [ruflo](https://github.com/ruvnet/ruflo), and in which form?** Not *how to use it* — that presumes the answer. **Why now:** 2026-08-18 produced four commits of feed plumbing and seven cross-provider review findings, none of which is why anyone would use this blueprint. The differentiator is the four-state lifecycle, the DoD gates, the cross-provider review rule and the `a2bp` sync model; the feed is infrastructure, and infrastructure is what you buy rather than debug. Founder asked whether an off-the-shelf answer exists, which is ladder rung 1 — *does this need to exist at all* — and nobody had asked it about the feed. **THREE QUESTIONS DECIDE IT, and none needs a commitment to answer.** (1) Does its observability capture a **`codex exec` dispatch**, or only Claude agents? Ruflo advertises 5 providers with failover, but that is MODEL ROUTING — our reviews run a different *harness* (`codex exec`, own sandbox, own context), and that is where the findings come from. If it cannot see the Codex half, it replaces the half of our feed that already works. (2) Does the **lite plugin path** (`/plugin install ruflo-core@ruflo`, *"slash commands only, zero workspace files"*) stay out of `CLAUDE.md`? The full CLI writes to `CLAUDE.md`, `.claude/` and `.claude-flow/` — and `CLAUDE.md` is a `MANAGED_FILES` entry that `blueprint pull` owns, so the full path collides head-on with the sync model. (3) Does the dashboard **replace** `tail -f logs/agent-activity.log`, or sit beside it as a second record — the failure mode this repo has fixed three times (TASK-005, the lifecycle triggers, the forwarding notes)? **Evaluate on the lite path only**; the full one adds Node + TypeScript + Rust and a daemon to a repo that is POSIX `sh`, `jq` and `awk`. **Exit criterion is a recommendation with evidence, not a migration.** **Strip test RUN 2026-08-18 — verdict separable-with-work, findings in [`TASK-012-strip-test.md`](TASK-012-strip-test.md).** **Method and sequencing below** — a `MANAGED_FILES` profile rather than a thin fork, and a no-orchestration bootstrap run FIRST, because it answers whether the differentiator is separable at all and cannot be invalidated by whatever ruflo turns out to do. Compare against [disler/claude-code-hooks-multi-agent-observability](https://github.com/disler/claude-code-hooks-multi-agent-observability) as the narrower alternative — 12 hook types, but Claude-only and its subagent handling is the bookend approach BUG-027 just replaced. | 2026-08-20 | S2 | REOPENED |
| **TASK-014** | **For acceptance — what to check, since nothing here is a passing test.** The deliverable was a recommendation with evidence, and it is [`TASK-014-attachment-trial.md`](TASK-014-attachment-trial.md) Parts 1–3. Two claims are worth re-verifying by hand if you want to: (1) `npx ruflo providers usage` in any fresh install reports thousands of Anthropic requests and a dollar total with no API key configured — that is the disqualifying finding and it reproduces in one command; (2) `npx ruflo swarm start -o "<anything>" -s development` prints an 8-agent plan, writes no files, and leaves agents/sessions at 0. **Everything else is reading.** The socket finding (170.9 s bare vs 175.9 s with ruflo installed) is the part that outlives this verdict, and TASK-015 is the coupling it exposed. ORIGINAL ROW FOLLOWS. **ANSWERED 2026-08-20 against the founder's real criterion — ruflo: DO NOT ADOPT, because it does not execute work at all.** Evidence in Part 3 of [`TASK-014-attachment-trial.md`](TASK-014-attachment-trial.md). Run on local `qwen3-coder:30b` at zero quota and zero spend, after the founder lifted the cost gate. **Setup succeeded** — Ollama is a first-class ruflo provider, `providers test` returns `PASS Ollama: Connected`, and the config persists to `claude-flow.config.json`. **Then `swarm start` on a real objective printed an eight-agent deployment plan and executed nothing**: *"initialized with 8 agent slots — execution happens via Claude Code Agent tool / `claude -p` / `hive-mind spawn --claude`"*. Measured after: 0 agents, 0 sessions, 0 tokens, no files written — and **"Overall Progress: 5.0%"**. Every execution route it names is Claude Code, so **no route runs a local model** and the tiering cannot be served by it. **Disqualifying on this repo's own terms:** `providers usage` reports 12,847 Anthropic requests, 4.2M tokens and $12.60 spent — on an installation created that day, with a sandboxed HOME, no API keys, and those providers reported *"Not configured"* minutes earlier — while Ollama, the only connected provider, is absent from the table and `/api/ps` confirms no model was ever loaded. CLAUDE.md §"Cost is a main concern" requires actual per-invocation spend, and **a dashboard that invents its numbers is worse than none because it is trusted**. Third of three honesty findings, with "7 hooks enabled" (enabled zero) and the 5% progress — a consistent posture of reporting the state the system is designed to have rather than the state it is in, which is exactly BUG-027 and BUG-031's shape. **Corrects Part 1:** the `swarm-activity.json` staleness finding was backwards — the file was accurate (nothing was coordinating, ever), the CLI was the unreliable half. **Unaffected and standing:** the socket is real (170.9 s bare, 175.9 s with ruflo installed), TASK-015, the `Bash(node .claude/*)` grant, the machine-global `~/.claude/CLAUDE.md` write, and both corrections in ruflo's favour. **Not claimed:** only `swarm start` was exercised — `hive-mind spawn --claude` and `autopilot` were not run, and while neither can put work on a local model, this does not assert they fail to execute. **Consequence:** FEATURE-007 does not need ruflo and is now the cheaper path to the same goal. — the trial is INCOMPLETE against the real criterion and blocked on a budget decision.** The disqualifier rested on the baton's `Holder` row being a requirement. It is not: *"I don't care if the agents have a name or not, I have the names only to avoid the orchestrator doing all the work, instead of loading fresh and specialized agents."* The requirement is **work is dispatched to fresh specialized agents rather than absorbed by the orchestrator, observably** — which anonymous workers satisfy as readily as named ones. **That was never tested**, because running work through a swarm is a billable LLM path and the trial ran credential-less under the cost rule. Also reversed: §7G did **not** "fail closed correctly" — it asserts *our mechanism* rather than *the project's coordination is healthy*, so the vacuous-adapter result is evidence against §7G's shape, not against ruflo. **Third iteration of one error** (brief → Part 2 → this trial): a competent measurement inferring past an unexamined premise, caught by the founder rather than by the method. **What stands, none of it resting on the void premise:** the socket is real (bootstrap green 170.9 s, still green with full ruflo installed 175.9 s); `tests/state-dir` is a core guard whose sample population is the orchestration layer and fails *vacuous* on the strip; the install grants itself `Bash(node .claude/*)` over a gitignored directory it just filled with 110 files, reports "7 hook types enabled" while enabling zero, and writes machine-global `~/.claude/CLAUDE.md`; and both corrections to TASK-012 Part 2 hold (no project `CLAUDE.md` collision, `settings.json` merged not clobbered). **Blocked on:** a founder budget decision for the delegation test. Full evidence in [`TASK-014-attachment-trial.md`](TASK-014-attachment-trial.md).** The seam holds under construction as well as deletion: a fresh bootstrap passes its own gate (44 stages, 170.9 s) and still passes with the full ruflo CLI installed alongside (175.9 s, ~5 s overhead, no failures). **Ruflo is not what fills it, and not for Part 2's reason** — the baton asks *who holds the mic, on what, and who do they hand to*; a ruflo swarm coordinates anonymous workers over a task queue, and **none of the three baton rows has a source** in either its files or its CLI. Sharpest datum: with a swarm initialised and a 7-worker daemon live (one worker already executed), `swarm-activity.json` still read `coordination_active: false` at its install-time timestamp and the `sessions` table was empty — a file-reading adapter reports IDLE while a swarm runs, which is worse than no adapter. **§7G needs neither of the two fixes anticipated:** it failed closed over a coordination model it could not describe, which is it working. **Two further findings raised for their own items:** `tests/state-dir` is a core guard (`both`) whose sample population IS the orchestration layer, so the strip makes it fail *vacuous* by design and it cannot be suppressed without defeating the control; and the install grants itself `Bash(node .claude/*)` over a gitignored directory it just wrote 110 files into. **Corrections to Part 2:** the `CLAUDE.md` collision does **not** reproduce (project file byte-identical; it writes machine-global `~/.claude/CLAUDE.md` instead), and `.claude/settings.json` is merged rather than clobbered with deny, ask and hooks untouched. **Not determined:** no work item was pushed through with ruflo actually orchestrating — that is a billable LLM path and the trial ran credential-less by design. ORIGINAL PROMOTION NOTE FOLLOWS. **PROMOTED 2026-08-19 — the founder authorised the trial on reopening TASK-012.** The two soft blockers below stand and are now understood as the trial's *first output* rather than its precondition: what §7G and `drift`/`pull` actually need is one of the things attaching an orchestrator is supposed to reveal, and specifying them ahead of the trial would repeat TASK-012's own mistake — deciding the shape of the seam before anything was plugged into it. **The attachment trial — can a foreign orchestrator actually plug into the socket?** TASK-012 measured **detach** and inferred **attach**; see Part 3 of [`TASK-012-strip-test.md`](TASK-012-strip-test.md) for why that step is invalid. Nothing has ever been plugged in, so there is no verdict on ruflo — the earlier DO NOT ADOPT answered a different topology. **Method mirrors the strip test, run in the other direction:** bootstrap a throwaway project, install the **full** ruflo CLI, mount the four-state lifecycle and the DoD gate **on top of it**, push one real work item through, and let the failures be the coupling map. Deletion proved the seam opens; only construction proves something fits it. **FOUR THINGS IT MUST ANSWER, none of which a detach-only run can see.** (1) **§7G** — with no orchestrator there is no baton and `pipe_skip` looks sufficient; with a foreign one there IS coordination state, just not ours. Can ruflo's state be **translated into the baton contract** (`Holder`/`State`/`Task`)? That is a mapping problem, and the answer decides whether §7G moves, skips, or dies. (2) **Does adoption CREATE the second-record defect it was rejected for?** `lib/feed.sh` and `log-activity.sh` are the observability lane and correctly stay; ruflo brings its own. Two writers to one activity log is exactly Q3's objection, manufactured by the fix rather than avoided by the rejection. (3) **`CLAUDE.md` collision** — the full CLI writes it and `blueprint pull` owns it. Real integration cost; price it, do not let it close the question the way it closed Part 2. (4) **Who invokes the gate**, and do the DoD stages still receive the inputs they assume when the orchestrator drives rather than a human. **TWO COSTS TO PRICE, NEITHER A BLOCKER:** Node + `npx` on every Bash and Edit call in a repo that is POSIX `sh`, `jq` and `awk` (their own note says `npx` can take 30+s cold); and the load-bearing half of the product moving onto a third party's upgrade cadence — 68k stars, mid-rename from claude-flow, with a self-declared `"_legacy_unaudited_shim": true` at its root whose `PreCompact` hook injects "54 available agents" and SPARC workflows that do not exist here. You would own and delete that file on install; it is evidence about hygiene, not an inherited defect. **NOT THE ANSWER:** re-running the lite path (an add-on by construction — it cannot fill a socket); deciding from source reading again (Part 2's own stated gap is that **nothing was ever installed**); or treating this as a migration. **Exit criterion is a recommendation with evidence, same as TASK-012 — the deliverable is a verdict, not an adoption.** **Blocked-by, soft:** the two follow-ups Part 1 named are the socket's other half and there is nothing to attach to until they exist — §7G out of the core DoD gate, and `drift`/`pull` learning the profile (today a profiled project is permanently drifted and one `blueprint pull --yes` away from having the layer back, which under attachment would collide with the attached orchestrator rather than merely reappear). | 2026-08-20 | S2 | PROMOTED |
| **FEATURE-005** | **Wait to be notified instead of watching — replace the always-on mic `Monitor` with a one-shot wait.** A `Monitor` polls forever and **its death is indistinguishable from quiet**; two died silently in one session and the founder noticed before I did. A-40 records that `ps` cannot see them, so no liveness check works either. Invert it: a background process watches the baton, prints the new `Holder`/`State` when it changes, and **exits**. The harness notifies on task exit whatever the code, so *fired* and *stopped* are both events — a long-lived watcher only gives you the first. It also cannot be rate-limited into silence, because it emits once. **Reviewed by Alexey (Architect) before implementation** — see the notes below for what he changed. **Accepted on a live probe:** the founder ran `wait-mic.sh`, flipped the baton, and it printed `MIC: Holder=Test State=ACTIVE` and exited — while a second waiter armed in the session caught the same publish independently. Two confirmations from one flip. | 2026-08-18 | S3 | KEEP |
| **FEATURE-003** | **`scripts/session-resume.sh` derives where a woken session is** — git state, the four lifecycle folders, the live baton, and the journal events since the last handoff marker. It holds nothing, so it cannot go stale. `--mark` rolls the handoff window in one append; `--rollback` stashes rather than discards. Exit 9 = incomplete or untrusted. The writer stays parked; only the marker append survives of it. Nine cross-provider rounds, sixteen findings, fifteen defects, each with a regression case red on its own parent. **The feed probe was deleted** (−544 lines) after the founder's *"you are both overcomplicating this"* — it guarded a file the tool never reads and fired on the ordinary path. Post-mortem: [`PLAN-FEATURE-003-session-snapshot.md`](PLAN-FEATURE-003-session-snapshot.md) §8b. **Accepted on the tool accepting itself:** the 2026-08-17 wake, ten days after the handoff, ran it first — exit 0, silent, replaying exactly the one handoff event. The thing being accepted produced its own evidence. | 2026-08-17 | — | KEEP |
| **TASK-010** | **`HANDOVER.md` carries take-over data only — 530 lines → 79.** Three categories earn a place: WIP with the constraints a reader would otherwise get wrong, EPHEMERAL state no command can reconstruct, and live hazards still armed. Everything derivable from `ls` or `git log` is forbidden. **DoD §10 required the opposite** and changed in the same commit, or the file regrows on the next handoff and §G gates on the old rule. Host quirks and standing founder decisions moved to `project_config_overview.md` rather than being deleted. **Accepted on a reading test**, deliberately: the failure mode is prose that restates what a command already answers, and no command can check that. | 2026-08-06 | S3 | KEEP |
| **TASK-008** | **The pre-push gate came in under its SLO without testing less** — 198.6 s → **159.1 s**, `signal-dispatch` 74.9 s → 32.5 s, all six assertions intact and no suite moved a tier. Implemented by Jesko (Codex) on the founder's instruction to delegate; reviewed and landed here because the Codex sandbox could not branch or commit. One idea applied three times: every sleep was asked whether it was *required by an assertion* or merely *how long someone guessed*. Readiness now waits on the watcher's liveness lock (BUG-022) instead of a settle window; the negative-case wait is `settle + 2·poll`, which is the real bound; and a wait about poll cadence stopped being expressed in settle units. **Accepted on:** a healthy gate printing no SLO line, `tests/manifest` holding every tier, and `tests/signal-dispatch` still reporting all six assertions — additionally at the original pacing (`SIGNAL_TEST_SETTLE=3`), so no assertion depends on the faster clock. | 2026-08-06 | S3 | KEEP |
| **TASK-007** | Render the DoD as PIPELINE STAGES, so the handoff checklist prints like every other gate step and its ABSENCE is visible — the FEATURE-002 argument applied to §7. Unlocked by TASK-002: every commit subject now starts with its item, so the gate can derive which items a push serves and check the DoD against them. Judgement-only steps print rather than pretend to verify. | 2026-08-05 | — | KEEP |
| **TASK-006** | Reconcile the documentation with what the gates now ENFORCE. Six drifts: CLAUDE.md and DoD still documented `fix(BUG-XXX):`, which the commit-msg hook rejects; `doing/BUGS.md` said BUG/FEATURE were the "only" lifecycle IDs though TASK now exists; the deck was missing the eight intake rules, both new gates, and the fifth lcm point; README and A2BP_PLAYBOOK still prescribed Conventional Commits. | 2026-08-05 | — | KEEP |
| **TASK-005** | Delete `waiting-acceptance/INDEX.md`. The lifecycle has TWO record files — `BACKLOG.md` and `BUGS.md` — that travel through the four folders; INDEX was a third, holding a copy of the same membership plus history and process that belong elsewhere. When it drifted (5 rows listed vs 14 real) the first response was a test to hold the two in step instead of asking why there were two. | 2026-08-05 | — | KEEP |
| **TASK-003** | Refuse a commit (and a push) on `main` in a BLUEPRINT checkout. CLAUDE.md §"Never push to the blueprint's `main`" says "commit on a branch, or do not commit yet" and neither door was guarded. Does NOT fire in derived projects — they are trunk-based and commit to `main` by design. | 2026-08-05 | — | KEEP |
| **TASK-002** | Every commit names the backlog item it serves. **Rejected on first delivery and re-fixed**: the `commit-msg` hook is client-side, so it could never reach the path every blueprint change takes — GitHub composes the squash-merge subject from the PR title, on its own servers. Now one shared rule (`scripts/lib/commit-subject.sh`) backs both the hook and `scripts/check-commit-subjects.sh`, and CI checks the PR title, the branch's commits, and what lands on `main`. | 2026-08-05 | — | KEEP |
| **TASK-001** | Write the concrete work-intake rules into `docs/DoD.md` (§1b) and move the `lcm` checklist there from CLAUDE.md, adding the fifth point the passes showed was missing — that the lifecycle DOCUMENTS say something true, not just that folder membership is right. Establishes `TASK-XXX` as a third lifecycle ID alongside `BUG-`/`FEATURE-`. | 2026-08-05 | — | KEEP |

**TASK-002 was rejected on its first pass and accepted on its second, the same
day.** Worth keeping visible: the rejection was correct and cheap, and it found a
defect no amount of reviewing the hook would have — the hook was never wrong. It
was in the wrong place. The evidence was a real commit (`5fe89e0`) that had
already landed on `main` in violation while every gate reported green.

## What to test — FEATURE-005

```bash
sh scripts/wait-mic.sh logs/state/signal.md &
```

It should print nothing while the mic is unchanged. Flip the baton with
`scripts/signal-set.sh` and it must print `MIC: Holder=… State=…` **and exit**.
That exit is the whole feature: the harness reports a task ending whatever the
reason, so a waiter that stops is an event rather than silence.

- `bash tests/wait-mic/test.sh` — 13 cases, 8 of them negatives.
- **It does NOT close the blindness**, and the row must not be read as claiming
  it does. It has to be re-armed after every event, and forgetting is silent —
  which happened to its author on 2026-08-18 and cost 40 minutes.

## What the review changed, before a line was written

Recorded here because two of the three are constraints on the *claims*, not the
code, and those are the ones that quietly get dropped.

**Alexey (Architect), 2026-08-17 — "build it, with narrower claims":**

1. **It is not self-healing, and must not be described as eliminating
   blindness.** The one-shot converts three things into visible events — the
   baton changes, the waiter crashes, the harness stops it — and that is the
   whole gain. But it must be RE-ARMED after each event, and forgetting is
   silent. The third shape that would close it is a **supervisor**, which this
   repo does not have. Say the gap is **narrowed, not closed**.
2. **Replace the Monitor, do not run both.** Two mechanisms watching one file is
   the A-09 shape one level up.
3. **Pilot project-locally.** Not in `MANAGED_FILES`, and `AGENTS.md` stays
   silent — it travels, and a project told to run a command it does not have is
   worse off than one told nothing. The wake instruction lives in the
   project-owned `HANDOVER.md` until it has proved itself here.

**The design changed once before that, and the artefact is being deleted rather
than carried.** The parked prototype used inotify through Python `ctypes` —
~40 lines, and it needed "watch the DIRECTORY not the file" because
`signal-set.sh` publishes by atomic rename. The founder's *"OS agnostic or Linux
based"* killed it: inotify is Linux-only, `inotifywait` is not installed, and the
`Brewfile` is macOS-only, so it would have been a Linux-only dependency shipped
through a macOS-only package manager. Six lines of POSIX `sh` replace it, and the
rename subtlety disappears with it.
