# Bugs — user-accepted (delivered)

The source of truth for **what has actually been delivered** — not what has been
merged. A row lands here only on an explicit acceptance signal.

> **Final acceptance is the founder's** (`project_config_dod.md` §"Acceptance
> authority"). An agent may propose a QA pass by an agent from the other
> provider, and run it once agreed; its verdict becomes acceptance only when the
> founder explicitly delegates that item. There is no standing delegation for any
> class of work.
>
> *History:* the 2026-07-29 delegation covered that day's QA pass only — BUG-001,
> BUG-002, BUG-003, A-01/A-12/A-14, A-05/A-27, A-03, A-07 and A-09 accepted,
> A-22 rejected by reproducing the gap live
> ([ACCEPTANCE-JESKO-2026-07-29.md](ACCEPTANCE-JESKO-2026-07-29.md)).

| # | Bug | Severity | Status | Plan |
|---|---|---|---|---|
| **BUG-145** | **After a port landed, every later push was refused: the shell inventory called the port's own shim `NEW`.** Red CI on `3cfa5e1`, the first push after BUG-144. | S1 | **ACCEPTED by the founder 2026-09-22, all waiting bugs at once.** Status when accepted: **LANDED**, CI green on `e774cfe` (2026-09-22) | **What was tested:** Run the CI inventory step against the base that went red: `BP_SHELL_INVENTORY_BASE=3cfa5e1`, then source `scripts/run-ts-suites.sh` and call `ts_shell_inventory . "$(command -v node)"`. It passes, where CI failed. A shim whose `.mts` is absent or untracked is still refused. **Fix:** `scripts/shell-inventory-check.mts` accepts a valid shim with a tracked target whether or not any list names it: migrated, not new. One line, reusing `isValidShim`. Authored and committed by Thomas (Kimi). Elias (Codex) was skipped because the proof is fixture-git suites. Reviewed by the Orchestrator (Claude). **Missed by everyone on BUG-144, author, reviewer and Orchestrator:** all three checked the port push, and none checked the push after it. **Regression test:** `tests/shell-inventory` #13, named BUG-145. **Re-open if** any push after a port is refused for the port's own shim. |
| **BUG-144** | **A dispatch that failed left the mic at `OVER_TO_<X>`, and nothing told the Orchestrator.** Observed live 2026-09-21: a Kimi quota 403 stranded the baton for 30 minutes. | S2 | **ACCEPTED by the founder 2026-09-22, all waiting bugs at once.** Status when accepted: **LANDED**, CI green on `45b0f87` (2026-09-22) | **What was tested:** Dispatch a persona whose CLI exits without handing back (e.g. a provider out of quota): within a poll or two the baton reads `Holder=<Orchestrator> State=OVER_TO_CLAUDE`, and its Task says the dispatch ended without handing back. A dispatch that DOES hand back is left alone. `pgrep -af signal-watch.mts` shows both watchers on the TypeScript poller, each with a `flock -n … exec cat` lifeline child. **Fix:** in the poller, once for every provider. After the wake command returns, if the baton still names exactly the Holder and State it dispatched, `recoverStrandedMic()` hands the mic to the Orchestrator through `signal-set.sh`. **The first whole-file TASK-067 port:** `scripts/signal-watch.sh` is now the two-line shim, and `signal-watch.mts` carries the poller. It locks with a lifeline pipe (Node has no flock) and reaches `state-dir.sh`/`watcher-lock.sh` across a process boundary, never copied. Elias (Codex) attempted it first, blind: his sandbox cannot run fixture-git suites, so it came out minified with 25 tests failing and was rejected unpushed. The founder reassigned it to Philipp (Claude). Reviewed CLEAN by Thomas (Kimi), with three garbage-input or theoretical notes not taken (DoD 1b rule 4). **Regression test:** `tests/mic-recovery`, named BUG-144, red against the pre-fix port for the right reason. **Re-open if** a failed dispatch can leave the mic with a provider that is no longer running. |
| **BUG-143** | **A Codex dispatch reported `finished` and pointed at the PREVIOUS run's last message**, so a run that died, or one that handed the mic back before writing its report, looked like a fresh completion. | S2 | **ACCEPTED by the founder 2026-09-22, all waiting bugs at once.** Status when accepted: **LANDED**, CI green on `3c4d133` (2026-09-21) | **What was tested:** Dispatch a Codex persona and read `codex-last-message.md` while it runs: it says `[in-progress] codex exec dispatched …`, never the previous report. A Codex run that exits non-zero logs `FAILED (exit N)`. **Fix:** `scripts/start-codex-signal-watch.sh` captures the exit status via a status file (dash has no `PIPESTATUS`) and stamps `$OUTPUT_LAST` before dispatch. Authored by Philipp (Claude), reviewed CLEAN by Thomas (Kimi). **Regression test:** `tests/codex-dispatch-status`, named BUG-143 (4 of 5 fail pre-fix). **Re-open if** any dispatcher reports an outcome it did not verify. |
| **BUG-142** | **`agent-activity.sh --whoami` answers with the Orchestrator inside a dispatch**, so a dispatched persona asking who it is gets the wrong name. | S3 | **ACCEPTED by the founder 2026-09-22, all waiting bugs at once.** Status when accepted: **LANDED**, CI green on `3c4d133` (2026-09-21), fix `dda4b21` | **What was tested:** With the baton on a dispatched persona, have it run `bash scripts/agent-activity.sh --whoami`: it answers with its own name, not `Eto`. In your own session it still answers `Eto`. **Fix:** all three launchers export `AGENT_PERSONA` from `AGENT_SIGNAL_HOLDER`. Authored and committed by Thomas (Kimi), reviewed by the Orchestrator (Claude). **Regression test:** `tests/dispatch-identity`, named BUG-142. **Re-open if** any identity resolver answers for the session it is not running in. |
| **BUG-140** | **The baton check passes an empty or unknown Holder, and never runs in CI.** `dod_stage_signal` (`scripts/lib/dod-gate.sh:460-491`) only checks that the `Holder`, `State` and `Task` rows exist. A hand-edited baton with an empty Holder, or a persona no roster names, is reported well-formed, and `scripts/signal-set.sh --holder NoSuchPersona` publishes it. `AGENT_SIGNAL.md` says `Holder` is a persona name from the roster. Found 2026-09-17 in the TASK-022 audit (rules C004/C005/D119). | S3 | **ACCEPTED by the founder 2026-09-22, all waiting bugs at once.** Status when accepted: **LANDED**, CI green on `892d4ca` (2026-09-18) | **What was tested:** `bash scripts/signal-set.sh --holder NoSuchPersona …` refuses and points at `AGENT_ROSTER.md`; `--holder Nobody` passes. **Not done:** the title's "never runs in CI" half — `.github/workflows/security.yml` still runs only `dod_stage_rows` and `dod_stage_bugtests`. The baton is untracked, so CI has none to check and that half may be moot: accept it as such, or reopen. **Fix:** `signal-set.sh` refuses a Holder that is not a roster persona (and not the founder's own `USER` convention, if one exists — check), and the gate stage checks the same. **Regression test:** named BUG-140. **Re-open if** a baton naming nobody on the roster is published or passes the gate. |
| **BUG-139** | **The bug-regression-test check is satisfied by a comment, so a fix can land with no test.** `scripts/lib/dod-gate.sh:425` greps the declared test roots for `BUG-0*<n>\b` in any file. `// BUG-9999` in any spec passes it, and no test title is ever required. DoD §2 calls this the guarantee that every bug ships with a reproducer. Found 2026-09-17 in the TASK-022 audit (rules D027/D099) and confirmed in the source. | S2 | **ACCEPTED by the founder 2026-09-22, all waiting bugs at once.** Status when accepted: **LANDED**, CI green on `892d4ca` (2026-09-18) | **What was tested:** Name a bug in a commit whose only mention in `tests/` is a `// BUG-NNN` comment: the pre-push bug-test stage refuses it. A row carrying `**No regression test:** <reason>` passes. Known limit: a test title split across lines is not matched. **Fix:** the bug number must appear in a test TITLE (`it(...)`/`describe(...)`), not anywhere in a file. Check first which already-landed bugs would fail the stricter rule and report before changing it. **Regression test:** named BUG-139. **Re-open if** a push naming a bug passes with no test whose title carries that number. |
| **BUG-138** | **The "artefacts travel with their item" check only sees BUG folders, so a stranded TASK or SPIKE artefact passes.** `docs/DoD.md` §1b rule 8 cites `tests/lifecycle-docs` #3 for plans, reviews, mockups and spike code alike, but the scan matches `BUG-*` folders and `PLAN-BUG-*.md` only. Found 2026-09-17 by the founder: TASK-022's plan and six refreshed audit files sat loose in `waiting-acceptance/`, where a later move would have taken the row and left them behind, and nothing failed. Fixed by hand in `b1b70f0`. | S2 | **ACCEPTED by the founder 2026-09-22, all waiting bugs at once.** Status when accepted: **LANDED**, CI green on `892d4ca` (2026-09-18) | **What was tested:** Move a `TASK-`/`FEATURE-`/`SPIKE-` row to another state folder and leave its `PLAN-*.md` or folder behind: `tests/lifecycle-docs` fails naming it. Two loose files of one item are REPORTED (`looseGroups`), not failed. **Fix:** the scan covers every item prefix the lifecycle uses (`BUG`, `TASK`, `FEATURE`, `SPIKE`) for both folders and `PLAN-*.md`, and a multi-file item with loose files beside its row is reported. **Regression test:** named BUG-138. **Re-open if** an artefact of any item type can sit in a state folder its row has left. |
| **BUG-141** | **A Gemini dispatch logged as `[GEMINI]`, not as the persona holding the mic** — the defect TASK-063 fixed for Kimi, in the launcher Kimi was mirrored from. | S3 | **ACCEPTED by the founder 2026-09-22, on the live log.** Status when accepted: **FIXED — `f22557e`, CI green on `3c4d133` (2026-09-21)**. Single commit, not the §3.1 two-commit form, accepted by the founder at push. | **What was tested:** a live Gemini dispatch on 2026-09-22 (Gemma, QA-4, reviewing this very bug). Every feed line, over 40 of them, read `[Gemma - Gemini]`, never `[GEMINI]`. The CLI then crashed on its free-tier quota, and the launcher logged `FAILED (exit 1)`, not `finished`. The crash's final line reached the feed before it. The hand-back to `Eto` was accepted by `signal-set.sh`. The founder accepted on that log. Gemma's own `ACCEPT-READY` was **not** counted: she could not read her brief (the Gemini CLI honours `.gitignore`, and the brief was in `.scratch/`) and ran out of quota, so it was a verdict with no review behind it. **Fix:** `scripts/start-gemini-signal-watch.sh` labels through `bp_roster_label`, keeps the CLI exit status, and flushes an unterminated last line. The dead `raw` pump mode is deleted. Authored by Elias (Codex), reviewed by Philipp (Claude). **Regression test:** named BUG-141. **Re-open if** any provider's dispatch logs under a provider tag instead of a persona. |
| **BUG-137** | **The feed drops a subagent's first lines when its transcript appears after the feed started.** Observed 2026-09-17 19:52 on a live probe: Klaus's nested general-purpose helper ran `wc -l CLAUDE.md` then `wc -l README.md` (both in its transcript, `agent-a3b9e55dba265076c.jsonl`), and `logs/agent-activity.log` shows only the README line. `seed_offset` (`scripts/agent-activity.sh:524`) starts every newly discovered file at its current size. That is correct for files that already existed when the supervisor started (a restart must not replay history), but a transcript born while the feed runs loses whatever was written before the next scan. | S2 | **ACCEPTED by the founder 2026-09-17.** Status when accepted: **FIXED — `48282c8` reproducer, `9b2f088` fix, pushed 2026-09-17**, CI green on `9b2f088` (run 35256472298) | **What was tested:** Dispatch a persona that spawns its own helper, then compare the helper's transcript with the feed: every tool call in `~/.claude/projects/…/subagents/agent-<id>.jsonl` has a line in `logs/agent-activity.log`, including its first. Restart the feed while agents have run before (`agent-activity.sh --daemon`): their old lines are not replayed. **Fix:** only files present at the supervisor's first scan start at end of file; a file first seen later is read from its start. **Regression test:** named BUG-137. **Re-open if** a subagent's first tool call is in its transcript but not in the feed. |
| **BUG-134** | **`tests/lifecycle-docs` refuses a bug cancelled the way the DoD prescribes, so the cancellation cannot be pushed.** Observed 2026-09-16 while applying the founder-approved `doing/` triage (TASK-058): BUG-101 and BUG-108 were cancelled by deleting their rows and recording them in `docs/config/findings.md` F-005, and "THE REAL TREE" failed with "bugs with commits on HEAD and no row". It reads only `BUGS.md` rows, while `scripts/lib/dod-gate.sh` learned in BUG-130 that a findings pointer is a record. Same defect, second reader. | S3 | **ACCEPTED by the founder 2026-09-17.** Status when accepted: **FIXED — landed 3bf6351 (reproducer), cd0b827 (fix), pushed 2026-09-16, CI green on `cd0b827` (run 35151645886)** | **What was tested:** Cancel a bug the DoD way (delete its row, name it in `docs/config/findings.md`) and push: `lifecycle-docs` and the DoD gate both accept it. A bug with commits and no record anywhere still fails. Found 2026-09-16 by Eto. **Fix:** `rowedBugIds` also counts bug ids named in `docs/config/findings.md`, matching the DoD gate. **Regression test:** `lifecycle-docs` #6 BUG-134. **Re-open if** a findings-recorded cancellation fails either check. |
| **BUG-133** | **A signalled deferred bookend child leaves its `sleep 0.1` running for up to 0.1 s after it exits, and CI's harness caught it as a leftover process.** CI run 35141833622 on `b26c0db`, the first run with suites in parallel: `subagent-feed` #17 failed with "Scenario left 1 process(es) running". The child in `scripts/log-activity.sh` waited with a foreground `sleep 0.1`, so a TERM ran the handler only after the sleep returned, and the sleep outlived its parent. Harmless in production (a sleep living 100 ms), but it turned CI red on a loaded 4-core runner, where this 32-core machine never saw it. | S3 | **ACCEPTED by the founder 2026-09-17.** Status when accepted: **FIXED — landed `0752a4e`, pushed 2026-09-16, CI green** | **What was tested:** Look at CI on any push: `subagent-feed` #17 (release tier) must pass. It failed on `b26c0db` with "Scenario left 1 process(es) running" and passes since the fix. Found 2026-09-16 by Eto from the CI log. **Fix:** the sleep runs in the background and is waited on, so the signal interrupts the wait at once and the handler kills the sleep. **Regression test:** the existing `subagent-feed` #17, now titled with BUG-133; it is the test that went red in CI. **Re-open if** #17 reports a leftover process again. **That trigger fired on 2026-09-17** (CI runs 35212210548 and 35212650644). Diagnosis by Vitali: not the product. The trap killed its sleep in 300 of 300 targeted TERMs under synthetic 4-core load. #17 waited only for the child's pid, so teardown sampled the still-dying sleep. `9a8a6fd` makes #17 wait for the child's whole process group, which is what its title claims. Under load, #17 failed 14 of 100 runs before and 1 of 260 after, and the sleep was never left behind. **Known limit:** the one remaining failure was the harness's generic leftover message, on a process not yet identified. CI green on `9a8a6fd`. |
| **BUG-083** | **The pre-push gate's colour codes lost their escape byte, so every push from a terminal prints `[2m`, `[32m` and `[K` as literal text — and `tests/pipeline` #9/#17 cannot see it.** `scripts/lib/pipeline.sh:91-92` sets `_C_DIM='[2m'`, `_C_OK='[32m'`, `_C_BAD='[31m'`, `_C_WARN='[33m'`, `_C_OFF='[0m'` and `_PIPE_CLR='[K'` with the `ESC` (0x1b) byte ABSENT: `grep -c $'\033' scripts/lib/pipeline.sh` is **0**. Verified on a real pty (`script -qec`): the TTY branch emits zero ANSI bytes and renders `[2m╭─ gate [0m`, `[32m✓[0m`, `a…\r[K[2m…` as visible garbage. Two consequences. (1) Cosmetic, but on the founder's screen on every push. (2) **`#9` ("non-TTY output is free of ANSI escapes") and `#17` ("feed lines are plain text") are VACUOUS with respect to the colour path** — there is no escape anywhere to leak, so a mutant forcing `[ -t 1 ]` true survives BOTH the retiring shell suite and the port. Only a mutant that restores real escape bytes AND forces the TTY branch turns `#9` red, which is how this was found. `tests/pipeline/pipeline.spec.ts` `#9b` PINS the defect and goes RED when it is fixed, at which point `#9`/`#17` become live for the first time and `#9b` should be replaced by a pty-driven assertion. `scripts/lib/pipeline.sh` is the Pipeline agent's file under TASK-018-CONVENTIONS, so this row reports it and does not fix it. | S3 | **ACCEPTED by the founder 2026-09-17.** Status when accepted: **FIXED — landed f0660f0 (reproducer), e56c031 (fix), pushed 2026-09-16, CI green on `cd0b827` (run 35151645886)** | **What was tested:** Run `git push` from a terminal: the gate's dim and green text shows as colour, not as literal `[2m` / `[32m`. Piped output (e.g. `git push 2>&1 cat`) carries no escape codes. | Found 2026-09-11 by Vitali (QA-1) porting the gate/hooks suites for TASK-018, by mutation sweep rather than by reading. Re-open if the escapes are restored without making `#9`/`#17` pty-driven. |
| **BUG-111** | **`tests/harness/process.ts` reports a process it killed ITSELF as a leftover, so a push goes red on load rather than on a defect.** Observed 2026-09-14: the push gate failed `harness` › *reaps a background process the scenario forgot* at **1019 ms** with *"Scenario left 1 process(es) running"*; the same case passed alone at 1008 ms, 41/41, with nothing changed in the tree. Vitali saw the same case go red once on 2026-09-11 and read it as an unrelated timing flake. **Mechanism, read from the code rather than inferred:** on timeout, `run()` calls `killGroup(child)` (`process.ts:177-180`), which sends SIGKILL to the group and returns immediately; the run's promise settles on the direct child's `close`, so the call throws *Timed out* while a grandchild in that group (`sleep 30`) may still be dying. Teardown then runs `disposeAll()`, whose FIRST act is `if (!groupAlive(pgid)) continue` (`:251`) — and a killed-but-unreaped member is still signalable, so it lands in `survivors`. The grace period that would absorb this (`waitGroupGone(pgid, TERM_GRACE_MS)`, `:267`) runs only AFTER the group is already recorded as a survivor. On an idle machine reaping wins the race; under gate load it does not. **Not a test being flaky — the harness asserting on a proxy (signalable now) for the property (still running independently of us).** Fix direction: after the timeout path's SIGKILL, await the group being gone within `KILL_GRACE_MS` before the run settles, so teardown only ever sees groups nobody has already killed. Reproducer: hold the grandchild's reap (e.g. a subreaper that delays `wait`) and assert teardown does not report it. | S2 | **ACCEPTED by the founder 2026-09-17.** Status when accepted: **FIXED again — `3fd0fee` reproducer, `b001190` fix, pushed 2026-09-17**, CI green on `b001190` (run 35230016211) | **What was tested:** Watch CI over the next pushes: `subagent-feed` #4 and #17 no longer fail with "Scenario left 1 process(es) running" after their own assertions pass. Locally, from the repo root (the `-u` flags remove variables a VS Code terminal sets, which the harness refuses): `env -u GIT_ASKPASS -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA tests/node_modules/.bin/vitest run --root "$PWD/tests" harness -t "BUG-111"` passes 3 tests. **REOPENED 2026-09-17**, its re-open trigger fired twice: CI teardown reported "Scenario left 1 process(es) running" for subagent-feed #17 (runs 35212210548, 35212650644) and #4 (run 35225569171). The 2026-09-15 fix covered only the timeout path in `run()`; `disposeAll()`'s own first `groupAlive` sample was the same proxy. Measured under `taskset -c 0-3` plus 18 busy loops: every group caught alive was gone within 10-15 ms. **Fix:** `disposeAll` gives a group found alive `PROCESS_SNAPSHOT_GRACE_MS` (250 ms) to finish before recording it; one still alive after that fails as before. **Regression tests:** `tests/harness` "BUG-111 reopened", one test for each side of the grace. Previously: **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. Found 2026-09-14 by Eto (Orchestrator) on a red push that a re-run of the same commit would clear. **Re-open trigger: the next occurrence, or immediately if anyone retries a red gate on this case without citing this row.** **Promoted 2026-09-15 under TASK-028**, porting PR #68 (linkedin-watcher-agent). Reproducer: `tests/harness` BUG-111 makes the window certain rather than likely. A helper forks `sleep` into the run's group, leaves the group, and reaps its child 500 ms after it dies. The case requires the group to be gone (`ESRCH`) when the timed-out `run()` returns. **Fixed under TASK-028:** after the timeout's SIGKILL, `run()` now awaits `waitGroupGone(pid, KILL_GRACE_MS)` before throwing, which is the fix direction this row stated. **What was to be tested:** Run `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" harness` — the slow-reaper case must pass, and a push whose gate is under load must no longer go red on `harness` › reaps a background process the scenario forgot. **Status when accepted:** **FIXED — landed `e8a99b7`, pushed 2026-09-14** |
| **BUG-136** | **The pre-push SCA stage throws away osv-scanner's stderr, so a failed scan cannot say why.** CI run 35220731328 on `9ba218f`: bootstrap-gate failed on `osv-scanner could not complete (exit 127)`, and a re-run passed unchanged. `.githooks/pre-push` redirects the scanner's stderr to `/dev/null`, so the log could not tell a network failure from anything else. That breaks the observability rule that every captured error is diagnosable. | S3 | **ACCEPTED by the founder 2026-09-17.** Status when accepted: **FIXED — `4144a9b` reproducer, `dff1e35` fix, pushed 2026-09-17**, CI green on `b001190` (run 35230016211) | **What was tested:** Make osv-scanner fail in a push: put a script named `osv-scanner` first on PATH that prints `unreachable` to stderr and exits 127, then run the gate. It blocks and prints `osv-scanner's own error` followed by `unreachable`. A normal push shows no extra output from the SCA stage. **Fix:** a tool failure prints the scanner's own stderr, while a clean or findings run stays quiet. **Regression test:** named BUG-136. **Re-open if** a failed scan's gate output omits the scanner's error. |
| **BUG-135** | **A running feed daemon keeps the roster code it started with, so a fixed label never reaches the feed.** On 2026-09-17 the TASK-059 label fix passed its tests, yet the live feed still wrote `[matthias - Claude Code]`. The `--supervise` daemon had sourced `scripts/lib/roster.sh` at start, and `scripts/session-start.sh` leaves a running daemon alone. Every derived project hits this after a `blueprint pull` that changes the feed or roster code. | S2 | **ACCEPTED by the founder 2026-09-17.** Status when accepted: **FIXED — `0c567e0` reproducer, `4bb0a8a` fix, pushed 2026-09-17**, CI green on `b001190` (run 35230016211) | **What was tested:** With the feed running, change any `scripts/lib/roster.sh` byte (or pull a change to it) and run `bash scripts/agent-activity.sh --daemon`: it prints `running daemon's code has changed since it started (BUG-135) — restarting.` Run it again with nothing changed: `already running — leaving it.` **Fix:** session start restarts a running daemon whose code has changed since it started. **Regression test:** named BUG-135. **Re-open if** the feed shows labels from code older than the checkout. |
| **BUG-066** | **The pre-push gate can render `PASSED` having executed zero suites, and `tests/manifest` — the control built to prevent exactly this — agrees with it.** Three ingredients compose. (1) All 37 shell-suite invocations in `.githooks/pre-push-project` are guarded **cwd-relatively**: `if [ -f tests/<suite>/test.sh ]` … `else pipe_skip "<suite>" "suite absent"`. (2) `pipe_skip` (`scripts/lib/pipeline.sh:259`) records a skip and **does not fail** — by design, and correct downstream, where a project that has not yet pulled a new suite legitimately lacks it. (3) `tests/manifest` #4 proves invocation by grepping the hook's **text** for `bash tests/<suite>/…` (`tests/manifest/test.sh:729`), which cannot distinguish *invoked* from *named inside a guard that is false*. **Measured three times, ending end-to-end.** Christian (Senior Architect) and Alexey (Architect, Codex) each reached `rc=0` / `PASSED` / zero shell suites in separate probe trees. Matthias (Back-End-1) then ran it to completion: a `--no-hardlinks` clone, a local bare remote, `core.hooksPath=.githooks`, and a **real `git push` that was accepted** — `PASSED · 4 stages · 47 skipped · 8.1s`, against a `43 passed · 8 skipped · 180.0s` baseline in the same clone. The four survivors are gitleaks, semgrep, osv-scanner and the settings guard, all cwd-independent because they scan the whole tree or a fixed dotfile. **The blast radius is wider than the 37 shell suites**: the DoD checklist (`scripts/lib/dod-gate.sh`) and the **entire vitest batch — 8 TS suites** — sit behind the same guard shape and skip with it. **And the false green is not the first thing that happens.** The naive move fails LOUDLY twice first: `.githooks/commit-msg` refuses the commit (cannot read `scripts/lib/commit-subject.sh`) and `.githooks/pre-push:61` refuses the push (`scripts/lib/pipeline.sh is missing — the gate renderer is absent`). Both fail closed, correctly. The silent pass appears only after those two lines are repointed — which is a two-line `sed` and the first thing any restructurer does. **That is the trap: the loud failures teach you the move is fixed, and then the gate agrees with you.** **The restructure is the trigger, not the defect, and the fix does not wait for it.** The gate has two failure modes split by which file is missing, and the split is arbitrary: a missing `scripts/lib/pipeline.sh` exits 1 loudly (FEATURE-002 fail-closed), a missing `tests/<suite>/test.sh` skips silently and passes. The `if [ -f ]` guard exists so a derived project that has not pulled a suite does not block — correct downstream. **In the blueprint it is unconditionally wrong**: every one of those suites is always present, so the guard can only ever fire on a defect. Any cause fires it — a moved directory, a deleted suite, a mistyped `git mv`. Any cause of absence does this — a moved directory, a deleted suite, a bad `git mv` — and the gate reports success. **This is the repo's signature failure inside the control written to close it**: CLAUDE.md §"Pre-push tolerance" says *"a rule only a careful reviewer can check is not a control"*, and #4 is a text grep, which is a careful reviewer. Same class as BUG-004 (a hook that never ran), A-22 (a gate never armed), BUG-035 (a scanner examining a language the repo does not contain). **Fix direction, not settled:** derive the suite set from the filesystem and make a declared-but-absent suite **fail** in the blueprint while still skipping downstream, and give #4 a **runtime** assertion — a `pipe_stage` count compared against the declared count — instead of a grep over the hook's text. Alexey's review adds a constraint Christian's proposal does not meet: failing loudly tells you the suites are gone, it does not give the hook a code-root from which to run them. Both halves are needed. **Two corrections to the mechanism as first stated, both from Matthias's run.** (a) `tests/manifest` is not vacuity-blind: check #7 asserts the derived suite count is non-zero, and it is a real guard. It simply is not hit here — #7 derives from `$ROOT/tests`, and `$ROOT` follows the physical-script walk into `scaffolding/`, where all 43 suites still are. (b) Manifest *does* exit 1 in a moved tree, but for the wrong reason and with a useless diagnostic: `$ROOT` points `GATE`/`HOOK` at `scaffolding/.githooks/pre-push*`, which does not exist, so `GATE_CMDS` is empty and **every** suite reads as `shell runner never invoked` — including the ones the hook genuinely still invokes. It fails closed on an unreadable hook instead of naming the path mismatch. Moot in practice: its own invocation is one of the 37 guarded lines, so the gate never calls it. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#66 commits a164065, 0c346ce, 4f2f20c, ffbbb40 | Found 2026-09-10 by Christian (Senior Architect) while sweeping Codex finding 2, confirmed by execution by Alexey (Architect) in an independent probe tree. **Re-open trigger: TASK-021 Stage A** — this must land BEFORE `scaffolding/` exists, because after the move the gate that would prove the move correct is the thing that stops running. Also re-open immediately if any gate run shows a non-zero `skipped` count for a suite that is present in `git ls-files`. **FIXED 2026-09-11 by Christian (Senior Architect).** Promoted from `backlog/` on its own re-open trigger — TASK-021 Stage B is next. |
| **BUG-053** | **A `blueprint`-tier row was treated as an unmet obligation in derived projects, where the suite cannot exist — and underneath it, an assertion that had been passing vacuously for years.** The suite directories for `drift-in-blueprint`, `pull-exec-bit` and `harness` are blueprint-tier and correctly do not ship (`git archive HEAD \| tar -t \| grep -c` → **0**), but `tests/SUITES.md` **does** ship (→ **1**). So a derived project receives rows describing suites whose files are absent, and `tests/manifest` `#4`/`#4b`/`#5` demanded invocation for them. Caught by `tests/bootstrap-gate` #6 — the only suite that speaks for downstream projects — after `TASK-018` retired two shell runners. **The older defect is the more interesting half:** before retirement, `#4`/`#5` downstream fell through to asking whether the gate text contained `bash tests/drift-in-blueprint/test.sh`, which it did — inside an `if [ -f … ]` that always skips there. **The assertion was about a string, describing a file the project does not have, guarding a stage that never runs.** It passed vacuously for years, and retiring the runners is the only reason anyone found out. **Fix:** a `blueprint`-tier row is *not applicable* outside a blueprint, keyed on **`.blueprint-root`** — the same positive marker `drift` uses — and **never on the directory being absent**, because absence-keying would let a project silently drop a suite it should run by deleting a directory, which is BUG-005 with an extra step. Two guards on top: if every blocking row is skipped the assertions **fail** as vacuous rather than passing over nothing, and re-tiering a shipping suite to `blueprint` to buy the skip is refused by `#2b`. **Same defect, second instance:** `tests/git-isolation` names four anchors it proves *by execution* drive git, one of which (`bootstrap-identity`) is blueprint-only — fixed the same way, with a floor of three so the anchor set cannot collapse. Proven on a real derived layout (`git archive HEAD` extracted, manifest run there): 7 rows skipped, **36 still enforced**, rc=0. **Not fixed by stripping the rows on pull** — that would make the shipped file differ from its source and turn `blueprint pull` from a copy into a transformation. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#53 blueprint-tier rows not applicable downstream, squashed in fe08cb5 | Found 2026-09-09 by `bootstrap-gate` #6 while landing TASK-018. Fixed by Vitali (QA-1) and Sylvia the same session. Re-open if any control keys applicability on a file being missing rather than on `.blueprint-root`. |
| **BUG-046** | **BUG-030's root cause found and REPRODUCED — it is the whole bootstrap family, and the mechanism is env inheritance, not path derivation.** `scripts/new-project.sh:253-257` seeds a baton on every bootstrap via `signal-set.sh` **with no `--file`**. `signal-set.sh:56` → `state-dir.sh:87` honours `$AGENT_SIGNAL_FILE`, and `:56` honours `$AGENT_STATE_HOME`. `scripts/codex-signal-watch.sh:250` **exports `AGENT_SIGNAL_FILE` into every dispatched wake command**. Four suites drive `new-project.sh` and **none of them unsets those vars**: `template-source:26`, `bootstrap-contents:38`, `bootstrap-gate:47`, and `bootstrap-identity` (which unsets nothing at all). **Reproduced by execution:** running `template-source` and `bootstrap-contents` with a decoy baton and `AGENT_SIGNAL_FILE` set overwrote it with `Holder=Nobody / State=IDLE / Task="Bootstrapped from the blueprint. Claim the mic to begin."`, appended a journal row, **and exited 0** — byte-for-byte the damage BUG-030 records. **BUG-030 named `bootstrap-gate` as the suspect and guessed the cause was state-dir resolution; both are wrong.** Any of four suites does it, and only when run from a context that exports the baton env — which is exactly what a dispatched Codex wake does, and why it struck mid-review. **`.githooks/pre-push:46` unsets only `GIT_*`; there is no gate-level scrub for this class at all.** **Fix:** `--file` on the seed at `new-project.sh:253`, plus one `unset AGENT_SIGNAL_FILE AGENT_STATE_HOME AGENT_FEED_LOG` at the `pipe_stage` boundary, then delete the per-suite unsets that duplicate it (`dod-gate:21` is the pattern). Net deletion, retires a class with four prior instances. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#46 stop bootstrap writing live baton, squashed in fe08cb5 | Found 2026-09-09 by Vitali (QA-1) auditing isolation for TASK-018. Supersedes BUG-030's suspect list — amend that row when this lands. |
| **BUG-047** | **Two suites mutate the REAL repository through inherited `GIT_DIR`, and the guard that exists to catch exactly this cannot see them because its membership set is decided by COMMENTS.** Reproduced against a victim repo the way `git-isolation:80` does: `tests/commit-subjects` (rc=0) **rewrote the victim's config — adding `core.hooksPath=.githooks` and overwriting `user.email`/`user.name`.** Setting `core.hooksPath` on a real repo *is* the A-22/BUG-004 failure, produced by a test. `tests/bootstrap-identity` (rc=1) **left a commit in the victim repo** (`chore(bootstrap): initialize test-noident …`). Baseline: with `GIT_DIR` set, `git -C "$fixture" init` returns 0, creates **no** `.git` in the fixture, and the later commit lands in `GIT_DIR`. Both are protected today **only** by `.githooks/pre-push:46`, so either suite run directly — as an agent debugging one naturally does — hits the real repo. **The guard's own defect is the more interesting half:** `git-isolation:118` discovers fixture-builders with `grep -q 'git init'`, but `commit-subjects:195` is `git -C "$T6" init -q`, which contains no such substring. Meanwhile suites it *does* find match on **comment prose** — `branch-guard:22` says "this suite runs `git init`" while its code at `:52` is `git -C "$1" init -q -b main`. **The control's population is determined by what comments say, not by what code does**, and the non-vacuity check at `:121` passes at 20 members and masks it. Same shape as BUG-035 (a codebase annotated for a linter that never ran). | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#47 scrub GIT_DIR and execution-proven guard, squashed in fe08cb5 | Found 2026-09-09 by Vitali (QA-1), reproduced by execution. Fix the two suites AND the discovery predicate — fixing only the suites leaves the guard still blind. |
| **BUG-048** | **`tests/a2bp-contamination`'s headline assertion can never fail the suite.** `fail` is called inside `run_a2bp_in` (`:201`), which every caller invokes as `rc=$(run_a2bp …)` — a command substitution, i.e. a subshell. `FAILED=1` is set in the child and lost on return. The assertion so disarmed is **"a2bp MOVED THE BLUEPRINT'S MAIN BRANCH — a request must never land"**, which is the single most important thing this suite exists to check: A-07 and the whole request-not-delivery model rest on it. The suite would print its normal PASS while a2bp had pushed to `main`. Two sibling defects in the same family: `a2bp-build:258-259` (the `GIT_INDEX_FILE=` prefix is stripped by `request.sh:141`, so #8 passes for the wrong reason) and `a2bp-e2e:232` (a degenerate grep pattern makes #5 half-vacuous). **This is the repo's signature failure inside the guard on the door BUG-002 and A-09 came through.** | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#48 headline assertion can fail, fe08cb5; siblings fixed 47c0d30 | Found 2026-09-09 by Vitali (QA-1) while auditing isolation. Fix by returning a status and failing in the caller's shell, then add a mutant proving the assertion can fail. |
| **BUG-049** | **`tests/marker-merge` leaks a full blueprint archive on every run — 23 of them, 133 MB, measured.** `marker-merge:89` is `BP_CLONE="$(mktemp -d)/bp-clone"`; the parent `mktemp -d` is never captured, and the suite's trap covers only `$WORK`. Two leak per push rather than one, because `git-isolation:80` executes `marker-merge` a second time. Smaller instances of the same omission: `a2bp-contamination:68` (`$GH_SHIM`) and `state-dir:281` — and `tests/state-dir` has **no `trap` at all**. Relevant to TASK-018 beyond tidiness: an isolation contract that does not assert "a suite leaves nothing behind" would not have caught 133 MB of debris accumulating in `$TMPDIR`, and `a2bp-e2e:309` already scans that same directory for leaked dirs, so the debris is a live cross-suite hazard rather than only waste. | S3 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#49 own the temp roots, squashed in fe08cb5 | Found 2026-09-09 by Vitali (QA-1), measured. Fold into the isolation contract's teardown assertion (TASK-018 §4.3). |
| **BUG-043** | **`signal-set.sh` trimmed the baton's Task differently depending on the publisher's `LANG`, breaking its own documented contract.** The trim is `sed 's/^[[:space:]]*//; s/[[:space:]]*$//'`, and `[[:space:]]` is locale-dependent by definition. In a UTF-8 locale on BSD/macOS it also matches **U+00A0 NBSP**, so the trim removed characters the file's own header explicitly promises to leave alone: *"NOT SUPPORTED → Unicode whitespace (U+00A0 NBSP and friends) is NOT recognised, so it survives at the boundary."* The comment even said "in the current locale", which is the defect written down as if it were a specification. Caught by `tests/signal-set` #3h (*"the code now exceeds its documented `[[:space:]]` contract"*), pre-existing at `c4dc95b`, confirmed in a clean worktree. **Why it matters beyond the character:** this is the sole writer of the baton every agent on the team parses, so identical input published from a Mac and from Linux produced different bytes — a coordination primitive whose output depends on who published it. **Fix:** pin `LC_ALL=C` on that one `sed`, making `[[:space:]]` ASCII-only and identical on every platform. Same idiom the feed already uses for byte-exact length (`LC_ALL=C awk`). | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#43 in fe08cb5; signal-set.sh:169 uses LC_ALL=C sed | Found 2026-09-09 by the gate while landing TASK-017 on a Mac. Fixed the same session. Re-open if any shipped script matches a POSIX class without pinning the locale. |
| **BUG-042** | **`blueprint a2bp` does not work on a stock Mac, and its suite reports that as 19 assertion failures blaming the contamination guard rather than as the missing tool it is.** a2bp stages a request with `diff --unchanged-line-format/--old-line-format/--new-line-format`, which is GNU diffutils. macOS ships Apple's FreeBSD diff, which has none of those flags, so staging fails before any scanning happens and every file is refused with exit 4 and **no finding reported**. `tests/a2bp-contamination` then fails 19 cases with text like *"a2bp exited 4 on a legitimately generic file"* and *"the output does not quote the offending line"* — an accusation against the guard, for a tool that was never installed. **a2bp itself behaves correctly**: it fails closed and prints `'diff' must support --unchanged-line-format ... Install GNU diffutils and re-run.` The suite simply never surfaces that. **Fixed** by `scripts/install-toolchain.sh` (TASK-017) installing `diffutils` on macOS, checked by **capability rather than presence** — `have diff` is true on every Mac and tells you nothing, the same trap as `have coreutils` versus an actual `timeout` provider. **Still worth doing, not done here:** the suite should assert its precondition and `pipe_skip` with a stated reason (or block naming the tool), because 19 misdirected failures is strictly worse than one accurate one — this repo's whole objection to controls that misreport. **Also note the product verb was simply unavailable on macOS**, so back-propagation — the mechanism every derived project uses to send improvements upstream — has never worked from a Mac. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: TASK#17 diffutils capability install in fe08cb5; install-toolchain.sh:638,673 | Found 2026-09-09 by the gate while landing TASK-017. Tool gap fixed the same session. Re-open for the suite-precondition half. |
| **BUG-041** | **Bare `sed -i` in test fixtures is GNU-only, so on macOS the fixture edit silently never happens and the suite fails describing a product defect that does not exist.** BSD `sed` requires an argument to `-i` (the backup suffix), so `sed -i 's/x/y/' FILE` consumes the SCRIPT as the suffix and then treats the FILE PATH as the script, failing with `sed: 1: "/var/folders/...": invalid command code f` — where the "command code" is merely the first letter of the path. Four call sites: `tests/session-resume/test.sh:143,340,347` and `tests/roster/test.sh:267`. **The misdirection is the expensive part:** session-resume #11 reported *"a known PAST id was reported as an unbacked claim"*, an accusation against `scripts/session-resume.sh`, when the truth was that the fixture's marker had never been rewritten. A test that cannot edit its own fixture does not report "I could not set up" — it reports a failure of the thing it was pointed at. **The shipped scripts were already correct** (`scripts/blueprint:1088` and `scripts/new-project.sh:254` both use `sed -i.bak`), which is the recurring shape of this whole macOS sequence: the product is portable and the tests that gate it are not. **Fix:** `tests/helpers/sed-inplace.sh` providing `bp_sed_i`, using a temp file rather than `-i.bak` because a stray `FILE.bak` is its own hazard in a repo whose suites glob and count files, and writing back with `cat` rather than `mv` so the file keeps its inode (`agent-activity-bound` #8 watches inodes to detect rotation). | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#41 portable in-place sed helper in fe08cb5; shell runners since retired | Found 2026-09-09 by the gate while landing TASK-017 on a Mac. Fixed the same session. Re-open if a suite reintroduces bare `sed -i`. |
| **BUG-038** | **The feed suites synchronise on "a supervisor is resident", but the property they need is "the supervisor has registered this run log" — and on a slow host those are not the same instant.** `wait_sup` returns as soon as a process matching `--supervise` exists. The feed deliberately does not replay pre-existing content (case #16), so a payload appended between `wait_sup` returning and registration is folded into the baseline and never emitted. The suite then reports "the reader lost the record", which is **indistinguishable from the regression the case exists to catch**. Isolated by single-variable experiment: inserting one `sleep 2` after `wait_sup 1` — the only change — turned #7 from fail to pass. **Fixed** by a `reader_ready` handshake that writes a unique sentinel to the very file the case will use and waits for it to come out the other end, proving liveness AND that this file is registered. Applied to #7/#10/#18/#5f, and to `tests/subagent-feed` #1/#2 which had the same shape. **Both suites now pass on macOS — `agent-activity-bound` for the first time.** Two refinements the first attempt got wrong: the sentinel must be RE-WRITTEN rather than written once, because `seed_offset` skips anything behind the offset PERMANENTLY, so a longer timeout cannot help (proved by raising the bound to 45s against a 32.6s latency, which changed no outcome and cost 2m41s); and #5f's `sleep 1.5` had to become a poll, because by that point case #2 has created 80 subagent transcripts and the effective tick is ~10s rather than the configured 0.25s. A first version of the helper returned a status no caller checked, so a failed handshake silently degraded to the unsynchronised behaviour it exists to prevent — a guard whose failure mode is invisible is not a guard, and every call site now fails loudly. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#38 reader_ready handshake commits squashed in fe08cb5 | Found 2026-09-09 while fixing BUG-036. Fixed for #10/#18. Re-open if any new case appends to a run log without a handshake first. |
| **BUG-037** | **`agent-activity.sh --daemon` reports "failed to start" and exits 1 while the supervisor it just launched is alive and healthy — and a caller that believes it spawns a second one.** `cmd_daemon` launched the supervisor, slept a fixed `0.4`s, checked `feed_is_running` **once**, and gave up. Measured on macOS the supervisor acquires its `flock` after **~0.6s**, so the check ran before the thing it was checking for. On a faster host startup fits inside 0.4s, which is why a fixed sleep looked correct for months. **This is not a cosmetic exit code.** `cmd_daemon`'s own idempotency guard is `feed_is_running`, so a caller that trusts the failure and retries gets a SECOND supervisor — the unbounded-spawn shape BUG-001 rode to load 175 for 2.7 days, reachable here by an honest retry rather than by a broken guard. It also made `tests/agent-activity-bound` report "could not start a supervisor" for cases #10/#13/#14/#18. **Observed live in this session's very first command:** the wake-time `--daemon` printed `failed to start` while `--status` immediately after reported it running. **Fix:** poll for the lock on the same 5s bound and 0.1s tick `cmd_stop` already uses, rather than guessing a single interval — a timing assumption removed beats a magic number enlarged. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#37 daemon lock polling in fe08cb5; agent-activity.sh:316-321 polls | Found 2026-09-09 while fixing BUG-036. Fixed in the same session. Re-open if any launcher regains a fixed-sleep verification. |
| **BUG-036** | **The pre-push gate cannot pass on macOS: three test helpers identify a process's cwd through `/proc`, which does not exist there, so the supervisor count is always 0 and `agent-activity-bound` fails closed.** `tests/agent-activity-bound/test.sh:159` and `tests/subagent-feed/test.sh:49,125` run `readlink -f /proc/$p/cwd`; on macOS that is always empty, the `case` never matches, and cases #1/#6/#7/#13/#14/#15 fail with "0 supervisors" while the feed is in fact running. Verified pre-existing at `c4dc95b` (tip of `main`) in a clean worktree, with the live feed stopped — **`main` is red on any Mac, and no push can be made from one.** The product itself is portable: `scripts/agent-activity.sh:226` branches on `/proc/<pid>/stat` vs BSD `ps -o lstart=`. Only the tests assume Linux, so this is the Brewfile's defect (TASK-017) recurring one layer down — the thing that decides whether work can ship is Linux-only while the thing it tests is not. Second defect in the same file: `test.sh:420` interpolates `$n1→$n2`, and under the `LC_ALL=C` the suite sets, bash absorbs the multibyte arrow into the identifier (`n1\xe2: unbound variable`) — needs `${n1}`. Third: the suite reports "no UTF-8 locale available" and SKIPs #19 on a host where `locale` is `en_US.UTF-8` and `locale -a` lists it, so its own detection is wrong. **Not the fix:** skipping the suite on Darwin — that is a suite silently covering nothing (BUG-005). Port the cwd lookup to a shared helper that uses procfs where present and `lsof -a -p <pid> -d cwd -Fn` on BSD. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#36 portable cwd lookup squashed in fe08cb5 (#63) on origin/main | Found 2026-09-09 while landing TASK-017, by the gate refusing that push. Blocks every push from macOS. |
| **BUG-050** | **`tests/pipeline` #19's real-feed escape canary is wedged RED in this checkout, and the mechanism means any agent that merely *writes about* the suite disarms it permanently.** The case asserts two things about `logs/agent-activity.log` after running a fixture stage: the `[GATE]` line count is unchanged, and the fixture's canary label does not appear. The label is a **fixed literal**, and `logs/agent-activity.log` is the feed **every agent's prose lands in** — so on 2026-09-09 at 17:38:34 a `general-purpose` agent quoting this very case in its TASK-018 isolation audit wrote the literal into the live feed, and case #19 has failed on every run since. Verified by execution: the count half passes (299 → 299), the fixture appends nothing (1 occurrence before, 1 after, and the fixture writes to a pinned `AGENT_FEED_LOG`), and the sole match is that agent's sentence. **The failure text accuses the suite of polluting the production log, which is exactly backwards** — the same misdirection class as BUG-041/BUG-042, and it blocks every push from this checkout until someone rotates an untracked log for reasons the message does not explain (`logs/` is gitignored, so a fresh clone is clean and this reads as "works on my machine"). The repo already carries this false-positive class knowingly — CLAUDE.md notes `Bash(* --no-verify*)` blocks a task string that merely *discusses* `--no-verify`. **Fix:** make the token unique per run (`canary-$$-$(date +%s)`) so quoting the suite cannot match a future fixture, and scope the search to lines the fixture's own run could have produced rather than the whole file. Vitali's S2.1 finding (PLAN-TASK-018 §4.1) reached the opposite conclusion — *"the token check is unique per run and survives anything — keep it"* — which is true of the **fixture instance** but not of the **literal**, and that gap is the defect. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#50 unique per-run canary token, squashed in fe08cb5 | Found 2026-09-09 by Philipp (Infrastructure-1) while regression-testing `scripts/lib/pipeline.sh` for TASK-018. Pre-existing and unrelated to that change, proven by execution. Fold into TASK-018 §4.3's isolation contract — a canary keyed on a literal shared with the audit trail is not isolated. |
| **BUG-051** | **`tests/manifest` was anchored on `*.sh` in every assertion, so the TypeScript migration it is supposed to govern was invisible to it: migrate a suite, delete its `.sh`, delete its `tests/SUITES.md` row, and NOTHING fails.** No unclassified-suite error, because no shell file remained for `#1` to discover (`find "$ROOT/tests" -name '*.sh'`); no missing-invocation error, because no row remained for `#4`/`#5` to check. That is BUG-005 itself — a silent coverage cut behind a green gate — re-entering through the one door the control could not see (PLAN-TASK-018 §7.1). **Reproduced by execution:** with `tests/zz-fake-probe/zz-fake-probe.spec.ts` on disk and no row, the pre-change file at `c4dc95b` printed `ok — #1 every suite directory containing shell files is classified`. **A second, older weakness surfaced while fixing it:** `#2` tested `[ -f "$ROOT/tests/$s/test.sh" ]` — the literal filename `#1` had already stopped trusting, one assertion further down the same file — so a suite whose only runner is named anything else (`tests/staleness/drift-integration.sh` is one, and it passes only because a `test.sh` happens to sit beside it) was reported a GHOST while being perfectly real. **Fixed** by making a runner mean `*.sh` OR `*.spec.ts` in `#1`, `#2`, `#2b`, `#4` and `#5`. **`#2b` then needed a second correction, because "shipped" and "runnable" stopped being the same word:** with TASK-018 phase 1 holding the TS toolchain out of derived projects, `tests/proc-cwd` legitimately ships its `test.sh` and withholds its `.spec.ts`, which the old rule reported as `proc-cwd(1/2 runners)` — a correct boundary called broken, which is how a control gets switched off. The invariant that holds in both phases is *ships at least one runner the recipient can execute, and none it cannot*, with `TS_SHIPS` derived from the archive rather than declared so phase 2 re-reads it with nothing to remember. **New `#2c` makes the phase transition all-or-nothing**: the two propagation paths disagree by construction — bootstrap ships the whole archive, `pull` ships `MANAGED_FILES` and `tests/` is managed while the root files are not — so a half-done move gives an *existing* project specs with no runner while a project bootstrapped the same day is fine. `#2c` requires the toolchain to ship and be managed together, or neither, and asserts its own `MANAGED_FILES` parse is non-vacuous first (a stale parse would have passed phase 1 silently). **`#4` then had to follow a sourced bridge**, because the gate does not run `vitest` itself — it sources `scripts/run-ts-suites.sh` and calls `ts_suites_stage`. The hop is discovered, not named: a sourced file counts only when it exists AND the hook calls a function it defines, so sourcing a file that merely *declares* one proves nothing, and an absent bridge (`pipe_skip`ped at runtime) fails a blocking suite rather than passing it. **That generic bridge discovery immediately found a live propagation gap** — `scripts/run-ts-suites.sh` ships but is not in `MANAGED_FILES`, so a bootstrapped project freezes it forever while an existing project pulling the managed hook never receives it at all and `pipe_skip`s that stage on every push, permanently, with a remedy message (`blueprint pull scripts/run-ts-suites.sh`) that cannot work on an unmanaged file. `#2c` now checks every file the managed hooks source, both directions. **Finally, `#4b`** — the founder's policy that a migrated suite keeps its `.sh` but stops being run needed the decision to be DECLARED, because "we deliberately stopped running it" and "it fell out of the gate" are otherwise the same observation. A `RETIRED-SHELL-RUNNERS` table carries the mutation recipe PLAN-TASK-018 §5 requires, checked for content (names a concrete existing path, names the case it turned red, and may not argue from the clock — the saving is a consequence of retirement, never its justification), and `#4` accepts a retirement only when the suite has a spec that the chain proves is running. A spec's invocation is proven as a chain — the spec exists, the gate runs `vitest` with no path filter, and `vitest.config.ts`'s include actually covers it — so that breaking any single link fails the push, and "a spec exists somewhere" is never accepted. **`#9` asserts by execution that this control still needs no Node**, re-running the whole file with `node`/`npm`/`npx`/`tsc`/`vitest` replaced by recording shims that exit 127: a project part-way through the migration must not get a wrong answer out of its own coverage control. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: manifest runner = *.sh OR *.spec.ts landed in fe08cb5; later TASK#20 bcd85d8 | Found 2026-09-09 by Philipp (Infrastructure-1) reading the manifest for TASK-018 §7.1; closed by Vitali (QA-1) on `task-018-ts-test-harness`, with 15 negative controls in `.scratch/rig.py` proving each new assertion fails when it should. **Not green yet:** `#4`/`#5` now correctly report that `tests/harness` and `tests/proc-cwd` ship specs the gate and the workflow invoke nowhere — the vitest stage in `.githooks/pre-push-project` and `.github/workflows/security.yml` is the missing piece, and it is owned by the pipeline agent. |
| **BUG-052** | **The marker-aware merge has NEVER run on either of the two managed files that need it, because both describe their own markers in prose — so `blueprint pull` has been falling back to a whole-file copy and destroying every derived project's own content on every pull.** `marker_aware_merge` (`scripts/blueprint:311-323`) counts `BLUEPRINT:BEGIN` and `BLUEPRINT:END` as **substrings** and returns 1 unless the counts match; `pull_file` then warns, writes a `.bp-bak`, and copies the whole file over. Measured: `tests/SUITES.md` **1 BEGIN / 4 END**, `.githooks/pre-push-project` **1 BEGIN / 3 END** — in both cases because a comment or a sentence *explains* where the end marker is (`# BLUEPRINT:BEGIN — blueprint-managed. Put YOUR guards after BLUEPRINT:END.`, and `everything after BLUEPRINT:END is yours and is preserved`). The two things CLAUDE.md promises are "preserved byte-for-byte" — a project's own suite rows and its own gate guards after the managed region — are exactly the two things being replaced, and the only symptom is a yellow warning line inside a command that prints many. **`tests/marker-merge` cannot catch it:** it drives the MECHANISM against fixture files that satisfy the precondition and never asks whether the real managed files do — a control that tests the machine and not the instance, the same gap as `git-isolation` choosing its population from comments (BUG-047). **Fixed for `tests/SUITES.md`** (prose now says "the managed region"); `.githooks/pre-push-project:2,5` still carries it and is a two-line reword. **Guarded** by `tests/manifest` #7b, which asserts BEGIN/END balance for every marker vocabulary in `tests/SUITES.md`, `.githooks/pre-push` and `.githooks/pre-push-project` — so the next marker anyone adds cannot be poisoned by the sentence that documents it. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: SUITES.md reword in fe08cb5; line-anchored markers 1dcdf4c (BUG#112); pre-push-project:2 reworded | Found 2026-09-09 by Vitali (QA-1) while adding a marker region to `tests/SUITES.md` for TASK-018 — reading the merge's precondition to check the new marker was safe is what revealed the old ones never had been. Re-open if any managed file's markers stop balancing. |
| **BUG-078** | **`tests/staleness` was green over three separate real defects in the probe it guards — including the one WRITE path into another operator's repository, whose fast-forward-only constraint has never been enforced.** Found by running both implementations of the suite over ten perturbed trees during the TASK-018 port and diffing verdicts mechanically; each is a case passing for a reason other than its subject, which is the TASK-018-RULES R6 class. **(1) `#9` passes with `--ff-only` DELETED from `bp_staleness_fast_forward`.** The fixture rewrote the same `f.txt` on both sides, so a non-fast-forward merge CONFLICTS and git refuses it anyway — the case's "the merge was refused" was satisfied by the conflict, not by the constraint. The real hazard is the opposite shape: a cleanly-mergeable divergence, where dropping `--ff-only` silently creates a merge commit in a repository the operator is not in. That is the bound `scripts/lib/staleness.sh`'s own header calls "the contract, not implementation taste". **(2) `#8` cannot see a defect in the lib's own top-level assignments.** The suite sources the lib once and calls `BP_STALENESS_TIMEOUT=2 bp_staleness_assess …`; an assignment prefix on a FUNCTION CALL wins, because `BP_STALENESS_TIMEOUT="${BP_STALENESS_TIMEOUT:-8}"` already ran at source time. A real caller is a separate process that sources the lib AFTER the environment is set, so there the lib wins — so hardcoding the budget to 30s leaves `#8` green while a fresh-process caller measures 30.2s against the 2s it asked for. The suite was testing an invocation shape that cannot occur in production. **(3) `drift-integration` `#3` never consulted `BP_NO_PROMPT` at all.** It ran with stdin closed, so the `[ ! -t 0 ]` arm suppressed the prompt and the flag's own arm was dead code: deleting `[ "${BP_NO_PROMPT:-0}" = "1" ] \|\|` leaves the case green. Exactly BUG-054's lesson one suite over — a two-armed guard needs the other arm held false, or the case cannot say which fired. **All three are closed by `tests/staleness/staleness.spec.ts`**: `#9` advances with uniquely-named files so the divergence merges cleanly, `lib()` spawns a fresh shell per call, and `D#3` supplies a pty and asserts an unsuppressed control prompts FIRST. Verified by execution — mutants S6, S7 and S10 are PASS/FAIL splits with the shell suite green and the spec red. **One product finding came with them, not a defect:** the `&& [ "$blocker" = "none" ]` conjunct at `scripts/lib/staleness.sh:147` is DEAD — at that line `blocker` is `none` or `not-behind`, and `not-behind` implies `status != behind` — so no mutation of it is observable. Delete it rather than guard it. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: fixed in tests/staleness/staleness.spec.ts, c668d6d; shell runner retired dc845f1 | Found 2026-09-11 by Elias (Infrastructure-2) porting the sync suites for TASK-018, by mechanical verdict-diffing rather than by reading. Fixed in the spec in the same pass; the shell runners still carry all three and stay in the gate until the central retirement pass. Re-open if any case asserts a refusal whose fixture could be refused for a second reason. |
| **BUG-079** | **Codex R8's fix never worked: `scripts/session-resume.sh --mark` rolls the replay window in TWO `write()` calls, not one, so a concurrent mic flip can still land in a gap that belongs to no replay window — and `tests/session-resume` #13, the case written to catch exactly this, was GREEN against it.** The fix reads `printf '%s\|n' "$roll"` where `$roll` holds two lines, with a comment asserting "a single small `printf` to a file opened O_APPEND is one write(), and no other appender can interleave inside it". The premise is false: **bash's `printf` builtin flushes at every newline**, so strace shows `write(1, "[ts] </aaa>\|n", 12)` followed by `write(1, "[ts] <bbb> head=x\|n", 18)`. `echo`, `printf '%s\|n%s\|n'` and even coreutils `/usr/bin/printf` split identically; only a heredoc fed to `cat` is one write, because bash writes the body to a temp file before `cat` runs (`printf … \| cat` gets it right only by luck — `cat` may read the first line before the second arrives). Measured with a competing appender at ~170 kHz over 60 `--mark` calls: **36 events landed between a close marker and the next open in the UNMODIFIED script, 0 after the fix** (`.scratch/probe-r8.sh`). **Why nothing noticed for a month is the more useful half.** #13 drove the race with a full `bash signal-set.sh` per flip — about 33 Hz against a gap of microseconds, so the chance of landing inside it over 30 marks was under one percent. The case asserted a race it could not provoke, which is BUG-005's shape, and the review that accepted the fix read the same false premise the code states. **Fixed in `scripts/session-resume.sh`** (heredoc to `cat`, with the measurement in the comment so the claim is checkable) **and in `tests/session-resume/session-resume.spec.ts` #13**, whose appender is now the one `printf >>` that `signal-set.sh` actually contributes, issued directly at ~170 kHz over 60 marks — provably able to fail, verified against the reverting mutant. The shell runner's #13 is left as-is and is therefore the one check where the TypeScript port is deliberately STRICTER; it retires with the rest. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: BUG#79 fix d2ff400 on origin/main | Found 2026-09-11 by Matthias (Back-End-1) porting the signal/mic suites for TASK-018. Surfaced by the equivalence method rather than by reading: the mutant that reverts the fix left BOTH implementations green, which is the "neither covers this" verdict, and chasing it found the product defect underneath. Re-open if any journal writer ever emits a multi-line record through a shell builtin again — the general rule is that only ONE `write()` is indivisible, and no shell builtin promises one per statement. |
| **BUG-084** | **`tests/baton-durability` #5 (Codex F4) samples for 6 MILLISECONDS against a publication that takes 32, so all 400 of its samples see NO FILE AT ALL and it has been asserting "the seed was never visible" over an empty directory.** The case exists to prove `signal-set.sh`'s first creation is atomic — that a poller can never read the default `Holder=Nobody / State=IDLE` seed at the canonical path. Its sampler is `for _ in $(seq 1 400)`, and every iteration short-circuits on `[ -f "$abaton" ]` while the file does not exist yet, so the loop is **bounded by an iteration count rather than by the subject** and runs to completion before `signal-set.sh` has finished sourcing `state-dir.sh`. Measured (`.scratch/probe-f4-sampler.sh`, with the defect injected — the seed written straight to `$SIGNAL`): `sampler: ran 0.006s, 0 of 400 iterations saw a file at all` / `publisher: took 0.032s` / `MISSED the seed`. **The case would pass with the seed sitting at the canonical path for a full second**, which is the entire property it claims to guard. Same family as BUG-074 and Elias's BUG-078: a green that cannot go red, believed because it is green. **Closed by `tests/baton-durability/baton-durability.spec.ts` #5**, whose sampler loops `while (!publisher.exited)` — bounded by the SUBJECT, so it cannot finish early and cannot be vacuous; it also keeps the final assertion that the requested baton did arrive, so an empty sample set can no longer read as a pass. Verified by execution: mutant `b3-seed-written-to-the-canonical-path` is a PASS/FAIL split, shell green and spec red. The shell runner keeps its 6 ms sampler and retires with the rest. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: baton-durability.spec.ts:415 samples while !publisher.exited, added c668d6d | Found 2026-09-11 by Matthias (Back-End-1) porting the signal/mic suites for TASK-018. It is a PORT-ONLY-RED row in the equivalence table rather than a disagreement to fix, which is the verdict this method exists to distinguish. Re-open if any sampling or racing fixture bounds itself by a count, a duration or a retry limit instead of by the subject's own completion. |
| **BUG-080** | **Three assertions in the gate/hooks suites could not fail on the defect they name, all for one reason: the evidence they look for is satisfiable without the property.** Found by running both implementations over 58 perturbed trees and diffing verdicts; each is the TASK-018-RULES R6 class. **(1) `tests/pipeline` `#3` ("a failed stage halts the pipeline")** greps stdout for a line echoed by a following stage — but with the halt removed that stage RUNS AND PASSES, so `pipe_stage`'s buffer swallows exactly the evidence, and the case stays green. Ported to assert a filesystem side effect, which the buffer cannot hide. **(2) `tests/ts-bridge` `#1b`/`#2c`** grep the gate output for the suite name `demo` — which `pipe_batch_end`'s refusal message `declared but never reported: demo` CONTAINS, so deleting `pipe_stage_report` from the bridge leaves both green while saying, in the matched text, that no suite was reported. Ported to require a rendered result marker (`✓ demo`). **(3) `tests/pipeline` `#19`** compares `[GATE]` counts in the real activity feed across a fixture run, over a path nothing binds to that feed: pointed at a non-existent path the counts are `0 -> 0` and the guard reports clean. Its `[ -f ]` disarm switch was deleted earlier the same day, which was necessary and not sufficient — the vacuity survived one branch later. Ported to ASK `scripts/lib/feed.sh` for the path (`#19a` pins the rule, `#19b` proves the detector fires, `#19c` asserts the run neither wrote to nor created it). All three verified by execution: each perturbation is a PASS/FAIL split, shell green and spec red. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: fixed in pipeline/ts-bridge specs, c668d6d; shell runners retired dc845f1 | Found 2026-09-11 by Vitali (QA-1) during the TASK-018 port. Fixed in the specs in the same pass; the shell runners still carry all three and stay in the gate until the central retirement pass. Re-open if any assertion's evidence could be produced by the failure it is meant to detect. |
| **BUG-081** | **Two isolation/attribution defects in the retiring gate suites: `tests/pipeline` `#12` decides its verdict from the whole machine, and `tests/no-chain-guard` `#4` lets one fail-closed branch impersonate another.** **(1) `#12` ("no temp directories leak")** counts `/tmp/tmp.*` MACHINE-WIDE before and after five runs, so any unrelated process creating a temp dir in that window fails it. Observed twice in one equivalence sweep, under two mutants that touch no temp-dir code at all, and not at all on the re-run — a global read, which TASK-018-RULES R5 forbids and which is why the suite could not be made parallel. Ported to count inside the scenario's own pinned `TMPDIR`: same property, deterministic, parallel-safe. **(2) `#4`'s four fail-closed cases** assert only the exit code, and `jq` on empty input exits 0 with no output — so with the dedicated empty-payload branch DELETED the guard still blocks two branches later at `payload has no string .tool_name`, with the same code, and the case stays green. The retiring suite's own header states the rule it was breaking ("assert the CAUSE, because otherwise one fail-closed branch impersonates another") and applied it to the missing-jq case alone. Ported so each fail-closed branch asserts its own message. Both verified by execution as PASS/FAIL splits. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: fixed in pipeline #12 and no-chain-guard #4 specs, c668d6d | Found 2026-09-11 by Vitali (QA-1) during the TASK-018 port. Fixed in the specs; shell runners unchanged until the central retirement pass. Re-open if a suite's verdict can be changed by a process it does not own, or if two refusals share one assertion. |
| **BUG-082** | **Three assertions in the retiring a2bp shell suites cannot fail for the reason they name — found by running both implementations over one mutated tree, not by reading them.** All three were caught by the TASK-018 equivalence method (build a mutant that injects the defect one assertion exists to catch; run the shell suite and the TypeScript port over the same perturbed copy; diff the verdicts). **(1) `tests/a2bp-request` cannot witness the loss of length framing.** `#3`'s header calls itself *"the entire reason the key is framed rather than delimited"*, and with the byte count removed from BOTH framing primitives (`bp_request_frame`, `bp_request_frame_file`) the whole suite stays GREEN — `#1`, `#2`, `#3`, `#3b` and `#4` all pass. Every fixture shifts a byte into a field whose next neighbour is CONSTANT, so plain concatenation still differs; a witness needs the shifted byte to CROSS a boundary. Verified both ways with `.scratch/andreas/probe-framing.sh`: project `ab` + path `x` versus project `a` + path `bx` COLLIDES on the mutant and is distinct on the real library. **(2) `tests/a2bp-pr-filing` `#5` is fail-open, the BUG-074 shape.** It selects the caller's null-rejecting guard with `sed -n '/if \[ -n "$existing" \]/,/; then/p'` — but `; then` sits on the very line the clause under test occupies, so deleting the clause also deletes the range's TERMINATOR and the range runs ~90 lines on, through comment prose containing the word `null`. Verified: with the whole `&& [ "$existing" != "null\tnull" ] && …` continuation deleted, `#5` stays green. **(3) `tests/a2bp-inputs` `#2b` passes on inherited state.** It asserts an absent `blueprint_branch` defaults to `main`, reading `${BP_CFG_BRANCH:-}` — but `#2`, earlier in the SAME shell, already `eval`'d `BP_CFG_BRANCH=main`. With the default deleted, `bp_config_load` refuses and emits nothing, the `eval` is a no-op, and the stale value satisfies the assertion. R5 isolation is what exposes it. **Fixed in the TypeScript ports, each with a named regression case that goes red on its mutant and green on the real tree:** `a2bp-request` `#3c` (new: a byte shifted across a component boundary), `a2bp-pr-filing` `#5` (the condition is bounded by its own continuation backslashes, not by a `; then` pattern), `a2bp-inputs` `#2b` (one scenario per case, so nothing is inherited). **The shell suites are NOT changed by this row** — TASK-018 retires them in one consolidated pass, and both implementations running is how two of these three were found. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: 47c0d30 fixes a2bp-request #3c, pr-filing #5, inputs #2b in TS ports | Found 2026-09-11 by Andreas (Back-End-2) porting the six a2bp suites to TypeScript under TASK-018, by mechanical verdict diffing over 78 perturbed trees. Number taken after checking all four `docs/*/BUGS.md`; 078-081 were already claimed by concurrent agents, so renumber if 082 collided. Re-open if any assertion's range or fixture is found unable to distinguish the defect its own comment names. |
| **BUG-086** | **A bug number with COMMITS and no row is invisible to every control in the repo.** `tests/bug-numbers` guards the opposite direction — one number must not carry TWO rows — and nothing guarded it carrying NONE. BUG-073's row was dropped by a whole-file write (`5dd158e` rebuilt the table from `c59a2e3`, which predates the row that added it), and it stayed dropped through two further sessions while its fix sat on `main`; the 2026-09-11 `lcm` pass found it by reading the folders. Fourth instance of the stale-buffer clobber that day and the only one no test could see. The naive check is a false-positive factory — "every `BUG-NNN` mentioned anywhere has a row" reports twelve on a healthy tree, because `BUG-099`/`BUG-123`/`BUG-999` are SYNTHETIC numbers inside fixtures — which is why nobody built it. Asking git instead ("which numbers have a `BUG#N:` commit subject?") has no synthetic population at all, since a fixture never commits here: zero false positives on the real tree, and red at `2bde427^` where BUG-073 had two commits and no row. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: lifecycle-docs bugsWithoutRows check added c668d6d; spec :270 | Found 2026-09-11 by Timon (Front-End-1) during the TASK-018 port, from the gap the same day's `lcm` pass had to close by hand. Closed by `tests/lifecycle-docs` #6 (`bugsWithoutRows`), with the mention-is-not-a-row distinction asserted so a row's own commit message cannot satisfy it. Re-open if a rowless number can reach `main`. |
| **BUG-088** | **`tests/git-isolation`'s two hardcoded population floors fail a HEALTHY tree as the TASK-018 retirement wave proceeds, and the control goes red on the FIRST anchor retirement — through NEITHER floor.** `[ "$found" -lt 15 ]` sits at 20 git-driving shell suites today (`git-isolation`'s own runner among them); the isolation/lifecycle group retires three members (`commit-msg-gate`, `commit-subjects`, `state-dir`), leaving 17, so three FURTHER removals from any other group take the gate red while nothing is broken. What goes red on the FIRST anchor retirement is `#1`'s `[ ! -f "$s" ]` and `#3`'s `missing_anchor` branch — not the anchor floor, which cannot fire at all (see the mechanism correction in Notes). Both floors were correct when written and are now booby traps inside the migration they sit in — a test failing for the opposite of a defect, which is the shape CLAUDE.md rejects in §"The blueprint's `main` is its trunk" (a gate someone must route around to do their job protects nothing). Measured: the ported spec and the shell runner agree on ten of eleven perturbed trees, and the eleventh is exactly this — six fully-guarded suites including every anchor, shell red, port green. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: floors replaced in git-isolation.spec.ts c668d6d; row corrections a9b1485, db5b82d | Found 2026-09-11 by Timon (Front-End-1) during the TASK-018 port. FIXED in `tests/git-isolation/git-isolation.spec.ts`: the floors are replaced by the half that would actually have caught BUG-047 — every anchor still ON DISK must be found by the predicate — plus an explicit assertion when the population empties, so the intended end state is reached visibly rather than silently. **That end-state assertion was itself RED at the end state and is fixed under BUG-109** — it asserted no `*.sh` remained under `tests/`, and `scanGitIsolation` considers `tests/helpers/proc-cwd.sh`, which is alive; it now asserts every anchor became a spec. **So "the port is retirement-safe" was not true when this row claimed it**, and was verified rather than inherited only on 2026-09-11. The anchor list is now INTERSECTED with disk rather than asserted against it. **The central retirement pass must land this port before retiring any git-driving shell suite**, or the gate goes red on the retirement itself. Re-open if a healthy tree can fail this control. **ARITHMETIC CORRECTED 2026-09-11 by Alexey (Architect, Codex) in cross-provider review; conclusion unchanged.** The count floor is NOT one retirement from red. The population is 20, and only **three** of the reviewed runners are members of it — `state-dir`, `commit-msg-gate` and `commit-subjects` — so retiring them leaves **17**, and `[ "$found" -lt 15 ]` fails only after three FURTHER removals. **MECHANISM CORRECTED 2026-09-11 by Christian (Architect); the ordering conclusion is unchanged and correct.** This row twice named `[ "$_gi_n" -ge 3 ]` as the floor that "breaks FIRST" / "fails **immediately**". **It cannot fail on any tree.** It word-counts the hardcoded literal `GI_ANCHORS="marker-merge gate-arming commit-subjects"` (test.sh:101,107-109), which is never derived from disk, so `_gi_n` is 3 over every possible tree including an empty one — deleting an anchor *runner* does not shorten the *literal*. MEASURED by deleting `tests/commit-subjects/test.sh` from a scratch copy and running the real runner: baseline PASS (`#3 all 20 git-driving suites unset GIT_DIR themselves`), then `FAIL: #1 tests/commit-subjects/test.sh not found` and `FAIL: #3 discovery MISSED suites #1 proves drive git: commit-subjects`. **`git-isolation`'s port must still land before any git-driving shell suite is retired** — an anchor retirement IS immediately red, just two lines elsewhere. A reader sent to the anchor floor finds a line that can never fail, concludes the constraint is imaginary, and retires an anchor. Recorded rather than quietly fixed because this row is a SEQUENCING CONSTRAINT for the retirement pass, and a wrong number in a constraint is worse than no number: it invites the next reader to recompute the margin and act on it. |
| **BUG-089** | **`tests/agent-activity-bound` and `tests/subagent-feed` treat an empty `bp_proc_cwd` result as "does not match", which the helper's own contract forbids, and neither calls `bp_proc_cwd_available`.** `tests/helpers/proc-cwd.sh` states it explicitly — *"Callers MUST treat empty as unknown, never as does not match"* — and both consumers do exactly the forbidden thing with `case "$(bp_proc_cwd "$p")" in "$WORK"*)`. On a host with no `/proc` and no `lsof`, `supervisors()` is therefore always 0 and every assertion of the form `-eq 0` passes over zero information: `agent-activity-bound` #5b, *"--stop leaves zero residue"*, reports clean while a real supervisor leaks. Measured rather than reasoned — with one live process whose cwd IS under `$WORK`, the real helper counts 1 and the assertion correctly fails; a mechanism-less helper counts 0 and the assertion PASSES. That suite's own #6 message says "0 is the wedged/no-feed outcome and is not a pass", and #5b is the case where 0 is accepted as one. Latent on Linux, live on macOS — the BUG-036 host. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: ports guard via bp_proc_cwd_available, tests/helpers/feed-fixture.ts:318, c668d6d | Found 2026-09-11 by Timon (Front-End-1) while establishing whether `tests/proc-cwd/test.sh` was retirable. NOT FIXED — both suites belong to other porting groups. The vacuity guard itself is now safe: `bp_proc_cwd_available` had exactly one caller anywhere (the shell runner being retired), and `tests/proc-cwd/proc-cwd.spec.ts` now carries it plus #2-#5 by DRIVING the shipped helper, so the property survives retirement. Fix by calling `bp_proc_cwd_available` once at the top of each consumer and failing loudly, or by porting both suites. Re-open if a count of owned processes can be 0 for want of a mechanism. |
| **BUG-090** | **Every feed-driving shell suite pins `AGENT_STATE_HOME` and not `AGENT_SIGNAL_FILE`, so one exported variable points a fixture supervisor at the operator's LIVE baton.** `agent_signal_file` (`scripts/lib/state-dir.sh:211`) honours `$AGENT_SIGNAL_FILE` BEFORE it consults the state dir, so moving the state dir does NOT move the baton. `tests/watcher-liveness`, `tests/roster` #12, `tests/subagent-feed` and `tests/agent-activity-bound` all pass only `AGENT_STATE_HOME`, and are correct purely because nothing in their environment happens to set the other one. **Proven by execution:** `AGENT_SIGNAL_FILE=<live baton> bash tests/watcher-liveness/test.sh` → `FAIL: #6 the feed emitted no warning` and `FAIL: #6 the warning appeared 0 times`, because the fixture feed was reading the repository's real baton instead of its own. This is the BUG-046/BUG-047 class exactly — ONE missing line in a test, surviving months of review — and the same shape as the `unset AGENT_SIGNAL_FILE` BUG-046 was. It is worse than a red test: a suite in this state READS live coordination state, and `tests/watcher-liveness` #6 drives a supervisor that would then compare the live mic against a fixture lock. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: feed-fixture.ts:192-197 pins AGENT_SIGNAL_FILE, c668d6d; shell runners retired | Found 2026-09-11 by Philipp (Infrastructure-1) porting the feed/persona suites for TASK-018 — the harness sets `AGENT_SIGNAL_FILE` for every scenario, so the omission failed loudly instead of silently. FIXED in the ports: `tests/helpers/feed-fixture.ts` pins BOTH, with the reason stated where the pinning happens. The shell runners are NOT changed — TASK-018 retires them centrally, and both implementations running is what surfaced this. Re-open if a fixture can reach the live baton by inheriting one variable. |
| **BUG-095** | **`tests/agent-activity-bound` #15 and #2 counted `tail -n0 -F` MACHINE-WIDE, so both cases were unsatisfiable on any host running something whose argv merely CONTAINS that string — and the pre-push gate could not pass.** The supervisor half of each assertion was already scoped to the fixture by cwd (BUG-036); the follower half was not, and `ps -eo args | grep -c "[t]ail -n0 -F"` counts an agent's shell wrapper, and counts the `pgrep -fa 'tail -F'` you run to DIAGNOSE it. Measured on this host: **zero real followers resident, and the predicate answered two.** Same class as the note already in BUG-094 — the RC-2 mutant tree's 89 orphaned followers deciding six later trees' verdicts — but this instance needs no mutant at all, only a developer host with a second project's feed up (`linkedin-watcher-agent`'s supervisor, pid 9699, running legitimately from its own checkout). **Fixed by extracting the loop `supervisors()` already had into one `owned <argv-pattern>` predicate and deriving both `supervisors()` and a new `tails()` from it** — the counting predicate changed, no case added, nothing weakened: a follower our supervisor forks inherits the fixture cwd, so *zero followers rooted at OUR tree* still reds the RC-2 leak. **Non-vacuity measured rather than reasoned** (`.scratch/owned-check.sh`): with a real `tail -n0 -F` resident outside the fixture the old predicate answers 1 and the new answers 0, and with one resident INSIDE the fixture the new predicate answers 1. | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: f2dbda9 BUG#95 counts owned followers only | OPEN | Found 2026-09-11 by Markus (Security-1); the founder authorised it explicitly as a fix to an EXISTING shell suite, which the all-TypeScript ruling permits where new shell test coverage would not — the suite is unretirable until reviewed and was unpassable until scoped. Full suite re-run GREEN with the other project's feed up and a decoy follower resident. The port needed no change: `ownedProcesses()` (`tests/helpers/feed-fixture.ts`) already scopes every count to its scenario's workspace, which is the measurement `agent-activity-bound.spec.ts`'s docblock records. Re-open if any assertion counts a process it does not own. |
| **BUG-096** | **Every TASK-018 mutation harness builds its tree with a FRESH `git init` + `git add -A`, and a fresh git applies `.gitignore` in full — so the sixteen files the real repo TRACKS while `.gitignore` also names them are absent from the baseline, and BOTH questions that git is asked come back wrong.** Tracked beats ignored in the real repo; in a fresh init nothing is tracked yet, so `.gitignore` wins. **(1) The CHANGED-NOTHING guard goes blind.** `git status --porcelain` cannot see a mutation to a file it never added, so the guard Christian added *precisely because* a hand-written file list had silently encoded the answer now answers `MUTANT CHANGED NOTHING` for a mutant that rewrote its whole target. **Observed, not reasoned:** a mutant renaming every `bp_roster_label` in `scripts/start-codex-signal-watch.sh` — which is `tests/codex-persona-label`'s entire subject — was refused a verdict on exactly these grounds. **(2) `git archive HEAD` inside the copy loses TEN SHIPPING FILES**, and `build_tree`'s own comment says the fixture builders and `suite-sync` #1d depend on that query answering inside the tree. Measured (`.scratch/markus-archive-gap2.sh`): real=184 paths, copy=174; absent are `CLAUDE.md`, `docs/DoD.md`, `AGENTS.md`, `AGENT_SIGNAL.md`, `.claude/settings.json`, `docs/PUBLISHING.md`, `docs/way-of-working.pdf`, `scripts/new-project.sh`, `scripts/codex-signal-watch.sh` and `scripts/start-codex-signal-watch.sh`. So every mutant tree tests a tree that differs from the real one in a specific, silent way, and `git show HEAD:<file>` — which `gate-arming`'s fixture relies on — reports *does not exist in HEAD* for any of the sixteen. **Present in all three harnesses:** `.scratch/elias-equiv.sh` (sync group, the pass that certified it), `.scratch/vitali-equiv.py:build_tree` (gate group) and `.scratch/equiv.sh`. **A measuring apparatus carrying the defect it measures for, which is the fourth instance this week** — and the only reason it cost minutes rather than a run is that the guard fails LOUDLY (INVALID, no verdict) instead of reporting the fabricated *neither implementation covers this* a missing guard would have produced. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: add -A -f in tracked harnesses c53d14a/667a9f3 (iso-equiv.sh:727, equiv.sh:682) | Found 2026-09-11 by Markus (Security-1) closing the feed group's R6 gap, from a mutant that was refused a verdict. **Fix is `git add -A -f`** — the guard's question is *did the mutant change any byte of THIS TREE*, so every byte `rsync` copied belongs in the baseline; `rsync` already excludes `.git/`, `logs/`, `coverage/`, `.scratch/` and `tests/node_modules/`, which is what makes `-f` safe here. Applied in `.scratch/markus-feed-r6.sh` and verified: the same mutant then reported `changed=1` and reddened exactly its named assertion in both implementations. **NOT yet applied to the other three harnesses** — they are scratch, they have already produced their tables, and changing a measuring instrument after the measurement without re-running it is worse than leaving it. Regenerate the list with `git ls-files -i -c --exclude-standard` rather than copying it; it has grown twice. **BLAST RADIUS MEASURED, and it is smaller than the defect** (`.scratch/markus-suitesync-probe.sh`): `tests/suite-sync` is the one suite whose oracle asks `git archive HEAD` inside the tree, and its healthy control is GREEN in both implementations either way — 175 paths plain, 190 forced — so the sync group's certification is not invalidated. The a2bp group is untouched for a different reason: `cmd_a2bp` writes its managed list from the `MANAGED_FILES` array verbatim and works against the fetched REMOTE base, so it has no local HEAD to expand from. **Neither measurement is a mutant-by-mutant re-run**, and the honest statement is that no mutant in either published population targets a file in the blind set — which is luck, not design. **CORROBORATED FROM A SECOND HARNESS the same day:** `.scratch/vitali-equiv.py`'s `r6-guard-not-wired` mutant rewrites `.claude/settings.json` — also one of the sixteen — and aborted with the identical `MUTANT CHANGED NOTHING` while the file had in fact been rewritten. **The two halves have DIFFERENT blast radii and the distinction is what a reader needs:** a suite is affected only if it MUTATES one of the sixteen (half 1) or READS one through `git archive`/`git show HEAD:` in the tree (half 2). `gate-arming` does the latter but on `scripts/lib/gate.sh`, `scripts/agent-activity.sh` and `scripts/blueprint`, none of them in the set, so its verdicts were identical before and after. **And `-f` alone is not the whole guard.** It repairs every file `rsync` copied, which is the stated hole, but the stronger form ORs it with a byte comparison against what the mutator saved — a check derived from the mutant DEFINITION rather than from any list, so git catches side effects outside the declared paths and the byte compare catches a path git cannot see for any reason at all. Adopted in the gate group's harness. Re-open if a mutant tree's git can disagree with the tree on disk. |
| **BUG-100** | **The mic catalogue's own runner resolved vitest through `npx`, which is the silent-no-op hazard the founder's standing instruction names — and it clobbered its own evidence file on every invocation.** `run.py` invoked `npx vitest`, which resolved the PINNED binary only because the cwd happened to be the mutant tree's `tests/`; a miss fetches an unpinned vitest that runs no files and exits 0, and a zero exit with an empty report is indistinguishable from a green suite — so a whole equivalence run would read as *both implementations agree, all green* while nothing executed. This is the R6 measuring-apparatus class again (BUG-096 is the same week's other instance) and it is worse here than in scratch, because this catalogue is TRACKED deliberately: PLAN-TASK-018 §5 requires the mutant catalogue be a tracked artefact rather than oral history, so the defect shipped with the evidence it produces. **Second half:** `outputs/results-last-run.json` was written per invocation with no target, so each run overwrote the last — the recorded red sets for four of the five suites survived only as `outputs/*.log`, and the JSON that reads as the canonical record held one suite. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: e837c46 BUG#100 pinned vitest runner, per-target output | Found 2026-09-11 by Matthias (Back-End-1) while closing the mic group's R6 gap. **FIXED in the same pass:** `tests/node_modules/.bin/vitest` with an explicit `--root`, plus a `#no-tests-ran` verdict so an empty report is a distinct outcome rather than a green one, and `--out NAME` with `suite:mutant` targets so a run names its own artefact. Re-open if an evidence artefact can be produced by a runner that executed nothing, or overwritten by the next run. |
| **BUG-102** | **`sort -u` is not a set operation under a UTF-8 locale, and an R6 harness derived BOTH its denominator and its observed red sets through one.** glibc collation ignores `#` at the primary level, so `#2 names no item: BUG: no number` and `#2 names no item: BUG#: no number` COLLAPSE into a single entry — the case inventory read 22 where vitest ran 23, and `uniq -d` found nothing because it compares bytes and the two lines differ. A mutation matrix built this way undercounts the cases it must cover AND loses reds it did observe, in the same operation, and reports a fuller house than it measured. Same class as BUG-043. `LC_ALL=C` on every sort in a set position. **Second, and the reason this is one row: A ONE-COMMIT BASELINE IS NOT NEUTRAL EITHER.** `tests/lifecycle-docs`' REAL TREE case reads `git log --format=%s` in the tree it runs in and asserts a >50-subject floor as its OWN non-vacuity control — so over a fresh-init mutant tree it goes red on a PRISTINE copy, which reads exactly like a finding and is a harness artefact. Both are the same failure as BUG-096: the apparatus answering a question about itself and being read as answering one about the subject. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: LC_ALL=C and seeded baseline in iso-equiv.sh, c53d14a; row d6d68ba | Found 2026-09-11 by Timon (Front-End-1) during the isolation group's R6 pass, both by being contradicted — a count that disagreed with vitest's, and a red on a tree with no mutation in it. **FIXED in `docs/waiting-acceptance/TASK-018-R6-isolation/code/iso-equiv.sh`:** `LC_ALL=C` throughout, and `seed_history` replays the real commit subjects as empty commits rather than copying `$SRC/.git` (five agents commit into that repo, so a copy can catch a torn index) — with the CHANGED-NOTHING guard's baseline count taken AFTER seeding instead of hardcoded to 1. **NOT applied to the other harnesses**, which have published their tables; changing a measuring instrument after the measurement without re-running it is worse than leaving it and saying so. Re-open if a harness derives a case list or a red set through a locale-sensitive sort, or if a suite that reads its own git history is run over a synthetic baseline. |
| **BUG-103** | **A mutant can apply to a COMMENT, and the result is a both-green verdict indistinguishable from a real finding — *applied* is not *applied to code*.** The literal-substitution guard every R6 harness here relies on (`sub` exits non-zero when its target text is absent, because a mutant that matches nothing reads exactly like "neither implementation covers this") is one level too shallow. Measured: a mutant targeting `  local TL='{{PROJECT_NAME}}' TU='{{PROJECT_NAME_UPPER}}'` in `scripts/lib/placeholders.sh` matched the file's own HEADER, which quotes the line it documents — the code line's two-space indent is a substring of the comment's three. It applied, changed a byte, satisfied the CHANGED-NOTHING guard, and changed nothing that runs. **Any harness doing literal substitution against a SELF-DOCUMENTING file has this**, and this repo's files are self-documenting by policy — the same property `tests/codex-persona-label`'s port strips comments for, and the one BUG-092(2) is about from the other side. The properly-anchored replacement reds `a2bp-contamination` `#1 #1b #25 #25b`, so the assertion was never the problem. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: diff-audit.sh control shipped in 008ffce; row 5d297be | Found 2026-09-11 by Jesko (QA-2) closing the a2bp group's R6 gap for TASK-018. **Control shipped, not just a note:** `docs/waiting-acceptance/TASK-018-EQUIVALENCE-a2bp/code/diff-audit.sh` prints what each mutant actually changed, and every both-green mutant in that catalogue was audited through it before being recorded as a finding. Fourth member of the family BUG-096 and BUG-102 name — the applied-check consulting something that encodes its own answer instead of the executed bytes. Re-open if a mutant's application is established by anything weaker than "the changed bytes are on a line that runs". |
| **BUG-104** | **Five a2bp assertions cannot be falsified by the defect they NAME. None is dead — each has a red mutant — so the gap is between an assertion's TITLE and its witness, which is the version of this that no red-set count can show.** **(1) `a2bp-pr-filing` `#2` and `#3`** name `--json state,url` and `--state all`, and the `gh` shim reproduces gh's `--jq` handling while ignoring every other flag — so mutating either query flag reaches nothing, and only a mutation of the jq FILTER reds them. The cases pass whether or not `a2bp` asks `gh` for the right fields. **(2) `a2bp-request` `#3` ("content boundaries are framed") and `#3b` ("framed, not newline-delimited")** are reddened by no unframing mutant at all, because in both fixtures the shifted byte's next neighbour is constant — the same measurement `#3c`'s own comment reports from the other side. `#3c` IS the witness, and it is TS-only, so the shell runner has no witness for this property anywhere. **(3) `a2bp-contamination` `#21` ("a diff capability/runtime failure fails closed")** stays GREEN with the `diff_rc > 1` guard it names removed, because the residual-project-name scan blocks the same bytes; both have to go before anything reds. **This class is why a coverage number is not an answer.** All 109 assertions in this group now have an observed red — `build 17/17 · contamination 36/36 · e2e 13/13 · inputs 21/21 · pr-filing 7/7 · request 15/15` over 98 trees — and these five are inside that 109. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: 2766c5b BUG#104 fixtures make five assertions witness their defect | Found 2026-09-11 by Jesko (QA-2) closing the a2bp group's R6 gap for TASK-018, from a starting gap of ALL 109: five of the six docblocks pointed at a migration report that carries no per-assertion table, and `a2bp-contamination` had no equivalence record at all. **FIXED 2026-09-11 by Matthias (Back-End-1)**, after Andreas (Back-End-2, Codex) refused certification of the six ports over exactly these five. Every fix is in a FIXTURE, not in an assertion, so no verdict moved: `ghShim` now implements gh's `--state` filtering and `--json` field selection instead of consuming only `--jq` (so `F2` reds `#2` and `F3` reds `#3`); `#3` and `#3b` each carry a second key pair whose shifted bytes BECOME the next record's header, or forge a newline boundary, instead of landing in front of a constant neighbour (so `K3` reds `#3` and `K4` reds `#3b`); `#21`'s carrier line carries an `a2bp-allow` marker — the product's own sanctioned override, which suppresses the residual-name scan on that line — so the fail-closed `diff` guard is the only thing left and `C15` alone reds it. All five observed, not predicted: one perturbed tree per mutant, both implementations run over it. **`K3` and `K4` are port-stricter results**: the retiring shell suites stay entirely GREEN under both. Catalogue, per-mutant red sets and the caveat per suite in `docs/waiting-acceptance/TASK-018-EQUIVALENCE-a2bp/`, with each spec's docblock naming its own. **Fix `#2`/`#3` by asserting on what the shim was ASKED rather than on what it returned; `#3`/`#3b` with a fixture whose shifted byte has a varying neighbour; `#21` by removing the residual scan's cover for it.** Re-open if an assertion's title names a defect that cannot red it. |
| **BUG-105** | **Two `a2bp` product behaviours that NO assertion watches, in either implementation — both-green over the injected defect, re-confirmed serially.** These are the class mutation testing structurally cannot find by watching what goes red; they are found by watching what does not. **(1) `bp_file_base_content` can align at the ROOT coordinate instead of `bp_base_path`'s, and nothing reds.** No case in the six suites drives `a2bp` end to end against a base moved under `scaffolding/` — which is precisely the restructure TASK-018-TARGET §2 puts next, and the hazard that section names is path mapping. The function's own header states the consequence: an empty base for a file that EXISTS, so every line reads as new, the staged restore has nothing to align to, and A-07's placeholder restore degrades to nothing on exactly the lines it exists for. **(2) `cmd_a2bp`'s required-libs guard can become a `continue` and nothing reds.** Its comment cites BUG-003 — *"a guard that cannot run is not a guard that passed"* — and no assertion holds it to that. A2bp with a lib missing then proceeds with the contamination scan absent, which is the door BUG-002 and A-09 came through. | S2 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: 699a5bd BUG#105 new cases #29/#30 in a2bp-contamination spec | Found 2026-09-11 by Jesko (QA-2) closing the a2bp group's R6 gap for TASK-018, over 98 mutant trees. **NOT a port regression** — the shell runner is equally blind, so this is missing coverage in both and needs a NEW case rather than a stricter one. **FIXED 2026-09-11 by Matthias (Back-End-1)**, both with a new case in `tests/a2bp-contamination/a2bp-contamination.spec.ts`: `#29` drives a2bp end to end against a base whose copy of the carrier lives under `scaffolding/` (with a root file of the same name left in place, which is the state DURING the move and the wrong answer a root-coordinate alignment picks up) and requires the request to be FILED with the placeholder restored — `B15` reds it. `#30` runs a copied CLI with `scripts/lib/contamination.sh` deleted, over a `tests/*` path, which is managed and NOT substituted: with the refusal downgraded to `continue`, staging is a plain `cp`, `contamination_scan` is simply not a command, `findings` comes back empty, and the request is filed with the scan having never run — `E7` reds it, and a control run with the complete copy proves the CLI reaches the scan at all. **THE COMPLETENESS CLAIM IN THIS TITLE WAS FALSE: there were three, not two** — the missing third is BUG-108. **(1) will become reachable, and red, the moment the `scaffolding/`+`forge/` restructure lands**, so the case is cheapest to write now while the pre-move behaviour is still the baseline to compare against. Re-open if a guard whose own comment cites a bug is unasserted, or if `a2bp` can resolve an empty base for a file that exists. |
| **BUG-106** | **`tests/a2bp-e2e` #10 scans the SHARED `$TMPDIR` for `a2bp.*`, so any concurrent process decides its verdict — measured red for ELEVEN mutants that touch no scratch code at all.** Under 2-way parallelism it failed for eleven unrelated trees and was clean on serial re-runs of the same two; the port scans the scenario's own pinned TMPDIR and did not move on any of them. **This is BUG-049's cross-suite hazard measured rather than argued**, and it is the third instance of one shape found today: BUG-095 is the same defect in `tests/agent-activity-bound` #15/#2 (`tail -n0 -F` counted machine-wide), and BUG-094 records its earlier cost — one mutant tree's 89 orphaned followers deciding six later trees' verdicts. **An assertion that reads something it does not own is not a flaky test; it is a test of the machine.** | S3 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: fixed in the TypeScript port: a2bp-e2e #10 scans the scenario TMPDIR (a2bp-e2e.release.spec.ts:423) | Found 2026-09-11 by Jesko (QA-2) during the a2bp group's R6 pass, from a red set that made no sense for the mutants that produced it. **NOT FIXED, and deliberately: this is a reason to retire the shell runner, not to repair it.** The port already scopes the scan, so the property is covered — repairing the runner would change a verdict while the port is being proven against it, which is the one thing a port must not do silently. Contrast BUG-095, where the founder authorised the same repair in `agent-activity-bound` for the opposite reason: that suite's assertion was UNSATISFIABLE on a developer host and blocked every push, so leaving it was not an option. Re-open if any retiring runner's verdict depends on a process it did not start. |
| **BUG-107** | **Three claims in the TASK-018 ports that were true-enough rather than measured — the reporting half of the proxy-assertion defect F-002 names.** **(1) Ceilings were WIDENED while the port report said none was:** `expectLine` 20 s against the shell's 8 s, `readerReady` 30 s against 20 s, supervisor discovery 10 s against `wait_sup`'s 4 s — up to 3.75x, and a widened ceiling is the cheapest way to make a flaky port look green. **(2) `scripts/session-resume.sh`'s BUG-079 comment stated single-`write(2)` `cat` as a GUARANTEE**, which POSIX does not give; it is a property of this implementation at this payload size, and the next person to grow the payload would have reasoned from the stronger claim. **(3) The `sleep` PATH shim records its marker BEFORE the sleep**, so N markers prove N comparisons but only N−1 completed polls — the elapsed-time claim was overstated by one poll. | S3 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: c6d99a2, 35be206, 92fc8cd BUG#107 ceilings, claim and shim fixed | Raised 2026-09-11 by two cross-provider reviewers independently, each by re-MEASURING rather than re-reading. **FIXED by Nicole (UX) in the same pass:** all three ceilings restored to the shell's own numbers with the measurement beside them (4 full runs of the four feed suites: `expectLine` worst 3.31 s of 8 s, `readerReady` worst 3.26 s of 20 s, `expectSupervisors` worst 120 ms of 4 s), the one caller that genuinely needs more (#10 `RACE-C`, deliberately slowed read, 4.60 s) carrying the same explicit 30 s the shell gave it; the `cat` claim rewritten with its strace and its falsifier (the same binary splits a 1 MiB body into 65 writes); and the shim arithmetic corrected by raising the constants rather than weakening the claim (`wait-mic` 6→7, `signal-dispatch` +2→+3), so the ported bound is again at least the shell's wall-clock bound AND a comparison count the sleep could not promise. **No separate regression test:** these are the tests, and each correction is witnessed by the suites it lives in — a ceiling that cannot come down fails them. BUG-067 was checked as a possible floor and is not one here: nothing measured needed a `--daemon` ceiling above the shell's. Re-open if a ported bound is wider than the one it replaces without a measurement beside it. |
| **BUG-109** | **Six controls that are green today go RED the moment the TASK-018 shell runners retire — every one of them pinned to a runner FILE rather than to the property it guards.** MEASURED in a scratchpad copy of the finished tree (36 runners deleted, 36 gate blocks and the CI step removed, `sed-inplace.sh` gone), not reasoned: 6 failed / 80 passed across the six. **(1) `tests/manifest` #1b** reports `tests/helpers/proc-cwd.sh` as an orphan; its `walk()` collected only `*.sh`, and after the wave the helper's consumers are TypeScript (`tests/helpers/feed-fixture.ts:301`, BUG-089's fail-open guard; `tests/proc-cwd/proc-cwd.spec.ts`). A live helper killed by a scan that cannot see its references. **(2) `tests/git-isolation` #3's terminal branch** asserts no `*.sh` remains under `tests/` — but `scanGitIsolation` walks `tests/<dir>/*.sh` and `tests/helpers/` is such a directory, so `proc-cwd.sh` is CONSIDERED, is not a MEMBER, population is zero and the end-state assertion fires on a correct file. **This is the one that matters: BUG-088's row claimed the port was already retirement-safe, and it was not.** **(3) `tests/live-state-canary` #4** `readFile`s three `test.sh` the wave deletes → ENOENT. **(4) `tests/bootstrap-contents` #3b** probes downstream for `tests/marker-merge/test.sh`. **(5) `tests/state-root` #F**'s four non-vacuity anchors are all shell fixtures, so `inScope` empties and the guard fires — correctly; it exists to refuse a scan that reaches nothing. Found ONLY by running the simulation. **(6) `tests/doc-links`** — eight markdown hrefs in `docs/DoD.md` and `docs/done/BUGS.md` name runners that will not exist; five sit in user-accepted history nobody expected the wave to touch, and no amount of reading the test tree would have found them. | S1 | **ACCEPTED by the founder 2026-09-16** — bulk acceptance of fixed work that sat in doing/ with a stale OPEN status ("yes, straight to done"). Fix on main: 443388f, 1b9aa6f, 22e680b, 585b89c BUG#109 commits on origin/main | Found 2026-09-11 by Klaus (PO) via the scratch-copy simulation in [PLAN-TASK-018-RETIREMENT.md](TASK-018-ts-suites/PLAN-TASK-018-RETIREMENT.md) §1; fixed and independently re-measured the same day by Christian (Architect). **Fixed WITHOUT performing the retirement** — each fix is green on today's tree AND on a rebuilt end-state copy (84/84 both sides), which is the acceptance test. (1) `#1b`'s walk now reads `*.ts` as well as `*.sh`, and enters `tests/helpers/` for `.ts` only — the `.sh` exclusion exists so a helper's own header cannot vouch for itself, an argument about the file being judged and not about its neighbours. **That widening alone is green for the WRONG REASON, found by mutating it rather than by reading it, and both halves matter.** It made the check vouch for its OWN subject: the comment explaining the fix names `proc-cwd.sh`, and `manifest.ts` is a `.ts` file under `tests/`, so with both real consumers mutated away `#1b` stayed GREEN — the BUG-035 shape (membership decided by what comments SAY) reintroduced one level up from the exclusion that exists to prevent it. Comments are stripped first. And the match is on the FILENAME rather than on `helpers/<name>`, because the consumer the plan cites — `tests/helpers/feed-fixture.ts`, which sources the helper via `join(REPO_ROOT, 'tests', 'helpers', 'proc-cwd.sh')` — contains no `helpers/proc-cwd.sh` substring at all, so under the prefix match that live consumer was invisible and only its comment made it look otherwise. Non-vacuity re-measured after both: deleting the two CODE references reports `#1b shared helpers that no suite sources: proc-cwd.sh`, and `sed-inplace.sh` is still correctly orphaned if the wave forgets to delete it — no laundering path was opened. (2) the terminal branch states the end state POSITIVELY — every `DECLARED_ANCHOR` now owns a `*.spec.ts` — so it says the migration COMPLETED rather than that the directory is empty, and it survives both a live shell helper and a deliberately-retained runner. (3) and (5) DISSOLVE with their subjects, recorded in each spec's header per the `drift-in-blueprint` precedent; neither was replaced by an iteration over "whatever `.sh` still exist", which is a vacuous pass wearing the old name. (4) re-points at `marker-merge.spec.ts`. (6) every href now names the suite DIRECTORY, which is what its visible label already said, so no future port can break it again. **No runner, gate stage or CI line was deleted** — the retirement is a separate pass with a forced order (plan §3). Re-open if any of the six can go red on a healthy tree, before or after the wave. |
| **BUG-132** | **CI's `ts-tests` job runs the suites on a machine the installer never prepared, so a tool the suites need locally is simply absent there: since BUG-127 every real-`gitleaks` a2bp scenario fails in CI and passes on every developer machine.** CI run 35126720424 on `71388b9` is red: `ts-tests` (`.github/workflows/security.yml`) failed 6 of 55 test files, every failure an a2bp scenario (`a2bp-contamination` #1 #1b #2 #4 #6 #7 #7b…, `sync-by-address` #31, …), each saying `bp_inputs: gitleaks is not installed, so nothing has scanned the bytes this request would publish.` **Cause:** BUG-127 made `a2bp` REFUSE without gitleaks, correctly. The suites use whatever gitleaks is on PATH. A developer machine has it — `scripts/install-toolchain.sh` installs it, and the local pre-push gate is green — but `ts-tests` installs only ShellCheck (by `apt`, unpinned, one tool by name) and nothing else from the installer's set. gitleaks reaches CI only through `gitleaks-action` in the separate `secret-scan` job, on another VM. **Same class as BUG-117** (local and CI environments diverge, and each mode is green over its own assumption). **The defect is the mechanism, not the missing name:** a job that installs tools one at a time by name catches only the tools someone remembered, so the next tool a shipped script starts requiring reds CI again, after the push. | S2 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-16 by Philipp (Infrastructure-1), diagnosed by Eto (Orchestrator) from the CI run. **Fix shape:** prepare the `ts-tests` machine with `scripts/install-toolchain.sh`, the one script that prepares every developer machine, instead of per-tool steps — so CI's tool set is the installer's by construction. **Reproducer:** execute the job's provisioning steps offline (stubbed fetches) on a machine lacking the installer's tools, then require `install-toolchain.sh check` to pass. Red while the job only installs ShellCheck. **What was to be tested:** Look at CI for any push that touches the suites: the `TypeScript regression suites (vitest)` job must run the `Gate toolchain` step (`bash scripts/install-toolchain.sh`) and the a2bp scenarios must pass there, not only locally. Before this, run 35126720424 failed 6 of 55 files with `gitleaks is not installed`. Locally, `tests/ts-bridge` #9 runs the job's provisioning steps offline with every installer tool off PATH and requires `install-toolchain.sh check` to pass; #9b–#9e are red when a provisioning guard is false, the manifest is missing, a step is `if: false`, or a step exits nonzero. **It is an offline wiring check, not a real install:** downloads and package commands are stubbed, and only CI proves the installer really runs (it did on `c3e2044`; the suites job took 16.5 min). **Cross-provider review:** Jesko (Codex) — push after fixes; both S2 findings (guards and exit codes ignored) and the S3 harness-less ShellCheck fallback were closed in `40f4699`. **Known limits:** a tool the suites need that the installer does not declare is still invisible; `pipx install semgrep` is unpinned. **Status when accepted:** **FIXED — landed `8a792c5`, reproducer `35b02a3`, review fixes `40f4699`/`c3e2044`, pushed 2026-09-16; CI green on `c3e2044` (run 35131958151)** |
| **BUG-131** | **The subagent deferral slot directory is not gitignored, so every project that dispatches a subagent gets a permanently dirty `git status`.** Observed 2026-09-16: `git status --short` in the blueprint reported `?? subagent-defer/` (holding only `.lock`) immediately after BUG-124's rework began creating it. **The path is correct, not a bug:** `log-activity.sh:234` builds `$BP_STATE_ROOT/subagent-defer`, and `bp_state_root` walks up from `BP_CODE_ROOT` to the nearest `.blueprint-root` / `.blueprint-source` / `.git` marker — the repo root — so `<repo>/subagent-defer` is what the code intends and what `tests/subagent-feed/subagent-feed.spec.ts:970` documents. **The defect is that nothing ignores it.** `.gitignore` ships to every derived project and covers `node_modules/`, `dist/`, `*.log`, `.env` and the rest, but not this. **Why it matters beyond tidiness:** a permanently untracked directory erodes the clean-tree precondition the pre-push gate, `blueprint drift` and every "is the tree clean before pushing" check lean on — and a dirty status that is always dirty is one nobody reads. | S3 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-16 by Eto (Orchestrator), caught while confirming a clean tree before a re-push. **Fix shape:** ignore the directory in `.gitignore`, beside the other runtime-state entries and with a one-line reason, since the file ships. Consider whether it belongs under `logs/` with the rest of the transient state instead — that is a placement question for the founder, not a prerequisite. **Witness:** a bootstrap or scenario that dispatches a deferred bookend must leave `git status --porcelain` empty; red before the ignore line. **What was to be tested:** In a project that has dispatched a subagent, run `git status --short` — it must be EMPTY. The deferral slot directory `subagent-defer/` is created at the state root, which is the repo root, and nothing ignored it, so every derived project would have carried a permanently dirty status — which quietly erodes the clean-tree precondition the gate, `blueprint drift` and every push check rely on. The witness copies the SHIPPED `.gitignore` into a real repo, asserts clean, fires a real dispatch, and asserts clean again. **Status when accepted:** **FIXED — landed `baf52bc` and `f78392b`, pushed 2026-09-15** |
| **BUG-130** | **The DoD gate refuses the cancellation the DoD prescribes: an item closed with a findings pointer has no row, so rule 1 blocks the push that closes it.** Observed 2026-09-16: a 161-commit push failed at `§1b·1 every item has a backlog row` with `These items have NO backlog row anywhere: TASK-23`. TASK-023 was cancelled exactly as `docs/DoD.md:33` specifies — *"cancellation (delete + one-line pointer in `docs/config/findings.md`)"* — and `F-003` records what was lost and why. `dod_find_row` (`scripts/lib/dod-gate.sh`) searches only the lifecycle row files, so a correctly cancelled item is indistinguishable from work with no item at all. **The stage's own comment shows the gap was never considered:** it explains that it asserts existence rather than folder placement "without guessing intent", and cancellation is a third state it does not model. **It is not a one-off:** A-13 was cancelled into `F-004` the same day; it escaped only because its number never appears in a commit subject. Any future cancellation whose closing commit names the item hits this. | S2 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-16 by Eto (Orchestrator), who instructed the cancellation that triggered it. **Fix shape:** teach `dod_find_row` that a `findings.md` pointer is a valid record of an item — a cancelled item HAS an item, recorded where the rule says to record it — and report it as `cancelled` the way a row outside `doing/` is noted rather than failed. Reproducer first: a push naming an item whose only record is a findings pointer must pass, and a push naming an item with no record anywhere must still fail. Check `dod_items_in_push`'s normalisation (`TASK-23` vs `TASK-023`) matches however findings entries name items. **What was to be tested:** Cancel a backlog item the way the DoD prescribes — delete its row, leave a pointer in `docs/config/findings.md` — and push the commit that closes it. The gate must ACCEPT it, reporting the item as `cancelled` in a note. Before this it refused: `dod_find_row` searched only the row files, so a correctly cancelled item looked identical to work with no item at all, and a 161-commit push was blocked on TASK-23. Then confirm the rule still has teeth: a commit naming an item with NO record anywhere must still fail §1b·1. **Status when accepted:** **FIXED — landed `54849ea` and `e54a97c`, pushed 2026-09-15** |
| **BUG-129** | **The activity feed's idempotency guard does not hold: four `agent-activity.sh --supervise` daemons are running at once, and their log rotation turns a push-gate suite red.** Observed 2026-09-16 by Eto: `pgrep -af agent-activity.sh` returns PIDs 1154186 (started 11:37), 3275474, 3373642 and 3423373. CLAUDE.md §"On wake" states the opposite — *"it is idempotent (a `flock` makes a second call a no-op)"* — and instructs every wake to call it, so the guard is the only thing standing between that instruction and unbounded daemons. **This repo has the precedent:** BUG-001 was a broken idempotency guard whose every-wake spawning left this machine at load 175 for 2.7 days. **Already costing a gate:** Philipp (Infrastructure-1) traced `subagent-feed` #9 to the team's own feed rotating the log mid-scenario — the real-state canary hard-fails on feed truncation while only NOTING concurrent baton writes, so the suite goes red for something the pushed change never touched. His child only ever wrote to the fixture's `AGENT_FEED_LOG`. | S2 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-16 by Eto (Orchestrator). **Two defects, and they should not be conflated:** (1) the `flock` guard in `scripts/agent-activity.sh` does not prevent a second supervisor — find whether the lock is taken on the wrong path, released too early, or skipped on the `--supervise` path, and reproduce with two concurrent starts before fixing; (2) the canary's asymmetry — feed truncation hard-fails while baton writes only note — which makes every suite run under a live feed a coin toss. Fix (1) first; (2) may be a note-and-continue change, but decide it with the evidence. **Do not kill the running daemons before capturing what started them** — the founder's session state is the only record of how four came to exist. **EVIDENCE CAPTURED 2026-09-16 12:44 by Vitali, daemons left running:** all four are PPID 1 (`start_daemon` uses `setsid`, so the tree cannot say which session started them — start times are the only record), and each holds a DIFFERENT lock: 1154186 cwd `blueprint` → `blueprint/logs/.agent-activity.lock` (Sep 15 11:37:45; its fd 255 reads `scripts/agent-activity.sh (deleted)`, so it is running a since-replaced inode); 3275474 cwd `linkedin-watcher-agent` → that repo's lock (14:10:11); 3373642 cwd `storm2flow` → that repo's lock (14:24:35); 3423373 cwd `struct2flow-www` → that repo's lock (14:27:18). Two `tail -F logs/agent-activity.log` survive with real parents — 1851515←1333482 and 3388557←3385380, both VS Code terminal bash under 2464117 — the only live link back to sessions. Real feed at capture: size 397769, inode 81657865, mtime 12:47:57. **DEFECT (1) DOES NOT REPRODUCE, and no fix was made.** `lock_file="$BP_STATE_ROOT/logs/.agent-activity.lock"` is per-repository by design (BUG-077), so four projects legitimately means four supervisors, exactly one each; `pgrep -af agent-activity.sh` spans repositories, and four instances of one repo was the reading, not the fact. `tests/agent-activity-bound` passes 36/36 today including **#1 "50 concurrent starts → exactly 1 supervisor"** and #6, so the guard is both correct and already pinned. CLAUDE.md's idempotency claim is true *per repository* and would be worth saying so. **DEFECT (2) IS REAL and is the only defect here.** The real feed has exactly two writers: `emit` appends (`agent-activity.sh:336`) and `supervise_body` truncates once at startup (`:557`). That is not periodic rotation — it is ONE wipe per supervisor start, in place, preserving the inode, which is why `tail -F` survives and why Philipp's mechanism description is right. So the flake strikes when a wake or restart coincides with a suite run, and CLAUDE.md instructs every wake to start the feed. **Philipp (Infrastructure-1):** *"`tests/subagent-feed` #9 fails intermittently with 'The scenario mutated real state outside its fixture: activity feed was rewritten or truncated (`logs/agent-activity.log`)', although no scenario writes there… the canary already handles the same class correctly for the baton, where a concurrent `signal-set.sh` write emits a CANARY-NOTE and the run continues; feed rotation should get the same asymmetry… Until then this suite goes red in the push gate for reasons unrelated to the change being pushed, which is the failure mode the canary itself exists to prevent people from shrugging at."* **PROPOSED, not decided (Vitali):** the canary's append-only model is CORRECT and the feed breaks its own contract once per restart, so the preferred fix is to stop truncating — emit a "feed restarted" banner instead of `: >"$out"` — which makes the claim true and keeps feed truncation a hard failure that still means something. If unbounded growth must be capped, rotate by rename at a size cap, but note that a rename also trips the prefix check, so rotation implies the canary note anyway. **Fallback if the wipe stays:** mirror BUG-068's journal witness — downgrade to `CANARY-NOTE` only when the post-change content BEGINS with the supervisor's own start banner (`[agent-activity] feed started →`) and the scenario's escape token is absent, keeping the hard failure otherwise. **Can the two be told apart? Yes, by that banner witness** — with the same residual BUG-068 already documents (a fixture could forge it), which is precisely why the preferred option is better: it removes the false red at the source instead of teaching the guard to excuse a class of writes. **ORDER — the brief's worry is moot:** the guard is sound, so the canary is not a symptom hiding behind a cause, and there is no green run that could justify keeping the asymmetry. **APPROVED AND FIXED 2026-09-16 (coordinator):** the wipe is gone — `supervise_body` now emits `feed restarted` over a non-empty log and `feed started` over an empty one, so the feed keeps the append-only contract the canary already assumes, the canary needs no new tolerance, and feed truncation stays a hard failure that still means something. Regression: `tests/agent-activity-bound` #20, which stages the real script, a real second supervisor start and the real canary over a fixture feed. **THE REPRODUCER'S OWN VACUITY TRAP, recorded because it was measured rather than foreseen:** the first version PASSED against the unfixed script — the fixture is deterministic and fast, so both supervisors emitted the same banner and baton line inside the same second and the rewritten bytes were IDENTICAL, leaving the prefix check nothing to see. Live, the starts are minutes apart, which is why the same wipe reds `subagent-feed` #9 on the founder's host and redded nothing in a fixture. #20 now appends a history marker a restart cannot reproduce and asserts it is present before capturing, so the observation cannot go vacuous again. **THAT CLAIM WAS FALSE, AND CODEX MEASURED IT (S2 rework, 2026-09-16).** This row said "the feed now grows across restarts. Size-capped rotation is NOT built". Size-capped rotation HAD been built all along: `scripts/lib/feed.sh` rotated at 4,000 lines by `tail` to a temp file followed by `cat >"$_fa_log"`, and every hook and pipeline producer calls that appender on the same feed. So only the STARTUP half of BUG-129 was fixed, and two consequences survived: the canary could still report an innocent live feed as "rewritten or truncated" (the flake this row exists to kill), and **a concurrent append during rotation was LOST** — Codex appended a line after `tail` took its snapshot and before the rewrite, and it vanished. Nothing coordinated the rewrite with any other writer. The live feed in this checkout was 3,699 lines against the 4,000 cap, so this was imminent rather than theoretical. **THE FIX: ROTATE BY RENAME.** `mv` to `<feed>.1`, then one marker line into the new file. Chosen over the two alternatives on evidence: a LOCK was rejected because feed.sh is POSIX sh sourced by `#!/bin/sh` hooks and `flock` is absent on macOS — it would put a tool the platform may not have on the one path that must never fail a push — and rename needs no coordination anyway, because a writer holding the old file appends into the archive while a later one appends into the new file, so nothing is dropped either way. Rename also keeps the feed APPEND-ONLY in the sense the canary needs: history MOVES rather than being deleted, so archive+live still begins with whatever was captured. **The canary is now archive-aware** (`tests/harness/canary.ts`): a rotation is a `CANARY-NOTE`, never silent, while a rotation that LOST or rewrote history still fails, and the escape token is searched in the archive too so rotating cannot launder a leaked line out of view. **THE INODE PREMISE THAT FORBADE `mv` WAS ALSO FALSE**, and it is what made the in-place rewrite look mandatory: `emit` is `printf '%s\n' "$1" >>"$out"`, which REOPENS per line, and the only `exec N>` in `agent-activity.sh` targets its lock file. Nothing tracks the feed by descriptor. Consequence paid in the same commit: every doc that told a reader to follow the feed now says `tail -F` (follow by name), and `AGENT_FEED_KEEP_LINES` is gone with the trim that used it. Regressions: `tests/pipeline` #18 (rotation moves history, does not delete it) and #18b (an append racing the rotation is not lost, proved with a `cat` shim that holds the window open), plus a `tests/harness` canary case for the note. **FOLLOW-UP HELD BY THE COORDINATOR:** PID 1154186 runs a deleted inode of `scripts/agent-activity.sh` (pre-BUG-124 code, including the old labelling), so this repo's feed needs restarting after the push for the founder to see the fixed version — live state, not code. **What was to be tested:** Restart the feed (`bash scripts/agent-activity.sh --stop`, then `--daemon`) while a test suite is running: the suite must NOT go red. The feed is append-only now — a restart writes `feed restarted` instead of wiping the log, and rotation renames history to `logs/agent-activity.log.1` with a marker instead of overwriting in place, so a concurrent line can no longer be lost. The real-state canary reports a rotation as a NOTE and still hard-fails on genuinely lost history. **Also corrected:** four `agent-activity.sh` processes on this machine are four REPOSITORIES, one supervisor each — the lock is per repository by design, and CLAUDE.md now says so. **Status when accepted:** **FIXED — landed `a2d7ca1`/`65b0492`, rotation `8e9e21e`/`713d291`, wording `2010ffd`, pushed 2026-09-15** |
| **BUG-127** | **[SEC] `blueprint a2bp` files a request with no secret scan when `gitleaks` is absent, and misreports an unsupported scanner command as a found secret.** From Alexey's (Codex) re-check of TASK-037, `.scratch/ALEXEY-task037-recheck.md` findings 1 and 6. TASK-037 is accepted and pushed, so its defects are carried here rather than reopening it. **(1)** `scripts/lib/request-inputs.sh:126` returns success when gitleaks is missing, matching the pre-push gate's skip. With gitleaks off PATH, both private-key probes (a key in `notes/deploy.txt`, and one in managed `docs/DoD.md`) reached the fetch; only a transport-denying shim stopped them. A CI scan of the PR runs after upload: it can refuse the merge, never undo the disclosure. Filename and ignore guards still apply, so this is the content check only. **(2)** `request-inputs.sh:137` treats every scanner exit 1 as a finding. A shim printing `unknown command dir for gitleaks` and exiting 1 produced "gitleaks found a secret" with rotation advice. It still exits 4 before transport, so it is false attribution, not a bypass. | S2 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-16 by Eto (Orchestrator). **Fix shape:** require a working local scanner before a request is filed (a2bp is a push to a remote, unlike the local gate), and separate "scanner cannot run" from "secret found" — check command support, or classify a non-findings failure as an incomplete scan. Reproducer first, for both: gitleaks off PATH, and an unsupported-command shim. **What was to be tested:** In a derived project, take `gitleaks` off PATH and run `blueprint a2bp <file>` — it must REFUSE (exit 4, no branch pushed) and name `bash scripts/install-toolchain.sh`, rather than filing bytes nothing scanned. The pre-push gate still SKIPS when gitleaks is missing, and the difference is the point: the gate's skip keeps unscanned bytes on your machine, while a2bp publishes them to the blueprint's remote, where a later CI scan can refuse the merge but cannot un-disclose them. Then check the second half: a scanner that cannot run (an older gitleaks without `dir`) must be reported as an incomplete scan, not as 'gitleaks found a secret' with rotation advice. **Status when accepted:** **FIXED — landed `8747b88` and `0217cbc`, pushed 2026-09-15** |
| **BUG-124** | **The activity feed labels a Claude subagent's start/finish markers with its agent type (`general-purpose`), and labels a helper that a persona spawns by the helper's type (`claude-code-guide`) instead of by the persona that started it. So a persona's work can be invisible to the founder watching the feed.** Observed 2026-09-15. The founder could not see Christian's activity. The feed did hold 88 `[Christian - Claude Code]` lines (23:03–23:22), but its `→ dispatched`/`← finished` markers read `[general-purpose - Claude Code]`, and the helper Christian started to research `@`-imports wrote 43 lines as `[claude-code-guide - Claude Code]`. `scripts/log-activity.sh:105-151` resolves the persona with `bp_roster_name_in_text` over `$summary`. On the bookend events that text evidently carries no roster name, so the label falls back to `.subagent_type`. For a nested helper, nothing ties the helper to its parent persona. Codex lines are unaffected, because the dispatcher labels them. | S3 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-15 by Eto (Orchestrator) from the founder's report; **promoted the same day on founder decision** ("File and fix now"). **Fix shape:** find which hook payload field carries the dispatch description, or the transcript meta the streamed lines already use, so the bookends resolve the same persona as the streamed lines. Label a nested helper `[<parent persona> › <type> - Claude Code]`, or as the parent. Reproducer first, with a recorded hook payload. **What was to be tested:** Watch the team feed (`tail -F logs/agent-activity.log`) while an agent is dispatched: the `→ dispatched` and `← finished` markers must carry the PERSONA name, and a helper an agent starts must read `[<persona> › <type> - Claude Code]`. Before this they said `general-purpose` and `claude-code-guide`, which is why Christian's work looked invisible. **On a Mac:** `bash scripts/install-toolchain.sh` now installs `flock` (util-linux); without it the feed says once that deferral is unavailable and labels by type. **Cross-provider review, twice:** the burst cap was a check/create race (twelve concurrent hooks took twelve slots), then its reclamation was non-atomic (two owners, one slot). Slots are now reserved by `mkdir` under one lock spanning reclaim, reserve and spawn; the owner's pid is recorded rather than its age. **Status when accepted:** **FIXED — landed `51d4978`/`fc64966`, rework `f479e85`/`6df5c56`, race `5abbd03`/`7f44bf2`, macOS `6285fbe`/`9af5e74`, pushed 2026-09-15** |
| **BUG-126** | **[SEC] The pre-push semgrep stage passes a partial scan as clean: it never reads semgrep's `.errors`, so code semgrep could not parse is approved unanalysed.** Reported 2026-09-16 by storm2flow's orchestrator (Sylvia), from Jesko's (Codex) review of storm2flow's CodeBuild copy of `_st_semgrep`. `.githooks/pre-push:376-404` classifies a run as clean when semgrep exits 0 and `.results` is an array with zero entries. `.errors` is never inspected. Reproduced with the pinned semgrep 1.171.0: an offline JS rule (`pattern: eval(...)`, WARNING) run over a file containing `function broken( {` followed by `eval("x")` exits **0** with `results: []` and an `errors` entry of type `PartialParsing`. The gate reads that as clean and never retries. **CI likely shares the hole:** `.github/workflows/security.yml:60-65` runs `semgrep scan --error`, which fails only on findings. | S2 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-16 by Eto (Orchestrator); started at once under CLAUDE.md §"Security is a main concern" capability 4 (the agent fixes security findings autonomously). **Fix shape:** a non-empty `.errors` makes the run incomplete. Keep the `--jobs 1` retry, and fail closed if errors remain. Any tolerated diagnostic becomes an explicit reviewed policy (by error type and path), never silent acceptance. Apply the same classification in CI. Reproducer first: the malformed-file case above must not classify as clean. **What was to be tested:** Put a file semgrep cannot fully parse into a project — a `.js` file with a syntax error plus `eval("x")` is the reproducer — and push. The gate must BLOCK, saying the SAST scan is INCOMPLETE and naming the error type and path, where before it printed 0 findings and passed. Then check the accepted class is still only shell: the gate reports N shell parse errors accepted (20 here) and passes. CI applies the same rules through the shipped `scripts/semgrep-verdict.sh`, which the hook sources and the workflow executes, so the two cannot drift. **Cross-provider review:** Codex — push as is; `#ci` parses the real workflow YAML and runs its step, and the hook fails closed when the script is missing. **Known limit, parked as BUG-128:** a file semgrep never opened lands in `paths.skipped` with no error entry, and nothing reads that key yet. **Status when accepted:** **FIXED — landed `ab38a96`/`6a08518`, decoding `d97d370`/`4177121`, extraction `b683eb9`, budget `4f23962`, pushed 2026-09-15** |
| **BUG-125** | **doc-links accepts a link that leaves the repository whenever its target happens to exist on the local disk.** Reported 2026-09-15 by storm2flow's orchestrator (Sylvia), under her founder's rule that "checks read only from the repository". `tests/doc-links/doc-links.ts:111` checks a target with `stat(resolve(dirname(file), target))`. A link like `../../../../blueprint/docs/DoD.md` in storm2flow's docs therefore passes on any machine with a sibling blueprint checkout. The same link is dead in a fresh clone, in CI, and for every other reader. storm2flow's founder caught one such link the same day. | S3 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-15 by Eto (Orchestrator) from storm2flow's report. It sits in the suite Christian is changing for TASK-045, so he takes it next. **Fix shape:** normalise each target against the repo root, fail any that climbs out, and check whether resolving against tracked files (`git ls-files`) is needed so an untracked local file cannot satisfy a link. Reproducer first: a doc linking `../../outside.md` must be reported even when that file exists outside the repo. **What was to be tested:** In a project's docs, link a file OUTSIDE the repository (`../../outside.md`) while that file exists on disk — the doc-links check must report it, because a clone would not have it. Same for a symlink pointing out, a symlinked web root, and a symlink escaping the served directory into another repo folder. A symlink that stays inside the repository must still resolve, and so must a file the project gitignores but has locally (`CLAUDE.md`) — accepting those is deliberate: requiring tracked files broke every fresh bootstrap, since managed docs link the methodology files. **Cross-provider review:** Alexey — push as is; the resolve-then-stat window is gone because the patch removes `stat`. **Known limit:** a doc linking a gitignored file passes here and is dead in a clone; TASK-046 is the fix for that. **Status when accepted:** **FIXED — landed `f4fe83d`, revised in `3a07a46`, containment hardened in `02e1f31`, pushed 2026-09-15** |
| **BUG-122** | **A full `blueprint pull` that refused a file still advances `bootstrap_sha`, so a project with a refused file reads as synced.** From storm2flow, `FR-storm2flow-refused-pull-sha`. `scripts/blueprint` sets `refused_guard=1` when a file is refused (`:1611`, `:1634`, `:1712`), but that only changes the exit status to 4 (`:1765`). The update at `:1727-1748` advances `bootstrap_sha` whenever at least one file was pulled and no paths were named, yet its own comment defines the value as "synced UP TO this commit". A partial pull already leaves it unchanged. | S2 | **ACCEPTED by the founder 2026-09-16** — accepted with everything else waiting for acceptance ("accept everything in waiting-acceptance"). A regression re-opens the row. | Filed 2026-09-15 by storm2flow; **promoted the same day on founder decision** ("Start all now"). **Fix shape:** leave `bootstrap_sha` unchanged when any file was refused, and name the files that held it back. Reproducer first: a full pull with one refused file must leave `bootstrap_sha` unchanged. **What was to be tested:** In a derived project, run `blueprint pull` and skip one file at the prompt (or let a guard refuse one, or stop the run with `q`). `.blueprint-source`'s `bootstrap_sha` must be UNCHANGED, and the output must name the files that held it back: `bootstrap_sha left unchanged — these files were not synced: …`. A clean full pull that lands everything must still advance it. **Cross-provider review:** Alexey (Codex) — push as is; he confirmed all four unsynced paths hold the SHA and that a clean pull is not suppressed. Known limit he noted: `q` records only the file it stopped at, and a no-op full pull after completing everything piecemeal still leaves the SHA stale — neither is new. **Status when accepted:** **FIXED — landed `f4b1c25`, reproducer `da4f7b2`, pushed 2026-09-15** |
| **BUG-121** | **Any Codex sandbox on the machine can turn the whole push gate red, because scenario workspaces default to `/tmp`, and since TASK-028 the harness refuses every scenario while a `/tmp/.git` exists.** Observed 2026-09-15 on a docs-only push (`b2e3b49`, 52 of 55 suites red, each failing in ~0 ms) and reproduced by re-running the gate's own vitest batch: `/tmp/.git` was absent at the start, appeared mid-run (commit-msg-gate refused 12 cases with "Project marker above every scenario workspace: /tmp/.git"), and was gone again at the end. **No Codex ran in this checkout.** Running elsewhere on the same machine: storm2flow's `scripts/codex-signal-watch.sh`, which dispatches Codex runs on its own baton flips, and VS Code Codex app-server sessions. BUG-110 attributed the empty `/tmp/.git` to Codex's workspace-write sandbox, so the practical rule "do not push while a Codex review is running" cannot be kept from this checkout: the source is any project on the machine. **The blast radius grew with TASK-028:** before its preflight a stray marker turned `state-root` #A6 red; now it refuses every scenario, which is the correct refusal inside a design that still puts fixtures under a shared `/tmp`. **Workaround:** run the gate with `TMPDIR` pointed at a private directory with no marker above it (`/home/luiz/.cache/bp-harness-tmp`), which is what the refusal message itself says and what every manual run on 2026-09-15 used. | S2 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found 2026-09-15 by Eto (Orchestrator). **Promoted 2026-09-15**; Philipp (Infrastructure-1) is fixing it, reproducer first. The shape agreed at promotion: the gate and CI should not depend on what other processes do to the shared `/tmp`, e.g. scenario workspaces default to a private base (under `XDG_CACHE_HOME` or `~/.cache`) unless `TMPDIR` is set deliberately, with the preflight kept as the guard for a marker above THAT base. **Review fix (Jesko, S2):** `mkdir` with mode 0700 sets nothing on a base that already exists, and the real default base here was 0775, so "private" held only for a base the harness created. The default base is now checked with `lstat`: an existing one with a looser mode is set to 0700, and a symlink, a non-directory or a base owned by another user is refused with a message naming the path. An explicit `TMPDIR` is never changed, and the BUG-110 preflight stays the guard there. **Re-check fix (Jesko, S2):** `chmod`, `realpath` and `mkdtemp` look the path up again after `lstat`, so anyone who could replace entries in a directory above the base could swap it for a symlink in between. Before the base is trusted, every directory from the realpath of its parent up to `/` must be owned by the current user or root, and must not be writable by group or others unless it has the sticky bit; otherwise the harness refuses, naming that directory. Every later step uses that resolved path. **Gate fix:** that check blocked the push, because bootstrap-gate runs a fresh project's harness with TMPDIR set to a scenario's `tmp`, and the harness created that directory with the umask, 0775 under 0002. Every directory a scenario's workspace creates is now 0700 whatever the umask. **What was to be tested:** While a Codex run is active in ANY project on this machine (it leaves an empty `/tmp/.git`), run `git push` or `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA tests/node_modules/.bin/vitest run --root "$PWD/tests" harness state-root` WITHOUT setting TMPDIR — scenarios must not be refused; they now live under `~/.cache/bp-harness-tmp`, and `stat -c %a ~/.cache/bp-harness-tmp` must print `700` afterwards even if you `chmod 775` it first. Setting `TMPDIR` to a directory under a stray `.git` must still be refused with the marker named. With TMPDIR unset, pointing `XDG_CACHE_HOME` at a directory whose parent is `chmod 777` (no sticky bit) must be refused, naming that parent. **Status when accepted:** **FIXED — landed `a528fb8`, `87407c1` and `dbed972`, pushed 2026-09-15** |
| **BUG-120** | **A signal can still let the refresh fetch START after drift or pull was interrupted, and the run then waits out the whole fetch budget — `tests/sync-by-address` #20c goes red under gate load.** Seen on two consecutive gate runs on 2026-09-15 (08:06 and 08:29, both docs-only commits over `d984a45`), each with *"a fetch started AFTER the run was signalled — cleanup could not see the refresh child"*. `6c024f1` launches the refresh as one process so `$!` is the fetch itself, and #20c holds that child on a FIFO at its `fetch.err` before anything runs. The comment and plan §9.2 stated the remaining window as a TERM lost between bash's fork and its signal reset, costing at most the budget. **The mechanism, measured, is neither that nor the inherited-user-trap guess in the first draft of this row.** (1) A child held at its stderr FIFO ignores INT and holds only BASH'S OWN terminating handler for TERM (`/proc` SigIgn 0x6, SigCgt 0x4000), and dies of a TERM sent while it waits (E1). (2) A trap never ran between `cmd &` and `$!` in 1000 tries, pinned and unpinned: cleanup always knew the child (E2). (3) Every red run, classified by duration, lasted ~3010 ms after the signal, which is `BP_FETCH_TIMEOUT=3`: cleanup's TERM left at 0.2–1.2 ms and the fetch SURVIVED it (30/30). So: when the release and cleanup's TERM both land before the child runs, its FIFO open SUCCEEDS, bash's handler only RECORDS the TERM, and the exec into `env` discards the record. The fetch starts after cleanup has signalled it, and cleanup's `wait` sits out the budget (30 s in production). **Rates:** #20c pinned to one CPU with `taskset` — 40/40 red on an unmodified copy, 90/90 on instrumented copies; unpinned, beside one other pinned loop — 48/60 red. A SIGSTOP seam cannot reach the window: on CONT the pending TERM interrupts the open (EINTR) and bash dies of it (probe). **What it costs:** a fetch into the per-machine cache after the operator's Ctrl-C, and a run held up to 30 s; no project or config write, since those are shielded, and the command still dies. **What it costs the team now:** a gate stage that fails on any push under load. | S2 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found 2026-09-15 by Eto (Orchestrator) on the lifecycle push after TASK-025 landed. **What differs between the passing and the red runs is contention, not ordering or cache state.** The TS suites run as one parallel vitest batch, so #20c always runs beside bootstrap-gate's nested gate; that was equally true in the passing runs. #20c creates its own cache in its scenario HOME, so no cache state carries between runs. What moved is CPU time: sync-by-address took 17.7–17.9 s in the passing gates overnight, 22.5 s at 07:47 (passed), and 25.2 s and 25.3 s in the two red gates, beside a 270 s bootstrap-gate. The loss needs the test's release AND cleanup's TERM to reach the child before it next runs, which a busy CPU makes likely and one pinned CPU makes certain; the 816/816 full run and CI had spare cores. **Promoted 2026-09-15**; Christian (Senior Architect) is fixing it, reproducer first, with a design in which a fetch cannot start after cleanup has signalled it. **If you retry a red push on #20c before the fix lands, cite this row.** Evidence scripts: `.scratch/b120/`. **FIXED 2026-09-15 by Christian (Senior Architect), not yet pushed.** The fetch is gated after its first exec on a token that cleanup empties with a builtin BEFORE its TERM, so a TERM lost before that exec meets a revoked token, and one sent after it cannot be lost. Reproducer #20d: red 10/10 pinned on the parent, every run sitting out the 3 s budget. After the fix, pinned to one CPU: #20c 50/50 and #20d 50/50 green; unpinned: #20c 50/50 green. #20e pins the orderings from the code's text. A first version that revoked with `rm -f` was 7/50 red pinned (the fork yielded the CPU to the child). **What cannot be closed, stated:** a child released at the instant the run is signalled can pass the gate before the run's handler begins; that fetch is killed at once, never waited out. **What was to be tested:** Run `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" sync-by-address` a few times — #20c, #20d and #20e must pass every time, including while the machine is busy (the gate runs them under load, which is where #20c failed). Re-open if a push goes red on #20c or #20d again. **Status when accepted:** **FIXED — landed `7981fcc`, pushed 2026-09-14** |
| **BUG-119** | **`tsc --noEmit` over `tests/` failed, and two harness helpers rebuilt their option objects field by field.** `tests/code-root/code-root.spec.ts`'s `runHook` returned and #3 asserted on `childRoot`, but its declared return type omitted it: two long-standing TS errors (TS2353, TS2339). Nothing noticed, because no gate stage and no CI job runs `tsc`; vitest strips types without checking them. Separately, `s.runScript` (`tests/harness/index.ts`) and `startWatcher` (`tests/harness/watcher.ts`) copied named fields into a new options object instead of spreading, while `run()` and `background()` spread. Today every declared field is forwarded, so no option is lost at runtime. The rebuild still passes an explicit `undefined`, which `exactOptionalPropertyTypes` rejects, and any field added to `SpawnOptions` would be dropped silently. | S3 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found by linkedin-watcher-agent (PR #68) and confirmed on current main by Jesko (QA-2, Codex); ported under TASK-028 by Philipp (Infrastructure-1). **Fixed:** the return type declares `childRoot`, and both helpers spread `...options` before their defaults. **Witness:** `tsc --noEmit -p tests` printed the two errors on main and is clean after, also under `exactOptionalPropertyTypes`. **Regression control is weak, and that is stated rather than hidden:** it is `tsc` itself, which nothing runs automatically. Test-only change with no production code, so DoD §3's test-only exception applies to the reproducer commit. Re-open if `tsc --noEmit -p tests` fails again. A typecheck stage in the gate or CI is the real fix for recurrence and is not part of this row. **What was to be tested:** Run `tests/node_modules/.bin/tsc --noEmit -p tests` — it must exit 0 with no TS2353 or TS2339 on `childRoot`. Note: no gate stage runs tsc yet (TASK-031). **Status when accepted:** **FIXED — landed `7d6b99a`, pushed 2026-09-14** |
| **BUG-118** | **`Bash(aws codepipeline put-approval-result *)` sits in `.claude/settings.json` `permissions.allow`, with nothing in `ask` or `deny` overriding it, so an agent in any derived project can approve a PRODUCTION deployment with no confirmation.** A manual approval action exists to put a human in the loop, and the editor's permission prompt is where that human stands — while the pattern is auto-approved, the gate approves itself. `settings.json` is a managed file, so the entry fans out to every project on pull. **Fix (founder's edit — agents may not modify `.claude/settings.json`):** remove the line from `allow`, add `"Bash(aws codepipeline put-approval-result *)"` to `ask`. MOVED, not deleted: approving from the editor is the point, it just has to be confirmed. | S2 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Surfaced from storm2flow via PR #64 (branch `bug/110-approval-result-ask`, numbered BUG-110 there, which this repo had already allocated), after storm2flow granted its agent user `codepipeline:PutApprovalResult` — before that no identity could reach the path, so the entry sat unexercisable and unnoticed. Rebuilt on main rather than merged, by founder decision 2026-09-14; not an `a2bp` request because storm2flow's `settings.json` is ~5 months stale and whole-file adoption would have deleted every `deny` guard while keeping the offending entry. Regression test: [`tests/permission-policy`](../../tests/permission-policy/permission-policy.spec.ts) — parses the real `settings.json` and matches on the substring `put-approval-result`, because storm2flow already spells it `put-approval-result*` and a test recognising one spelling goes green on the other. Rowed 2026-09-15 by Vitali (QA-1). Re-open if any `put-approval-result` spelling reappears in `allow`. **What was to be tested:** Open `.claude/settings.json` — `"Bash(aws codepipeline put-approval-result *)"` must be in `permissions.ask` and in no other list, so an agent is prompted before approving a production deployment. Then `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" permission-policy` must pass 3/3. In a derived project, pull `tests/` and `.claude/settings.json` together. **Status when accepted:** **FIXED — landed `903117f`, pushed 2026-09-14** |
| **BUG-117** | **CI refuses 697 of 770 TypeScript tests, because the workflow starts vitest without the scrub the gate applies.** Since `8ee949f` (TASK-025 commit 1, `6ed93b1`, H5), `assertProcessEnvClean` refuses a test process carrying ANY undeclared `GIT_*` / `AGENT_*` name, so a direct run and the gate's runner remove the same population. The gate runs vitest through `scripts/run-ts-suites.sh`, which unsets every `GIT_`/`AGENT_`/`BP_` name and `BLUEPRINT_ROOT` first. `.github/workflows/security.yml`'s ts-tests step runs `npx vitest run` directly, so it inherits the runner's environment — and GitHub-hosted runners export `AGENT_TOOLSDIRECTORY`. Run 34902101002 failed every scenario with *"The test process is carrying variables that must never reach a fixture: AGENT_TOOLSDIRECTORY"*. **Why the local gate could not see it:** the gate never executes the workflow's command. It runs the bridge, which scrubs, and `tests/manifest` #5 reads the workflow as TEXT and checks that it names a blanket vitest run, not the environment that run receives. **Not fixed by declaring `AGENT_TOOLSDIRECTORY` inert:** the scrub boundary is the prefix population, so naming one runner variable leaves the next one to break CI again. The two execution modes must share ONE scrub. **Regression:** `tests/ts-bridge` #3 parses the workflow, executes every step that starts vitest against a recording stub with undeclared `AGENT_`/`GIT_`/`BP_` decoys and `BLUEPRINT_ROOT` exported, and requires the stub to see none of them — the class, not the name. | S1 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found 2026-09-15 by the Orchestrator from CI run 34902101002, root cause confirmed by Christian (Senior Architect) against the workflow and the bridge. Re-open if any CI job, hook or script starts vitest or a harness scenario without going through `ts_scrubbed`. **What was to be tested:** Run `gh run list --repo LuizStruct2Flow/blueprint --limit 3` — the newest `security.yml` run's "TypeScript regression suites (vitest)" job must be green, not refusing tests for `AGENT_TOOLSDIRECTORY`. Then `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA tests/node_modules/.bin/vitest run --root "$PWD/tests" ts-bridge` must pass; #3 executes the workflow's vitest steps with decoy `AGENT_`/`GIT_`/`BP_` variables and goes red if the step stops using the gate's scrub. **Status when accepted:** **FIXED — landed `3036646`, pushed 2026-09-14** |
| **BUG-115** | **The blueprint had no CI for four days: GitHub could not parse `.github/workflows/security.yml`, so every push since 2026-09-10 21:06 UTC ran zero jobs, while every local gate reported green.** `44bcdf7` (TASK#18 phase 2) rewrote the `ts-tests` job's last step to `run: echo "::notice::… Restore it with: blueprint pull tests/"`. That value is an unquoted YAML plain scalar, and a plain scalar cannot contain `: `, so the file is invalid YAML (`mapping values are not allowed here`, line 256, column 301). GitHub rejects the whole file and creates no jobs, and `gh run view` shows only "This run likely failed because of a workflow file issue". Last green run: `8baa10f` (2026-09-10 19:11 UTC). First zero-job run: `a9813c5`, the first push that contained `44bcdf7`. `dc845f1` was suspected at first, but it only touched the file again while it was already broken. **Why nothing caught it.** (1) The pre-push gate cannot see CI. It prints "watching CI" and the push exits 0, so a CI that cannot start looks the same as one that is running. (2) `tests/manifest` #5 reads this file every push to prove CI invokes every suite, but it reads it as TEXT. An unparseable workflow still contains `npx vitest run`, so #5 passed while CI executed nothing. The control that claims "every suite runs in the workflow" had no way to tell that no job runs at all. **Fix:** quote the step properly (a block scalar). **Regression:** new manifest check #5b parses every file in `.github/workflows/` with a real YAML parser (`yaml`, now a harness devDependency) and requires the minimum Actions shape: an `on:` trigger, a non-empty `jobs:`, and for each job either `uses` or `runs-on` plus steps that each have exactly one of `run`/`uses`. It covers the class, not the single character. #5b runs against the real tree (`#live`), against the fixture, and inside a bootstrapped project (bootstrap-gate #6). | S1 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found 2026-09-14 by the Orchestrator reading `gh run list`, and diagnosed by Philipp (Infrastructure-1). Re-open if any CI run shows zero jobs, or if a check proves a workflow invokes something without first proving that GitHub can parse the workflow. **What was to be tested:** Run `gh run list --repo LuizStruct2Flow/blueprint --limit 3` — the newest `security.yml` run must have jobs (not "a workflow file issue"). Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" manifest` must pass; putting an unquoted `: ` back into any `run:` line of the workflow turns #5b red. **Status when accepted:** **FIXED — landed `d27e1db`, pushed 2026-09-14** |
| **BUG-113** | **`drift`, `pull`'s file selection and `pull`'s preview each compare a marker file differently, so they disagree about the same file.** All three compared WHOLE files, while the merge `pull` actually performs keeps everything outside the managed regions. Reproduced: a project's own guards after the end marker made `drift` report the hook drifted, made `pull --yes` select it, and made the preview show those guards as DELETED — then the merge kept them and printed `✓ Pulled 1 file(s)`. Every derived project with guards in `.githooks/pre-push-project` is in this state. PR #66 (U6) fixed drift alone, which Jesko's review showed leaves drift clean while pull still selects and previews damage. | S2 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found by struct2flow-www (PR #66, U6), made one-caller-proof by Jesko (QA-2, Codex). TASK-026. Regression: `tests/marker-merge` #7 (drift), #8 (selection), #9 (preview), #10 (drift names a file pull would refuse). Re-open if any of the three computes its answer other than through the one prospective-pull result. **What was to be tested:** In a derived project whose `.githooks/pre-push-project` has its own guards after the end marker and an up-to-date managed region, `blueprint drift` must NOT list the hook, and `blueprint pull` must not select or preview it. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" marker-merge` must pass (cases #7–#10). **Status when accepted:** **FIXED — landed `4076faf`, pushed 2026-09-14** |
| **BUG-110** | **An empty `/tmp/.git` makes every scenario workspace under `/tmp` resolve its state root to `/tmp`, so the gate goes red on `state-root` #A6 — and in a suite without that canary it would have gone SILENTLY wrong instead.** The state-root walk (TASK-021 Stage A) terminates at the first ancestor carrying `.git`, `.blueprint-root` or `.blueprint-source`, testing `.git` with `-e` so worktrees and submodules work. The harness builds scenario workspaces with `mkdtemp` under the system temp dir (`TMPDIR` is unset here, so `/tmp`). An empty `/tmp/.git` appeared at 19:44 on 2026-09-14, and from that moment a markerless, non-git tree resolved to `/tmp` with exit 0. Proven by probe (`.scratch/probe-a6.sh`): `resolved=[/tmp]`, terminator `/tmp/.git`. **#A6 did exactly its job** — it asserts a markerless tree FAILS LOUDLY, and it failed loudly on the first push after the directory appeared, both at top level and inside `bootstrap-gate`'s nested gate. **The defect is the exposure, not the canary:** any process that creates `/tmp/.git` changes what every scenario under `/tmp` believes its project root is, and only #A6 checks the premise. A scenario that writes state would write it relative to `/tmp` without complaint. **Attribution NOT proven.** It appeared while a `codex exec --sandbox workspace-write` review was running, and that sandbox treats `.git` specially under its writable roots, which include `/tmp` — consistent, and unconfirmed. The directory is not a mount in the host namespace (`findmnt`). **It VANISHED ON ITS OWN:** listed empty and unmounted, then seconds later `rmdir` found it already gone — nothing in this session removed it. So something creates it and cleans it up again, which is what a sandbox setting it up per command would look like, and it makes the failure **intermittent by construction**: a gate run is red only if it overlaps that window. Do not go looking for a persistent directory. **Attribution OBSERVED FROM INSIDE, 2026-09-14:** Alexey (Architect, Codex), running under `codex exec --sandbox workspace-write` for the TASK-025 plan review, reported that *"this Codex sandbox currently provides an actual empty `/tmp/.git`"*. That is the sandbox describing its own environment, not an inference from timing, and it matches the host-side evidence. **Treat the Codex workspace-write sandbox as the cause** until shown otherwise. Practical rule meanwhile: do not run the push gate or the `state-root` suite while a Codex review is in progress. He also measured that a clone made under `/tmp` still resolves to its own tree because its `.git` is nearer — so code that always runs git as `git -C <tree>` is safe, and code that resolves a root from a scratch PARENT is not. **Fix direction, not decided:** the harness should refuse to create a workspace under an ancestor that already carries a terminator, rather than rely on #A6 noticing afterwards — failing at workspace creation names the cause; failing in #A6 names a symptom. | S2 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found 2026-09-14 by Eto (Orchestrator) on a red docs-only push. Re-open if any scenario resolves a state root outside its own workspace. **Promoted 2026-09-15 under TASK-028**, porting PR #68 (linkedin-watcher-agent) onto the current H5 harness. Reproducers: `tests/state-root` #A7 (a ceiling one level below a planted `.git` must stop the walk), and two `tests/harness` BUG-110 cases (every child is handed its workspace root as `BP_STATE_ROOT_CEILING`; a marker in or above `$TMPDIR` aborts workspace creation, naming the path). **Fixed under TASK-028** by an optional `BP_STATE_ROOT_CEILING` in `scripts/lib/state-dir.sh`, set by the harness to every workspace root, plus a preflight in `tests/harness/workspace.ts` that refuses a temp dir with a marker in or above it. **Consequence, deliberate:** while a stray marker sits in or above `TMPDIR`, EVERY scenario refuses to start. The remedy is to remove the marker, or to point `TMPDIR` at a directory with no marker above it. **What was to be tested:** Put an empty `.git` directory above a temporary folder and run any suite with `TMPDIR` inside it — it must refuse at workspace creation, naming the marker and the remedy, instead of resolving state roots outside the fixture. Then `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" harness state-root` must pass. **Status when accepted:** **FIXED — landed `b5c2c29`, witness strengthened in `74db453`, pushed 2026-09-14** |
| **BUG-077** | **`scripts/lib/feed.sh:37` locates the activity feed with `git rev-parse --show-toplevel` — the idiom banned everywhere else for a hazard that applies here identically.** The ban exists because **git exports `GIT_DIR` into every hook**, and the gate runs the suites from a hook, so a fixture's git environment silently redirects the resolution (BUG-014's mechanism, A-09's consequence). `tests/state-dir` #6c enforced it — and its scope is *state-dir consumers*, which `feed.sh` is not. The scope was never wrong, it was just narrower than the hazard. **The concrete consequence is already documented:** it is the mechanism behind the split-feed finding in the root-split review — after a `scaffolding/` move the supervisor writes one feed while the gate and the subagent hooks write another, because this line answers a different question than the physical-script walk does. **Found by method, and specifically by the method a mutant cannot reach.** The equivalence comparison ran 19 trees through both `#6c` and its TypeScript port and diffed verdicts: 12 agreed, 7 disagreed (every one shell PASS → port FAIL, which is the widening working), and **2 passed under BOTH** — `lib/feed.sh` and `lib/gate.sh`. A mutation of either implementation would have shown nothing, because both are faithfully out of scope. Only comparing them against a population that included the un-covered cases surfaced it. **Fix is a rule question, not a line edit:** the ban should be scoped to the hazard (anything resolving a path that a hook's exported git environment can redirect) rather than to a subsystem, and then `feed.sh` follows from the rule instead of being named by it. Check `lib/gate.sh` in the same pass. | S2 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Found 2026-09-11 by Christian (Senior Architect) during the Stage A equivalence comparison; rowed by Eto (Orchestrator). He declined to widen the rule inside the port, correctly — a port that changes the rule it is being compared against cannot be shown equivalent to anything. **Re-open trigger: before TASK-021 Stage B**, because the split-feed failure it causes is invisible (two feeds, both written, neither empty) and Stage B is what triggers it. **FIXED 2026-09-11 by Christian (Senior Architect).** Promoted from `backlog/` on its own re-open trigger — TASK-021 Stage B is next. **What was to be tested:** Run `grep -rn 'rev-parse --show-toplevel' scripts .githooks` — every hit must carry an inline `bp-allow-toplevel: <why>` justification, and none may remain in `scripts/lib/feed.sh` or `scripts/lib/gate.sh`. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" forbidden-idiom` must pass. The suite is proven able to fail: restoring the idiom in either file turns it red, naming the file and line. **Status when accepted:** **FIXED — landed `4c3c5a2`, pushed 2026-09-12** |
| **BUG-034** | **`blueprint pull` can silently delete a project's own content — `marker_aware_merge` compares marker COUNTS and never their ORDER.** `scripts/blueprint:311` guards with `bp_begin -ne bp_end || proj_begin -ne proj_end || bp_begin -ne proj_begin`. A file carrying `BLUEPRINT:END` **before** `BLUEPRINT:BEGIN` has counts 1 and 1, passes every arm, and reaches the awk — which sets `capture` on BEGIN and clears it on END, so with them inverted the region tracking is wrong while `pull` reports the project's half preserved. **This is the core sync verb**, the one thing every derived project runs against this repo, and the failure is silent by construction. Was A-11, buried in the `AUDIT-TRIAGE` row of ~12 undispositioned findings — found by sampling that row at random during the 2026-08-20 grooming, which is the argument for triaging the remaining ~11 rather than for fixing this one. **REPRODUCED LIVE 2026-09-11 by Elias (Infrastructure-2)** during the TypeScript port of `tests/marker-merge`, and it is worse than the wording above. Measured, not inferred (`.scratch/elias-bug034.sh`): counts 1/1, every guard arm passes, no `.bp-bak` is written, and the outcome is `PROJECT-OWNED-FOOTER: LOST` — **while `pull` prints *"(marker-aware merge: project outside-marker content preserved)"* and *"✓ Pulled 1 file(s)"*.** So the verb does not merely lose the content silently; it states that the content it destroyed was preserved, and exits 0. **And the loss is two-sided:** the BLUEPRINT's own region content is not delivered either, so the file ends in a state neither side wanted — a project that hits this gets a file that is neither its own nor the blueprint's, with a success message. **Raised from S2 to S1 on that basis**: a wrong answer that announces itself as right is the class this repo has spent a week naming (`docs/config/findings.md` F-002), and this instance sits in the one command every derived project runs against this repo. **Neither test suite has a case that would see it** — grep for an inverted-marker fixture returns 0 in both the shell runner and the port. **It is not mutatable**, because the defect is an ABSENT guard rather than a wrong one, which is why a mutation-based approach had never surfaced it and a direct reproduction did. Deliberately NOT asserted in the new spec: asserting correct behaviour is red on `main`, and asserting current behaviour pins the defect. It belongs to this row. **Reproducer first:** a fixture file with inverted markers, asserting the project region survives — the fix is worthless without a test that fails on the parent commit. **Note the sibling still live in the same row: A-10**, the watcher replaying the current baton at startup, an implicit billable replay and a direct CLAUDE.md §Cost capability-4 violation. | S1 | **ACCEPTED by the founder 2026-09-15** — accepted with every other bug waiting for acceptance ("also the bugs in waiting-acceptance you can move to done, I cannot test most of them, but in worst case we re-open them"). Most were not tested by hand, so a regression re-opens the row rather than counting as a new bug. | Raised 2026-08-20 by the grooming pass. Re-open immediately — this is the product's core verb and nothing currently guards marker order. **Promoted 2026-09-14 into TASK-026** (PR #66's drift change needed one prospective-pull result, and the review required it to refuse invalid markers). Fixed there: marker structure is validated for ORDER and balance before any merge, and an invalid file is REFUSED — nothing written, no backup, pull exits 4 — instead of merged. Regression: `tests/marker-merge` #5 (inverted) and #6 (BEGIN without END). **What was to be tested:** Put the end marker before the begin marker in a derived project's `.githooks/pre-push-project` and run `blueprint pull` — it must exit 4, say the file was refused, and leave the file byte-for-byte unchanged with no backup. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" marker-merge` must pass (cases #5, #5b, #6). **Status when accepted:** **FIXED — landed `9f8549e`, pushed 2026-09-14** |
| **BUG-114** | **An existing project that pulls never receives three scripts its managed files call.** `pull` delivers `MANAGED_FILES` only, and `scripts/no-chain-guard.sh` (run as a PreToolUse hook by the managed `.claude/settings.json`), `scripts/session-resume.sh` and `scripts/wait-mic.sh` (driven by shipped suites and the harness) were never listed. A fresh bootstrap is NOT affected — it is `git archive HEAD` — which is why PR #66 named the wrong victim (Jesko's review: 39/39 on a fresh bootstrap before the fix). Reproduced: an existing project's `pull --yes` reports nothing to pull and the scripts stay absent. | S2 | **ACCEPTED by the founder 2026-09-15** — verified by the founder in a real derived project: storm2flow's pull delivered `scripts/no-chain-guard.sh`, `scripts/session-resume.sh` and `scripts/wait-mic.sh`. On the blueprint side, `blueprint files` lists all three and `tests/managed-references` passed 3/3 the same day. storm2flow has no copy of that suite, by design: it is `export-ignore`d (blueprint-only), and storm2flow has not yet pulled the shipped `tests/` suites (its partial pull). | Found by struct2flow-www (PR #66, U1), corrected by Jesko (QA-2, Codex). TASK-026. Regression: `tests/managed-references` — #1 DERIVES the referenced-script set rather than listing three, #2 pulls it into an existing project. Re-open if a shipped managed file names a shipped script `MANAGED_FILES` does not. **What was to be tested:** Run `blueprint files` — it must list `scripts/no-chain-guard.sh`, `scripts/session-resume.sh` and `scripts/wait-mic.sh`. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" managed-references` must pass. In a derived project, `blueprint pull` must deliver the three scripts. **Status when accepted:** **FIXED — landed `1d4bcb4`, pushed 2026-09-14** |
| **BUG-112** | **`pull` decides a file is marker-bearing by SUBSTRING, so a managed file that merely mentions the markers takes the whole-file fallback on every pull.** `grep -q 'BLUEPRINT:BEGIN'` matches prose and code alike. Reproduced: `CLAUDE.md` (1 BEGIN / 2 END in prose) and `scripts/blueprint` itself (7 / 6, in its own greps and comments) each left a `.bp-bak` and a whole-file copy on a single pull; `way-of-working.md` and `pre-push-project.example` are in the same state. Worse than noise: the day a mentioning file's counts balance, the awk would "merge" it — for the CLI, into itself. Rewording CLAUDE.md alone (PR #66 U2) fixes one file and leaves the next sentence as the trap. | S2 | **ACCEPTED by the founder 2026-09-15** — accepted after review of the waiting-acceptance row; `tests/marker-merge` #3 and #4 pin that a file which only mentions the markers takes no marker handling and leaves no `.bp-bak`. | Found by struct2flow-www (PR #66, U2), reproduced at pull level by Jesko (QA-2, Codex); the CLI-itself instance found implementing TASK-026. Regression: `tests/marker-merge` #3 (prose) and #4 (the CLI). Re-open if marker classification can be satisfied by a line that is not a marker. **What was to be tested:** In a derived project, `blueprint pull CLAUDE.md` and `blueprint pull scripts/blueprint` must leave no `.bp-bak` file behind. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" marker-merge` must pass (cases #3 and #4). **Status when accepted:** **FIXED — landed `1dcdf4c`, pushed 2026-09-14** |
| **BUG-076** | **`tests/state-dir` #6c banned `git rev-parse --show-toplevel` everywhere except the one file that contained it.** #6c swept `scripts/agent-activity.sh` plus three dispatchers; it never looked in `scripts/lib/`. `scripts/lib/state-dir.sh:54` held `_asd_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"` as the default for the positional root — the exact expression the guard exists to forbid, because it answers about the CALLER's exported `GIT_DIR`, git exports `GIT_DIR` to every hook, and the gate runs the suites from a pre-push hook (BUG-014's mechanism, A-09's consequence). It stayed green for months and was safe only by accident: every caller happened to pass an explicit root, so the default never ran. The moment anyone "simplified" a call site by dropping the argument — which is the obvious tidy-up, and which TASK-021 nearly did — they would have landed on the banned path with **no test to stop them**. **Fixed** by porting #6c to `tests/forbidden-idiom` (TypeScript, per the no-new-shell-suites rule) and widening the sweep to `scripts/lib/`; the shell case is deleted rather than duplicated, leaving a pointer. The match is on the **command**, not the substring `git`, because `bp_state_root` legitimately tests for a `.git` path and a guard that fires on the benign case gets ignored — the rule `tests/state-dir` #5b already states about its own artefact matching. Mutant — restore the `rev-parse` fallback — turns it **red naming the file and line, observed**, which #6c could not do because it never read the file. | S2 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Christian (Senior Architect) while building TASK-021 Stage A. Re-open if any guard's population is narrower than the rule it states. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-075** | **`bp_roster_file` served the SHIPPED EXAMPLE roster whenever the live one was missing, returning 0, so a wrong identity was indistinguishable from a right one.** It tried `<dir>/AGENT_ROSTER.md`, then `<dir>/AGENT_ROSTER.example.md`, and succeeded either way. The live roster is per-engineer and **gitignored**; the example is **tracked and ships**. Harmless only while both sit in the same directory — which stops being true the moment `scripts/` moves under `scaffolding/`, because the example is then the file beside the code while the live roster stays at the real root. Measured in a post-move tree: `--whoami` answered **`Sylvia`** (the template's Orchestrator) where the live roster says `REAL-ORCHESTRATOR`, **at rc=0 with nothing printed**. Every feed persona label, every `--whoami`, and every `OVER_TO_<NAME>` handoff would name a persona no dispatcher watches. That is **BUG-010's shape** — *"renaming a persona appears to do nothing"* — defeating the `--whoami` instruction added to prevent BUG-010. **Two halves, and either alone leaves it reachable:** every roster call site now passes the resolved STATE root rather than its own code root (TASK-021), and the example fallback is replaced by a `bp_roster_warn` naming the one-line fix (`cp AGENT_ROSTER.example.md AGENT_ROSTER.md`). Naming the example explicitly as a FILE still works, which is how tests read the template. Guarded by `tests/roster-resolution` (4 cases); mutant — restore the fallback line — turns **#2 red, observed**. | S2 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Christian (Senior Architect) while building TASK-021 Stage A, by execution in a post-move tree. Re-open if any resolver substitutes a shipped template for missing per-engineer state. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-074** | **`tests/manifest` could only discover a sourced bridge whose `source` line was INDENTED, and the half that mattered failed OPEN.** The shell implementation stripped the verb with `s/.*[[:space:]]\(\.\|source\)[[:space:]]+//`, which requires whitespace *before* the dot. An unindented `. ./scripts/run-ts-suites.sh` therefore came out of the `sed` as the literal line, failed its `[ -f ]` test, and was never discovered as a bridge at all. Two consequences, and they differ in direction: `#4` failed for the wrong reason, which is fail-CLOSED and merely misleading; but **`#2c`'s ships-⟺-managed check skipped that bridge entirely** — fail-OPEN, so a bridge whose two propagation paths disagreed would have shipped silently, which is BUG-061's exact shape and the thing `#2c` exists to refuse. **Latent rather than live**, because every `source` line in this repo's hooks happens to be indented — so the control was held up by a whitespace convention nothing asserts. **Found by method, not by reading:** it surfaced only when both implementations were run over the same 21 perturbed trees and their per-check verdicts diffed mechanically. Months of review over this file did not find it. Fixed by the TypeScript port, which anchors on the match rather than re-scanning the stripped line; two cases in `tests/manifest/manifest.spec.ts` pin it. | S3 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Timon (Front-End-1) while porting `tests/manifest` to TypeScript (`9a85c97`). Row filed by Eto (Orchestrator) because `docs/doing/BUGS.md` was held uncommitted by a concurrent session. Re-open if any control in this repo recognises a construct by re-scanning text it has already transformed, rather than by anchoring on the match. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-071** | **A bug number is allocated by reading "the highest in use" out of a `BUGS.md`, which is racy until it is committed — three collisions in two days, and two of them are live on `main` right now.** `docs/doing/BUGS.md` carries **two rows each for BUG-052 and BUG-053**, pushed and unnoticed for a day; BUG-057 went to two different defects in parallel; and BUG-066/BUG-067 were re-issued by a second session working from a copy of the table that predated the first session's push, which also dropped BUG-068's row. **It is not cosmetic:** the commit convention and DoD §2's regression-test check both key off the number, so a duplicate makes §2 satisfiable by the WRONG test — `dod_stage_bugtests` greps `tests/` for `BUG-0*<n>\b`, so a bug with no test passes because its twin has one. **The rule was already written down and stopped nothing.** BUG-062's own row states the mechanism verbatim — *"the next number is not a per-file maximum, it is the maximum across `docs/{doing,backlog,waiting-acceptance,done}/BUGS.md`, and it is racy until it is committed"* — and the two duplicates above landed after it. A rule that must be remembered at the moment the author is busy is the wrong shape of fix (BUG-004, BUG-014, the no-chain hook). **Fix: `tests/lifecycle-docs` #6**, which fails the push when one number opens more than one table row across the four lifecycle tables, naming every file and line that carries it. It counts a ROW and not a MENTION — these files quote other numbers constantly in prose, BUG-062's row alone naming half a dozen — and it catches the SAME-file case, which is the one that actually happened. **It fails closed on its own inability in two directions:** a lifecycle table that exists but cannot be read is a FAILURE rather than a smaller scan reported as ok, and the row extractor is run against an inline fixture on every invocation and must find the planted duplicate while ignoring the planted prose mentions — because a parser whose subject changes form goes quiet rather than red (BUG-063), and F-002 counted six checks inferring a property from a proxy satisfiable without it, all failing toward "pass". An ABSENT table is legitimate and not a failure: `new-project.sh` seeds `docs/backlog/BUGS.md` alone, so demanding all four would fail every derived project on its first push (BUG-028's class). **No allocator was built**, deliberately: two agents can both run an allocator before either commits, and an allocator cannot see the other's uncommitted intent, so it would hand out the same number with more ceremony. Detection at the gate is the only point where all the intent is visible. | S2 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by the founder, reading the tables, after the third collision in two days. Reproducer verified red on the parent: `#6 BUG-052 is the number of 2 different rows` / `#6 BUG-053 …`. Four mutants killed. Re-open if two rows are ever found sharing a number, or if #6's self-fixture is weakened to something a broken extractor would still satisfy. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-068** | **The real-state canary fails a suite in TEARDOWN, its own assertions having passed, whenever a concurrent agent flips the mic — which on a repo whose premise is concurrent agents is always.** `assertUnchanged` byte-compared the live baton, so `tests/bootstrap-gate` went red three times in a row for Elias today, each run window holding a matching flip in `logs/state/signal-history.log` written by somebody else. A control that goes red for innocent reasons gets muted, and `canary.ts` already argued exactly that about the activity feed while doing the opposite to the baton. **The discriminator was already in the repo and is exact** (Philipp, Infrastructure-1): BUG-030's clobber writes `signal.md` DIRECTLY and appends NOTHING to the journal, while a real flip goes through `scripts/signal-set.sh` which ALWAYS appends one line. So the journal — already a watched target — became the witness: baton changed AND journal grew is REPORTED, baton changed and journal did not is a FAILURE, which is BUG-030 itself. **The guard gets sharper, not weaker**: before this it could not tell the two apart and blamed the test for both. No lock, no snapshot-and-restore, no loss of scope. **The report is deliberately visible** — `CANARY-NOTE:` is emitted by the canary and grepped out of vitest's output by `scripts/run-ts-suites.sh` even on a PASSING run, where that output is otherwise captured and deleted; a silent wave-through would make this canary indistinguishable from one switched off, which is the BUG-004 / A-22 / BUG-066 shape. **Residual, stated rather than accepted:** a test that itself called `signal-set.sh` against the REAL baton would be waved through. It is small — `AGENT_SIGNAL_FILE` is on the harness's forbidden list (R3), so no scenario child inherits a path to the real baton — and it is not closed, because closing it means attributing an append and the journal carries nothing to attribute by. Recorded in `canary.ts` where the next reader sees it. | S2 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Designed by Philipp (Infrastructure-1), built by Vitali (QA-1). Five cases in `tests/harness/harness.spec.ts`, all named `BUG-068:`; **six mutants killed** via `.scratch/mutants.py`, including `>` → `>=` on the growth test, which is the one that proves BUG-030's own reproduction is still live. Reproducer verified red on the parent (3 failed / 38 passed). Re-open if a suite is ever seen mutating the real baton without a journal line, or if `CANARY-NOTE:` is renamed in one of its two places. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-064** | **Four assertions in `tests/a2bp-contamination` pass while the defect they guard is present, and the dead code that misled the auditor is deleted.** (a) `_contamination_suppressed()` (`contamination.sh:90`) had **zero callers**: deleting its justification requirement left #14 printing `ok`, while mutating the REAL gate — `grep -nE 'a2bp-allow:[[:space:]]*[^[:space:]]'` at `:392` — failed #14 immediately. It was not merely dead; it sent the auditor at the wrong mechanism. (b) #5 asserts `--force` is refused, but replacing the `die` with `--force) : ;;` — a pure silent swallow, "the worst outcome" by the case's own comment — still printed `ok`, because every remaining branch is satisfied by the contamination block the fixture *also* triggers. (c) #13's fixture put the marker on the LOWER line, the direction the `\|N\|` delimiters do not protect, so `*"$1"*` in place of `*"\|$1\|"*` behaved identically on it. (d) #6's blueprint-side fixture was a bare `SENTINEL\n`, sharing no line with the project file, so diff aligned nothing and NO line was ever reverse-substituted — `if true` in place of `_should_substitute "$path"` (`scripts/blueprint:1253`) left it green. Also corrected two header claims: a `--force` that no longer exists, and a shared-lib reuse (`the gate and new-project.sh`) that never happened — `contamination_scan` has one caller. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Markus (Security-1) in a read-only audit; reproduced in place, fixed and re-mutated by Vitali (QA-1) the same day — each of the four is now red under its own mutation and green without it (R6). Sibling of **BUG-048**, which named the subshell mechanism in this same suite; kept separate because these are four different mechanisms found by a later audit. Re-open if an assertion is added to this suite without a recorded mutation that turns it red. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-063** | **`tests/ts-bridge` #1c has been failing on `main` since 4e6bce2, so the pre-push gate is red for everyone — and the cross-check it performs has been running over nothing.** #1c reads the harness's forbidden-variable set out of `tests/harness/env.ts` and asserts `scripts/run-ts-suites.sh` scrubs every name in it before vitest starts. It parsed a literal `FORBIDDEN_ENV = [ … ]` array. BUG-060's round-2 fix replaced that array with a **derivation** over the `ENV_KIND` table, the `sed` range matched nothing, and the case took its own explicit "could not read … this check would pass over nothing" branch — which is the correct behaviour and is why this is visible at all: a cross-check that cannot see its subject refuses to report success. Measured: `bash tests/ts-bridge/test.sh` → `FAIL: #1c BUG-055: could not read FORBIDDEN_ENV from tests/harness/env.ts` and `FAILED: see the FAIL lines above`, with `.githooks/pre-push-project:470` failing the `ts-bridge · BUG-055` stage on it. **Fix:** parse the `ENV_KIND` table — every `GIT_*`/`AGENT_*` name whose kind is not `inert`, which is what `FORBIDDEN_ENV` derives — so the names still come from the file and only the one-line selection rule is mirrored. Verified non-vacuous: 19 names, matching `FORBIDDEN_ENV` exactly. **The lesson is not "fix the regex":** a control that reads a declaration from another language cannot be validated by reading it, and this one survived a five-round review because every reviewer read the shell and the TypeScript separately. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Timon (Front-End-1) while changing `FORBIDDEN_ENV` for BUG-060. Re-open if any suite parses a TypeScript declaration in a form that a legal refactor of that declaration can silently invalidate. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-062** | **The real-state canary allowed arbitrary appends to the operator's activity feed while claiming a unique escape token covered them; the token was minted but never searched.** **Fix, and it has two halves — the first landed alone and detected nothing.** *Detection:* teardown searches the append-only feed for that scenario's per-run token while retaining prefix protection. *Emission:* `scenarioEnv` sets **`AGENT_FEED_TAG`** and **`AGENT_PERSONA`** to the token, so the two dominant feed writers label every line with it — `scripts/lib/pipeline.sh:173` renders each gate line as `[${AGENT_FEED_TAG:-GATE}]`, and `scripts/agent-activity.sh:113` takes `AGENT_PERSONA` as its session persona **while ignoring `AGENT_FEED_LOG` entirely** (line 78 derives the path from the repo root), which makes it the one writer a scenario can invoke that reaches the real feed with the pinning intact. Without the emission half the search was over a case no fixture could produce — a control checking something that cannot happen, which is BUG-005's shape inside the guard built to prevent it. **Scope, CORRECTED — the wording this row shipped with claimed coverage the code does not have:** an append that carries no token is **not detected at all**. It was described as "caught only by the prefix check"; an append leaves the captured content intact as a prefix, so that check passes by construction and the token check finds nothing to match. The prefix check catches a rewrite or a truncation, which is not what an append is. *Covered writers:* `scripts/lib/pipeline.sh:173` (via `AGENT_FEED_TAG`) and `scripts/agent-activity.sh:113` (via `AGENT_PERSONA`). *Not covered:* `scripts/watch-ci.sh:45`'s literal `[CI]`; `scripts/log-activity.sh:172`, whose persona comes from the hook payload; `.githooks/pre-push-project:838,852`, which **re-sets `AGENT_FEED_TAG` itself**, so a fixture that runs a whole gate (`tests/bootstrap-gate` does) emits untagged stage lines; and any direct `feed_append` of prose. Closing it needs **attribution**, which the feed cannot offer — a live daemon's line and a leaked fixture line are the same bytes — and forbidding new bytes outright would fail honest runs, so the limit is stated and pinned rather than papered over with a check that would go red on innocent traffic. Guarded by `tests/harness` cases `BUG-062` — one for detection, one proving a real `pipe_stage` run carries the token (read back out of the scenario's own feed, so the proof writes nothing to the operator's), and one pinning both halves of the residual: the untagged append that is NOT detected, and the rewrite that is. **Filed as BUG-057 and renumbered on review:** BUG-057 was already taken by a different, already-public defect in `docs/backlog/BUGS.md` (a failing gate stage killing every stage after it), and two agents working in parallel each read "the highest number in use" from the file in front of them and reached the same answer. Both the commit convention and the DoD §2 regression-test check key off the number, so a duplicate makes §2 satisfiable by the wrong test. **With more agents this recurs**: the next number is not a per-file maximum, it is the maximum across `docs/{doing,backlog,waiting-acceptance,done}/BUGS.md`, and it is racy until it is committed. **Round 5 — the fix shipped with a one-line route to defeat itself.** `AGENT_FEED_TAG` was declared `inert` in the harness's environment table and a per-call `env` merges **over** `scenarioEnv`, so `{ env: { AGENT_FEED_TAG: 'DoD-Gate' } }` — the value `.githooks/pre-push-project:838` itself uses — replaced the token and converted a **detectable** leak into exactly the untagged append this row documents as undetected. It is now `scenario-token`: an override must still **contain** the scenario's token (`${s.escapeToken}-my-tag` composes, so a fixture that needs its own label keeps detection), and unsetting it is refused. **`AGENT_PERSONA` is deliberately not protected the same way** — it is a persona NAME and `tests/roster` / `tests/codex-persona-label` exist to assert what a named persona renders as, so a scenario that overrides it gives up the `agent-activity.sh` half of the detection for that scenario; the `AGENT_FEED_TAG` half still holds. Guarded by `tests/harness` case `BUG-062 REFUSES an AGENT_FEED_TAG override that drops the escape token`. **Round 6 — the refusal-on-unset was one case too strict, and the push gate caught it.** `AGENT_FEED_TAG` is EXPORTED, so `tests/bootstrap-gate` #2/#3 — which runs a derived project's ENTIRE pre-push gate — tagged every line that nested gate rendered with the token, and the derived project's own `tests/pipeline` #16 greps for the literal `[GATE] PASSED`. It failed with "stage results missing from the feed": true, and naming the wrong cause, which is the BUG-041/042 misdirection class. Composition does not solve it — `[GATE-<token>]` fails that grep too. **The rule now:** dropping the token is permitted **iff `AGENT_FEED_LOG` is unset in the same call AND the child's cwd resolves inside the workspace**. The first clause alone was the obvious rule and is *inverted*: unsetting `AGENT_FEED_LOG` is exactly what makes the destination AMBIENT (`scripts/lib/feed.sh:37` derives it from `git rev-parse --show-toplevel`, falling back to `pwd`), so from the real repository it resolves to **the operator's own feed** — the one place the token exists to be seen. The cwd clause is what turns "derives its own feed" into "derives a feed inside this workspace", and it holds because the workspace is an `mkdtemp` under the system temp dir and therefore inside no git tree. `options.cwd` is now threaded to `fixtureEnv`, since whether the token may be dropped is a question only the spawn site can answer. It is a rule, not an exemption (TASK-018 R5): bootstrap-gate qualifies by meeting both clauses, not by being named. Guarded by `tests/harness` cases `BUG-062 PERMITS dropping the token when the child is handed its own contained feed` (red under the old unconditional refusal) and `BUG-062 REFUSES dropping the token on either half of that condition` (red under the `AGENT_FEED_LOG`-only rule, which lets a fixture in the real repo drop the token). | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Jesko (QA-2) reviewing TASK-018; the residual-scope claim corrected the same day after Andreas (Codex) proved it false in round-3 review. Re-open if a scenario can append its escape token to the real feed without failing, or if this row is ever found claiming more coverage than the code has. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-061** | **`package-lock.json` has been shipping to every derived project on its own, and it is live on `main` today.** Verified: `git archive origin/main \| tar -t \| grep -E "^package\|^vitest\|^tsconfig"` returns exactly `package-lock.json` — the four siblings of the TASK-018 phase-1 block (`package.json`, `tsconfig.json`, `vitest.config.ts`, `tests/harness/`) are export-ignore'd and it never was. **What a derived project actually experiences:** `.github/workflows/security.yml` is MANAGED, so every bootstrapped project inherits its `ts-tests` job, which runs `npm ci` and dies with `npm ERR! code ENOENT … open '.../package.json'`. The project's CI goes red on its **first push**, on a job it never wrote — `tests/bootstrap-gate`'s exact failure class, arriving on someone else's machine. **Why no control saw it:** `tests/manifest` #2b/#2c enforce the invariant `.gitattributes` states — *the TS toolchain ships iff a shipping suite is TypeScript* — but `TS_SHIPS` was an AND over a **hand-written subset** naming only those four. A file in neither the subset nor the export-ignore block ships alone and both checks still print "phase 1 is whole": an AND over a remembered list cannot see a file it does not know about. Third instance of a hand-enumerated list going stale (BUG-051's `*.sh` anchor, BUG-053's `blueprint`-tier rows). **Fix:** the toolchain set is declared **once** in `tests/manifest` and read by both the archive check and the `MANAGED_FILES` check, and — the half that actually closes the class — #2c now tallies **partial** ship and **partial** management, so any member shipping while others do not is a hard failure naming the file. The set-membership fix alone would not have caught this. **Second half, and the one that actually stops the red CI:** export-ignoring the lockfile does NOT fix the derived project — `.github/workflows/security.yml` still ships the whole `ts-tests` job, and without a lockfile `actions/setup-node`'s `cache: 'npm'` now fails *earlier* with "Dependencies lock file is not found". A job inside a managed file cannot be export-ignore'd, so its three steps are guarded on `hashFiles('package.json') != ''` — step-level, because at job level `hashFiles` sees an empty pre-checkout workspace — with a fourth step that ANNOUNCES the skip, since a job whose steps all skip renders green and this repo's signature defect is green-because-it-checked-nothing. The guard dissolves at phase 2 with no edit. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Elias (Infrastructure-2). Proof the control fires: on the unfixed HEAD, `bash tests/manifest/test.sh` → `FAIL: #2c the TS toolchain ships in PART — package-lock.json reach every derived project while package.json tsconfig.json vitest.config.ts tests/harness/ do not`. Re-open if any propagation control derives its subject from a list written by hand rather than from the whole set. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-060** | **The harness scrubbed forbidden environment variables and then accepted arbitrary caller overrides, so every `s.run` could restore `GIT_DIR=/real/repo/.git`.** The override door is required for hostile-pointer and private-index fixtures, but it was an unrestricted unsandboxed primitive. **Fix:** forbidden path overrides must be contained by the scenario workspace; `/dev/null` remains the explicit read-only exception for Git config suppression. **Then corrected twice, because the first containment rule judged every forbidden variable as a path** — refusing `AGENT_PERSONA=Vitali` with a message about paths, and accepting `<ws>/objects-a:/tmp/not-mine` because a colon-joined list is not a path — so each variable now declares the KIND of value it holds. **And once more, because `GIT_CONFIG_COUNT` was declared `opaque` on the reasoning that a number redirects no write:** it is the SWITCH that activates `GIT_CONFIG_KEY_<n>`/`GIT_CONFIG_VALUE_<n>`, which were in no list and through no check, so a caller could set `core.hooksPath` — the A-22 vector BUG-047 produced from inside a test — past the whole containment model, with the guard reporting the value harmless. Probed, not reasoned: `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.hooksPath GIT_CONFIG_VALUE_0=/tmp/evil-hooks git config --get core.hooksPath` → `/tmp/evil-hooks`. Both switches (`GIT_CONFIG_COUNT`, `GIT_CONFIG_PARAMETERS`) are now **denied** as overrides rather than validated — a fixture has two contained ways to get config (its own repository, or `GIT_CONFIG_GLOBAL` inside the workspace), so validating a third would mean modelling what every git key does with its value — the inherited `GIT_CONFIG*` namespace is scrubbed by prefix, and **an undeclared `GIT_*`/`AGENT_*` override is refused**, which is the same hole one level up: the table said what happened to the names in it and nothing about the rest. Guarded by `tests/harness` cases `BUG-060` — containment, kinds, the refusals, and one proving the injection is real on this machine's git. **And a fourth time, on the two variables that were in NEITHER namespace.** `HOME` and `TMPDIR` are what `scenarioEnv` sets to give a scenario its own dotfiles and its own temp dir (TASK-018 R3), and the override check only ever looked at `GIT_*`/`AGENT_*` — so `{ env: { HOME: '/home/<operator>' } }` went through no check at all and R3's strongest promise was a convention again. Measured rather than reasoned: on the unfixed HEAD the new case's fixture child printed `/home/luiz`. Both are now `scenario-path` — an override must resolve inside the workspace, and **unsetting is refused too**, because an absent `HOME` resolves to the operator's real home via `getpwuid` and an absent `TMPDIR` to `/tmp` (BUG-049's debris). **`AGENT_CI_WATCH` was declared `inert` and is a behavioural switch:** `.githooks/pre-push:569` backgrounds `scripts/watch-ci.sh` whenever `${AGENT_CI_WATCH:-1}` is 1, so an inherited 1 made any fixture that runs a gate spawn a real CI watcher out of a test. It is now `opaque` — scrubbed on inherit, still passable explicitly, which `tests/bootstrap-gate` needs (it passes 0 to suppress the watcher). `FORBIDDEN_ENV` is filtered to the two namespaces so the scrub list stays what `assertProcessEnvClean` can demand of the test process, which always carries `HOME`. Guarded by three further `tests/harness` cases: the primitive, the door a spec actually uses (`s.run`), and the switch. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Jesko (QA-2) reviewing TASK-018; the `opaque` misclassification of `GIT_CONFIG_COUNT` found the same day by Andreas (Codex) in round-3 review. Re-open if any fixture API accepts a forbidden path outside its owned workspace, or any `GIT_*`/`AGENT_*` variable reaches a child without a declared kind. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-059** | **`ProcessRegistry.run()` forgot a process group as soon as its direct child closed, so a shell could background a descendant, exit zero, and leave teardown with no handle to reap or report.** **Fix:** retain every spawned process-group id through teardown, probe the group, kill survivors, and fail the scenario. Guarded by `tests/harness` case `BUG-059`. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Jesko (QA-2) reviewing TASK-018. Re-open if a completed direct child can leave its group alive without failing teardown. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-058** | **`ScopedFs.resolve()` checked paths lexically, so an in-workspace symlink to an external directory let write/chmod/rm/copyIn escape despite the API's containment promise.** **Fix:** resolve the nearest existing ancestor physically and verify the resulting target remains below the physical workspace root. Guarded by `tests/harness` case `BUG-058`. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Jesko (QA-2) reviewing TASK-018. Re-open if a filesystem mutation can traverse an outward symlink. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-045** | **The local pre-push gate is strictly harsher than CI on dependency CVEs, so a LOW-severity advisory anywhere in a project's transitive tree blocks every local push while CI stays green.** `.githooks/pre-push:399-408` treats **any** non-zero `osv-scanner` exit that is not "No package sources found" as a failure — **there is no severity filter at all**. `.github/workflows/security.yml:110-119` blocks only **MEDIUM+ (CVSS ≥ 4.0)**. The two gates are documented as the same gate with CI as the backstop, and they are not. **This is live today, not a TASK-018 consequence:** every derived project is a Node/TypeScript project (`STACK_DEFAULTS.md:32,39,55`) with a lockfile, so the local scan has real package sources and the harsher policy is already in force there. Only the blueprint itself is currently exempt, because it has no lockfile and takes the "No package sources found" no-op branch — which is why nobody here has felt it. **The failure mode is worse than noise:** a developer blocked by a LOW advisory with no upgrade available has no in-gate escape, so the pressure is toward `--no-verify`, and a gate people route around protects nothing (BUG-031's process half, where red CI stopped meaning anything). **Fix:** align the local policy with CI's MEDIUM+ threshold, using the same jq-based severity classification CI already performs, and keep `docs/config/findings.md` as the documented home for tracked lower-severity CVEs — which the hook's own message already tells the operator to do while giving them no way to proceed. **Do not fix by loosening CI.** Found while costing TASK-018, which would have removed the blueprint's own exemption by adding a lockfile. | S2 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-09 by Philipp (Infrastructure-1) while costing TASK-018. Re-open immediately — it affects every derived project now, and TASK-018 would extend it to the blueprint. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-073** | **TASK-018 phase 1's "no shipping suite is TypeScript" rule forced a wrong-shaped answer twice in one day, so phase 2 was brought forward and landed.** `tests/manifest` #2b refuses a TypeScript-only suite that ships, offering two exits: keep a shell runner, or `export-ignore` the suite. `tests/bug-numbers` (BUG-071) fit neither — **every project has its own `BUGS.md` files and its own bug numbers, so every project has the defect it catches**, and export-ignoring it would have removed the check from the projects that need it, a coverage decision made to fit a constraint rather than to match risk. A shell twin would have been a second implementation of one check, the copy-that-drifts this repo has deleted twice already (`INDEX.md`, `tests/SUITES.md`); `tests/proc-cwd` still carries one and is the standing proof. **The invariant named its own resolution**: #2b enforces *the TS toolchain ships IFF a shipping suite is TypeScript*, and this was a shipping suite that is TypeScript, so the consistent state was to ship the toolchain. **Second instance the same day** — Elias hit the identical wall with `tests/blueprint-relocation/` and took the `export-ignore` exit, flagging it in `.gitattributes` as a phase-1 consequence to revisit; two suites in one day is evidence about the rule rather than about the suites. **Fixed by landing TASK-018 phase 2 whole** — the six `export-ignore` lines deleted, `#2c` given a phase-2 arm that fails when the toolchain ships with no spec or with a partial harness, `run-ts-suites.sh` taught to block when `tests/node_modules` is absent instead of letting `npx` fetch an unpinned vitest from the registry, and BUG-045's severity policy aligned per Markus's spec. | S2 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-10 by Vitali (QA-1) landing BUG-071's suite, after the Orchestrator instructed reporting the tension rather than resolving it locally — which is the only reason phase 2 was considered instead of a seventh `export-ignore` line. Re-open if a suite is ever export-ignore'd to satisfy a manifest check rather than because it is genuinely blueprint-only machinery. **No acceptance test was recorded for this row**, and it was accepted unverified. **Status when accepted:** OPEN |
| **BUG-032** | **A push runs the full gate, reports nothing wrong, and pushes nothing — `git push` dies of SIGPIPE after the gate succeeds.** Seven occurrences on 2026-08-19, each costing a ~361 s gate. **Signature, measured:** `git push` exits **141** (`128 + 13` = SIGPIPE), the gate prints `PASSED`, there is no `To github.com:` line, and `git ls-remote` shows the ref absent. **A re-run is NOT a workaround** — it appeared to be one for the first five, then failed twice consecutively on an unchanged branch, which is what made the mechanism testable. **Mechanism — CONFIRMED by experiment:** git contacts the remote for its ref state *before* running `pre-push` — that is how it fills the `<remote sha>` field the hook reads on stdin — then the gate holds the connection idle for ~361 s, then git writes to a socket the server has since closed. **The proof:** after two consecutive failures on an unchanged branch, the identical push with `GIT_SSH_COMMAND="ssh -o ServerAliveInterval=20"` landed first try, exit 0. Nothing else differed. The intermittency is the gate sitting on a ~5-minute idle boundary. **This is TASK-013's re-open trigger firing** — *"the gate crosses an interval that changes behaviour"* — just not the behaviour predicted (nobody started skipping the hook; the push started dying). The two items should be read together. **Half of the original description was measurement error and is corrected here:** the handover recorded this as *"a push can report success"*. It never reported success — the usual invocation is `git push … 2>&1 | tail -N`, and **a pipeline's exit status is the last command's**, so the `0` came from `tail` and was never evidence about git. The vanished push is real; `git ls-remote` is the only authority. **Not the fix:** `--no-verify` (denied by policy, and it defeats the gate), or retrying blindly (it works, but pays a full gate per attempt). **Candidate fixes, in order of preference:** get the gate under the idle boundary (TASK-013 — `bootstrap-gate` alone is 50.6%); set an ssh `ServerAliveInterval` so the connection is not idle; or run the gate out-of-band and leave a short hook on the push path. **Reproducer needed before any fix:** a hook that sleeps past the boundary, asserting the push still lands — without it there is no way to tell a fix from a lucky run. | PUSHED | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Raised 2026-08-19 after five occurrences in one session. Re-open immediately if a push is ever believed to have landed without `git ls-remote` confirming it — that is the failure this bug makes possible. **PROMOTED AND FIXED 2026-09-09.** Eleven pushes in one session each needed `GIT_SSH_COMMAND` with a 20 s keepalive passed by hand; without it the gate printed PASSED and the push died with *"Connection to github.com closed by remote host"*. `arm_push_keepalive` (`scripts/lib/gate.sh`) now sets `core.sshCommand` on the same two wake paths that arm `core.hooksPath`, for the same reason — it is repo-LOCAL config, so a clone starts without it (BUG-004/A-22). Same non-clobber contract: a deliberate `core.sshCommand` is warned about and preserved. Guarded by `tests/gate-arming` #10/#10b/#10c/#10d. **The row's stated mechanism was flagged by the 2026-08-20 grooming as overstated** ("causation confounded with gate duration — the ordering is confirmed, the cause is not"); this fix does not depend on resolving that, because it addresses the confirmed part (a gate longer than the idle timeout) and the remedy was already recorded as tested 5/5. **The acceptance test that was NOT run** (recorded because this was accepted unverified): `git config --get core.sshCommand` → shows a ServerAliveInterval. Then push with a plain `git push` (no `GIT_SSH_COMMAND`) and confirm it survives the ~380 s gate. `bash tests/gate-arming/test.sh` → #10/#10b/#10c/#10d. **Status when accepted:** OPEN |
| **BUG-056** | **`tests/drift-in-blueprint` #1 mutated the developer's REAL repository — it ran `blueprint drift` in `REPO_ROOT`, and `drift` arms the gate, so `arm_gate` wrote `core.hooksPath` into the real `.git/config`.** That is **BUG-047 exactly** — a test setting `core.hooksPath` on the real repo, the A-22 failure produced from inside a test — reappearing in the very migration whose canary exists to catch it. The spec's header called it *"intentionally runs the real blueprint CLI in the real blueprint checkout"*, which is the declared exemption TASK-018 R5 says must not exist: *"if a 'run this one alone' marker exists, it will be used"*. **Why it hid for the whole migration:** `arm_gate` only writes when `core.hooksPath` differs, and on every developer machine it is already `.githooks` — armed on wake by the feed and by drift itself. So the mutation is a no-op locally and real on a fresh clone, meaning the case passed for every human and failed on the first machine that had never run it. **Found by CI**, on the first push of the harness, which is the first time these specs ever ran on an unarmed checkout. **Fourth instance this week of one shape** — a test that agrees with its code on the machine it was written on (BUG-054's `</dev/null`, BUG-044's `#2` passing vacuously on Linux, BUG-055's fixture returning 0). **Fix:** the case builds a fixture carrying `.blueprint-root` and no `.blueprint-source` — the entire input BUG-007 is about — and owns the `.git/config` that drift arms. New `#1c` asserts the arming lands in the FIXTURE, so a revert to `REPO_ROOT` fails the scenario canary rather than passing quietly. Verified under CI's own condition: with `core.hooksPath` unset locally, 9 cases pass and `.git/config` is byte-unchanged. Also 7x faster (3282ms -> 460ms), because it no longer drives the whole real repo. **What was lost, stated:** "drift works in THIS checkout" is no longer asserted by a test. It is not a regression test — it is the wake protocol, which runs `blueprint drift` every session. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-09 by CI on PR #63 — the real-state canary added by TASK-018 catching a violation in TASK-018's own suite. Re-open if any spec takes `REPO_ROOT` as a working directory for a command that writes. **The acceptance test that was NOT run** (recorded because this was accepted unverified): `npx vitest run tests/drift-in-blueprint` → 9 green. The real check: `git config --unset core.hooksPath`, re-run, and confirm `.git/config` is byte-unchanged and the gate is still unarmed afterwards — the spec must not touch your repo. **Status when accepted:** PUSHED |
| **BUG-055** | **`blueprint`'s vitest stage died between two statements on every `git push`, printing nothing at all — no stage, no skip, no summary, no error — so a refused push named no cause.** `bp_suites_with_spec` (`scripts/lib/suites.sh:143`) ends its loop on `[ -n "$(find … *.spec.ts)" ]`, so the LAST classified row decides its exit status; a final row that owns no spec makes it **return 1 while printing a perfectly correct list**. `suites.sh` documents exactly this and calls it *"harmless to the one caller that reads it through `$( )`"* — reasoning about the VALUE while the caller runs under `set -e`, where a non-zero status is fatal however good the list is. `_ts_expect="$(ts_declared_suites …)"` was unprotected, so the hook was destroyed mid-function. **Measured, not inferred:** an instrumented push recorded `rc=1 declared=[proc-cwd drift-in-blueprint pull-exec-bit harness]` — the failing status beside the correct four-suite list. **A second, independent defect in the same function made it expensive to find:** the runner was `( … ) >/dev/null 2>&1` with `_ts_rc=$?` on the next line, also unreachable under `set -e`, and its output discarded — so *every* failure in this stage rendered as an absence, and an absence names nothing. `pipe_batch_end` exists to reconcile that status and could never receive it, while the per-suite note read *"see the vitest output above"* pointing at `/dev/null`. It took **eight pushes** to locate. **Correction to this row's first version, kept rather than overwritten:** it claimed the cause was git exporting `GIT_DIR` and the harness's `assertProcessEnvClean` refusing every scenario. That reproduces a real silent death and is a real hazard, but it is **not what happened here** — measured, git exports only `GIT_EDITOR`, `GIT_EXEC_PATH` and `GIT_PREFIX` to a pre-push hook, so that guard never fired. The env scrub is retained as hardening and labelled as such in the code. **Guarded by `tests/ts-bridge`** (hermetic, stubbed `npx`): `#1d` injects a non-zero status with a good list rather than coaxing one out of the manifest parser — coaxed reproduction depends on which row sorts last and would stop reproducing silently. Verified red on the mutant with empty output, the exact signature. | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-09 by Eto (Orchestrator) — surfaced by the founder's push being refused with a truncated stage list. Re-open if any gate stage consumes a command substitution's status without masking it, or if a stage can fail without printing. **The acceptance test that was NOT run** (recorded because this was accepted unverified): Push anything and watch the gate: four per-suite vitest stages (`proc-cwd`, `drift-in-blueprint`, `pull-exec-bit`, `harness`) render at the end. Before this they never ran under a push at all. `bash tests/ts-bridge/test.sh` for the guard. **Status when accepted:** PUSHED |
| **BUG-054** | **`blueprint pull` decides whether it may prompt by asking whether the PROCESS has a controlling terminal, not whether its CALLER gave it an interactive stdin — so from any pre-push gate started in a terminal it prompts on the operator's real tty and hangs, and the third fix for the same case is the one that reintroduced it.** `scripts/blueprint:1047` guarded with `: 2>/dev/null </dev/tty` and read the answer with `read -r ans </dev/tty`. Both reach past the caller to the terminal. The two conditions differ exactly when a script that HAS a terminal invokes pull with redirected stdin — which is every `git push` a human types — and there the guard does not fire. Under `set -euo pipefail` (`:37`) the subsequent `read` then either **blocks forever** on a live terminal or **aborts the CLI** at EOF; it never reaches the deliberate refusal. **Measured, not inferred:** `tests/pull-behaviour` reported `426.2s · exit 1` on an interactive push and **0.778 s** once fixed — the suite was never slow, it was blocked on a prompt, and the 426 s idle is also what made the push's own connection die with *"Connection to github.com closed by remote host"*. One defect, three symptoms, and the transport looked guilty. **Fix:** `[ -t 0 ]` and `read -r ans \|\| ans=""` — ask the caller, and let the array loop (`for f in "${files[@]}"`, which never consumes stdin) keep the fd it already had. **The case was defective too, and that is the more valuable half:** `#1` passed `</dev/null` and believed it had expressed "non-interactive", but the guard under test asked a question `</dev/null` does not answer, so the case exercised the refusal path when run detached and the broken path when run from a terminal — **green on exactly the runs that mattered least**. It now PROVIDES the condition with a pty (`script`, stdin redirected to `/dev/null` inside it) instead of inheriting whatever launched it. Verified both directions: red on the parent commit, green after. **Third appearance of "a command that declines to act must not report success"** (BUG-016, BUG-011, BUG-018) and second of "the guard tested the wrong thing and its test agreed with it" (BUG-047 chose its population by grepping comments). | S1 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-09 by Eto (Orchestrator) — surfaced by the founder's push failing, diagnosed from the gate output rather than the transport error it presented as. Re-open if any prompt in a shipped script reads from `/dev/tty` rather than stdin, or if a case simulates non-interactivity with redirection alone. **The acceptance test that was NOT run** (recorded because this was accepted unverified): `bash tests/pull-behaviour/test.sh` → runs in <1 s (was 426 s). Then, FROM A TERMINAL, `blueprint pull docs/DoD.md </dev/null` → refuses with an actionable message and exit 7 instead of prompting on your tty and hanging. **Status when accepted:** PUSHED |
| **BUG-044** | **The bootstrap's "never assume an identity" guard was itself assuming one on macOS — `new-project.sh` would create a project and commit as `<user>@<hostname>.local` with no identity configured anywhere.** The probe is `git var GIT_AUTHOR_IDENT`, chosen because it resolves exactly what git would use at commit time and honours `GIT_AUTHOR_*`. What the comment did not account for is that **`git var` does not fail when identity is absent — it GUESSES**, from the passwd gecos name and the hostname, and on macOS that always succeeds. Verified: with `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null` it returned `Luiz Scheidegger <luizscheidegger@MacBook-Pro.local>`. So the check passed, bootstrap proceeded, and the initial commit carried a machine-invented author — **exactly the outcome A-14 exists to prevent** ("baking one person into every derived repo makes other operators commit silently under someone else's name"), reached by the guard rather than around it. **Fix:** `git -c user.useConfigOnly=true var GIT_AUTHOR_IDENT`, which makes git refuse to auto-detect (`fatal: no email was given and auto-detection is disabled`). Verified on all three paths: absent identity now fails, a real configured identity passes, and the documented one-shot `GIT_AUTHOR_NAME/EMAIL` override still works because env vars outrank config-only mode. **Caught by `tests/bootstrap-identity`**, which asserted the *consequence* ("target directory was created despite missing identity") rather than the probe's return value — the reason it caught a defect the probe's own reasoning had argued was impossible. Pre-existing at `c4dc95b`. | S2 | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-09 by the gate while landing TASK-017 on a Mac. Fixed the same session. Re-open if any identity check trusts `git var` without `user.useConfigOnly`. **The acceptance test that was NOT run** (recorded because this was accepted unverified): `bash tests/bootstrap-identity/test.sh` → #2b present. Flip `user.useConfigOnly` to false in `scripts/new-project.sh` and re-run: #2b goes red and #2 stays GREEN, which is the vacuity this case exists to cover. **Status when accepted:** PUSHED |
| **BUG-040** | **The DoD gate silently enforced NOTHING on macOS — three of its rules passed vacuously on every push from a Mac.** `dod_items_in_push` (`scripts/lib/dod-gate.sh:47`) extracted the items in a push with a BRE, `sed -n 's/^\(BUG\|FEATURE\|TASK\)#\([0-9][0-9]*\):.*/\1-\2/p'`. **Alternation inside `\(...\)` is a GNU sed extension that BSD sed does not implement**, so on macOS the expression matched nothing and the function returned an empty list — and an empty item list means every stage built on it has nothing to check. Verified by execution: `printf 'BUG#36: x' | sed -n '<the BRE>'` prints nothing here, while the ERE form prints `BUG-36`. Disabled: **§1b rule 1 / §7C "every item in this push has a backlog row"**, and both regression-test requirements (`#4` a BUG with no test naming it, `#4c` the same in `doing/`). The gate kept printing PASSED over work it had stopped examining. **This is the repo's signature failure — BUG-004, A-22, BUG-005, BUG-035 — occurring inside the guard that enforces the Definition of Done itself**, which makes it the most expensive instance yet: every macOS push since the DoD gate landed was gated by a stage that examined nothing, and the green check read as "the DoD was verified". **`tests/dod-gate` caught it exactly as designed**, reporting three cases as "PASSED — the rule is unenforced", which is the argument for meta-tests that assert a guard still *fails* when it should. **Fix:** `sed -nE` with POSIX ERE alternation, which both seds implement. One line. Pre-existing at `c4dc95b`, confirmed in a clean worktree. Only one other `sed` alternation exists in shipped code (`signal-set.sh:165`) and it is escaping a literal pipe, not alternating. | PUSHED | **ACCEPTED by the founder 2026-09-11** — accepted as a batch, with the founder stating they could not verify these personally. What stands in place of that: each carries a regression test naming its number (DoD §2 fails the push otherwise, so this is mechanical rather than asserted), each landed on `main` through the full gate, and most were additionally reviewed by a second provider during the TASK-018 migration. Same basis as BUG-031, accepted on the CI record rather than by hand. | Found 2026-09-09 by the gate, while landing TASK-017 on a Mac. Fixed the same session. Re-open if any shipped script reintroduces a BRE alternation. **The acceptance test that was NOT run** (recorded because this was accepted unverified): `bash tests/dod-gate/test.sh` → #8 present and green. To see it fail closed, break the extraction in `scripts/lib/dod-gate.sh` (`#` → `@`) and re-run: #8 names the cause while #1/#4/#4c name misleading consequences. **Status when accepted:** OPEN |
| **BUG-031** | CI has been red all day and five PRs were merged over it | S1 | **ACCEPTED by the founder 2026-08-19** — verified by the CI record rather than by a test that can only speak locally. The last red run is `32280780352` at 17:17:47Z on `bug/031-ci-red-subagent-feed`; the next run on that same branch after the fix (`32281386612`, 17:24:10Z) is green, and every run since is green — two pull-request runs and two `push`-to-`main` runs, all executing `shell regression suites (full)`, the job that had failed on every run of 2026-08-19 before this. The suite that never once passed in CI (`tests/subagent-feed` #5 and #6) now passes there. Locally, `tests/subagent-feed` #7 asserts the guard directly, and reverting `scripts/log-activity.sh` to `set -uo pipefail` makes it fail. **What the green does NOT cover:** the process half. Nothing mechanically prevents the next `--admin` merge over an unregistered check — that remains a rule in the handover, not a gate, and it is the half that let a red CI run for a full day. | **CI has been red on every run since BUG-027 landed, and five PRs were merged over it with `--admin`.** The failing job is `shell regression suites (full)`, the failing assertions are `tests/subagent-feed` #5 and #6 — the suite BUG-027 itself added this morning. **That suite has never once passed in CI**: it was written, added to the workflow, and verified only on a developer machine. Both cases report `rc=2` from the hook and nothing else. **The real defect is that the assertion could not say why.** It ran the hook with `2>&1` discarded, so a full day of CI failures produced a single number that names the symptom, and the only remaining route in was local reproduction — which does not reproduce. Ruled out by measurement, not reasoning: the gitignored `AGENT_ROSTER.md` being absent (clean clone passes), suite-ordering contamination (CI's full sequence in order passes), the environment (`env -i` passes), `jq` absent (passes, on a synthetic PATH — the first probe of this was invalid because `/usr/bin` was still on PATH), and dash rejecting `set -o pipefail` (the runner is Ubuntu 24.04 with the same dash 0.5.12 that accepts it). `rc=2` is what dash returns for an unbound variable under `set -u` or an illegal `set -o`, and neither reproduces here. **The process failure is the bigger half**: `gh pr checks` was consulted once, reported "no checks reported" because the workflow had not registered yet, and was never re-checked — then `--admin` merged past the branch policy five times. A green local gate was treated as evidence about CI, which it is not.  |
| **BUG-028** | A freshly bootstrapped project cannot pass its own pre-push gate | S1 | **ACCEPTED by the founder 2026-08-19** — verified continuously rather than once: `tests/bootstrap-gate` is a pre-push stage, so it has bootstrapped a project and run that project's whole gate green on every push since the fix landed (177.9 s, the single most expensive stage in the gate). **What the green does NOT cover:** projects bootstrapped BEFORE the fix already carry the broken `.githooks/pre-push-project` on disk and are not retroactively repaired — they need a `blueprint pull`. Inherent to the fix, not a gap in it, but no passing test can speak to it. | Found by the TASK-012 strip test, which needed a healthy control and did not get one: `EXIT=1` at 70 s on a zero-second-old bootstrap. **Root cause of five of the six: `.githooks/pre-push-project` ships downstream.** `MANAGED_FILES` excludes it explicitly (*"project guards, never synced"*) and `new-project.sh` tells the operator to copy the `.example` — but `.gitattributes` never `export-ignore`s it, so `git archive` carries the blueprint's own 34 KB guard into every derived project. **Every derived project's gate is the blueprint's self-test suite**, including `bootstrap-contents`, `bootstrap-identity`, `template-source` and `drift-in-blueprint` — four suites that test blueprint-only machinery and cannot pass anywhere else. The sixth, `pull-exec-bit`, reports itself **vacuous** downstream because placeholders are already substituted. Separately, `blueprint drift` reports **5 drifted files on a zero-second-old bootstrap** — files carrying `{{PROJECT_NAME}}` in prose that are missing from the substitution targets. **Same shape as A-22 and BUG-004**: the gate looks armed and is measuring the wrong thing, and nobody notices because the failure arrives on someone else's machine.   |
| **BUG-029** | A derived project's test suites never update, while the scripts they test do | S1 | **ACCEPTED by the founder 2026-08-19** on a demonstrated run: a bootstrapped project's corrupted `tests/pipeline/test.sh` was reported by `blueprint drift`, where before the fix drift had no opinion about test files at all | **FIX.** `MANAGED_FILES` gains two entries: `tests/` — the first managed **directory** — and `.githooks/pre-push-project`, which has to travel with it. `bp_expand_managed_dirs` (called at the end of `read_blueprint_source`) replaces a trailing-slash entry with the files `git archive HEAD <dir>` ships, so `drift` and both `pull` loops are unchanged. **`git check-attr` is not the query**: a trailing-slash directory pattern in `.gitattributes` does not propagate to the files under it, so it reports `unspecified` for a suite git genuinely drops — `tests/manifest`:139-141 had already rejected it for this and `tests/suite-sync` #1c now pins the trap. **HEAD, not the working tree**, on the same argument as manifest #2b; content still comes from the working tree. **Additive only, no delete path** — the project cannot distinguish "the blueprint dropped this" from "we wrote this", and a prefix delete would take out project-authored suites; the announcement already exists, fail-closed and by name, in `tests/manifest` #1. `bp_should_substitute` now exempts `tests/*`: three suites carry a literal `{{PROJECT_NAME}}` as fixture data, safe only while `tests/` was unmanaged (the BUG-028 self-corruption class). `a2bp` validates a managed directory as a **path prefix**, because `cmd_a2bp` deliberately never resolves a local HEAD to expand from. `.githooks/pre-push-project` and `tests/SUITES.md` are split by `BLUEPRINT:BEGIN`/`END` markers, driving `marker_aware_merge` — built for this and until now used by nothing. **REVIEW R2 (Andreas, Codex) — REJECTED, two findings, both fixed.** **S1: the expansion swallowed failures**, so a broken or empty one degraded to "no test files are managed" and `drift` reported clean and exited **0** over it — BUG-029 reproduced inside its own fix, and the fifth instance of the shape. Now fail-closed: `git archive` and `tar` are run and checked **separately**, never as `x=$(a \| b \| c)` where only the last stage's status survives (the trap behind BUG-027 finding (b)), and an empty expansion is a hard exit naming the entry. **S2: the substitution exclusion was too broad** — but it was a **call-site bug**, not a pattern bug: `substituted_blueprint_copy` was the one site of five passing an ABSOLUTE path to `bp_should_substitute`, so a leading-component rule matched the checkout's own location and a blueprint under any directory named `tests/` exempted every managed file. Fixed at the call site (it now takes the repo-relative path, with the contract documented on the predicate); the pattern narrowed to `tests/*`. The symptom is on the **comparison** path, not the pulled bytes — the file that lands is correct either way, but drift compares against an unsubstituted copy and reports every templated file drifted forever. **Consequence of fail-closed, accepted deliberately:** a checkout with no `tests/` at HEAD is no longer a usable blueprint, so four suites' minimal fixture blueprints (`drift-in-blueprint`, `pull-behaviour`, `a2bp-contamination`, `staleness/drift-integration`, and `marker-merge`'s tree-only clone, which is now a real git repo) had to start shipping suites. That is a real coupling tax on every future fixture, and it is the price of the alternative being a silent zero-sync. **REVIEW R3 (Andreas, Codex) — REJECTED, one finding, fixed.** The same status-swallow, a third time, in a consumer R1/R2 did not reach: `new-project.sh` read `blueprint files` as `done < <(_synced_files)` — process substitution discards the status — and `_synced_files` was itself `blueprint files \| sed`, whose status is `sed`'s. Two nested swallows. An empty list means the substitution loop body never runs, so no guard inside it could fire: measured on `9e5bc1f`, a broken CLI gave rc=0, "Substituted placeholders in 0 file(s)", "Happy struct2flowing", and a delivered project with five literal `{{PROJECT_NAME}}` in CLAUDE.md. Worse than the drift case — a stale project pulls forward, a project that never had its identity written is broken from its first minute. **Fixed before anything is created**, beside the git-identity probe, which already carries the argument: a late check leaves a half-bootstrapped directory whose presence defeats the "fix it and re-run" advice. **Two corrections to the finding, verified empirically:** `cmd_files` never calls `read_blueprint_source`, so `blueprint files` does not expand `tests/` and cannot fail from the expansion (73 lines, rc=0 throughout); and bootstrap does not produce "zero tests" — the suites arrive via `git archive`, 36 of them present in the broken run. The defect is the swallow itself, so the regression injects the failure directly rather than staging an unreachable cause. **The sweep found a fourth instance, in a control:** `tests/bootstrap-gate` #5 read its own list the same way and PASSED over an empty stream; now resolved first, asserted non-empty, with a floor on paths examined. `tests/bootstrap-contents`:113 and `tests/env-namespace` use the same substitution but already fail closed on an empty result — not instances. No fifth found. **Regression:** `tests/bootstrap-gate` #7 (exit status, message names the cause, no project directory survives, no success banner) — red on `9e5bc1f` on three of four. **REVIEW R4 (Andreas, Codex) — production path CONFIRMED sound three rounds over; remaining findings were all in the CONTROLS, and all one theme.** R3's sweep tested for **empty** output on failure; the gap is **partial** output on failure — a command that dies after printing some of its lines, where a non-empty result makes a content check look satisfied. My "not an instance" reasoning for `tests/bootstrap-contents` and `tests/env-namespace` ("an empty result fails loudly", "`[ -n ]` plus a count floor") holds for the empty case and **does not hold for the partial case**: a floor of 20 passes on 25 lines from a command that died at line 25 of 70. Same shape as BUG-027 finding (b), where a killed roster lookup's partial stdout was accepted as a persona name. **Fixed in all three** — `tests/bootstrap-gate` #5, `tests/bootstrap-contents`, `tests/env-namespace` now check the **exit status** before the content, and `env-namespace`'s `$(a \| b)` was split because the pipeline reported `sed`'s status regardless. Verified by injecting a CLI that prints a plausible prefix and exits 4: all three fire, and in `bootstrap-contents` the status failure now reports **first**, so the diagnosis is the truncated list rather than the misleading "libs are not in MANAGED_FILES" it would otherwise produce. **Added `tests/bootstrap-gate` #7b** — the successful-but-empty refusal in `new-project.sh`, which had no case: #7 drives the non-zero path, #7b the status-0-with-nothing-to-iterate path, and neither check alone catches the other's case. **Regression:** `bash tests/suite-sync/test.sh` (14 assertions, ~3 s, in the gate + CI) — red on the parent at #1, #2, #3, #5, #6, #7, and red on R1 at #8, #8b, #9; plus `tests/bootstrap-gate` #6, #7 and #7b (a full pull is a no-op on a fresh bootstrap and its manifest still passes). **One-time cost for existing projects**, stated rather than hidden: the first pull shows ~40 new files, and the markerless→markered hook takes `pull_file`'s asymmetric branch — the project's guards are preserved in `.githooks/pre-push-project.bp-bak` and must be re-appended after `BLUEPRINT:END` once, loudly. **Original report.** `tests/` is not in `MANAGED_FILES`, so a derived project's test suites are frozen at the day it was bootstrapped while `blueprint pull` keeps updating the scripts they test.** Bootstrap seeds the whole tree, so the suites arrive; `pull` then walks `MANAGED_FILES` file by file and `tests/` appears nowhere in it. Every project that exists is already carrying suites that are older than the code under them, and the gap widens on every pull. **The failure is silent in the worst direction**: a stale suite still passes, so the project's gate goes green over assertions written against a version of the machinery it no longer runs — the recurring shape this repo has hit four times (A-22, BUG-004, BUG-018, BUG-028), a mechanism present and reporting nothing, whose silence is indistinguishable from success. BUG-028 is the proof it is not theoretical: a bootstrapped project failed six of its own suites and nothing downstream could ever have told anyone. **What ships is already computed** — BUG-028 made `export-ignore` the predicate for the five blueprint-only suites, so the managed set is the complement of that and does not need a second hand-maintained list. The consumers (`files`, `drift`, `pull`, `a2bp`) all treat entries as plain paths, so the shape of the fix is expanding a directory entry once after `BLUEPRINT_ROOT` resolves, not four new loops. **A hand-listed set of 33 suite paths is the wrong answer**: it drifts the moment someone adds the 34th, which is this bug again one level up.   |
| **BUG-027** | The activity feed goes dark for the whole duration of any Claude-persona work | S2 | **ACCEPTED by the founder 2026-08-19** (verified live: a Claude persona's work streamed under its roster name while it worked) | Two verified causes. **(1)** `project_jsonl:296` filters `.isSidechain != true`; every assistant record in a subagent transcript has `isSidechain: true`, so the projection drops 100% of them. The filter is correct for the MAIN transcript and exactly wrong for a subagent's own — one function, two files with opposite requirements. **(2)** `log-activity.sh:63` treats `.subagent_type` as the persona; it is `general-purpose`, so every label and every `.subagent-map` row is the same string. **Impact:** 28 tool calls over 4 minutes produced two lines, and the founder watched work happen in the UI while the feed stayed blank. **It punishes the rule it serves** — delegating to a Claude persona buys a blackout, so working solo keeps the feed live. **THREE FINDINGS LEFT UNFIXED, deliberately** (Elias R3, Infrastructure-2/Codex). (a) `BP_ROSTER_LOOKUP_TIMEOUT=0` disables the bound — GNU `timeout 0` means *no* timeout, so the knob added to prevent hanging has a value that guarantees it. (b) A lookup killed mid-write has its **partial stdout accepted as a persona**: a poisoned lib emitted `Eli` then hung, and the hook logged `[Eli - Claude Code]` and persisted it — a stable false identity rather than a graceful fallback, because the timeout's exit status is lost inside command substitution. (c) `agent-activity.sh` sources `roster.sh` into the **supervisor's own shell**, so a poisoned lib hangs `--whoami` and the feed daemon — Elias reproduced it and said explicitly it should not block this landing, since the daemon exists to run that lookup and guarding it needs a different design from the hook's one-shot. **Landed with these open because the blackout itself is fixed and each round of repair was producing the next finding** — the fix rate had stopped exceeding the discovery rate.   |
| **BUG-023** | `signal-set.sh` swallowed a failed journal append, so a mic flip could publish while its event vanished | S2 | **ACCEPTED by the founder 2026-08-17** | The journal was a BACKSTOP "read by nothing that makes a decision", and `\|\| true` was correct under that contract. **FEATURE-003 made it the replay's SOURCE and did not update the writer.** Found by Jesko (Codex) in review round 7 — after I claimed a durable append-only journal reduced "incomplete replay" to two states and challenged him to find a third. He found it in the *writer*, which was not in the feature's diff at all: a flip published, its append failed, a later flip succeeded, and `session-resume` reported one event with no warning and exit 0. **Durable is not complete** — a file nothing truncates still has holes if its writer drops records, and I reasoned from "append-only" straight to "cannot be silently short" without checking who writes it. Exit **8** = "published, but not journalled", a distinct status because the baton is already published by then and a caller retrying a plain failure would double-publish; the message leads with *do NOT retry*. **The stale contract then had to be corrected in three more places** (`lib/state-dir.sh`, then two comments in R8 and R9) — a contract change that is not propagated is a defect **per site, not per file**, and I fixed it per instance three times before naming the class. Test: `tests/signal-set/test.sh` #6, red on its parent. |
| **BUG-005** | The pre-push gate was at its 30 s ceiling, and coverage was being decided by the clock | S2 | **ACCEPTED by the founder 2026-08-02** (two Codex rounds) ([review](CODEX-REVIEW-BUG-005.md)) | Founder decision: *"I don't need this 30 seconds ceiling, as long as I see that the gates are working and doing what they need to do."* The ceiling is removed and replaced with a rule — **coverage is decided on risk, never on the clock** (CLAUDE.md §"Pre-push tolerance", DoD §3.7). The diagnosis held up exactly as written: the budget had started deciding what was tested, silently, while the gate still reported "all checks passed". **Restored to the gate:** `a2bp-contamination` (41 assertions guarding the door BUG-002 and A-09 came through — exiled for growing 2.3s→6.0s), `staleness/test.sh` (offer bounds + unknown-never-current, called "a forced choice, not a preference" at the time), `signal-set` (0.2s — excluded by a rule that only existed because the gate sat at 27.6s of 30s), and the four `--fast` downgrades now run in full: `agent-activity-bound` (+10.4s — restores the race / fault-injection cases guarding BUG-001), `pre-push-secrets` (+5.1s — the budget cases proving the scan fails CLOSED when it cannot finish), `roster` (+3.0s — the live-supervisor case the founder actually hit), `drift-integration` (5 of 5 cases, was 2). **Gate: 19 → 22 stages, 32.5s → 63.8s.** The replacement discipline is visibility, not a budget: every run reports its total AND its slowest stage, so cost is argued with a number instead of paid silently in coverage. **One exclusion remains and it is a risk call, stated with its number:** `tests/signal-dispatch/` at **125.4s** — 2× everything else combined, and it guards the dispatcher watcher, which is not on the push path. Founder should confirm or overrule that one. **REOPENED the same day by Alexey (Codex), and both findings verified here.** **F2 HIGH — the `signal-dispatch` exclusion reproduces this bug's own structure.** The 125.4 s is NOT inherent, it is test scaffolding: `run_watch` always starts an infinite watcher under `timeout` and discards the timeout status, so every case burns its full bound no matter when its assertion becomes decidable. Verified: `run_watch` bounds are 22+18+26+13+26+20 = **exactly 125**. The watcher already accepts `--poll` and settle is configurable via `AGENT_SIGNAL_SETTLE`, and no assertion needs production-scale seconds. So "125 s, therefore a risk call" was a rationalised unexamined cost — the same clock-first move, one commit after criticising it. Also verified: `.github/workflows/security.yml:163-165` **still cites the deleted 30 s ceiling** as the reason, so the claimed risk-based reclassification never existed in executable config. **F1 MEDIUM — the replacement rule was a slogan.** I claimed "a silent skip is not expressible" because `pipe_skip` requires a reason. False: a suite **omitted** from the hook never reaches `pipe_skip` at all, and `pipe_skip` only renders `$2` without validating it. `signal-dispatch` is the live proof — the gate cannot report an exclusion it does not know about. Fix order (Codex's, agreed): fix the harness FIRST with a test clock, re-measure, reclassify on the number; then a versioned suite manifest declaring every suite's tier + risk rationale with a test that fails on an unclassified suite or a clock-only reason; then a non-blocking performance SLO that warns and never auto-demotes. Codex could not certify the 63.8 s number from his sandbox (semgrep could not write `~/.semgrep`, failed closed at 1.82 s), so it is single-sourced to this host. **RESOLUTION.** *F2:* re-clocked `tests/signal-dispatch/` — everything is now expressed in settle units and each watcher is stopped as soon as its assertion is decidable, instead of running to a fixed `timeout` and discarding the status. **125.4 s → 75.0 s with all six assertions intact**, verified additionally at the original pacing (`SIGNAL_TEST_SETTLE=3`) in CI so a timing assumption cannot hide in the fast clock. `SETTLE` stays an integer second deliberately: the watcher compares `date +%s`, so sub-second settle would mean changing dispatch timing in production code for test convenience. Suite is now in the gate. Stale CI rationale citing the deleted ceiling is fixed. *F1:* the rule now has two real controls — `tests/SUITES.md` classified every suite (tier, risk-if-absent, rationale) — deleted by TASK-020, which derives the suite set from the runners on disk and the tier from `.gitattributes` instead — and [`tests/manifest/`](../../tests/manifest/manifest.spec.ts) — its shell runner retired by TASK-018 phase 2 and ported to TypeScript, assertions proven equivalent across 21 perturbed trees — fails the push on an unclassified suite, a `pre-push` suite the gate never invokes, or a rationale arguing from cost; plus a **non-blocking** SLO (120 s total / 45 s stage) that warns and points at optimising, with no power to demote — the old rule blocked, which is exactly what made moving a suite out the cheapest way to satisfy it. **The manifest earned itself on its first run:** it found `a2bp-e2e` gate-only with no CI backstop, and `drift-in-blueprint` running **nowhere at all** — in neither the gate nor CI, audit finding A-15 exactly. Both now run in both. **Gate: 22 → 25 stages, 63.8 s → 140.1 s.** **ROUND 2 (Codex, [R2 review](CODEX-REVIEW-BUG-005-R2.md)) — timing audit CLEAN, two more findings, both fixed.** Alexey independently verified `SETTLE=2` is sound for the quantisation race (a 0.8 s interval crosses at most one integral boundary, so observed elapsed ≤1 and stays strictly below 2), confirmed case #5 sits comfortably on the other side, confirmed early-stop did **not** weaken #1/#5/#6 (each holds the watcher two further settle windows before its exact-count check), endorsed leaving production settle semantics alone, and reproduced ~75 s three times (75.23/74.92/74.98). He could not certify the full-gate number — semgrep cannot write `~/.semgrep` in his sandbox, failing closed at 1.93 s — so that figure stays single-sourced to this host. **R2-F1 MEDIUM: the manifest proved strings existed, not that suites execute.** Two bypasses, neither requiring a lie in the manifest: commenting out an invocation still satisfied the unanchored `grep`, and discovery recognised only `tests/*/test.sh`, so `tests/unclassified/check.sh` was invisible. Both reproduced here, then fixed — comments are stripped and an anchored `bash tests/<suite>/<file>.sh` is required, and every shell file under `tests/` must belong to a declared suite. Both bypasses now fail closed, verified. **R2-F2 LOW: the SLO warned on every successful run** — 120 s/45 s against a 142.3 s/75 s baseline, so it could not distinguish regression from normal operation and would have been trained out. Raised to 180 s/95 s (~26% headroom above the measured baseline) with a documented ratchet-down as optimisation lands, plus a test asserting a healthy gate is SILENT. Also scoped the word "sound" to the integral-clock race only: a 0.8 s sleep is a minimum, not a bound, and a multi-second scheduler stall can still stretch it — case #5 characterises that and `signal-set.sh` is the real boundary. |
| **FEATURE-002** | The pre-push gate renders as a pipeline, and the push watches CI afterwards | — | **ACCEPTED by the founder 2026-08-02** | Founder ask: "is it possible to have it visual, like a release pipeline?". The reason it is worth more than looks: **a gate that does not run prints nothing, which looks exactly like a gate that passed** — which is how the ungated push of `fdae0e2` went unnoticed the same day (BUG-004 evidence). A banner makes absence visible. New `scripts/lib/pipeline.sh` (POSIX sh — the hook is `#!/bin/sh`) renders one line per stage with status + duration, buffers each stage and prints its output only on failure, ends in PASSED/FAILED with a tally. `pipe_note` lets a passing stage keep a fact that buffering would otherwise swallow (the semgrep `--jobs 1` retry). **Fails closed by construction**: `pipe_stage` exits rather than returning a status one of ~18 call sites could drop; `tests/pipeline/` asserts that against non-zero exit, signal kill, missing binary, final-stage failure, and no scratch dir. Degrades to unbuffered rather than refusing to start when `mktemp` is unavailable — "cannot create a temp dir" is a bad reason to block a push and a worse one to allow one. `scripts/watch-ci.sh` streams the required checks after the push, detached, hard-deadlined, never touching the push exit status (`AGENT_CI_WATCH=0` opts out). **Side benefit that matters for BUG-005:** per-stage timings show `agent-activity-bound` is 19.2s of 31.4s, so the budget is now arguable rather than guessed. |
| **BUG-010** | Renaming a persona in `AGENT_ROSTER.md` changed nothing — every surface kept using the shipped example name | S2 | **ACCEPTED by the founder 2026-08-02** (`fda2971`) | Nothing read the roster to decide who a session IS: the name lived in the scripts. Because the roster is gitignored and per-engineer (no two fleets share names) and both scripts are in `MANAGED_FILES`, one fleet's names were shipping to every derived project as literals — BUG-002's contamination class, in the one file whose purpose is to be per-person. Four sites, each with its own copy of the rule: the feed took the name from `${AGENT_PERSONA:-<literal>}`; `team-kickoff.sh` carried all 15 personas as a literal array, so the one script documented as "confirm the roster after editing it" couldn't see the roster and wrote the example's names into the live baton, undoing the rename it existed to confirm; `persona_label()` did read the roster but matched `"\| $name \|"` on literal single spaces, so a column-padded table never matched — and a miss was indistinguishable from "no roster", so it failed **silently**, which is how it survived; `CLAUDE.md` + `AGENTS.md` named the default Orchestrator as if it were the answer. **Fix:** one parser, [`scripts/lib/roster.sh`](../../scripts/lib/roster.sh), sourced by every reader — the same "one mechanism, never two that agree only by coincidence" rule `lib/state-dir.sh` enforces for A-09. ROLE is the key and name is data (a rename is one cell); fields are trimmed; only the Members table is parsed; a miss warns on stderr once per key per process. Unresolved identity falls back to the **role**, not to a name — `[Orchestrator]` is visibly broken where a plausible name is not. Adds `agent-activity.sh --whoami`, closing the observability gap that let this hide: identity was only observable by reading log lines. **Verified live:** this repo's roster says the Orchestrator is Anna and every surface said Sylvia. Regression: [`tests/roster/test.sh`](../../tests/roster/), 14 assertions, 0.185 s, in the gate; red on the parent commit (`git worktree` verified, exit 1). Doc-sync in the same commit: deck slide 9, README, AGENTS.md, CLAUDE.md §"On wake" + concern-9 list. |
| **BUG-006** | `LWA_FEED_*` env-var namespace in `scripts/log-activity.sh` — BUG-002's contamination in env-var form | S3 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | Same class as BUG-002: a name specific to one project baked into a generic script. Detail in [BLUEPRINT-AUDIT-2026-07-23.md](../config/BLUEPRINT-AUDIT-2026-07-23.md). Was `A-08`. **FIX.** `log-activity.sh` now routes through `scripts/lib/feed.sh` — the shared appender — instead of carrying its own copy of "append then rotate" keyed on `LWA_FEED_*`. Two defects in one: the namespace was one project's initials in a file that ships everywhere, and the duplicated rotation is exactly where the inode-preserving trim drifts — a `mv`-based rotate in one copy would orphan the feed supervisor's open handle while the other stayed correct. **No back-compat alias**: keeping `LWA_` would preserve the very string the bug is about, and these were undocumented knobs whose defaults are unchanged. **The guard is generic** — [`tests/env-namespace/`](../../tests/env-namespace/) derives its file list from `MANAGED_FILES` and fails on any externally-settable knob outside the approved prefixes. It immediately found two more that I had introduced myself in the BUG-010 rewrite (`KICKOFF_CLAIM_PAUSE`, `KICKOFF_HANDOFF_PAUSE`) plus `FOREGROUND`, all now `AGENT_`-prefixed. Fixing three names would have left the next one to be found by hand, and this repo has found four of this class by hand already (BUG-002, 006, 009, 010). |
| **BUG-009** | `project_config_*.md` are simultaneously this repo's own config AND the seed template, so blueprint-specific content propagates to every new project | S2 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | A hard-coded wake-time Monitor row for the agent-exchange board sat in `project_config_paths.md` — including a rationale describing an incident that happened in *this* stream — and was seeded verbatim into linkedin-watcher-agent. Same class as BUG-002: one project's specifics in a file that travels. **Mitigation:** the Monitors table now ships empty with guidance, and this stream's own row moved to `HANDOVER.md` (project-owned, never synced). **Structural fix not done:** the template and this repo's own config are still the same file, so the next concrete thing written there propagates again. Real fix is a separate template source (e.g. `templates/project_config_*.md`) that `new-project.sh` seeds from, letting this repo keep its own live config. **STRUCTURAL FIX.** `templates/project_config_*.md` is now the seed source; the root copies are this repo's own and are `export-ignore`d, so `git archive` — what bootstrap ships — cannot carry them. `new-project.sh` copies from `templates/` straight out of the blueprint working tree and **fails loudly** if a template is missing, because a project bootstrapped without its config looks fine until someone needs one. `templates/` is export-ignored too: a derived project carrying a seed source could bootstrap from it and would look like a blueprint. **The guard asserts against `git archive` itself**, not against `.gitattributes` — the export BEHAVIOUR is what matters, and a rule present but not taking effect is exactly the failure being guarded — and runs a real bootstrap to prove the split did not silently drop the five configs. It immediately caught `tests/bootstrap-contents/`, whose fixture built its stand-in blueprint from `git archive` and was therefore no longer a blueprint at all. Fourth and last instance of the travelling-contamination class in `doing/`, with BUG-002, BUG-006 and BUG-010. |
| **BUG-008** | `blueprint pull` strips the executable bit, so a pulled hook is armed but never runs | S1 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | The marker-aware merge writes to a `mktemp` (mode 600) and `mv`s it into place; `mv` carries the temp's mode onto the destination. **Silent and severe:** `core.hooksPath` is set, so the gate looks armed, but git skips a non-executable hook without a word — BUG-004's failure mode through a different door. The 2026-07-30 fix added `_bp_sync_exec_bit` to all four write paths — **but every one of them calls `substitute_placeholders` on the very next line, and `bp_substitute_in_place` ([lib/placeholders.sh:190-199](../../scripts/lib/placeholders.sh#L190-L199)) does its OWN `mktemp`(600) + `mv`.** The chmod is undone by the next statement, so the fix repaired the caller and left the primitive. Affects any managed file that is both executable and placeholder-bearing, minus the `_should_substitute` exemptions (`scripts/blueprint`, `scripts/new-project.sh`): **`.githooks/pre-push`** — the gate itself — plus `scripts/start-codex-signal-watch.sh` and `scripts/start-gemini-signal-watch.sh`, all three verified 775 + placeholder-bearing. **Reproducer:** a temp project with a 755 `.githooks/pre-push`, then `blueprint pull --yes .githooks/pre-push` → **600, not executable**. Surfaced by the linkedin-watcher-agent session, which verified the primitive and call order but explicitly flagged the end-to-end path as unconfirmed; confirmed here. Mitigation that masked it: `gate.sh` refuses to arm on a non-executable hook, and `cp` onto an existing file preserves that file's mode, so a project already at 755 via a different path looks healthy. Fix direction: preserve the destination mode inside `bp_substitute_in_place` (`chmod --reference` before the `mv`) — repair the primitive, not the four call sites, since `mktemp`+`mv` mode loss resurfaces wherever else the pattern is used. **The missing regression test is why this shipped as "fixed".** **Closed after TWO Codex rounds that found five defects in the fix itself** — recorded because the ratio is the useful part: BUG-011 survived my first fix on the COMMON path (`command -v gh` still returned "filed"), and my BUG-008 fix reintroduced BUG-008 through its own error path (`bp_copy_mode` returned success when both mode probes failed, so the 600 temp was installed anyway). A third round found a further no-PR path — `gh pr create` exiting zero with no usable URL. Each round's fix was verified by re-running Codex's own mutation. |
| **BUG-011** | `blueprint a2bp` pushes the branch, opens no PR, prints a green ✓ and exits 3 ("filed") | S1 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | `bp_file_existing_pr` ([lib/request-file.sh:115-120](../../scripts/lib/request-file.sh#L115-L120)) runs `gh pr list … --jq '.[0] \| "\(.state)\t\(.url)"'`. On an empty list `.[0]` is `null`, and jq interpolates that as the **literal string** `null`, so the function prints `null\tnull` instead of nothing — verified: `echo '[]' \| jq -r '.[0] \| "\(.state)\t\(.url)"'` → `null^Inull`. The caller ([scripts/blueprint:1116](../../scripts/blueprint#L1116)) guards with `[ -n "$existing" ] && [ "$existing" != "<tab>" ]` — it anticipated empty fields but not the `null` literal — so a non-existent PR passes as an existing one, falls to the `*)` branch, prints `✓ request already open: null` and returns `BP_RC_PENDING` (3). **Worse than a silent failure: the exit code asserts filed while nothing was filed**, which is precisely what CLAUDE.md forbids ("no script may read 'PR opened' as 'the blueprint has this'"). It breaks the only sanctioned path for improvements to reach the blueprint. Hit both of that session's requests; the branches and commits were correct, only the PR step failed, and both PRs were opened by hand ([#1](https://github.com/LuizStruct2Flow/blueprint/pull/1), [#2](https://github.com/LuizStruct2Flow/blueprint/pull/2)). Fix direction: treat `null`/empty as "no PR"; only return 3 once a PR URL is actually in hand; branch-pushed-but-no-PR is exit 5 (operational failure), never 3. **Folded in 2026-08-02 (founder decision) — the trust half of the same defect.** Filing a request needs push access to the blueprint remote, and in the same-owner setup every agent authenticates as the owner: verified `enforce_admins: false`, `restrictions: null`, effective permission **admin**, so branch protection's PR requirement does not apply to the identity the agents present. A derived project's agent that runs plain `git push` reaches `main` directly — evidenced by every push this session printing `remote: Bypassed rule violations for refs/heads/main`. So "a2bp cannot write into the blueprint" was an overclaim: the discipline is a convention `a2bp` implements, not a boundary the repository enforces. This bug is what makes it bite today — the branch is pushed, the PR never opens, and exit 3 reports "filed", so the review step is skipped while success is asserted. **Not fixable with repository settings**: no ruleset can distinguish a derived project's agent from the owner while both present the same credential; it needs a separate narrower credential (or a fork), which is independent of `enforce_admins` and so does NOT cost trunk-based development. **Founder decision 2026-08-02: accept the model, document it honestly** — done in CLAUDE.md, README.md, docs/A2BP_PLAYBOOK.md and project_config_paths.md §"Back-propagation trust boundary". The code fix for the `null` PR remains open. **Closed after TWO Codex rounds that found five defects in the fix itself** — recorded because the ratio is the useful part: BUG-011 survived my first fix on the COMMON path (`command -v gh` still returned "filed"), and my BUG-008 fix reintroduced BUG-008 through its own error path (`bp_copy_mode` returned success when both mode probes failed, so the 600 temp was installed anyway). A third round found a further no-PR path — `gh pr create` exiting zero with no usable URL. Each round's fix was verified by re-running Codex's own mutation. |
| **BUG-016** | Pulling ONE file bumps `bootstrap_sha` to blueprint HEAD, so drift then reports the project as fully synced | S3 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | `blueprint pull scripts/blueprint --yes` — a deliberate single-file pull — ended with `Updated .blueprint-source bootstrap_sha → 3d09ba1…`, the blueprint's HEAD. `bootstrap_sha` is what `drift` measures "commits since sync" from, so after any single-file pull the commit-log half of the report reads **empty** while every other managed file is still behind. The file-by-file half still works, which is the only reason the gap stayed visible at all. Observed in a session that pulled one file and was afterwards told it was level with a HEAD it had adopted almost nothing from. Fix direction: only advance `bootstrap_sha` when the pull covered every drifted file (or record it per-file), and say which of the two happened in the output. |
| **BUG-017** | `pull`'s confirmation diff is rendered BEFORE substitution, so the diff you approve is not the content that lands | S3 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | The per-file preview diffs the project's file against the raw blueprint temp, but `substitute_placeholders` runs *after* the write. So the operator is shown `{{PROJECT_NAME}}` being written **into** their files — e.g. `-JSON lines to ~/.linkedin-watcher-agent/logs/…` / `+JSON lines to ~/.{{PROJECT_NAME}}/logs/…` — and approves a change that is the reverse of what happens. Verified benign in outcome (0 literal placeholders after the pull) but not benign in effect: it trains operators to approve diffs they have read as wrong, and it is indistinguishable from the genuine de-substitution failure it imitates. In this session it directly caused a false alarm and a detour to disprove it. Fix: substitute into the temp before rendering the preview, so preview and result are the same bytes. **CLOSED OBSOLETE 2026-08-03 — verified NOT reproducible at HEAD.** The confirmation diff is already rendered against the substituted copy; probe output and reasoning in [`tests/pull-behaviour/README-BUG-017.md`](../../tests/pull-behaviour/README-BUG-017.md). The report was accurate about the code that project was RUNNING — a CLI 18 commits behind, which it could not know because that is BUG-013. Recorded rather than deleted, with the rule it produced: **reproduce a derived-project report against blueprint HEAD before working it**, since "which code was actually running?" is a question a derived project has historically been unable to answer for itself. |
| **BUG-004** | A fresh clone is ungated: `core.hooksPath` is repo-local, so cloning and pushing never invokes the gate | S1 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | Reopened 2026-07-29 after QA-2 rejected it with a live reproduction: a fresh clone, a real high-entropy token committed, `origin` redirected to a throwaway bare repo, and a real push executed without ever running the feed or drift — `real_ungated_push_rc=0`, `secret_commit_reached_destination=yes`. `arm_gate` works; the acceptance boundary was never "the arming paths work" but "a human cannot clone and push without invoking them". **Not closable by another local hook** — a pre-push hook is repo-local, absent on a clone, and defeated by `--no-verify`. **SPLIT IN TWO on 2026-07-30 after a founder correction** (I had written this as one all-or-nothing trade against trunk-based development, which was wrong). **Half A — DONE 2026-07-30:** branch protection on `main` with `enforce_admins: false`, four required checks, and a PR required (0 approvals) for non-admins. Verified by probe: admin direct-push to `main` still works, so trunk-based is untouched. **Its real value is narrower than I first said, and the narrower claim is the honest one** — the repo is public and user-owned, so outsiders already could not push (no write access; GitHub forces fork+PR). What this actually buys: (1) a PR **cannot merge red**, which is what puts FEATURE-001's contamination scan server-side on a2bp request PRs where a requester cannot skip it, and (2) any future write grant — collaborator, derived-project token — is constrained. **Half B — the owner's own fresh clone** is BUG-004 as reproduced, and Half A deliberately does not close it; levers are accept-explicitly, detect-via-CI (already runs on push to `main`; adequate for tests, **weak for secrets: the repo is PUBLIC, so a pushed secret is world-readable before gitleaks starts — CI's answer is "rotate", never "that did not happen"**), or drop the bypass (the only option that actually costs trunk-based). **New evidence 2026-08-02 — arming is REVERSIBLE, and silently.** Something on this host rewrote `.git/config` at 12:40:15, setting `core.bare = true` and wiping `core.hooksPath`. Git then refused every operation (`fatal: this operation must be run in a work tree`) until `core.bare` was unset — but the wiped `hooksPath` was NOT noticed, because nothing reports it at push time. The push of `fdae0e2` at ~13:02 therefore went out with the gate **not running**; the next `agent-activity.sh --daemon` re-armed it and printed `(was unset)`, which is the only reason it surfaced at all. Damage bounded: it was docs-only and the 4 required CI checks passed on that SHA. This is Half B's argument from a new direction — the local gate is not merely absent on a fresh clone, it can be *disarmed underneath a working checkout* by an unrelated process, and the push that follows looks identical to a gated one. Whatever wrote that config is unidentified; a concurrent session was active in this repo. Folder: [BUG-004-gate-arming/](BUG-004-gate-arming/). Was `A-22`; the server-side option was `A-37`. **HALF B CLOSED 2026-08-02 (founder decision), and the framing that kept it open for days was mine and wrong.** I repeatedly presented three levers — accept the risk, rely on CI detection, or drop the admin bypass (which ends trunk-based) — all policy trades, and kept handing them back. I never checked whether GitHub offered a **server-side preventive** control. It does: secret-scanning **push protection**, free on public repos, was `disabled`. It blocks a push containing a detected secret at GitHub's side — before the objects land — regardless of whether a local hook ran, regardless of `core.hooksPath`, and regardless of `enforce_admins: false`. **Enabled 2026-08-02** (`secret_scanning=enabled`, `secret_scanning_push_protection=enabled`), which closes the exact residual risk QA-2 rejected the original fix on — fresh clone, real secret, real push, `secret_commit_reached_destination=yes` — and costs nothing in trunk-based development, since direct pushes to `main` still work. It also covers the failure mode no local mechanism can: **BUG-014** proved the gate can be disarmed underneath a live checkout, and a hook cannot defend against its own absence. What remains is a fresh clone silently skipping *tests* until someone runs the feed or drift; CI runs the same suites on every push, so that is accepted explicitly rather than left open. Folder: [BUG-004-gate-arming/](BUG-004-gate-arming/). |
| **BUG-012** | `new-project.sh` writes an absolute `blueprint_source`, so a derived project breaks the moment either checkout moves | S3 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | [`scripts/new-project.sh:189`](../../scripts/new-project.sh#L189) wrote `blueprint_source = $BLUEPRINT_ROOT`, an absolute host path — the one field in `.blueprint-source` that cannot be right on two machines at once. **storm2flow is the observed instance:** it carried a `/Users/…` path onto a Linux box and every blueprint command died on it ([HANDOVER.md:89](../doing/HANDOVER.md#L89)). BUG-007 answered this with a three-step *resolution ladder* (`$BLUEPRINT_ROOT` → file → the CLI's own repo) rather than fixing what bootstrap writes; the ladder's step 3 is itself unsound (see the derived-project misdetection). Fix is to write a path relative to the project root, which costs nothing because `read_blueprint_source` already requires cwd to be the project root (it greps `./.blueprint-source`). Relative pins the LAYOUT — blueprint beside project — which is what bootstrap actually knows. Pure-bash relativiser, not `realpath --relative-to`: that flag is GNU-only and macOS is exactly the platform the storm2flow incident came from. Pinned by `tests/bootstrap-contents/` §8. Raised from linkedin-watcher-agent by the founder. |
| **BUG-013** | `blueprint drift` tells every derived project "This IS the blueprint" and reports zero drift | S1 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | `_bp_is_blueprint_itself` ([scripts/blueprint:646-658](../../scripts/blueprint#L646-L658)) identifies the blueprint by "the CLI being executed lives in the repo you are standing in", verified with `CLAUDE.md` + `docs/DoD.md` + `scripts/blueprint`. **Every one of those is true in every derived project** — `scripts/blueprint` is itself in `MANAGED_FILES`, so each project ships a copy at exactly that path. `bash scripts/blueprint drift` — the invocation CLAUDE.md documents as the fallback when `blueprint` is not on PATH, and the one a fresh clone and every spawned persona use — therefore short-circuits at [:665](../../scripts/blueprint#L665), prints `✓ This IS the blueprint`, and **exits 0 having compared nothing**. The wake-mandated drift check becomes a silent no-op. **Not theoretical:** linkedin-watcher-agent reported clean all morning while **18 commits and 4 required new files behind**, including `scripts/lib/pipeline.sh`, without which the pulled `.githooks/pre-push` fails closed. It surfaced only when the blueprint's own copy of the CLI was invoked instead. **Second site, same root cause:** `_bp_resolve_blueprint_root` step 3 ([:435](../../scripts/blueprint#L435)) validates its fallback with the same always-true test, so a project whose `blueprint_source` fails to resolve silently adopts **itself** as the comparison base and reports clean forever — the exact outcome the comment above it says it is avoiding ("a silently-substituted comparison base would make every drift report untrustworthy"). Fix direction: the signal that actually separates them is `.blueprint-source`, which the blueprint does not have and every registered project does. The existing comment rejects it as a *sole* signal, correctly — but as an additional conjunct it is sound, because a config-less derived project should fall through to the existing "unregistered struct2flow project" path, which is the right thing to tell it. `tests/drift-in-blueprint/` exists and passes; it only ever exercises the blueprint side, which is why nothing caught this. |
| **BUG-014** | A test suite writes commits and config into the repository under test, disarming the gate | S1 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | Git exports `GIT_DIR` to every hook. The pre-push gate runs the suites, so all of them inherit it — and thirteen build fixtures with `( cd "$WORK" && git init && git add . && git commit )`. **`cd` protects nothing**: with `GIT_DIR` set git ignores the working directory entirely and operates on the directory that variable names, so the fixture's commit lands in the REAL repository. Reproduced in seconds: `GIT_DIR=/tmp/victim/.git bash tests/marker-merge/test.sh` moved the victim's HEAD `43c2949 → 54a4d6f`. **This closes the open question on BUG-004** — that row records `.git/config` rewritten at 12:40:15 with `core.bare=true` and `core.hooksPath` wiped by "an unrelated process it could not identify". It was these suites. The suite that exists to prove the gate arms is what disarmed it, and the ungated push of `fdae0e2` follows directly. **Fix, two layers:** `.githooks/pre-push` strips `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE`/`GIT_OBJECT_DIRECTORY` once at the source (safe — git runs hooks from the repo top level, so discovery from cwd finds the same repository), and all 13 fixture-building suites strip them individually so a suite is safe run from any other hook context. Regression: [`tests/git-isolation/`](../../tests/git-isolation/), in the gate and CI. **A near-miss worth recording:** my first reproducer exported `GIT_DIR` *and* `GIT_WORK_TREE` and passed — setting both changes git's behaviour and the fixture stops landing in the victim, so the test would have gone green against unfixed code. Git hands a pre-push hook `GIT_DIR` alone. **Their original report, kept because it is evidence I could not have produced:** the defect surfaced when pushing a branch from a `git worktree` — `tests/marker-merge/test.sh:92` landed its fixture commit *on the branch being pushed*, reflog-confirmed six seconds after theirs, tree replaced, the push then failing with `path '.githooks/pre-push' exists on disk, but not in 'HEAD'`. They flagged the `GIT_DIR` attribution as inferred rather than proven; the reproduction here proves it. **This is also why [PR #3](https://github.com/LuizStruct2Flow/blueprint/pull/3) flagged its own worktree recipe as unsafe — that gap is this bug, and it is now closed.** |
| **BUG-015** | Six libs the CLI requires are absent from `MANAGED_FILES`, so `a2bp` cannot run in any derived project | S2 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | `scripts/lib/staleness.sh` and `request.sh` / `request-build.sh` / `request-config.sh` / `request-file.sh` / `request-inputs.sh` are sourced by the CLI ([:545](../../scripts/blueprint#L545), [:888](../../scripts/blueprint#L888), [:1176](../../scripts/blueprint#L1176)) but appear nowhere in `MANAGED_FILES`, so `blueprint pull` never delivers them. Consequences in every derived project: `blueprint a2bp` dies at `scripts/lib/request-config.sh is missing`, and `drift` reports `remote staleness: unknown`, losing the one check that tells you the comparison base is itself behind. Verified in linkedin-watcher-agent both before and **after** a full sync to HEAD `344656b` — a complete pull does not fix it, because the files are not managed. **This is the same fail-closed reasoning already written into the list for `contamination.sh`** ("a derived project missing this file cannot back-propagate at all — which is the intended fail-closed behaviour, but only useful if the file travels"): the intent is stated and the file does not travel. It also compounds BUG-011 — the sanctioned upstream path is doubly unavailable from a derived project, once because the libs are missing and once because filing reports success without opening a PR. Fix: add the six to `MANAGED_FILES` with the same MUST-ship rationale comments the neighbouring entries carry. |
| **BUG-020** | Agent state lives OUTSIDE the project in `~/.<repo-name>`, contradicting the scratch discipline and breaking "delete the project, delete its state" | S3 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | `scripts/lib/state-dir.sh` derives `~/.<repo-basename>`, and the Codex/Gemini dispatchers write their run logs there — so this repo has **two kinds of agent log in opposite places**: `logs/agent-activity.log` (inside, correct) and `~/.blueprint/codex-runs.log` (outside). The A-09 fix made both sides *agree* on a path; nobody asked whether that path should be outside the project at all. Applying the test adopted today — **"would being inside a git tree break this?"** — the answer for append-only run logs is no: `logs/` is already gitignored and the feed writes there happily. It also breaks a promise made the same day: deleting the project no longer deletes its state, and a project bootstrapped at the same path later inherits it — the same "stale record survives deletion" trap redcare flagged for workspace trust. Fix direction: move dispatcher state under `logs/` (or `.agent-state/`), keep `AGENT_STATE_HOME` as the override, and keep the single shared derivation so A-09 cannot return. Touches three dispatchers, the feed and `state-dir.sh` — all behind one function. |
| **BUG-019** | The coordination baton is a TRACKED file, so any branch operation silently reverts a live dispatch | S2 | **ACCEPTED 2026-08-03** — verified by Codex acceptance testing (empirical, not a re-read of this row); founder delegated acceptance for items needing no founder judgement | `AGENT_SIGNAL.md` is committed, so `git switch`, `git checkout <file>`, `git stash` and `git rebase` all rewrite it — including while another agent holds the mic. **Reproduced against a running agent:** I dispatched Codex, then ran `git checkout AGENT_SIGNAL.md` on another branch; the baton reverted to `Holder=Eto State=OVER_TO_USER`, and Codex correctly refused to proceed — *"I stopped because the baton changed before I could claim it… Under the radio-over protocol, I cannot overwrite that."* The dispatch was lost silently: nothing failed, the watcher simply had nothing to claim. **This was rare before today and is now routine**, because PR #3 made branch operations constant — every fix is a branch, and the baton lives in the working tree those branches rewrite. Fix directions, none chosen: (a) make the baton untracked per-checkout state like `AGENT_ROSTER.md`, which loses the `git log -p` history the protocol relies on; (b) keep it tracked but have the dispatcher record the dispatch identity separately, so a reverted file cannot orphan a live run; (c) a lock the dispatcher checks before and after. **Do not fix it with a rule telling agents to remember** — that is the shape this repo has rejected three times (BUG-004's "flip the mic last", BUG-014's fixture isolation, and the no-chaining rule that needed a hook). |
| **BUG-018** | Interactive `pull` dies on `/dev/tty` in a non-interactive session, while `drift` handles the same case gracefully | S3 | **ACCEPTED 2026-08-03 (second round)** — rejected once, re-fixed, re-verified by Codex at `270b248`: refusal exits 7; partial, full, interactive-yes, quit and in-sync pulls all exit 0; and **no gate, drift, bootstrap or watcher calls `blueprint pull` and checks its status**, so the new non-zero exit breaks no existing flow | `blueprint pull <file>` without `--yes` reaches its prompt and aborts with `scripts/blueprint:820: /dev/tty: No such device or address` — after printing the diff, so the operator sees a proposal and then a crash. Any agent session, CI job, or `nohup` run hits this. `drift` already solves exactly this: `tests/staleness/drift-integration.sh` case #1 pins "no TTY: warns, offers a copy-pasteable command, never prompts". `pull` should degrade the same way — detect the absent TTY, print the `--yes` invocation, and exit non-zero without having half-run. Same class as BUG-011's exit-code contract: a command must not report progress it did not make. |
| BUG-001 | `scripts/agent-activity.sh` leaks processes without bound (fork-bomb class). Race-prone single-instance guard + one never-exiting `tail -F` per subagent transcript **per instance** + teardown that only runs via EXIT traps of a process that never exits. Observed on a founder host: **17,432** live script instances, **8,715** `tail` processes, load avg **175** on a 32-thread box for **2.7 days** at zero application load — inotify instance exhaustion (`max_user_instances=128`) flipped every `tail -F` into 1 s poll-spin. | **CRITICAL** | Plan written — awaiting multi-AI consensus (CLAUDE.md §"Major Bug Process" step 3) **ACCEPTED 2026-07-24 by Jesko (QA-2), acceptance delegated by the founder** — see `ACCEPTANCE-JESKO-2026-07-24.md`. | [BUG-001-fork-bomb/PLAN-BUG-001.md](BUG-001-fork-bomb/PLAN-BUG-001.md) |
| BUG-002 | **Blueprint contamination** — `scripts/agent-activity.sh` hardcoded a project-specific state dir (`~/.linkedin-watcher-agent`) in a blueprint-managed generic file, so every derived project read another project's Codex/Gemini run logs and the `[CODEX]`/`[GEMINI]` feed lines silently never appeared. Rejected once on 2026-07-24 (the **Gemini** half still wrote a literal `$HOME/.{{PROJECT_NAME}}/`); root cause was A-09. | **HIGH** | Fixed with A-09 — feed and all dispatchers derive one per-project state dir via `scripts/lib/state-dir.sh`. **ACCEPTED 2026-07-29 by Jesko (QA-2)** — see [ACCEPTANCE-JESKO-2026-07-29.md](ACCEPTANCE-JESKO-2026-07-29.md). | [A-09-state-dir/](A-09-state-dir/) |
| **BUG-021** | Codex output reached the feed as a bare `[CODEX]`, never `[Persona - Codex]`, so nobody could tell which persona produced a line | S3 | **ACCEPTED by the founder 2026-08-05** | Measured before assuming: 11 bare vs 2 labelled here, 103 vs 5 in linkedin-watcher-agent, **0 vs 237** in the redcare blueprint, which had already fixed it. **Structural, which is why the obvious patch was wrong:** the feed pumps a long-lived run log under a label bound ONCE at daemon start, while the persona is a PER-DISPATCH fact. No string chosen inside the feed can be correct, however it is written. The label has to come from the launcher, the only place that holds `$AGENT_SIGNAL_HOLDER` at the moment it dispatches. **Implemented in the blueprint's own idiom rather than copied** (CLAUDE.md §"Back-propagating" — a back-propagation is a request, not a patch): redcare hardcoded `[$HOLDER - Codex]` and a raw `>>`; here the label comes from `bp_roster_label`, the SAME lookup the feed uses for mic flips so the two formats cannot drift and a non-Codex backing renders correctly, and the write goes through `feed_append`, which owns the timestamp and the inode-preserving rotation. The enabling change was moving `persona_label` out of `agent-activity.sh` into `scripts/lib/roster.sh` — it was a LOCAL function, so nothing else could reuse it. Third "one definition, two callers" fix of the week, with TASK-005 and TASK-002. **The gate caught the serious defect:** sourcing the two libs unguarded made them hard dependencies of DISPATCHING, so a tree without them stopped dispatching entirely — `tests/state-dir` reported it as "the stub never ran". That would have traded a silent mislabel for a silent NON-dispatch. The label now **fails open**, where `commit-msg` fails closed; both are right, because there the check IS the work and here the label is decoration on top of it. Two fixtures also broke by copying the hook without its new dependency, and `tests/agent-activity-bound` (12 assertions, none about Codex) was instrumenting through the run log that stopped being pumped. Regression: [`tests/codex-persona-label/`](../../tests/codex-persona-label/), 10 assertions, in the gate and CI. Verified live against this repo's roster: `[Alexis - Codex]`, the exact label missing when her dispatch ran invisibly that morning. |
| **BUG-022** | A dispatch to a dead watcher failed SILENTLY — the mic read `OVER_TO_CODEX`, the feed was quiet exactly as it looks when an agent is thinking, and the run log is the one surface nobody reads | S2 | **ACCEPTED by the founder 2026-08-05** | Cost redcare ~40 minutes (their BUG-033) and cost this repo a BA dispatch the same morning, found only because the founder asked. The mic state and the dispatcher's liveness are two facts unremarkable alone and conclusive together; nothing compared them. **The feed is the right place** because it already polls on a timer and already reads the baton, so the comparison is free. Three answers, and the third is what makes it safe to run unconditionally: `none` (no lock file — no watcher ever claimed this state here, so a project that runs none is never warned), `alive`, `dead`. **Liveness comes from an `flock`, never the process table** — every check written during redcare's incident matched the checking shell's OWN command line via `pgrep -f` and was wrong three different ways; a lock cannot self-match, is repo-scoped, and is released by the KERNEL on death including SIGKILL, so there is no cleanup path to get wrong and no stale pid to disambiguate. **Edge-triggered, and deliberately OUTSIDE the signal-changed guard**: the dangerous case is a baton that has not changed in a long time while its listener died under it, which a change-driven check would never see. **Three defects found while building it, all kept in the history:** the lock path first hardcoded `logs/state` (`AGENT_STATE_HOME` overrides it, so the two sides would disagree while each worked alone — A-09 exactly); the first end-to-end test ran the feed against the REAL repo, which would have read the live baton (BUG-014); and `flock FILE cmd` forks rather than execs, so the child inherits the fd and holds the lock after its parent dies. **A fourth landed as a follow-up before acceptance:** the lock was derived from the repo ROOT, so `tests/signal-dispatch` — which runs the real watcher against a FIXTURE baton — took the LIVE repo's lock, leaving a record that made a checkout which never ran a watcher report `dead` forever, and refusing any genuine watcher started while that suite ran. It now derives from the BATON's own directory, so isolation falls out of the path instead of being remembered. Regression: [`tests/watcher-liveness/`](../../tests/watcher-liveness/), 15 assertions including a real supervisor and a real lock holder. Acceptance was a script (`scripts/accept-bug-022.sh`, deleted by TASK-021 once accepted) because the fix is an ABSENCE — confirming one by hand means trusting that you waited long enough and looked in the right file, which is the condition that let the bug hide. **Scope, stated rather than assumed away:** a held lock proves the claiming PROCESS EXISTS, not that it is HEALTHY. A wedged watcher still reads alive; closing that needs a heartbeat every dispatcher writes plus an mtime threshold, deliberately out of scope. |
| BUG-003 | **The pre-push security gate could not tell a scanner FAILURE from a scanner FINDING.** `semgrep … \|\| { echo "found a WARNING+ finding"; exit 1; }` treated every non-zero exit as a finding, but semgrep exits 1 for findings and **≥2 for a fatal error** — and `--quiet` suppressed the reason entirely. Observed on this repo: identical back-to-back runs alternating exit 0 and exit 2 with zero output, in both bash and sh, while the same command outside the hook always passed. `gitleaks` had the same conflation. Dangerous in both directions: a broken scanner reported as a vulnerability teaches operators to shrug off the gate, and that shrug is what carries over to a real finding. | **HIGH** | Fixed — exit 1 reports+prints the finding; exit ≥2 retries once (semgrep only) then fails closed with "the SAST gate did NOT run". Regression: `tests/pre-push-scanners/test.sh` (15 assertions, verified failing against the pre-fix hook). **ACCEPTED 2026-07-24 by Jesko (QA-2), acceptance delegated by the founder** — see `ACCEPTANCE-JESKO-2026-07-24.md`. | — |


**Accepted live, not on paper.** The founder renamed the Orchestrator row to `Eto` and `--whoami` followed it immediately — no other edit, no `AGENT_PERSONA`, no restart. The first fix was incomplete and the founder caught it ("you are still logging as anna"): it resolved persona once at startup, so the long-lived feed daemon kept the name the roster had held when it booted. Fixed in `257ac1d` — the supervisor re-resolves when the roster changes and announces the change in the feed. The lesson worth carrying: `--whoami` passing is what made the incomplete fix look finished, because a one-shot re-reads by construction and can never exhibit a caching bug.
