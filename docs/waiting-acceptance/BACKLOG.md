# Landed rows awaiting founder acceptance

Rows whose work is **on `main`** and which are waiting for the founder to accept
("done") or reject ("reopen"). See [README.md](README.md) for the triggers.

**This file appears when the first promoted row lands** — its absence means none
have, not that it is missing.

| # | Item | Sev | Category | Re-open trigger / next-step gate |
|---|---|---|---|---|
| **TASK-040** | **The managed `.githooks/pre-push` stops telling projects to edit it, and finds IaC under `infrastructure/` as well as `infra/`.** From storm2flow, `FR-storm2flow-prepush-layout`. `.githooks/pre-push:528-529` says to "edit this block during bootstrap", but the hook has no marker lines, so a pull replaces the whole file and those edits are lost. TASK-026's skip messages (`:542-547`) already point at the pull-safe home below the end marker of `.githooks/pre-push-project`. The IaC stages detect only `infra/` (`:598-641`), so storm2flow's CDK app in `infrastructure/` is skipped. **Do:** replace the instruction with a pointer to `pre-push-project`, and detect `infrastructure/` alongside `infra/` for the CDK, Terraform and Helm recipes, with the skip message naming both. | S3 | Gate | **Pushed 2026-09-16** (`9acffae`, `3682b53`, `6ae87e0`, `2285f22`). **What to test:** in a project whose IaC lives in `infrastructure/`, the pre-push gate must run the CDK, Terraform and Helm stages — even with an empty `infra/` sitting beside it, which used to silence all three. Each stage names the directory it ran in (`IaC synth · cdk · infrastructure`). With neither directory present, the skip says "no infra/ or infrastructure/ directory". The hook no longer tells you to edit it at bootstrap; project stages go after the end marker in `.githooks/pre-push-project`. **Cross-provider review:** Alex (Codex) found the mixed layout silently unchecked; after the fix Alexey ran the real IaC block with recording tools and confirmed a failing second tree still fails the whole stage — **push as is**. From storm2flow's `FR-storm2flow-prepush-layout`. **Re-open if** one directory can hide the other, or a stage runs in the wrong directory. |
| **TASK-041** | **One dependency-vulnerability threshold everywhere: MEDIUM+ (CVSS >= 4.0) blocks.** From storm2flow, `FR-storm2flow-osv-threshold`. The pre-push hook (`.githooks/pre-push:448-517`) and CI (`.github/workflows/security.yml:98-124`) already block MEDIUM+. `docs/DoD.md` §6.2 (`:486-487`, and `:510`) says only zero `HIGH`+ CVEs is required. **Founder decision 2026-09-15: MEDIUM+**, keeping what the two enforcing layers already do. Nothing gets looser. **Do:** correct DoD §6.2 and every other doc that states the SCA threshold (grep SECURITY.md, the deck, the templates' security config) to MEDIUM+ blocking, with lower severities reported. Leave the `trivy config` HIGH+ line alone unless its enforcement also differs. | S3 | Security / docs | **Pushed 2026-09-16** (`d6fa8ca`, root configs `bf8d37c`). **What to test:** `git grep -n "HIGH+" -- docs/DoD.md docs/SECURITY.md project_config_security.md project_config_dod.md templates` must show no osv-scanner or SCA row — every live statement says MEDIUM+ (CVSS >= 4.0), which is what `.githooks/pre-push` and `.github/workflows/security.yml` already enforce. Trivy's HIGH+ lines stay: nothing runs `trivy config` (`git grep -i trivy -- .githooks .github scripts` is empty), so they describe a recipe, not an enforced threshold. **Cross-provider review:** Alex (Codex) — **push as is**; he re-searched the tree and found no conflicting live threshold. From storm2flow's `FR-storm2flow-osv-threshold`; founder decision 2026-09-15: MEDIUM+. **Re-open if** any doc, hook or workflow states a different SCA threshold than the other two. |
| **TASK-042** | **A project keeps its own Claude Code permission rules in a tracked file that `blueprint pull` never touches, merged with the blueprint's rules.** From storm2flow, `FR-storm2flow-settings-project-layer`. `.claude/settings.json` is whole-file managed (`scripts/blueprint:275`) and JSON cannot carry markers, so every pull replaces it. The only other home, `.claude/settings.local.json`, is gitignored and per-machine. storm2flow's tracked file has 89 entries that are not the blueprint's, 71 of them AWS CLI reads its diagnosis runbook depends on, and a pull deletes them. **Founder decision 2026-09-15: a project file merged in.** A tracked project-owned file (e.g. `.claude/settings.project.json`) is never pulled. Pull writes `settings.json` as the blueprint's rules plus the project's, and **the blueprint's `ask` and `deny` always win over a project `allow`**, so a rule the blueprint tightens (BUG-118 moved `put-approval-result` to `ask`) cannot be undone by a project entry. Chosen over merging arrays on pull, which cannot tell a project rule from a blueprint rule the blueprint deliberately removed. **Do:** the merge in pull, with drift comparing the merged result; `tests/permission-policy` extended; a migration note for projects whose tracked `settings.json` already carries their own rules. | S2 | Sync / permissions | **Pushed 2026-09-16** (`29aaa0f`, `1e62d68`, review fixes `49ef420`/`dc9c496`/`1c8ce26`; docs `0df1fb8`, `e4c32be`, `e747511`). **What to test:** in a derived project whose `.claude/settings.json` carries rules the blueprint does not ship — storm2flow's 89 AWS entries, for instance — run `blueprint pull`. It must **refuse** `settings.json`, print those rules as a ready `.claude/settings.project.json`, and leave `bootstrap_sha` unchanged (BUG-122). Save that file, `git add -f` it (`.claude/` is gitignored on purpose), and pull again: the merged `settings.json` must carry the blueprint's rules plus yours, with any project `allow` dropped that the blueprint names in `ask` or `deny` — check `put-approval-result` is still `ask`. `blueprint drift` must then be clean, not permanently drifted. **Cross-provider review:** Alexey (Codex) twice — **push as is** on the second pass; `_bp_one_object` refuses JSON streams, non-objects and `null` on all three inputs before anything merges. **Stated limits, not regressions:** duplicate JSON keys follow jq's last-wins, and there is no document-size ceiling. **Re-open if** a pull deletes a project's own rules, or a project `allow` re-enables something the blueprint asks or denies. |
| **TASK-045** | **doc-links resolves site-absolute links through a declared web root or allowlist, instead of reporting served pages as broken.** From storm2flow's orchestrator (Sylvia), `FR-storm2flow-doc-links-web-root`. The doc-links suite reports `/security.html`, `/privacy.html`, `/terms/v1.html` and `/impressum.html` (13 links in storm2flow's `docs/RELEASE-NOTES.md` and `docs/config/FEATURES.md`) as unresolved. They are served web pages, not repo files. **Do:** a project declares a web root (a repo directory that site-absolute links resolve against) and/or an allowlist of site-absolute paths, in `project_config_paths.md`. Without a declaration, a site-absolute link is still reported, so a typo is not silently accepted. | S3 | Test suites / docs | **Pushed 2026-09-16** (`a9267bf`, `e515d0f`, containment hardened with BUG-125 in `8429e95`/`02e1f31`). **What to test:** in a project whose docs link served pages, declare `- BP_WEB_ROOT: \`site\`` in `project_config_paths.md` and a link to `/security.html` resolves when `site/security.html` exists; `- BP_WEB_PATHS: \`/a.html /b.html\`` lets exactly those pass with no file behind them; with neither declared, the link is reported and the message names both lines. Matching is exact, so `/ok` does not allow `/okay`. A web root outside the repository, or a link escaping the served directory, is refused. **Cross-provider review:** Jesko found `stat` following symlinks straight through containment; after the fix Alexey retried all four escapes plus a relative outside root and a dangling target, all reported — **push as is**. From storm2flow's `FR-storm2flow-doc-links-web-root`. **Re-open if** a served-page link resolves without a declaration, or an escape passes. |
| **TASK-039** | **The DoD bug-test stage finds a project's own regression tests, and a blueprint suite can no longer vouch for a project bug that shares its number.** From storm2flow, `FR-storm2flow-dod-test-roots` (branch `fr/storm2flow/2026-09-15`; text saved in `.scratch/fr-storm2flow-2026-09-15.diff`). **The defect:** `scripts/lib/dod-gate.sh:154` searches only `${BP_CODE_ROOT:-.}/tests/`. In a derived project, that directory holds the blueprint's shipped suites, which name about 109 BUG numbers, about 97 of which are also storm2flow bug numbers. So those storm2flow bugs pass with no test of their own, while storm2flow's real tests (`backend/src`, `frontend/src`, `frontend/e2e`, `infrastructure/test`) are never found. **Do:** a project-declared list of test roots (for example `BP_TEST_ROOTS` in `project_config_paths.md`, defaulting to today's `tests/`). Do NOT make `BP_CODE_ROOT` configurable, because `.githooks/pre-push:83-103` and `dod-gate.sh:187-189` resolve from it. Scope the match so a blueprint-shipped suite cannot satisfy a project bug. Also: `dod_items_in_push` (`dod-gate.sh:58-64`) parses subjects with its own `sed` pattern instead of `scripts/lib/commit-subject.sh`; use the shared parser. Update `docs/DoD.md` §2 and the templates' paths config. | S2 | DoD gate | **Pushed 2026-09-16** (`e7e1d68`..`8473a3d`, guards `4a926aa`/`f10eff3`, rework `47d6ce7`/`998b6cb`/`ef3ec3d`/`9e076e1`). **What to test:** in a derived project, declare your test folders in `project_config_paths.md` as `- BP_TEST_ROOTS: \`backend/src frontend/e2e\``, then push a commit naming a bug whose test lives there — it must pass. Then check the gate cannot be fooled: a bug whose number appears only in a README, a CHANGELOG or a blueprint fixture under `tests/` must be reported UNTESTED. `tests/e2e` does not count in a derived project; a test file sitting directly in `tests/` does. A malformed declaration (indented, a table row, `BP_TEST_ROOTS =`, a glob, or two lines) is refused rather than silently defaulting. **Cross-provider review, three rounds:** the rule was enforced only at the `tests/` top level — every declared root was a recursive text search over ANY file, so a README satisfied the gate while a passing case hid it. Also fixed: containment is symmetric, roots must resolve inside the repo, and `#18` now pins the symlink and file-type halves of the filter. From storm2flow's `FR-storm2flow-dod-test-roots`. **Re-open if** any non-spec file counts as a bug's regression test, or a declared root reaches outside the project. |

**Each row's "what to test" travels WITH it** — into `done/` on acceptance, back
into `doing/` on a rejection. It is not dropped at the boundary: a rejected item
needs its test instructions more than a waiting one does, and an accepted item's
instructions are the record of what "accepted" actually meant. An empty table
here is the good state, not a missing section.


*(Empty.)*
