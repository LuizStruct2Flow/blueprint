# Landed rows awaiting founder acceptance

Rows whose work is **on `main`** and which are waiting for the founder to accept
("done") or reject ("reopen"). See [README.md](README.md) for the triggers.

**This file appears when the first promoted row lands** — its absence means none
have, not that it is missing.

| # | Item | Sev | Category | Re-open trigger / next-step gate |
|---|---|---|---|---|
| **TASK-058** | **Lifecycle pass: `doing/` holds only work in flight.** Founder request 2026-09-16 ("we have too many things in doing"). 46 bug rows from the TASK-018 port (BUG-036..109) sat in `doing/` with a stale `OPEN` status. A read-only classification (Vitali) checked each against `origin/main`: 35 fixed rows moved straight to `done/` on the founder's bulk acceptance ("yes, straight to done"), each carrying its fix commit. The 11 still-open bugs and TASK-021/022/024 go to `backlog/` or are cancelled per the founder's call on the review rule. | S3 | Lifecycle | **Pushed 2026-09-16/17.** **What to test:** `docs/doing/` holds only work in flight. The 35 fixed bugs are in `docs/done/BUGS.md`, BUG-039 and BUG-072 are in backlog, and eight bugs plus TASK-024 are cancelled into `docs/config/findings.md` F-005. Raised 2026-09-16 by the founder. **Re-open if** `doing/` again holds rows nobody is working on. |
| **TASK-022** | **Anchor the rules in mechanisms, and shrink what agents must remember.** Founder direction, 2026-09-10: *"I think we should rely in automation and not in agents having all rules in their context and being disciplinated on applying these rules… the md files will be used for documentation, and we will anchor the controls in the ci and development lifecycle."* Triggered by a cross-review of `docs/way-of-working.md` in which two reviewers independently found its strongest untold story was that **controls are required to be checkable and are repeatedly caught lying** — a framing the founder rejected as pitched, while accepting the evidence behind it. Artefacts: [`PLAN-TASK-022.md`](PLAN-TASK-022.md), the rule-enforcement audit `TASK-022-rule-enforcement.{csv,xlsx}` (deleted with stage 1 as a second description of the rules; in history at `dc225c9`), and the talk at [`../talk-enforcing-agentic-quality.md`](../talk-enforcing-agentic-quality.md). **Partly landed:** `34d5d00` fixed the trunk rule contradicting itself across CLAUDE.md and DoD §6 — TASK-019 made `main` the trunk and deleted the section two other rules still pointed at — and reframed the landing table on the axis that was wrong all along: **the contributor, not the repo.** Stage 1 of the revised plan (the deletions) was committed 2026-09-17; stages 2 (CI anchors), 3 (SessionStart hook) and 4 (tell derived projects) are open. | S2 | Process | **Pushed 2026-09-17**, CI green on `8f56ba6` (run 35209754799). **What to test:** `wc -l CLAUDE.md docs/DoD.md` reads about 344 and 253 (were 1,318 and 916), and no rule you still want is gone. A push to main whose earlier commit breaks a subject, row or BUG-test rule turns CI red. Starting Claude Code, including from a subdirectory, shows a session-start report with the feed and a drift verdict (`drift: UNKNOWN` when offline). **Reviews:** Alexey (Codex) on the plan; Jesko (Codex) on stages 2 and 3, each real finding fixed (force-push refusal, hook paths relative to the cwd). **Known limits:** a declared test root under tests/ fails the BUG-test check; CI after a history rewrite checks the tip only. **Next-step gate:** the plan's inventory of which rules are prose-only versus mechanically enforced. This session produced eight instances of `docs/config/findings.md` F-002 — checks inferring a property from a proxy satisfiable without it — which is direct evidence for the plan's premise and should be folded in before the rest is sequenced. |
| **TASK-021** | **What ships, decided by one boundary (was: the `scaffolding/` + `forge/` restructure, TASK-018-TARGET §2).** Plan: [`PLAN-TASK-021-RESTRUCTURE.md`](PLAN-TASK-021-RESTRUCTURE.md). **The physical move was dropped by the founder on 2026-09-16:** it cost ~180 path moves plus permanent path translation in sync and a2bp, and once the managed set is derived from the export archive, the audience is split by `.gitattributes` directory rules and unshipped files are retired downstream, it adds legibility, not consistency. Stages 0, 1 and 2 are on `main`, and the `scaffolding/` resolver branches are removed. | S2 | Sync | **Pushed 2026-09-16/17**, CI green on `c567534` (run 35172596805). **What to test:** run `blueprint files` in the blueprint: no deck, talk, brand, requirements or playbook, and `AGENT_SIGNAL.md` is listed. Bootstrap a fresh project: no `new-project.sh`, `LICENSE` or `CLAUDE.blueprint.md`, and its root `CLAUDE.md` and `README.md` links resolve. In a derived project, `blueprint pull` offers to delete unedited copies of files that no longer ship (default No) and keeps edited ones as "yours now". The first pull with an old CLI may print one error after "Pulled N file(s)"; a second run is clean. **Reviews:** Alexey (Codex) twice, all S2 findings fixed; Jesko (Codex) once, fixed. Do not reimplement the move. Re-open if a shipped file is found that the export archive does not decide. |
| **TASK-057** | **Git permissions allow only the everyday commands; everything else prompts.** Founder request 2026-09-16. `Bash(git *)` allowed every git command, including `reset --hard`, `clean -fdx`, `branch -D` and `config`. Now allowed: push, add, commit, pull, fetch, checkout, switch, stash, and the read-only status, log, diff, show, rev-parse, ls-files, check-ignore, blame, grep and branch listing, each also as `git -C <dir> …`. Ask: `checkout -- *`, `checkout .`, `stash drop`, `stash clear` (they discard work) plus the existing force-push rules. Every other git command falls to the default prompt. | S2 | Permissions | **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** an agent running `git reset --hard` or `git clean -fd` is prompted; `git status` and `git commit` are not. **Known limit:** a git invocation with a global option other than `-C` (e.g. `git -c core.quotePath=false ls-files`) prompts. **Re-open if** a destructive git command runs unprompted. |
| **TASK-056** | **kill, pkill, curl -s and brew install ask before running.** Founder request 2026-09-16: moved from `permissions.allow` to `permissions.ask` in the managed `.claude/settings.json`, so every project that pulls it is prompted before an agent kills a process, fetches a URL silently or installs a package. **Known limit:** `Bash(curl -sS *)` is still allowed; the founder named only `curl -s`. | S3 | Permissions | **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** in a session, an agent running `kill`, `pkill`, `curl -s` or `brew install` is prompted. **Re-open if** any of the four runs unprompted. |
| **TASK-055** | **The TypeScript suites run in parallel.** `fileParallelism` goes from `false` to `true` in `tests/vitest.config.ts`, in the gate and in CI alike. The config comment claimed `staleness` #8 and `pre-push-secrets` #10 passed only because the run was serial; measured, both bound the subject's own timeout and take the same 2.08 s and 3.08 s either way, and no suite needed a fix. **Measured 2026-09-16 on this host (32 cores), the 50 non-release files:** 197.5 s serial, then 20.0 s, 20.3 s and 20.1 s in three parallel runs, all 877 tests green. **Known limit:** BUG-111 (the harness counting a process it just killed) is load-sensitive and may show up more often. | S2 | Tooling | **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder with TASK-054. **What to test:** push anything and read the vitest stage time in the gate summary: about 20 s on this host. **Re-open if** a suite fails only when run beside others. |
| **TASK-054** | **A release tier: the five most expensive suites run in CI only.** Founder decision 2026-09-16: the ~940 s pre-push gate was blocking. `bootstrap-gate` (460 s), `subagent-feed` (127 s), `a2bp-e2e` (56 s), `agent-activity-bound` (50 s) and `signal-dispatch` (34 s) are renamed `*.release.spec.ts`. The gate's vitest run excludes `**/*.release.spec.*` and prints a skip line naming each; CI runs everything, and `released` moves only on green CI, so CI gates what derived projects pull. `tests/manifest` now requires CI to run every suite and the gate every non-release suite; any other exclude still counts as a narrowed run. This reverses BUG-005's "never CI-only"; CLAUDE.md §"Pre-push tolerance", DoD §3.7 and the deck say why. **Known limit:** a regression in a release suite now shows as a red `main` in CI instead of a refused push. | S2 | Tooling | **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** push anything: the gate lists five `release tier` skips and no longer runs those suites; the CI `ts-tests` job still runs them. **Re-open if** a release suite stops running in CI, or a suite leaves the gate without the `.release.` name. |
| **TASK-053** | **A push that changes only `.md` files skips the code stages.** Founder decision 2026-09-16, root files such as `CLAUDE.md` included. `.githooks/pre-push` lists the files in the push range (`git log -m --no-renames --name-only` over `push_log_opts`); if every one ends in `.md` it runs gitleaks, the host-path guard, the four DoD checklist stages and one `docs` stage running `doc-links`, `lifecycle-docs` and `bug-numbers`. semgrep, osv-scanner, backend, frontend, IaC, ShellCheck, the typecheck and the vitest batch skip with a `text-only push:` reason. An unknown range, an empty list or any other file gives the full gate; `--no-renames` keeps a script renamed to `.md` from passing as text. **Known limit:** a new branch is scanned over its whole history, so its first push almost always takes the full gate. | S2 | Tooling | **Pushed 2026-09-16**, CI green on `0752a4e` (run 35142714725). Raised 2026-09-16 by the founder. **What to test:** push a commit that changes only a `.md` file: the gate shows the `text-only push` skips and a `docs · TASK-053` stage, in a couple of seconds. Push one that also changes a script: the full gate. **Re-open if** a push containing a non-`.md` file skips a code stage. |
| **TASK-052** | **A review finding becomes work only if it is real and practical.** Founder decision 2026-09-16: "I want real and practical findings, we are generating 0 value". `docs/DoD.md` §1b rule 4 and the deck's rule 4 now say a finding is acted on only if it has happened, or sits on a core path with a realistic trigger; anything else is one "known limit" line, with no reproducer, mutant or re-review, and the implementer judges each finding instead of relaying the verdict. | S2 | Process | **Pushed 2026-09-16** (`3940d57`, CI green on `3940d57`). Raised 2026-09-16 by the founder after BUG-126 (16 commits for a case `tsc` already catches). **What to test:** the next cross-provider review — hypothetical findings end as known-limit lines, not commits. **Re-open if** a hypothetical finding produces a reproducer or a rework round. |
| **TASK-051** | **Every project's SonarQube scan covers its shell scripts.** Founder decision 2026-09-16: "every project should get it and scan it". SonarQube has no shell analyser, so `scripts/sonar.sh` runs ShellCheck over the files the gate lints (`sh_lint_files`) and imports the findings as external issues; `.scannerwork/` is ignored. SC2317 is left out (301 false positives on functions called by name). **Known limit:** the scanner never indexes files under a dot-directory, so `.githooks/` findings stay with the gate's ShellCheck stage. | S3 | Tooling | **Pushed 2026-09-16** (`aeeada4`, CI green on `3940d57`). Raised 2026-09-16 by the founder. **What to test:** run `bash scripts/sonar.sh` in any project with shell scripts: it prints `→ ShellCheck: N shell files` and the dashboard shows `external_shellcheck:SC…` issues. Verified on the blueprint: 30 issues across 13 files. **Re-open if** shell findings do not reach the dashboard. |
| **TASK-050** | **The seeded `sonar-project.properties` still recognises tests as `*.test.ts`, so SonarQube in a new project counts its specs as source.** TASK-047 made every test `*.spec.ts` / `*.spec.tsx` but missed this template: `sonar.test.inclusions` and `sonar.exclusions` still name `*.test.ts`, `*.snap.test.ts` and `*.integration.test.ts`. Found 2026-09-16 while adding the blueprint itself to the local SonarQube. **Do:** name `**/*.spec.ts` and `**/*.spec.tsx` in both keys. Existing projects own their copy (template file, not pulled) and edit it themselves. | S3 | Tooling | **Pushed 2026-09-16** (`ed46dd3`, CI green on `3940d57`). Raised 2026-09-16 by Eto. **Re-open if** a seeded Sonar config names a test pattern the runner does not run. |

**Each row's "what to test" travels WITH it** — into `done/` on acceptance, back
into `doing/` on a rejection. It is not dropped at the boundary: a rejected item
needs its test instructions more than a waiting one does, and an accepted item's
instructions are the record of what "accepted" actually meant. An empty table
here is the good state, not a missing section.


*(Empty.)*
