# Landed rows awaiting founder acceptance

Rows whose work is **on `main`** and which are waiting for the founder to accept
("done") or reject ("reopen"). See [README.md](README.md) for the triggers.

**This file appears when the first promoted row lands** — its absence means none
have, not that it is missing.

| # | Item | Sev | Category | Re-open trigger / next-step gate |
|---|---|---|---|---|
| **TASK-026** | **Implement the a2bp request PR #66 from struct2flow-www — all six changes, in the blueprint, with the review's corrections.** Founder decision, 2026-09-14, after a cross-provider review by Jesko (QA-2, Codex) that tested each change separately. **(1)** add `scripts/no-chain-guard.sh`, `scripts/session-resume.sh` and `scripts/wait-mic.sh` to `MANAGED_FILES` — the PR's stated reason is wrong: fresh bootstraps already receive them; the damage is to EXISTING projects updated by `pull`, which copies only managed files. Needs a regression over an existing project, and a check that DERIVES the set of referenced scripts rather than hand-listing three. **(2)** `CLAUDE.md` prose must not carry the literal marker tokens, because `pull` detects marker files by substring — reproduced: a prose mention triggered marker handling, a backup and a whole-file replace. Needs a `pull`-level regression. **(3)** gate skip reasons point a differently-laid-out project at where its own stages belong — as-is. **(4)** `drift` compares marker files on managed regions only — but as proposed, `drift` and `pull` disagree about the same file (drift clean; pull still selects it and previews the project footer as deleted). Build ONE comparison shared by drift, pull's selection and pull's preview, and have it refuse inverted markers, which closes the BUG-034 case where pull deletes a footer while reporting it preserved. **(5)** the historical-path annotation — as-is. **(6)** `agent-exchange` leaves the shipped `.claude/settings.json` and moves to the untracked `.claude/settings.local.json` in the same step. **Ordered before TASK-025**, whose `drift` rewrite builds on (4). Close PR #66 with a comment linking the commits. | S2 | Sync | **All six changes landed and pushed 2026-09-14; PR #66 closed with a comment linking the commits.** What to test: in a derived project, `blueprint pull` delivers `scripts/no-chain-guard.sh`, `scripts/session-resume.sh` and `scripts/wait-mic.sh`; `blueprint drift` does not list `.githooks/pre-push-project` when only the project's own guards after the end marker differ; `.claude/settings.json` no longer names `agent-exchange`. Bugs BUG-034, BUG-112, BUG-113 and BUG-114 carry their own tests. Re-open if drift, pull's selection, preview or write disagree about a marker file. |
| **TASK-030** | **a2bp request PR #70 from linkedin-watcher-agent: watcher-lock isolation test stops reading the real checkout's lock file.** `tests/watcher-liveness` #7 decides by whether the REAL lock file exists, but a correct dispatcher leaves that file in place for its whole life and after a clean stop, so the test can reject correct state; it passes only while no real lock exists. The request replaces it with a self-contained fixture over the lock's three real states (none / dead / alive) and adds the comment explaining why lock files must persist. Production behaviour unchanged. **Recommendation: accept as-is** — applies cleanly to current main, 20/20 on the branch. Review: `.scratch/JESKO-pr67-70-review.md`. | S3 | Test suites | **Landed on main 2026-09-15 as `b87491a`; PR #70 closed with a comment linking it, branch deleted.** What to test: run `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA tests/node_modules/.bin/vitest run --root "$PWD/tests" watcher-liveness` — case #7 reports one run per starting state of the real lock (none, dead, alive), and all pass whether or not a real watcher is running in this checkout. Re-open if #7 reads or writes the real checkout's lock file again. |
| **TASK-027** | **a2bp request PR #67 from linkedin-watcher-agent: the toolchain installer reads the harness's real Node range instead of a copied `18+`.** `scripts/install-toolchain.sh` checks `NODE_MIN_MAJOR=18` while `tests/package.json` requires `^20.19.0 or >=22.12.0`. **Reproduced on current main** by Jesko (QA-2, Codex): with a Node 20.0.0 shim, `install-toolchain.sh check` prints `All present`. **Recommendation: accept with changes, folded into TASK-025 commit 4** (which rewrites the same installer): keep the single source of truth and its four cases; fix or fail closed on the custom evaluator's `^0.x` caret semantics; record OBSERVED mutant red sets (the request's header admits they were derived). Review: `.scratch/JESKO-pr67-70-review.md`. | S2 | Toolchain | **Landed on main 2026-09-15 inside TASK-025's installer work; PR #67 closed with a comment linking the commits, branch deleted.** What to test: `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" install-toolchain` passes, including #1-#6: a Node below `tests/package.json`'s `engines.node` range is rejected, an uninterpretable range is UNVERIFIED, `^0.x` follows npm, and a leading `*` does not waive later comparators. Re-open if `install-toolchain.sh check` accepts a Node version the manifest's range excludes. |
| **TASK-028** | **a2bp request PR #68 from linkedin-watcher-agent: bound fixture root discovery and tighten harness process semantics (10 files).** Adds a test-only `BP_STATE_ROOT_CEILING`, refuses to create a workspace beneath an existing `.git` / `.blueprint-root` / `.blueprint-source`, fixes the `code-root` `childRoot` return type, preserves caller options in `runScript` / `startWatcher`, and waits for a timed-out process group to be gone. **Reproduced on current main** by Jesko (QA-2, Codex): with `/tmp/.git` present, `bp_state_root` returned `/tmp` and `state-root` #A6 went red; the timeout path returns before `waitGroupGone`. It no longer applies (H5 changed `env.ts` / `index.ts`). **Recommendation: accept with changes** — port onto the current H5 harness, reproducer-first for the marker-above-workspace inversion, the timeout returning early, and the option-loss / type mismatch; document that the workspace preflight makes the whole harness refuse to run while a stray `/tmp/.git` exists (BUG-110, BUG-111). Review: `.scratch/JESKO-pr67-70-review.md`. | S2 | Test harness | **Landed on main 2026-09-15; reviewed by Jesko (Codex), both findings resolved; PR #68 closed with a comment linking the commits, branch deleted.** What to test: with `TMPDIR` pointing below a directory holding `.git`, `.blueprint-root` or `.blueprint-source`, any suite refuses at workspace creation and names the marker and the remedy; `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" harness state-root state-dir code-root` passes; `tests/node_modules/.bin/tsc --noEmit -p tests` is clean. Carries BUG-110, BUG-111 and BUG-119. Re-open if a scenario resolves a state root outside its workspace, or a timed-out run returns while its killed group still exists. |
| **TASK-029** | **a2bp request PR #69 from linkedin-watcher-agent: deliver three managed scripts, and make a symlinked CLI load its helpers from its physical path.** **U1 is already done** by TASK-026 (BUG-114, with the stronger derived `tests/managed-references`). **U7 is still real** — helpers load beside an out-of-tree symlink — but the patch no longer applies to the TASK-025-rewritten CLI, and TASK-025 commit 4 replaces the symlinked command with an installer-owned one. **Recommendation: implement differently, decline the PR as a unit** — close U1 as done; carry U7's two assertions into TASK-025 commit 4 (installed command behaves like direct project-local invocation; an incomplete layout fails non-zero with gate status, never a clean drift report), citing #69 as provenance. Review: `.scratch/JESKO-pr67-70-review.md`. | S2 | Sync | **Landed on main 2026-09-15 inside TASK-025 commit 4's installer work; PR #69 closed with a comment linking the commits, branch deleted.** What to test: `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" install-toolchain` passes #U7a (the installed command reports exactly what the project CLI reports) and #U7b (a project CLI missing `lib/` or only `lib/gate.sh` exits non-zero, prints `gate: … NOT armed` and no report). Re-open if drift prints a report while it could not arm the gate. |
| **TASK-031** | **No pre-push stage and no CI job runs the TypeScript typecheck, so type errors in `tests/` land silently.** Two `tsc` errors in `tests/code-root/code-root.spec.ts` (TS2353, TS2339 on `childRoot`) sat on main unnoticed until the TASK-028 port fixed them as BUG-119, and `tests/tsconfig.json` now enables `exactOptionalPropertyTypes` — a setting nothing enforces. vitest transpiles without typechecking, so a green suite says nothing about types. Found by Philipp (Infrastructure-1) during TASK-028. **Shape of the fix:** one stage that runs `tsc --noEmit -p tests` in the gate and the same command in CI through the gate's own path (BUG-117's lesson: CI and the gate must not diverge), a `tests/manifest` assertion that both invoke it, and a reproducer showing a planted type error fails the push. | S2 | Test suites | **Landed on main 2026-09-15; reviewed by Jesko (Codex), all three findings answered (`835fb3f`, `f8f4102`).** What to test: plant a type error in any file under `tests/` and run `git push` — the gate stops at "typecheck · TASK-031" within about a second, showing the compiler error; revert it and the stage passes. Then `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" ts-bridge manifest` passes. The same check runs in CI as "Blueprint TypeScript typecheck". Re-open if a type error in `tests/` can pass either the gate or CI. |
| **TASK-033** | **ShellCheck is installed on every machine and run by both the pre-push gate and CI.** Today the shipped shell scripts carry `# shellcheck` directives as if they were linted, but nothing installs ShellCheck (`scripts/install-toolchain.sh` has no entry on macOS or Linux) and nothing runs it: no gate stage, no CI job, no DoD requirement. So the changed scripts of TASK-025, BUG-120 and TASK-031 went out unlinted. Founder decision 2026-09-15: it is needed on the Mac as well as Linux. **Shape, mirroring TASK-031:** the installer installs it on both OSes (brew / apt) and `check` reports it; one shared function lints the shipped shell scripts through the gate's scrub path, called by a managed gate stage and by CI; a parity control fails the push if either stops calling it; existing findings are fixed or justified with a reasoned disable, never by loosening the severity threshold. | S2 | Tooling | **Landed on main 2026-09-15; reviewed by Jesko (Codex), his one finding fixed.** What to test: (1) `bash scripts/install-toolchain.sh check` lists ShellCheck as present, or `bash scripts/install-toolchain.sh` installs it (brew on the Mac, the pinned release into `~/.local/bin` on Linux, no sudo); (2) add an unused variable to any script under `scripts/` and run `git push` — the gate stops at the ShellCheck stage and names the warning; revert it and the stage passes; (3) `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" ts-bridge install-toolchain` passes. Re-open if a ShellCheck warning in a shipped shell script can pass either the gate or CI, or if a machine without ShellCheck pushes without being stopped. |
| **TASK-034** | **Acceptance is the founder's, and so is any delegation of it: remove the standing "acceptance delegated to QA-2" rule everywhere it is stated.** Requested by struct2flow-www in a2bp PR #71 (one location) and in PR #72's `FR-www-qa2-standing-delegation` (all of them). **Founder decision 2026-09-15:** "an agent may not delegate acceptance to another agent without consulting me. I'm responsible for the acceptance or its delegation." The 2026-07-29 decision covered specific items; the text generalised it into a class of work. **Stated today in:** `templates/project_config_dod.md:181` (seeded into every new project, naming this repo's personas), `project_config_dod.md:162`, `docs/done/BUGS.md:6`, `docs/waiting-acceptance/README.md:28-33`. **Do:** replace each with the founder's rule (final acceptance is the founder's; an agent may propose a cross-provider QA pass, but its verdict becomes acceptance only when the founder explicitly delegates that item); keep the 2026-07-29 decision only as a history note naming the items it covered. Dated audit records stay as history. | S2 | Lifecycle / acceptance | **Pushed 2026-09-15** (`dfd3cd9`, review note `cd4c373`). **What to test:** `git grep -n "delegated to QA-2"` must hit only this row and the dated `docs/config/BLUEPRINT-AUDIT-2026-07-23.md`. Read `templates/project_config_dod.md` §"Acceptance authority": it must say final acceptance and any delegation of it are yours, item by item, with no persona names. `docs/waiting-acceptance/README.md` §"How acceptance works" must say the same. **Cross-provider review:** Alex (Codex) found no remaining standing grant. His finding about incident history in the shipped waiting-acceptance README was declined: those lines explain why the rules exist, as shipped docs do throughout. PR #71 closed pointing here. **Re-open if** any live rule lets an agent accept, or delegate acceptance, without you. |
| **TASK-035** | **Every work item named to the founder carries a link to the line that defines it and one plain sentence saying what it does.** Requested by storm2flow in a2bp PR #73 (AGENTS.md). **Founder decision 2026-09-15:** "I want it." The need showed the same day in this repo: the Orchestrator referred the founder back to "your answer about #71–#74" without restating the question or linking the items. **Do:** land #73's AGENTS.md wording in the blueprint, adapted if needed, with the deck slide for the persona-team concern if it describes how agents report. | S3 | Agent protocol | **Pushed 2026-09-15** (`e135324`, deck `5c4c5ba`). **What to test:** `AGENTS.md` line 113 says every work item named to you carries a link to the line that defines it and one plain sentence saying what it does. The persona-team slide in `docs/way-of-working.md` carries the same rule. In practice: the next time an agent names a bug, task or PR to you, it links it and says what it is. **Cross-provider review:** Alex (Codex) asked for the deck bullet, which was added. PR #73 closed pointing here. **Re-open if** an agent names an item to you without a link and a plain line. |
| **TASK-036** | **Three stale pointers in `docs/DoD.md`.** Requested by storm2flow in a2bp PR #74. **Founder decision 2026-09-15:** "seems ok." (1) the storm2flow citation carries no `a2bp-allow` annotation, so the contamination guard flags it on every request touching DoD.md; (2) the major-bug pointer should name `project_config_overview.md`, where the core USP paths are declared; (3) §G and the wake side still describe the tracked `AGENT_SIGNAL.md` as the baton, while the live baton is `logs/state/signal.md`, written only by `scripts/signal-set.sh`. **Do:** land those three changes in the blueprint, checking each against current text. | S3 | Docs / DoD | **Pushed 2026-09-15** (`8ae253a`, `d6faf8d` DoD; `83492f3` deck; `3c13c6a` roster example and publishing guide). **What to test:** in a derived project, `blueprint a2bp --dry-run docs/DoD.md` must no longer have the contamination guard flag the storm2flow citation (line 58). `docs/DoD.md` §G and "The wake side" must name `logs/state/signal.md` as the live baton, and the major-bug pointer must name `project_config_overview.md`. `AGENT_ROSTER.example.md` must put the persona name in the live baton's Holder field. **Cross-provider review:** Alex (Codex) found two more stale baton references, now fixed. PR #74 closed pointing here. **Re-open if** a managed doc still presents `AGENT_SIGNAL.md` as live state. |
| **TASK-037** | **`blueprint a2bp` can propose files the blueprint does not ship, and feature requests from derived projects have one visible inbox.** Requested by struct2flow-www in PR #72 (`FR-www-a2bp-feature-requests`): `blueprint a2bp docs/backlog/BUGS.md` exits 4 ("not a blueprint-managed file"), so that PR had to be opened by hand with plain `git`/`gh`, skipping the contamination guard. **Founder decision 2026-09-15:** a2bp should also carry files that are not in the blueprint's managed set; "when we analyse a PR, we can always reject it, if it brings any kind of contamination." The founder also asked for a place to see feature requests. **Do:** (a) let a2bp stage an unmanaged path (a new file or a blueprint-only file) on its request branch, still running the contamination scan and still filing a request, never landing; say in its output that the file is not shipped; keep the refusals that protect the repo (paths outside the project, `.git`, secrets). (b) Add `docs/backlog/feature-requests.md` from PR #72 as the intake inbox (code, from, filed, request, status), recording the QA-2 request as PROMOTED TASK-034 and this one as PROMOTED TASK-037. (c) Update CLAUDE.md §"Back-propagating", README and the sync deck slide where they say a2bp carries only managed files. | S2 | Sync / a2bp | **Pushed 2026-09-15** (`0c47b5b`..`f23e7cb`; inbox `38455ae`). **What to test**, in a derived project with `blueprint a2bp --dry-run <path>`: a new file, or one the blueprint does not ship, is accepted and labelled "not shipped". Each of these must exit 4 before any fetch: a `.env` or `*.key` file (even force-tracked), a file holding a private key (with gitleaks installed), any path under `.git`, `PROJECT_CONFIG_DOD.MD`, a gitignored file, and a path under a symlinked directory. `docs/backlog/feature-requests.md` shows both of PR #72's requests as PROMOTED. **Cross-provider review:** Alexey (Codex) found 2 P1 and 3 P2 issues, all fixed with failing tests first: secrets were proposable, the config refusal was case-sensitive, case-only base collisions, `scaffolding/` relocation, and a non-independent test. Vitali's first secret test held a literal fake key, so its five commits were rebuilt with the founder's OK. **Alexey's re-check of the fixes is still to run** (Codex quota, after 2026-09-16 02:10). PR #72 closed pointing here. **Re-open if** a2bp files a secret, a `.git` path or a project config, or places a new file anywhere but where it was named. |
| **TASK-038** | **Remove `CHANGES.md` from the lifecycle: `BACKLOG.md` and `BUGS.md` are the only record files in each state.** No `CHANGES.md` has ever existed in any lifecycle folder, and every task and feature row has travelled through `BACKLOG.md` (TASK-025..033 are in `waiting-acceptance/BACKLOG.md` today). But the rules still send non-defect changes there, and they contradict themselves: `docs/DoD.md` §7C line 661 moves backlog rows to `waiting-acceptance/BACKLOG.md`, and lines 669-673 send forward features to `waiting-acceptance/CHANGES.md`; `docs/waiting-acceptance/README.md` says it is created on first use (12-15) and that only BACKLOG.md and BUGS.md belong (17-20). An agent following the text files features where nobody looks. **Stated in:** `CLAUDE.md:288`, `docs/DoD.md:34-35` and `:663-673`, `docs/waiting-acceptance/README.md:12-15`, `docs/done/README.md:7-8`; read by `scripts/lib/dod-gate.sh:76` and `tests/lifecycle-docs/lifecycle-docs.ts:62` (with `.spec.ts` cases around 207-220 and a `tests/dod-gate` comment at 114). **Do:** drop CHANGES.md everywhere live; keep the rule that a defect is a `BUG-XXX` row, reworded as "never a backlog row"; update the deck lifecycle slide if it names it. Dated records (`docs/done/TASK-018-*`, `docs/doing/TASK-022-rule-enforcement.csv`) stay as history. | S3 | Lifecycle / docs | **Pushed 2026-09-15** (`a006606`, `1622afe`). **What to test:** `git grep -n -i changes.md` must hit only dated records, the handover and this row. `docs/DoD.md` §7C must send tasks, features and behaviour changes to `BACKLOG.md`, and defects to `BUGS.md`, never to a backlog row. **The founder waived the cross-provider review for this item.** **Re-open if** a rule, gate or test sends work to a `CHANGES.md` again. |

**Each row's "what to test" travels WITH it** — into `done/` on acceptance, back
into `doing/` on a rejection. It is not dropped at the boundary: a rejected item
needs its test instructions more than a waiting one does, and an accepted item's
instructions are the record of what "accepted" actually meant. An empty table
here is the good state, not a missing section.


*(Empty.)*
