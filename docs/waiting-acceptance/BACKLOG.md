# Landed rows awaiting founder acceptance

Rows whose work is **on `main`** and which are waiting for the founder to accept
("done") or reject ("reopen"). See [README.md](README.md) for the triggers.

**This file appears when the first promoted row lands** — its absence means none
have, not that it is missing.

| # | Item | Sev | Category | Re-open trigger / next-step gate |
|---|---|---|---|---|
| **TASK-071** | **TASK-062-05: a stray `HANDOVER-*.md` can never land.** Audit row D130. `docs/DoD.md:241-242` says the handover is ONE file, overwritten in place — a dated copy beside it is a second record of the same fact, and the pair drifts. Nothing checks it today, and zero exist now, so the check is cheap and permanent rather than a response to a mess. **Do:** a glob check that fails when any `docs/doing/HANDOVER-*.md` exists, wired where the other tree checks run, with the `enforced by` pointer added to the prose and audit row D130 updated in the same commit. Prove it red by planting one. | S3 | KEEP | **Done when** a planted `HANDOVER-2026-01-01.md` fails the check and the tree passes. **Re-open if** a second handover file lands. |
| **TASK-072** | **TASK-062-04: the doc-sync list names paths this project does not ship.** Audit row D058. `project_config_dod.md`'s doc-sync tables are still the seeded template: they name `docs-site/…`, `frontend/public/help.html` and other surfaces the blueprint has none of, so the handoff checklist (DoD §7.D) walks a list of fictions and the proposed `{{` check would pass vacuously. **Do (PO, judgement first):** write the project's REAL doc-sync list — for this repo mostly N/A rows, since it ships no docs-site — saying for each surface what it is and why. Then a check that every path NAMED in the list exists in the tree, which fails today until the list is real. The check is the second half and may be a separate commit; the list is the judgement only the PO can make. | S3 | KEEP | **Done when** the list names only real surfaces and a planted fictional path fails the check. **Re-open if** the list names a path the tree does not have. |
| **TASK-073** | **TASK-062-07: a swallowed error can never land silently.** Audit row C095, NARROWED on the Architects' review: `CLAUDE.md:182-183` says every error path is captured, which is judgement and cannot be mechanised whole. What CAN be checked is the syntactic sub-rule: a bindingless `catch {}` with no rethrow and no comment. Measured 48 such sites across `scripts/` and `tests/`, most a legitimate fallback, so this is a triage task before it is a check task. **Do:** triage the 48, fix or comment the ones that hide a real error path, then a TS-AST check in a vitest suite (NOT semgrep — its local config lives in `.githooks/pre-push`, a legacy file). State in the row and the prose exactly which sub-rule is now enforced and what remains judgement. **Landed (TASK-073):** 39 of the 48 were silent. Three directory walkers (`doc-links`, `lifecycle-docs`, `manifest`) no longer swallow a read failure — a listed subdirectory that cannot be read is never "absence" — and the other 36 carry a one-line reason at the catch; `tests/harness/workspace.ts`'s teardown now accepts ENOENT only, as its comment always claimed. **Enforced:** a bindingless `catch {}` under `scripts/` or `tests/` that neither rethrows nor carries a comment of at least two words fails `tests/forbidden-idiom` (word floor after Jesko's review: a bare `//` passed the first cut). **Still judgement:** whether those words are true, a bound `catch (e)` that never reads `e`, a catch that logs and returns success, every non-TypeScript path. | S2 | KEEP | **Done when** a planted bindingless silent catch fails, the tree passes, and the prose claims only the sub-rule. **Re-open if** a silent catch lands. |
| **TASK-074** | **TASK-062-09: `docs/config/findings.md` has no schema, so "what is still open" is unanswerable.** Audit rows D114, D075 and D079. Measured: all 5 findings lack a `Status:` line entirely, and none carries the `[SEC]` tag `docs/DoD.md:187-190` and `docs/SECURITY.md` describe. The format does not exist, so this is a PO judgement before it is a check. **Do:** (a) the PO establishes the `Status:` enum (`Open` / `Fixed` / `Deferred` + date / `Accepted` + who signed off) and, for a CVE-tagged finding, the upgrade-date field D075 wants; write it into the file's own header so the next writer sees it; give the 5 existing findings their status from their own text, stopping on any that needs a judgement call. (b) a check that every finding block carries a valid `Status:`, with the `enforced by` pointer and the three audit rows updated in the same commit. | S3 | KEEP | **Done when** a finding without a valid `Status:` fails the suite and all 5 carry one. **Re-open if** a finding lands with no status. |
| **TASK-075** | **TASK-062-06/-13/-14: three flip-time checks, warning-only.** Audit rows D092, D116, C029 and D098, grouped because the plan says one port decision, not three. At the moment the mic is handed back: (D092) `scripts/session-resume.sh`'s session-marker check exists but is wired nowhere; (D116) DoD §7F wants a clean tree, including untracked files, while a review is required; (C029/D098) DoD §7 wants CI's state known before a flip to `OVER_TO_USER`, and nothing looks. **Founder decision, 2026-09-22: warning-only first.** Each prints at flip time and blocks nothing, so `scripts/signal-set.sh` (legacy shell, 302 lines) is NOT ported now; port it later only if a warning proves ignorable. **Do:** one place that runs the three checks and prints, called where a flip happens without editing a legacy shell file — say in the commit where that place is and why it is not a shell edit. Each check states what it observed, never a guess. Pointers and the four audit rows in the same commit. | S2 | KEEP | **Done when** each of the three prints on a real flip, blocks nothing, and a planted violation of each is visible in the output. **Re-open if** a flip-time check reports something it did not observe. |
| **TASK-076** | **TASK-062-12a: whether the two-commit pattern applies to a bug is judgement, so the ROW must say it, not a checker guess it.** Audit row D100. `docs/DoD.md:124-126` narrows the pattern to product/runtime bugs, and any check that infers "product bug" from touched files, labels or prose is the findings.md F-002 shape this epic exists not to repeat. **Do (PO, XS):** add one machine-readable field to a bug row — `reproducer required` or `not applicable: <reason>` — following the BUG-139 pattern of turning a judgement call into data ON the row; write the field into `docs/DoD.md` §2 where bug rows are specified, and fill it for every bug row currently in `doing/`, `waiting-acceptance/` and `done/`, stopping on any that needs a founder call. No order check in this item — that is TASK-077, which this unblocks. | S3 | KEEP | **Done when** every existing bug row carries the field and the DoD specifies it. **Re-open if** a bug row lands without it. |
| **TASK-077** | **TASK-062-12b: with the field declared, a declared reproducer that is not a separate earlier commit fails.** Audit row D100, second half. **Blocked on TASK-076** — without the field the check has nothing sound to read, and reading anything else is the F-002 shape. **Do (Back-End, M):** over the pushed range, for each bug row declaring `reproducer required`, assert `git log` order — the reproducer commit precedes the fix commit for that BUG-NNN. A row declaring `not applicable` is skipped, visibly, with its reason printed. Never infer the declaration; if the field is missing, say so rather than assuming either answer. Pointer and audit row D100 in the same commit. | S3 | KEEP | **Done when** a fix committed before (or without) its declared reproducer fails, and a `not applicable` row is skipped with its reason visible. **Re-open if** the check ever infers a declaration it did not read. |
| **TASK-078** | **TASK-062-15: name what actually stops a `--no-verify` push, then check the parity it depends on.** Audit rows D097, D056. Git records nothing about whether a local hook ran — all three reviews said independently that bypass detection is impossible. **Reframe:** CI re-runs the checks on the pushed range and `security.yml`'s `release` job advances `released` only after `secret-scan, sast, sca, commit-subjects, ts-tests` are green, so in this repo a bypassed push cannot fan out to a derived project even though it can land on `main`. **Do (Security, M):** (a) write that mechanism where D097/D056 are claimed, replacing the undetectable claim; (b) add a local/CI parity check — every local-judging gate stage has a CI counterpart — which is the gap TASK-067's synthesis flagged and never landed (local semgrep runs `p/owasp-top-ten`, CI adds `p/javascript`+`p/typescript`). Cite the BUG-005 founder decision (2026-08-02), which restored CI-only suites into the local gate for this same reason: this is a documented precedent, not a silent reversal. **State the residual plainly:** not every local `dod-gate.sh` stage is re-run in CI, so "CI catches it" covers the security checks, not the whole gate. | S2 | KEEP | **Done when** the parity check fails on a planted local-only stage, and the DoD lines say what really holds rather than what cannot be detected. **Re-open if** a local stage gains no CI counterpart and nothing notices. |
| **TASK-079** | **TASK-062-16: nothing scans a push for contamination — the call site does not exist.** Audit row C168, premise corrected by Slava: `.githooks/pre-push` contains NO contamination call site at all; `contamination.sh` runs only from `scripts/blueprint`'s `a2bp` path. There is nothing to extend; the absence IS the finding. **Founder decision, 2026-09-22: option (a), the CI-only route** — do not port `contamination.sh` (477 lines) for this. **Do (Security, M):** a CI job reading the pushed diff and reusing `contamination.sh`'s checker logic rather than duplicating its patterns; where reuse is impossible from CI, say exactly why and what is duplicated. **Say plainly in the same commit what CI-only does NOT protect:** contamination publishes a project's identity to every downstream project on the next pull, and a CI scan detects that after the publication, not before. | S2 | KEEP | **Done when** a planted contaminated line fails the CI job, and the residual is written where a reader of C168 will find it. **Re-open if** the patterns are duplicated and drift from `contamination.sh`. |
| **TASK-080** | **An item id in a reply to the founder must be a link to the row it names, and nothing can see the reply but a `Stop` hook.** The founder asked for this twice — 2026-09-22 ("you are not linking the items") and 2026-09-23 ("you are not linking the issues") — and both times the reminder failed within a day. Every gate this repo has watches the REPOSITORY; this rule governs the Orchestrator's reply TEXT, which no hook but `Stop` ever sees. That is why prose could not hold it, and it is the same argument TASK-062 makes throughout. **Scope, settled by the founder 2026-09-24:** *"this is only relevant for the orchestrator, you are the only one communicating with me"* — so binding Claude Code only is not a ceiling here, it is the whole surface. **Do:** a `Stop` hook that reads the last assistant message, and for each `TASK-`/`BUG-`/`FEATURE-`/`SPIKE-NNN` id in it requires the FIRST mention to be a markdown link pointing at the lifecycle file that actually holds that row — not merely that some link exists, which would be the findings.md F-002 shape. Code blocks and inline code are not prose and are skipped. An id with no row anywhere demands nothing. On a violation the hook exits 2 naming each id and the exact path it should have linked to, so the fix is mechanical. | S3 | KEEP | **Done when** a reply naming a row without linking it is refused with the path it should use, a reply linking to the wrong lifecycle folder is refused, and code-block mentions pass. **Re-open if** a bare item id reaches the founder, or the guard blocks a reply that was correct. |

**Each row's "what to test" travels WITH it** — into `done/` on acceptance, back
into `doing/` on a rejection. It is not dropped at the boundary: a rejected item
needs its test instructions more than a waiting one does, and an accepted item's
instructions are the record of what "accepted" actually meant. An empty table
here is the good state, not a missing section.
