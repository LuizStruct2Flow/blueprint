# Landed rows awaiting founder acceptance

Rows whose work is **on `main`** and which are waiting for the founder to accept
("done") or reject ("reopen"). See [README.md](README.md) for the triggers.

**This file appears when the first promoted row lands** — its absence means none
have, not that it is missing.

| # | Item | Sev | Category | Re-open trigger / next-step gate |
|---|---|---|---|---|
| **TASK-047** | **Every test is a spec-driven TypeScript test: one extension, `*.spec.ts`, no exceptions.** Founder decision 2026-09-16: *"migrate the tests to be spec driven ts tests, we don't need exceptions"*, answering why the gate accepts five runner extensions while only `*.spec.ts` executes (TASK-039 finding 6, Alexey). **The facts behind it:** under `tests/` there are 55 TypeScript test files, one real shell runner (`tests/ts-bridge/test.sh`) and one shell helper (`tests/helpers/proc-cwd.sh`, invoked from a spec, not a test itself). `.spec.js` / `.test.js` were accepted only in case a derived project used JavaScript; no such file exists. **Do:** port the shell runner to a spec; collapse the accepted set to `*.spec.ts` in `scripts/lib/dod-gate.sh`, `scripts/lib/suites.sh` discovery, `tests/vitest.config.ts` and the three checks in `scripts/run-ts-suites.sh`, so what the gate counts and what actually runs are one set; simplify TASK-039's guard #14 accordingly. **Then the convention docs, which is where this reaches further than `tests/`:** CLAUDE.md §"Test Layers" prescribes `*.test.{js,ts}` for co-located unit tests in `src/` and `*.snap.test.{js,ts}` for snapshots, and §"Test directory layout" repeats it. Report the full ripple before rewriting those rows — whether co-located unit tests in a derived project's `src/` also become `*.spec.ts` is a convention change for every project and goes back to the founder if it is not obviously covered. | S2 | Test suites | **Pushed 2026-09-16** (`5e09d62`..`0a9ddb8`, retirement `da73f36`/`da352b6`, convention `ae7e634`, guards `8c609e2`/`a305e8d`/`7c54cb4`). **What to test:** every test in the blueprint and in a derived project is now `*.spec.ts`, or `*.spec.tsx` where it contains JSX — one convention, no exceptions, with the layer in the infix (`.integration.`, `.snap.`). Check the gate cannot drift from the runner: `tests/dod-gate` **#17** compares what the gate COUNTS as evidence, what discovery FINDS, and what vitest RUNS, and fails if any one side changes; `#17b` witnesses discovery behaviourally. Both were mutated on each side to prove it. **A migration note for existing projects is in CLAUDE.md beside the rule** — it arrives with the pull that creates the need. Its warning is worth reading: **check the test count, not the colour**, because a glob that stops matching reports zero failures and looks exactly like success. **What this cost, on the record:** `tests/ts-bridge/test.sh` is retired and TASK-023 closed as an accepted loss (`F-003`) — it was the only assertion about the vitest bridge not executed by the vitest it asserts about, so a silently dead bridge now produces no red case. **Re-open if** a test file that the runner never executes counts as evidence, or the three sides disagree. |
| **TASK-048** | **A derived project tracks all six methodology files: the `.gitignore` privacy block goes, with no exceptions.** Founder decision 2026-09-16, "All six, no exceptions", following TASK-046: private project notes now have a home in `claude.internal.md`, so excluding the framework's own docs protects nothing and costs every doc link into them (the limitation BUG-125 had to accept). **The six:** `/CLAUDE.md`, `/AGENTS.md`, `/AGENT_SIGNAL.md`, `docs/DoD.md`, `docs/PUBLISHING.md`, `docs/doing/HANDOVER.md`. Five are blueprint-managed or generic protocol and identical in every project; the sixth is the project's live handover, and **the founder accepted publishing it**. **Evidence that shapes the work (Christian):** `.gitignore` is NOT in `MANAGED_FILES` but IS in the bootstrap archive, so it is seeded once and project-owned — **a blueprint edit reaches NEW projects only**, and every existing project must edit its own copy. **Do:** (a) remove the six from the blueprint's `.gitignore` and **replace** the "multi-AI methodology (PRIVATE)" comment rather than delete it, since it records a deliberate decision — put the replacement wording in front of the founder; (b) same commit, update `docs/PUBLISHING.md`, which encodes the exclusions in §0, in §1b's preflight (it greps the index and prints `PRIVATE FILES STILL TRACKED — DO NOT PUSH`) and in §3a/§3b's allowlist and untrack commands — otherwise the runbook fails a project for tracking its own docs; (c) write the one-line instruction existing projects run, and say where it goes so storm2flow and the others actually get it; (d) check `tests/bootstrap-contents` (`:96-107`) and `tests/bootstrap-gate`, which force-add these files so the fixture mirrors today's repo. **Leave `project_config_*.md` ignored** — A-27 put the threat model and infra account IDs there deliberately. **Absorbs A-13**, cancelled 2026-09-16 with a pointer at `docs/config/findings.md` F-004: it recorded the same fact from the other end — `.gitignore` is not managed, so the privacy block's "they'd come back on next sync" instruction described a mechanism that does not exist, and a derived project's block silently never updated. That is (c) here, the instruction existing projects run themselves. Two live records of one fact drift, which is why A-13 does not stay open alongside this. | S2 | Privacy / sync | **Pushed 2026-09-16** (`9414ccd`, A-13 absorbed `9b1e409`, doc-links `066e0e8`, publishing fixes `6937540`, witness `a2123b6`). **What to test:** bootstrap a fresh project and run `git ls-files` in it — `CLAUDE.md`, `AGENTS.md`, `AGENT_SIGNAL.md`, `docs/DoD.md`, `docs/PUBLISHING.md` and `docs/doing/HANDOVER.md` must all be tracked, and the five `project_config_*.md` must NOT be (A-27 put the threat model and infra account IDs there). `bootstrap-contents` #5c asserts exactly that, because `git add -A` skips an ignored path in silence — "tracked" and "looks tracked" were otherwise indistinguishable. **Existing projects are not reached:** `.gitignore` is seeded at bootstrap and never pulled, so storm2flow and the others run the instruction in CLAUDE.md §"Blueprint sync" themselves. **Tracking is not publishing:** `docs/PUBLISHING.md` §3b now redacts the live handover before a public push and says so explicitly, rather than pointing at a scrub in §3a that §3b never executes; §3a's allowlist gained the three root documents it was missing. **A parked follow-up, TASK-049:** prose is not enforcement, and a guard that refuses to publish a live handover needs your ruling on managed-versus-project placement. **Re-open if** a fresh project excludes any of the six, or a published repo carries a live handover. |

**Each row's "what to test" travels WITH it** — into `done/` on acceptance, back
into `doing/` on a rejection. It is not dropped at the boundary: a rejected item
needs its test instructions more than a waiting one does, and an accepted item's
instructions are the record of what "accepted" actually meant. An empty table
here is the good state, not a missing section.


*(Empty.)*
