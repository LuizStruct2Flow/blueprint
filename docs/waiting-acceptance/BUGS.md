# Bugs — pushed to main, awaiting founder acceptance

Fixed and pushed; awaiting the founder's explicit acceptance signal before they
move to `docs/done/BUGS.md`. Claude does NOT auto-promote to `done/`. If a
regression is found, the row moves back to `docs/doing/BUGS.md`.

See [README.md](README.md) for the lifecycle.

**"What to test" is a column here, not a separate index.** There used to be an
`INDEX.md` holding the same membership plus per-item test instructions. It
drifted — 5 rows listed against 14 real ones, so nine fixes were invisible to
the only person who can accept them — and the first repair was a test to hold
the two files in step. That is the wrong repair: two records of one fact drift
by construction, and a guard only tells you afterwards. One record cannot
disagree with itself.

**Put the acceptance command in the CHAT, not only in this column.** BUG-022
shipped with `scripts/accept-bug-022.sh` and a pointer in its row, and the
founder still had no idea how to accept it — because this column lives in a file
he would have to open first. Klaus and Alexis both said acceptance instructions
belong where the decision happens.

| # | Bug | Severity | Status | What to test | Detail |
|---|---|---|---|---|---|
| **BUG-077** | **`scripts/lib/feed.sh:37` locates the activity feed with `git rev-parse --show-toplevel` — the idiom banned everywhere else for a hazard that applies here identically.** The ban exists because **git exports `GIT_DIR` into every hook**, and the gate runs the suites from a hook, so a fixture's git environment silently redirects the resolution (BUG-014's mechanism, A-09's consequence). `tests/state-dir` #6c enforced it — and its scope is *state-dir consumers*, which `feed.sh` is not. The scope was never wrong, it was just narrower than the hazard. **The concrete consequence is already documented:** it is the mechanism behind the split-feed finding in the root-split review — after a `scaffolding/` move the supervisor writes one feed while the gate and the subagent hooks write another, because this line answers a different question than the physical-script walk does. **Found by method, and specifically by the method a mutant cannot reach.** The equivalence comparison ran 19 trees through both `#6c` and its TypeScript port and diffed verdicts: 12 agreed, 7 disagreed (every one shell PASS → port FAIL, which is the widening working), and **2 passed under BOTH** — `lib/feed.sh` and `lib/gate.sh`. A mutation of either implementation would have shown nothing, because both are faithfully out of scope. Only comparing them against a population that included the un-covered cases surfaced it. **Fix is a rule question, not a line edit:** the ban should be scoped to the hazard (anything resolving a path that a hook's exported git environment can redirect) rather than to a subsystem, and then `feed.sh` follows from the rule instead of being named by it. Check `lib/gate.sh` in the same pass. | S2 | **FIXED — landed `4c3c5a2`, pushed 2026-09-12** | Run `grep -rn 'rev-parse --show-toplevel' scripts .githooks` — every hit must carry an inline `bp-allow-toplevel: <why>` justification, and none may remain in `scripts/lib/feed.sh` or `scripts/lib/gate.sh`. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" forbidden-idiom` must pass. The suite is proven able to fail: restoring the idiom in either file turns it red, naming the file and line. | Found 2026-09-11 by Christian (Senior Architect) during the Stage A equivalence comparison; rowed by Eto (Orchestrator). He declined to widen the rule inside the port, correctly — a port that changes the rule it is being compared against cannot be shown equivalent to anything. **Re-open trigger: before TASK-021 Stage B**, because the split-feed failure it causes is invisible (two feeds, both written, neither empty) and Stage B is what triggers it. **FIXED 2026-09-11 by Christian (Senior Architect).** Promoted from `backlog/` on its own re-open trigger — TASK-021 Stage B is next. |
| **BUG-114** | **An existing project that pulls never receives three scripts its managed files call.** `pull` delivers `MANAGED_FILES` only, and `scripts/no-chain-guard.sh` (run as a PreToolUse hook by the managed `.claude/settings.json`), `scripts/session-resume.sh` and `scripts/wait-mic.sh` (driven by shipped suites and the harness) were never listed. A fresh bootstrap is NOT affected — it is `git archive HEAD` — which is why PR #66 named the wrong victim (Jesko's review: 39/39 on a fresh bootstrap before the fix). Reproduced: an existing project's `pull --yes` reports nothing to pull and the scripts stay absent. | S2 | **FIXED — landed `1d4bcb4`, pushed 2026-09-14** | Run `blueprint files` — it must list `scripts/no-chain-guard.sh`, `scripts/session-resume.sh` and `scripts/wait-mic.sh`. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" managed-references` must pass. In a derived project, `blueprint pull` must deliver the three scripts. | Found by struct2flow-www (PR #66, U1), corrected by Jesko (QA-2, Codex). TASK-026. Regression: `tests/managed-references` — #1 DERIVES the referenced-script set rather than listing three, #2 pulls it into an existing project. Re-open if a shipped managed file names a shipped script `MANAGED_FILES` does not. |
| **BUG-112** | **`pull` decides a file is marker-bearing by SUBSTRING, so a managed file that merely mentions the markers takes the whole-file fallback on every pull.** `grep -q 'BLUEPRINT:BEGIN'` matches prose and code alike. Reproduced: `CLAUDE.md` (1 BEGIN / 2 END in prose) and `scripts/blueprint` itself (7 / 6, in its own greps and comments) each left a `.bp-bak` and a whole-file copy on a single pull; `way-of-working.md` and `pre-push-project.example` are in the same state. Worse than noise: the day a mentioning file's counts balance, the awk would "merge" it — for the CLI, into itself. Rewording CLAUDE.md alone (PR #66 U2) fixes one file and leaves the next sentence as the trap. | S2 | **FIXED — landed `1dcdf4c`, pushed 2026-09-14** | In a derived project, `blueprint pull CLAUDE.md` and `blueprint pull scripts/blueprint` must leave no `.bp-bak` file behind. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" marker-merge` must pass (cases #3 and #4). | Found by struct2flow-www (PR #66, U2), reproduced at pull level by Jesko (QA-2, Codex); the CLI-itself instance found implementing TASK-026. Regression: `tests/marker-merge` #3 (prose) and #4 (the CLI). Re-open if marker classification can be satisfied by a line that is not a marker. |
| **BUG-034** | **`blueprint pull` can silently delete a project's own content — `marker_aware_merge` compares marker COUNTS and never their ORDER.** `scripts/blueprint:311` guards with `bp_begin -ne bp_end || proj_begin -ne proj_end || bp_begin -ne proj_begin`. A file carrying `BLUEPRINT:END` **before** `BLUEPRINT:BEGIN` has counts 1 and 1, passes every arm, and reaches the awk — which sets `capture` on BEGIN and clears it on END, so with them inverted the region tracking is wrong while `pull` reports the project's half preserved. **This is the core sync verb**, the one thing every derived project runs against this repo, and the failure is silent by construction. Was A-11, buried in the `AUDIT-TRIAGE` row of ~12 undispositioned findings — found by sampling that row at random during the 2026-08-20 grooming, which is the argument for triaging the remaining ~11 rather than for fixing this one. **REPRODUCED LIVE 2026-09-11 by Elias (Infrastructure-2)** during the TypeScript port of `tests/marker-merge`, and it is worse than the wording above. Measured, not inferred (`.scratch/elias-bug034.sh`): counts 1/1, every guard arm passes, no `.bp-bak` is written, and the outcome is `PROJECT-OWNED-FOOTER: LOST` — **while `pull` prints *"(marker-aware merge: project outside-marker content preserved)"* and *"✓ Pulled 1 file(s)"*.** So the verb does not merely lose the content silently; it states that the content it destroyed was preserved, and exits 0. **And the loss is two-sided:** the BLUEPRINT's own region content is not delivered either, so the file ends in a state neither side wanted — a project that hits this gets a file that is neither its own nor the blueprint's, with a success message. **Raised from S2 to S1 on that basis**: a wrong answer that announces itself as right is the class this repo has spent a week naming (`docs/config/findings.md` F-002), and this instance sits in the one command every derived project runs against this repo. **Neither test suite has a case that would see it** — grep for an inverted-marker fixture returns 0 in both the shell runner and the port. **It is not mutatable**, because the defect is an ABSENT guard rather than a wrong one, which is why a mutation-based approach had never surfaced it and a direct reproduction did. Deliberately NOT asserted in the new spec: asserting correct behaviour is red on `main`, and asserting current behaviour pins the defect. It belongs to this row. **Reproducer first:** a fixture file with inverted markers, asserting the project region survives — the fix is worthless without a test that fails on the parent commit. **Note the sibling still live in the same row: A-10**, the watcher replaying the current baton at startup, an implicit billable replay and a direct CLAUDE.md §Cost capability-4 violation. | S1 | **FIXED — landed `9f8549e`, pushed 2026-09-14** | Put the end marker before the begin marker in a derived project's `.githooks/pre-push-project` and run `blueprint pull` — it must exit 4, say the file was refused, and leave the file byte-for-byte unchanged with no backup. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" marker-merge` must pass (cases #5, #5b, #6). | Raised 2026-08-20 by the grooming pass. Re-open immediately — this is the product's core verb and nothing currently guards marker order. **Promoted 2026-09-14 into TASK-026** (PR #66's drift change needed one prospective-pull result, and the review required it to refuse invalid markers). Fixed there: marker structure is validated for ORDER and balance before any merge, and an invalid file is REFUSED — nothing written, no backup, pull exits 4 — instead of merged. Regression: `tests/marker-merge` #5 (inverted) and #6 (BEGIN without END). |
| **BUG-113** | **`drift`, `pull`'s file selection and `pull`'s preview each compare a marker file differently, so they disagree about the same file.** All three compared WHOLE files, while the merge `pull` actually performs keeps everything outside the managed regions. Reproduced: a project's own guards after the end marker made `drift` report the hook drifted, made `pull --yes` select it, and made the preview show those guards as DELETED — then the merge kept them and printed `✓ Pulled 1 file(s)`. Every derived project with guards in `.githooks/pre-push-project` is in this state. PR #66 (U6) fixed drift alone, which Jesko's review showed leaves drift clean while pull still selects and previews damage. | S2 | **FIXED — landed `4076faf`, pushed 2026-09-14** | In a derived project whose `.githooks/pre-push-project` has its own guards after the end marker and an up-to-date managed region, `blueprint drift` must NOT list the hook, and `blueprint pull` must not select or preview it. Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" marker-merge` must pass (cases #7–#10). | Found by struct2flow-www (PR #66, U6), made one-caller-proof by Jesko (QA-2, Codex). TASK-026. Regression: `tests/marker-merge` #7 (drift), #8 (selection), #9 (preview), #10 (drift names a file pull would refuse). Re-open if any of the three computes its answer other than through the one prospective-pull result. |
| **BUG-115** | **The blueprint had no CI for four days: GitHub could not parse `.github/workflows/security.yml`, so every push since 2026-09-10 21:06 UTC ran zero jobs, while every local gate reported green.** `44bcdf7` (TASK#18 phase 2) rewrote the `ts-tests` job's last step to `run: echo "::notice::… Restore it with: blueprint pull tests/"`. That value is an unquoted YAML plain scalar, and a plain scalar cannot contain `: `, so the file is invalid YAML (`mapping values are not allowed here`, line 256, column 301). GitHub rejects the whole file and creates no jobs, and `gh run view` shows only "This run likely failed because of a workflow file issue". Last green run: `8baa10f` (2026-09-10 19:11 UTC). First zero-job run: `a9813c5`, the first push that contained `44bcdf7`. `dc845f1` was suspected at first, but it only touched the file again while it was already broken. **Why nothing caught it.** (1) The pre-push gate cannot see CI. It prints "watching CI" and the push exits 0, so a CI that cannot start looks the same as one that is running. (2) `tests/manifest` #5 reads this file every push to prove CI invokes every suite, but it reads it as TEXT. An unparseable workflow still contains `npx vitest run`, so #5 passed while CI executed nothing. The control that claims "every suite runs in the workflow" had no way to tell that no job runs at all. **Fix:** quote the step properly (a block scalar). **Regression:** new manifest check #5b parses every file in `.github/workflows/` with a real YAML parser (`yaml`, now a harness devDependency) and requires the minimum Actions shape: an `on:` trigger, a non-empty `jobs:`, and for each job either `uses` or `runs-on` plus steps that each have exactly one of `run`/`uses`. It covers the class, not the single character. #5b runs against the real tree (`#live`), against the fixture, and inside a bootstrapped project (bootstrap-gate #6). | S1 | **FIXED — landed `d27e1db`, pushed 2026-09-14** | Run `gh run list --repo LuizStruct2Flow/blueprint --limit 3` — the newest `security.yml` run must have jobs (not "a workflow file issue"). Then `tests/node_modules/.bin/vitest run --root "$PWD/tests" manifest` must pass; putting an unquoted `: ` back into any `run:` line of the workflow turns #5b red. | Found 2026-09-14 by the Orchestrator reading `gh run list`, and diagnosed by Philipp (Infrastructure-1). Re-open if any CI run shows zero jobs, or if a check proves a workflow invokes something without first proving that GitHub can parse the workflow. |
| **BUG-117** | **CI refuses 697 of 770 TypeScript tests, because the workflow starts vitest without the scrub the gate applies.** Since `8ee949f` (TASK-025 commit 1, `6ed93b1`, H5), `assertProcessEnvClean` refuses a test process carrying ANY undeclared `GIT_*` / `AGENT_*` name, so a direct run and the gate's runner remove the same population. The gate runs vitest through `scripts/run-ts-suites.sh`, which unsets every `GIT_`/`AGENT_`/`BP_` name and `BLUEPRINT_ROOT` first. `.github/workflows/security.yml`'s ts-tests step runs `npx vitest run` directly, so it inherits the runner's environment — and GitHub-hosted runners export `AGENT_TOOLSDIRECTORY`. Run 34902101002 failed every scenario with *"The test process is carrying variables that must never reach a fixture: AGENT_TOOLSDIRECTORY"*. **Why the local gate could not see it:** the gate never executes the workflow's command. It runs the bridge, which scrubs, and `tests/manifest` #5 reads the workflow as TEXT and checks that it names a blanket vitest run, not the environment that run receives. **Not fixed by declaring `AGENT_TOOLSDIRECTORY` inert:** the scrub boundary is the prefix population, so naming one runner variable leaves the next one to break CI again. The two execution modes must share ONE scrub. **Regression:** `tests/ts-bridge` #3 parses the workflow, executes every step that starts vitest against a recording stub with undeclared `AGENT_`/`GIT_`/`BP_` decoys and `BLUEPRINT_ROOT` exported, and requires the stub to see none of them — the class, not the name. | S1 | **FIXED — landed `3036646`, pushed 2026-09-14** | Run `gh run list --repo LuizStruct2Flow/blueprint --limit 3` — the newest `security.yml` run's "TypeScript regression suites (vitest)" job must be green, not refusing tests for `AGENT_TOOLSDIRECTORY`. Then `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA tests/node_modules/.bin/vitest run --root "$PWD/tests" ts-bridge` must pass; #3 executes the workflow's vitest steps with decoy `AGENT_`/`GIT_`/`BP_` variables and goes red if the step stops using the gate's scrub. | Found 2026-09-15 by the Orchestrator from CI run 34902101002, root cause confirmed by Christian (Senior Architect) against the workflow and the bridge. Re-open if any CI job, hook or script starts vitest or a harness scenario without going through `ts_scrubbed`. |
| **BUG-110** | **An empty `/tmp/.git` makes every scenario workspace under `/tmp` resolve its state root to `/tmp`, so the gate goes red on `state-root` #A6 — and in a suite without that canary it would have gone SILENTLY wrong instead.** The state-root walk (TASK-021 Stage A) terminates at the first ancestor carrying `.git`, `.blueprint-root` or `.blueprint-source`, testing `.git` with `-e` so worktrees and submodules work. The harness builds scenario workspaces with `mkdtemp` under the system temp dir (`TMPDIR` is unset here, so `/tmp`). An empty `/tmp/.git` appeared at 19:44 on 2026-09-14, and from that moment a markerless, non-git tree resolved to `/tmp` with exit 0. Proven by probe (`.scratch/probe-a6.sh`): `resolved=[/tmp]`, terminator `/tmp/.git`. **#A6 did exactly its job** — it asserts a markerless tree FAILS LOUDLY, and it failed loudly on the first push after the directory appeared, both at top level and inside `bootstrap-gate`'s nested gate. **The defect is the exposure, not the canary:** any process that creates `/tmp/.git` changes what every scenario under `/tmp` believes its project root is, and only #A6 checks the premise. A scenario that writes state would write it relative to `/tmp` without complaint. **Attribution NOT proven.** It appeared while a `codex exec --sandbox workspace-write` review was running, and that sandbox treats `.git` specially under its writable roots, which include `/tmp` — consistent, and unconfirmed. The directory is not a mount in the host namespace (`findmnt`). **It VANISHED ON ITS OWN:** listed empty and unmounted, then seconds later `rmdir` found it already gone — nothing in this session removed it. So something creates it and cleans it up again, which is what a sandbox setting it up per command would look like, and it makes the failure **intermittent by construction**: a gate run is red only if it overlaps that window. Do not go looking for a persistent directory. **Attribution OBSERVED FROM INSIDE, 2026-09-14:** Alexey (Architect, Codex), running under `codex exec --sandbox workspace-write` for the TASK-025 plan review, reported that *"this Codex sandbox currently provides an actual empty `/tmp/.git`"*. That is the sandbox describing its own environment, not an inference from timing, and it matches the host-side evidence. **Treat the Codex workspace-write sandbox as the cause** until shown otherwise. Practical rule meanwhile: do not run the push gate or the `state-root` suite while a Codex review is in progress. He also measured that a clone made under `/tmp` still resolves to its own tree because its `.git` is nearer — so code that always runs git as `git -C <tree>` is safe, and code that resolves a root from a scratch PARENT is not. **Fix direction, not decided:** the harness should refuse to create a workspace under an ancestor that already carries a terminator, rather than rely on #A6 noticing afterwards — failing at workspace creation names the cause; failing in #A6 names a symptom. | S2 | **FIXED — landed `b5c2c29`, witness strengthened in `74db453`, pushed 2026-09-14** | Put an empty `.git` directory above a temporary folder and run any suite with `TMPDIR` inside it — it must refuse at workspace creation, naming the marker and the remedy, instead of resolving state roots outside the fixture. Then `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" harness state-root` must pass. | Found 2026-09-14 by Eto (Orchestrator) on a red docs-only push. Re-open if any scenario resolves a state root outside its own workspace. **Promoted 2026-09-15 under TASK-028**, porting PR #68 (linkedin-watcher-agent) onto the current H5 harness. Reproducers: `tests/state-root` #A7 (a ceiling one level below a planted `.git` must stop the walk), and two `tests/harness` BUG-110 cases (every child is handed its workspace root as `BP_STATE_ROOT_CEILING`; a marker in or above `$TMPDIR` aborts workspace creation, naming the path). **Fixed under TASK-028** by an optional `BP_STATE_ROOT_CEILING` in `scripts/lib/state-dir.sh`, set by the harness to every workspace root, plus a preflight in `tests/harness/workspace.ts` that refuses a temp dir with a marker in or above it. **Consequence, deliberate:** while a stray marker sits in or above `TMPDIR`, EVERY scenario refuses to start. The remedy is to remove the marker, or to point `TMPDIR` at a directory with no marker above it. |
| **BUG-111** | **`tests/harness/process.ts` reports a process it killed ITSELF as a leftover, so a push goes red on load rather than on a defect.** Observed 2026-09-14: the push gate failed `harness` › *reaps a background process the scenario forgot* at **1019 ms** with *"Scenario left 1 process(es) running"*; the same case passed alone at 1008 ms, 41/41, with nothing changed in the tree. Vitali saw the same case go red once on 2026-09-11 and read it as an unrelated timing flake. **Mechanism, read from the code rather than inferred:** on timeout, `run()` calls `killGroup(child)` (`process.ts:177-180`), which sends SIGKILL to the group and returns immediately; the run's promise settles on the direct child's `close`, so the call throws *Timed out* while a grandchild in that group (`sleep 30`) may still be dying. Teardown then runs `disposeAll()`, whose FIRST act is `if (!groupAlive(pgid)) continue` (`:251`) — and a killed-but-unreaped member is still signalable, so it lands in `survivors`. The grace period that would absorb this (`waitGroupGone(pgid, TERM_GRACE_MS)`, `:267`) runs only AFTER the group is already recorded as a survivor. On an idle machine reaping wins the race; under gate load it does not. **Not a test being flaky — the harness asserting on a proxy (signalable now) for the property (still running independently of us).** Fix direction: after the timeout path's SIGKILL, await the group being gone within `KILL_GRACE_MS` before the run settles, so teardown only ever sees groups nobody has already killed. Reproducer: hold the grandchild's reap (e.g. a subreaper that delays `wait`) and assert teardown does not report it. | S2 | **FIXED — landed `e8a99b7`, pushed 2026-09-14** | Run `env -u GIT_EDITOR -u GIT_PAGER -u AGENT_PERSONA TMPDIR=/home/luiz/.cache/bp-harness-tmp tests/node_modules/.bin/vitest run --root "$PWD/tests" harness` — the slow-reaper case must pass, and a push whose gate is under load must no longer go red on `harness` › reaps a background process the scenario forgot. | Found 2026-09-14 by Eto (Orchestrator) on a red push that a re-run of the same commit would clear. **Re-open trigger: the next occurrence, or immediately if anyone retries a red gate on this case without citing this row.** **Promoted 2026-09-15 under TASK-028**, porting PR #68 (linkedin-watcher-agent). Reproducer: `tests/harness` BUG-111 makes the window certain rather than likely. A helper forks `sleep` into the run's group, leaves the group, and reaps its child 500 ms after it dies. The case requires the group to be gone (`ESRCH`) when the timed-out `run()` returns. **Fixed under TASK-028:** after the timeout's SIGKILL, `run()` now awaits `waitGroupGone(pid, KILL_GRACE_MS)` before throwing, which is the fix direction this row stated. |


The 2026-07-29 QA pass dispositioned the earlier bugs: BUG-001, BUG-002 and
BUG-003 are all ACCEPTED and live in [`../done/BUGS.md`](../done/BUGS.md) with
their review trails. The full disposition, including the one rejection (A-22, reopened as
**BUG-004** and since accepted), is in
[`../done/ACCEPTANCE-JESKO-2026-07-29.md`](../done/ACCEPTANCE-JESKO-2026-07-29.md).
