# Bugs — pushed to main, awaiting founder acceptance

Fixed and pushed; awaiting the founder's explicit acceptance signal before they
move to `docs/done/BUGS.md`. Claude does NOT auto-promote to `done/`. If a
regression is found, the row moves back to `docs/doing/BUGS.md`.

See [README.md](README.md) for the lifecycle.

**"What to test" is a column here, not a separate index.** There used to be an
`INDEX.md` holding the same membership plus per-item test instructions. It
drifted — 5 rows listed against 14 real ones, so nine fixes were invisible to
the only person who can accept them — and the first repair was a test to hold
the two files in step. That is the wrong repair: two records of one fact drift
by construction, and a guard only tells you afterwards. One record cannot
disagree with itself.

**Put the acceptance command in the CHAT, not only in this column.** BUG-022
shipped with `scripts/accept-bug-022.sh` and a pointer in its row, and the
founder still had no idea how to accept it — because this column lives in a file
he would have to open first. Klaus and Alexis both said acceptance instructions
belong where the decision happens.

| # | Bug | Severity | Status | What to test | Detail |
|---|---|---|---|---|---|
| **BUG-122** | **A full `blueprint pull` that refused a file still advances `bootstrap_sha`, so a project with a refused file reads as synced.** From storm2flow, `FR-storm2flow-refused-pull-sha`. `scripts/blueprint` sets `refused_guard=1` when a file is refused (`:1611`, `:1634`, `:1712`), but that only changes the exit status to 4 (`:1765`). The update at `:1727-1748` advances `bootstrap_sha` whenever at least one file was pulled and no paths were named, yet its own comment defines the value as "synced UP TO this commit". A partial pull already leaves it unchanged. | S2 | **FIXED — landed `f4b1c25`, reproducer `da4f7b2`, pushed 2026-09-15** | In a derived project, run `blueprint pull` and skip one file at the prompt (or let a guard refuse one, or stop the run with `q`). `.blueprint-source`'s `bootstrap_sha` must be UNCHANGED, and the output must name the files that held it back: `bootstrap_sha left unchanged — these files were not synced: …`. A clean full pull that lands everything must still advance it. **Cross-provider review:** Alexey (Codex) — push as is; he confirmed all four unsynced paths hold the SHA and that a clean pull is not suppressed. Known limit he noted: `q` records only the file it stopped at, and a no-op full pull after completing everything piecemeal still leaves the SHA stale — neither is new. | Filed 2026-09-15 by storm2flow; **promoted the same day on founder decision** ("Start all now"). **Fix shape:** leave `bootstrap_sha` unchanged when any file was refused, and name the files that held it back. Reproducer first: a full pull with one refused file must leave `bootstrap_sha` unchanged. |
| **BUG-125** | **doc-links accepts a link that leaves the repository whenever its target happens to exist on the local disk.** Reported 2026-09-15 by storm2flow's orchestrator (Sylvia), under her founder's rule that "checks read only from the repository". `tests/doc-links/doc-links.ts:111` checks a target with `stat(resolve(dirname(file), target))`. A link like `../../../../blueprint/docs/DoD.md` in storm2flow's docs therefore passes on any machine with a sibling blueprint checkout. The same link is dead in a fresh clone, in CI, and for every other reader. storm2flow's founder caught one such link the same day. | S3 | **FIXED — landed `f4fe83d`, revised in `3a07a46`, containment hardened in `02e1f31`, pushed 2026-09-15** | In a project's docs, link a file OUTSIDE the repository (`../../outside.md`) while that file exists on disk — the doc-links check must report it, because a clone would not have it. Same for a symlink pointing out, a symlinked web root, and a symlink escaping the served directory into another repo folder. A symlink that stays inside the repository must still resolve, and so must a file the project gitignores but has locally (`CLAUDE.md`) — accepting those is deliberate: requiring tracked files broke every fresh bootstrap, since managed docs link the methodology files. **Cross-provider review:** Alexey — push as is; the resolve-then-stat window is gone because the patch removes `stat`. **Known limit:** a doc linking a gitignored file passes here and is dead in a clone; TASK-046 is the fix for that. | Filed 2026-09-15 by Eto (Orchestrator) from storm2flow's report. It sits in the suite Christian is changing for TASK-045, so he takes it next. **Fix shape:** normalise each target against the repo root, fail any that climbs out, and check whether resolving against tracked files (`git ls-files`) is needed so an untracked local file cannot satisfy a link. Reproducer first: a doc linking `../../outside.md` must be reported even when that file exists outside the repo. |
| **BUG-126** | **[SEC] The pre-push semgrep stage passes a partial scan as clean: it never reads semgrep's `.errors`, so code semgrep could not parse is approved unanalysed.** Reported 2026-09-16 by storm2flow's orchestrator (Sylvia), from Jesko's (Codex) review of storm2flow's CodeBuild copy of `_st_semgrep`. `.githooks/pre-push:376-404` classifies a run as clean when semgrep exits 0 and `.results` is an array with zero entries. `.errors` is never inspected. Reproduced with the pinned semgrep 1.171.0: an offline JS rule (`pattern: eval(...)`, WARNING) run over a file containing `function broken( {` followed by `eval("x")` exits **0** with `results: []` and an `errors` entry of type `PartialParsing`. The gate reads that as clean and never retries. **CI likely shares the hole:** `.github/workflows/security.yml:60-65` runs `semgrep scan --error`, which fails only on findings. | S2 | **FIXED — landed `ab38a96`/`6a08518`, decoding `d97d370`/`4177121`, extraction `b683eb9`, budget `4f23962`, pushed 2026-09-15** | Put a file semgrep cannot fully parse into a project — a `.js` file with a syntax error plus `eval("x")` is the reproducer — and push. The gate must BLOCK, saying the SAST scan is INCOMPLETE and naming the error type and path, where before it printed 0 findings and passed. Then check the accepted class is still only shell: the gate reports N shell parse errors accepted (20 here) and passes. CI applies the same rules through the shipped `scripts/semgrep-verdict.sh`, which the hook sources and the workflow executes, so the two cannot drift. **Cross-provider review:** Codex — push as is; `#ci` parses the real workflow YAML and runs its step, and the hook fails closed when the script is missing. **Known limit, parked as BUG-128:** a file semgrep never opened lands in `paths.skipped` with no error entry, and nothing reads that key yet. | Filed 2026-09-16 by Eto (Orchestrator); started at once under CLAUDE.md §"Security is a main concern" capability 4 (the agent fixes security findings autonomously). **Fix shape:** a non-empty `.errors` makes the run incomplete. Keep the `--jobs 1` retry, and fail closed if errors remain. Any tolerated diagnostic becomes an explicit reviewed policy (by error type and path), never silent acceptance. Apply the same classification in CI. Reproducer first: the malformed-file case above must not classify as clean. |
| **BUG-124** | **The activity feed labels a Claude subagent's start/finish markers with its agent type (`general-purpose`), and labels a helper that a persona spawns by the helper's type (`claude-code-guide`) instead of by the persona that started it. So a persona's work can be invisible to the founder watching the feed.** Observed 2026-09-15. The founder could not see Christian's activity. The feed did hold 88 `[Christian - Claude Code]` lines (23:03–23:22), but its `→ dispatched`/`← finished` markers read `[general-purpose - Claude Code]`, and the helper Christian started to research `@`-imports wrote 43 lines as `[claude-code-guide - Claude Code]`. `scripts/log-activity.sh:105-151` resolves the persona with `bp_roster_name_in_text` over `$summary`. On the bookend events that text evidently carries no roster name, so the label falls back to `.subagent_type`. For a nested helper, nothing ties the helper to its parent persona. Codex lines are unaffected, because the dispatcher labels them. | S3 | **FIXED — landed `51d4978`/`fc64966`, rework `f479e85`/`6df5c56`, race `5abbd03`/`7f44bf2`, macOS `6285fbe`/`9af5e74`, pushed 2026-09-15** | Watch the team feed (`tail -F logs/agent-activity.log`) while an agent is dispatched: the `→ dispatched` and `← finished` markers must carry the PERSONA name, and a helper an agent starts must read `[<persona> › <type> - Claude Code]`. Before this they said `general-purpose` and `claude-code-guide`, which is why Christian's work looked invisible. **On a Mac:** `bash scripts/install-toolchain.sh` now installs `flock` (util-linux); without it the feed says once that deferral is unavailable and labels by type. **Cross-provider review, twice:** the burst cap was a check/create race (twelve concurrent hooks took twelve slots), then its reclamation was non-atomic (two owners, one slot). Slots are now reserved by `mkdir` under one lock spanning reclaim, reserve and spawn; the owner's pid is recorded rather than its age. | Filed 2026-09-15 by Eto (Orchestrator) from the founder's report; **promoted the same day on founder decision** ("File and fix now"). **Fix shape:** find which hook payload field carries the dispatch description, or the transcript meta the streamed lines already use, so the bookends resolve the same persona as the streamed lines. Label a nested helper `[<parent persona> › <type> - Claude Code]`, or as the parent. Reproducer first, with a recorded hook payload. |
| **BUG-127** | **[SEC] `blueprint a2bp` files a request with no secret scan when `gitleaks` is absent, and misreports an unsupported scanner command as a found secret.** From Alexey's (Codex) re-check of TASK-037, `.scratch/ALEXEY-task037-recheck.md` findings 1 and 6. TASK-037 is accepted and pushed, so its defects are carried here rather than reopening it. **(1)** `scripts/lib/request-inputs.sh:126` returns success when gitleaks is missing, matching the pre-push gate's skip. With gitleaks off PATH, both private-key probes (a key in `notes/deploy.txt`, and one in managed `docs/DoD.md`) reached the fetch; only a transport-denying shim stopped them. A CI scan of the PR runs after upload: it can refuse the merge, never undo the disclosure. Filename and ignore guards still apply, so this is the content check only. **(2)** `request-inputs.sh:137` treats every scanner exit 1 as a finding. A shim printing `unknown command dir for gitleaks` and exiting 1 produced "gitleaks found a secret" with rotation advice. It still exits 4 before transport, so it is false attribution, not a bypass. | S2 | **FIXED — landed `8747b88` and `0217cbc`, pushed 2026-09-15** | In a derived project, take `gitleaks` off PATH and run `blueprint a2bp <file>` — it must REFUSE (exit 4, no branch pushed) and name `bash scripts/install-toolchain.sh`, rather than filing bytes nothing scanned. The pre-push gate still SKIPS when gitleaks is missing, and the difference is the point: the gate's skip keeps unscanned bytes on your machine, while a2bp publishes them to the blueprint's remote, where a later CI scan can refuse the merge but cannot un-disclose them. Then check the second half: a scanner that cannot run (an older gitleaks without `dir`) must be reported as an incomplete scan, not as 'gitleaks found a secret' with rotation advice. | Filed 2026-09-16 by Eto (Orchestrator). **Fix shape:** require a working local scanner before a request is filed (a2bp is a push to a remote, unlike the local gate), and separate "scanner cannot run" from "secret found" — check command support, or classify a non-findings failure as an incomplete scan. Reproducer first, for both: gitleaks off PATH, and an unsupported-command shim. |
| **BUG-129** | **The activity feed's idempotency guard does not hold: four `agent-activity.sh --supervise` daemons are running at once, and their log rotation turns a push-gate suite red.** Observed 2026-09-16 by Eto: `pgrep -af agent-activity.sh` returns PIDs 1154186 (started 11:37), 3275474, 3373642 and 3423373. CLAUDE.md §"On wake" states the opposite — *"it is idempotent (a `flock` makes a second call a no-op)"* — and instructs every wake to call it, so the guard is the only thing standing between that instruction and unbounded daemons. **This repo has the precedent:** BUG-001 was a broken idempotency guard whose every-wake spawning left this machine at load 175 for 2.7 days. **Already costing a gate:** Philipp (Infrastructure-1) traced `subagent-feed` #9 to the team's own feed rotating the log mid-scenario — the real-state canary hard-fails on feed truncation while only NOTING concurrent baton writes, so the suite goes red for something the pushed change never touched. His child only ever wrote to the fixture's `AGENT_FEED_LOG`. | S2 | **FIXED — landed `a2d7ca1`/`65b0492`, rotation `8e9e21e`/`713d291`, wording `2010ffd`, pushed 2026-09-15** | Restart the feed (`bash scripts/agent-activity.sh --stop`, then `--daemon`) while a test suite is running: the suite must NOT go red. The feed is append-only now — a restart writes `feed restarted` instead of wiping the log, and rotation renames history to `logs/agent-activity.log.1` with a marker instead of overwriting in place, so a concurrent line can no longer be lost. The real-state canary reports a rotation as a NOTE and still hard-fails on genuinely lost history. **Also corrected:** four `agent-activity.sh` processes on this machine are four REPOSITORIES, one supervisor each — the lock is per repository by design, and CLAUDE.md now says so. | Filed 2026-09-16 by Eto (Orchestrator). **Two defects, and they should not be conflated:** (1) the `flock` guard in `scripts/agent-activity.sh` does not prevent a second supervisor — find whether the lock is taken on the wrong path, released too early, or skipped on the `--supervise` path, and reproduce with two concurrent starts before fixing; (2) the canary's asymmetry — feed truncation hard-fails while baton writes only note — which makes every suite run under a live feed a coin toss. Fix (1) first; (2) may be a note-and-continue change, but decide it with the evidence. **Do not kill the running daemons before capturing what started them** — the founder's session state is the only record of how four came to exist. **EVIDENCE CAPTURED 2026-09-16 12:44 by Vitali, daemons left running:** all four are PPID 1 (`start_daemon` uses `setsid`, so the tree cannot say which session started them — start times are the only record), and each holds a DIFFERENT lock: 1154186 cwd `blueprint` → `blueprint/logs/.agent-activity.lock` (Sep 15 11:37:45; its fd 255 reads `scripts/agent-activity.sh (deleted)`, so it is running a since-replaced inode); 3275474 cwd `linkedin-watcher-agent` → that repo's lock (14:10:11); 3373642 cwd `storm2flow` → that repo's lock (14:24:35); 3423373 cwd `struct2flow-www` → that repo's lock (14:27:18). Two `tail -F logs/agent-activity.log` survive with real parents — 1851515←1333482 and 3388557←3385380, both VS Code terminal bash under 2464117 — the only live link back to sessions. Real feed at capture: size 397769, inode 81657865, mtime 12:47:57. **DEFECT (1) DOES NOT REPRODUCE, and no fix was made.** `lock_file="$BP_STATE_ROOT/logs/.agent-activity.lock"` is per-repository by design (BUG-077), so four projects legitimately means four supervisors, exactly one each; `pgrep -af agent-activity.sh` spans repositories, and four instances of one repo was the reading, not the fact. `tests/agent-activity-bound` passes 36/36 today including **#1 "50 concurrent starts → exactly 1 supervisor"** and #6, so the guard is both correct and already pinned. CLAUDE.md's idempotency claim is true *per repository* and would be worth saying so. **DEFECT (2) IS REAL and is the only defect here.** The real feed has exactly two writers: `emit` appends (`agent-activity.sh:336`) and `supervise_body` truncates once at startup (`:557`). That is not periodic rotation — it is ONE wipe per supervisor start, in place, preserving the inode, which is why `tail -F` survives and why Philipp's mechanism description is right. So the flake strikes when a wake or restart coincides with a suite run, and CLAUDE.md instructs every wake to start the feed. **Philipp (Infrastructure-1):** *"`tests/subagent-feed` #9 fails intermittently with 'The scenario mutated real state outside its fixture: activity feed was rewritten or truncated (`logs/agent-activity.log`)', although no scenario writes there… the canary already handles the same class correctly for the baton, where a concurrent `signal-set.sh` write emits a CANARY-NOTE and the run continues; feed rotation should get the same asymmetry… Until then this suite goes red in the push gate for reasons unrelated to the change being pushed, which is the failure mode the canary itself exists to prevent people from shrugging at."* **PROPOSED, not decided (Vitali):** the canary's append-only model is CORRECT and the feed breaks its own contract once per restart, so the preferred fix is to stop truncating — emit a "feed restarted" banner instead of `: >"$out"` — which makes the claim true and keeps feed truncation a hard failure that still means something. If unbounded growth must be capped, rotate by rename at a size cap, but note that a rename also trips the prefix check, so rotation implies the canary note anyway. **Fallback if the wipe stays:** mirror BUG-068's journal witness — downgrade to `CANARY-NOTE` only when the post-change content BEGINS with the supervisor's own start banner (`[agent-activity] feed started →`) and the scenario's escape token is absent, keeping the hard failure otherwise. **Can the two be told apart? Yes, by that banner witness** — with the same residual BUG-068 already documents (a fixture could forge it), which is precisely why the preferred option is better: it removes the false red at the source instead of teaching the guard to excuse a class of writes. **ORDER — the brief's worry is moot:** the guard is sound, so the canary is not a symptom hiding behind a cause, and there is no green run that could justify keeping the asymmetry. **APPROVED AND FIXED 2026-09-16 (coordinator):** the wipe is gone — `supervise_body` now emits `feed restarted` over a non-empty log and `feed started` over an empty one, so the feed keeps the append-only contract the canary already assumes, the canary needs no new tolerance, and feed truncation stays a hard failure that still means something. Regression: `tests/agent-activity-bound` #20, which stages the real script, a real second supervisor start and the real canary over a fixture feed. **THE REPRODUCER'S OWN VACUITY TRAP, recorded because it was measured rather than foreseen:** the first version PASSED against the unfixed script — the fixture is deterministic and fast, so both supervisors emitted the same banner and baton line inside the same second and the rewritten bytes were IDENTICAL, leaving the prefix check nothing to see. Live, the starts are minutes apart, which is why the same wipe reds `subagent-feed` #9 on the founder's host and redded nothing in a fixture. #20 now appends a history marker a restart cannot reproduce and asserts it is present before capturing, so the observation cannot go vacuous again. **THAT CLAIM WAS FALSE, AND CODEX MEASURED IT (S2 rework, 2026-09-16).** This row said "the feed now grows across restarts. Size-capped rotation is NOT built". Size-capped rotation HAD been built all along: `scripts/lib/feed.sh` rotated at 4,000 lines by `tail` to a temp file followed by `cat >"$_fa_log"`, and every hook and pipeline producer calls that appender on the same feed. So only the STARTUP half of BUG-129 was fixed, and two consequences survived: the canary could still report an innocent live feed as "rewritten or truncated" (the flake this row exists to kill), and **a concurrent append during rotation was LOST** — Codex appended a line after `tail` took its snapshot and before the rewrite, and it vanished. Nothing coordinated the rewrite with any other writer. The live feed in this checkout was 3,699 lines against the 4,000 cap, so this was imminent rather than theoretical. **THE FIX: ROTATE BY RENAME.** `mv` to `<feed>.1`, then one marker line into the new file. Chosen over the two alternatives on evidence: a LOCK was rejected because feed.sh is POSIX sh sourced by `#!/bin/sh` hooks and `flock` is absent on macOS — it would put a tool the platform may not have on the one path that must never fail a push — and rename needs no coordination anyway, because a writer holding the old file appends into the archive while a later one appends into the new file, so nothing is dropped either way. Rename also keeps the feed APPEND-ONLY in the sense the canary needs: history MOVES rather than being deleted, so archive+live still begins with whatever was captured. **The canary is now archive-aware** (`tests/harness/canary.ts`): a rotation is a `CANARY-NOTE`, never silent, while a rotation that LOST or rewrote history still fails, and the escape token is searched in the archive too so rotating cannot launder a leaked line out of view. **THE INODE PREMISE THAT FORBADE `mv` WAS ALSO FALSE**, and it is what made the in-place rewrite look mandatory: `emit` is `printf '%s\n' "$1" >>"$out"`, which REOPENS per line, and the only `exec N>` in `agent-activity.sh` targets its lock file. Nothing tracks the feed by descriptor. Consequence paid in the same commit: every doc that told a reader to follow the feed now says `tail -F` (follow by name), and `AGENT_FEED_KEEP_LINES` is gone with the trim that used it. Regressions: `tests/pipeline` #18 (rotation moves history, does not delete it) and #18b (an append racing the rotation is not lost, proved with a `cat` shim that holds the window open), plus a `tests/harness` canary case for the note. **FOLLOW-UP HELD BY THE COORDINATOR:** PID 1154186 runs a deleted inode of `scripts/agent-activity.sh` (pre-BUG-124 code, including the old labelling), so this repo's feed needs restarting after the push for the founder to see the fixed version — live state, not code. |


The 2026-07-29 QA pass dispositioned the earlier bugs: BUG-001, BUG-002 and
BUG-003 are all ACCEPTED and live in [`../done/BUGS.md`](../done/BUGS.md) with
their review trails. The full disposition, including the one rejection (A-22, reopened as
**BUG-004** and since accepted), is in
[`../done/ACCEPTANCE-JESKO-2026-07-29.md`](../done/ACCEPTANCE-JESKO-2026-07-29.md).
