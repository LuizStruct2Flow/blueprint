# Bugs — pushed to main, awaiting founder acceptance

Fixed and pushed; awaiting the founder's explicit acceptance signal before they
move to `docs/done/BUGS.md`. Claude does NOT auto-promote to `done/`. If a
regression is found, the row moves back to `docs/doing/BUGS.md`.

See [README.md](README.md) for the lifecycle.

**"What to test" is a column here, not a separate index.** There used to be an
`INDEX.md` holding the same membership plus per-item test instructions. It
drifted — 5 rows listed against 14 real ones, so nine fixes were invisible to
the only person who can accept them — and the first repair was a test to hold
the two files in step. That is the wrong repair: two records of one fact drift
by construction, and a guard only tells you afterwards. One record cannot
disagree with itself.

**Put the acceptance command in the CHAT, not only in this column.** BUG-022
shipped with `scripts/accept-bug-022.sh` and a pointer in its row, and the
founder still had no idea how to accept it — because this column lives in a file
he would have to open first. Klaus and Alexis both said acceptance instructions
belong where the decision happens.

| # | Bug | Severity | Status | What to test | Detail |
|---|---|---|---|---|---|
| **BUG-153** | **Reproducer: required.** **`tests/subagent-feed` #16 goes red on CI about one run in N: two callers reclaim one stale feed slot.** | S2 | FIXED `89e5a1d`, hardened `9a9fd79` and `487b9d0`, CI green and released at `99ae853` | `npm --prefix tests test -- subagent-feed`: the three BUG-153 cases (an unreadable pid file, unparseable pid content, a failed pid write) each leave a live slot alone. Under load nothing visible changes: `tests/subagent-feed` #16 should simply stop going red on CI. | **Observed 2026-09-24 on `1d41617`**, a documentation-only commit — so nothing in the push could have caused it. The assertion is the one Codex's 2026-09-16 review added: *"two callers own one slot — the stale sweep deleted the other's fresh reservation and took the name for itself: expected 2 to be less than or equal to 1"*, naming `scripts/log-activity.sh`. **Diagnosed: candidate (a), a real defect in the cap, not a flaky test (b).** Thomas (Kimi), then Philipp (Claude), reassigned twice on quota exhaustion. **Mechanism:** the slot owner's pid was read with an external `cat` — `_ds_pid="$(cat "$_ds_slot/pid" 2>/dev/null)" || _ds_pid=""` — and ANY read failure (not only "no such file") was folded into "no owner". A transient `cat` failure (fork/exec EAGAIN on a loaded runner) made a caller delete another caller's LIVE reservation and take the slot: two children, one slot, exactly the CI signature — CI's 4-core contention makes the transient failure likelier than on a 32-core dev host, the BUG-133 shape. **Ported and fixed, 2026-09-24 (Philipp):** `scripts/log-activity.sh` is now the fixed two-line exec shim (TASK-067) and its whole implementation lives in `scripts/log-activity.mts`; the pid is now read IN-PROCESS (`fs.readFileSync`, no `cat` fork/exec at all) and fails CLOSED — any read failure on an EXISTING slot (anything but `ENOENT`) reads as "owner unknown, treat as live" rather than "no owner". **Round 2, 2026-09-24 (Philipp) — review found the regression test vacuous and a second fold-to-unowned path:** (1) the original test shimmed `cat` on PATH, but the ported hook never shells out for the read, so the shim never fired and the case passed regardless of the fix — reworked to swap the live slot's `pid` FILE for a same-named DIRECTORY, so the in-process `readFileSync` throws `EISDIR` (root-safe, unlike a permission bit) instead of `ENOENT`; proven RED against a reverted fail-closed branch (the pre-fix `spawnSync('cat', …)` shape from `7a8ee6c`). (2) `readSlotPid` only failed closed on a THROWN read; a pid file that read successfully as `''` or non-numeric content still returned `unknown: false`, so the caller's liveness check was skipped and a live-but-mid-write slot was reclaimed — fixed by treating present-but-unparseable content the same as a read error, ENOENT alone means "no owner". **Regression tests:** `tests/subagent-feed/subagent-feed.release.spec.ts` "BUG-153 case 1: an unreadable pid file on a LIVE slot must not read as unowned" (the reworked directory-injection case) and "BUG-153 case 2: present-but-unparseable pid content must not read as unowned" (new); both green against the fix, case 2 red on round-1-only code. **Round 3, 2026-09-24 (Philipp) — a Codex four-eyes review found two more real problems in `scripts/log-activity.mts`, both on the WRITE side and on exit hygiene, neither covered by rounds 1–2:** (1) `~396-404`: a failed pid-file write (`writeFileSync`, e.g. ENOSPC or an I/O error after the child spawns) was swallowed, leaving the slot claimed on disk with a live, UNRECORDED child — the next caller's read is a genuine `ENOENT` (correctly "no owner" per rounds 1–2), so it reclaims a slot whose owner is still running: BUG-153's exact failure through the write side instead of the read side. Fixed by killing the child and releasing the slot (`rmSync`) on a failed write, instead of limping on with `claimed = true`. (2) `~417-432` / `~468`: `deferNotice()`'s stderr writes (`process.stderr.write`) were followed by `process.exit(0)` in the same turn — Node can drop a pending async write on exit if stderr isn't a synchronous pipe on the host (cases #15/#18 pass here, but were reported red in such a sandbox). Fixed by writing every hook notice with `fs.writeSync(2, …)` (a new `stderrNotice` helper), the same synchronous guarantee the old shell hook's `printf >&2` had. **Regression test:** case 3 ("a failed pid-file write kills its child, so no live owner is orphaned") — caller A dispatches under `umask 0222` so its freshly-`mkdirSync`'d slot lands at mode 0555 (searchable/readable, never writable), making `writeFileSync(slot/pid, …)` fail deterministically with no shim and no race; asserts the "deferral failed" notice landed, no live child survives, the bookend still emits synchronously, and a second ordinary caller B is not blocked (gets a fresh slot, ends up the only live child, both bookends land). Red against a revert of fix (1) alone (`claimed=true` on write failure → no notice, assertion fails). Case 2 was not made deterministic for fix (2) on this host (stderr is a synchronous pipe here, per BUG-153's own note above) — the code change plus #15/#18 are what cover it. **Re-open if** `main` goes red on a suite that passes on a re-run with nothing changed. |
| **BUG-150** | **Reproducer: required.** **The stranded-mic recovery matched on Holder AND Task, but an agent rewrites Task when it claims ACTIVE, so recovery never fired for the common failure.** | S2 | FIXED `185d74b`, hardened `185ef3a`, CI green and released at `fa3578d` | `npm --prefix tests test -- mic-recovery`: the CHANGED-Task case recovers, a new dispatch to the same Holder is left alone, and a truncated or deleted history log leaves the mic alone with a logged reason. Live: the next time a Codex or Kimi dispatch dies after claiming ACTIVE, the watcher hands the mic back by itself (today it had to be taken back by hand twice). | `scripts/signal-watch.mts` no longer reads Task. At dispatch it records the line count of `logs/state/signal-history.log`; the mic is stranded unless a NEW `OVER_TO_*` line for the same Holder appears after that marker. Round 2 (a Claude review finding) makes it fail SAFE when the log is missing, unreadable or shorter than the marker. **Known residual, deliberately not engineered around:** if `scripts/signal-set.sh` publishes a new dispatch to the same Holder and then fails to journal it (its EXIT 8), the watcher cannot see it. Reviewed by Claude only, under the founder's four-eyes waiver of 2026-09-24. **Re-open if** a stranded mic is left unrecovered because the holder edited the Task. |
| **BUG-147** | **Reproducer: required.** **The DoD gate counted only `*.spec.ts`/`*.spec.tsx` as a bug's regression test, on every searched root, so a JavaScript project could never satisfy it.** | S2 | FIXED `a1b8e37` (port `e7ffb64`, hardening `67bb00f` and `5cf6343`), CI green at `703493b`, released | `npm --prefix tests test -- dod-gate shell-inventory`. #11c: a project bug tested only in a `*.spec.js` under a declared root passes the gate. #11d: a `.test.js` or README prose still does not count. #16b/#16c in `tests/shell-inventory`: an adapter carrying an extra or overriding forwarding pair is refused. In storm2flow, the real case: its BUG-216 `configLoader.bug216.spec.js` is accepted. | `scripts/lib/dod-gate.sh` was ported whole to `scripts/lib/dod-gate.mts` behind a generated sourced adapter (TASK-067, [PLAN-BUG-147-dod-gate-port.md](PLAN-BUG-147-dod-gate-port.md)). The port was proven by a byte-for-byte differential against the old shell (25 scenario classes; the one divergence, a symlinked declared root, was fixed to match) and mutants D1 to D8, each caught. Then the fix: a declared project root also counts `.spec.js`/`.jsx`/`.mjs`/`.cjs`; the blueprint's own `tests/` stays TypeScript-only. Codex's four-eyes review found the adapter validator accepted an overriding forwarding pair; the pairs must now equal the CLI's canonical table (`5cf6343`). **Still owed:** tell storm2flow (Sylvia) to remove her workaround, the forced `.spec.ts` rename and the extra include in `frontend/vite.config.mjs`, both commented BUG-147. **Re-open if** a declared project root's JavaScript spec whose title names a bug is refused. |
| **BUG-149** | **The founder cannot run the test suite: the obvious commands fail with 969 errors in an ordinary terminal.** | S2 | FIXED `97c7cb4`, CI green | In a VS Code terminal (which exports `GIT_ASKPASS`), run `npm --prefix tests test`. Expect the whole suite green. A bare `vitest run` still fails closed, by design. The regression test is `tests/ts-bridge` "BUG-149 — the documented entry point scrubs a normal terminal for itself". | `tests/package.json`'s `test` script now runs vitest through `run-ts-suites.sh`'s existing `ts_scrubbed`, and `tests/harness/env.ts` declares `GIT_ASKPASS` as a known override. The harness guard still protects specs that spawn directly. **Re-open if** the documented command fails in a normal terminal. |
| **BUG-148** | **`tests/mic-recovery` reads the operator's gitignored `AGENT_ROSTER.md`, so the suite cannot run in CI or in a fresh clone.** | S1 | FIXED `f15e18a`, CI green | CI has had no roster since that commit and has stayed green. To see it locally, move your `AGENT_ROSTER.md` out of the repo, run `npm --prefix tests test -- mic-recovery`, expect green, then move the roster back. | Each scenario now builds its own repo-shaped tree (a root marker, a fixture roster at that root, the baton under its own `logs/state`) and depends on no file outside the fixture. **No regression test:** the suite itself is the reproducer, and CI running it with no roster is the proof. **Re-open if** any suite depends on a gitignored, per-machine file. |

The 2026-07-29 QA pass dispositioned the earlier bugs: BUG-001, BUG-002 and
BUG-003 are all ACCEPTED and live in [`../done/BUGS.md`](../done/BUGS.md) with
their review trails. The full disposition, including the one rejection (A-22, reopened as
**BUG-004** and since accepted), is in
[`../done/ACCEPTANCE-JESKO-2026-07-29.md`](../done/ACCEPTANCE-JESKO-2026-07-29.md).
| **BUG-154** | **Reproducer: required.** **Test fixtures leave real signal-watch processes running after the suite ends.** | S2 | FIXED `6b09022` (reproducer `076fb23`), CI green and released at `bfe4984` | Run the whole suite (`npm --prefix tests test`), then `pgrep -af signal-watch.mts`: no watcher under `bp-harness-tmp`. `npm --prefix tests test -- harness -t BUG-154` runs the reproducer: it abandons a scenario body the way the runner abandons a timed-out test, and checks the process group is gone. | **Fixed 2026-09-25 by Vitali (Claude), reviewed CLEAN by Jesko (Codex):** vitest 4.1.11's `withTimeout` rejects without cancelling the body, and `runTest` awaits `onTestFinished` after pass, failure or timeout, so the scenario's reap now hangs off `onTestFinished` and no longer depends on the body's promise settling. **Observed 2026-09-24 19:12Z** while restarting the watchers: `pgrep -af signal-watch.mts` listed two fixture watchers still polling, `/home/luiz/.cache/bp-harness-tmp/mic-recovery-5-ZjU7V8/.../signal-watch.mts --state OVER_TO_KIMI --poll 0.2` (started 15:59, 5 h old) and `/home/luiz/.cache/bp-harness-tmp/baton-6c-Z5F8am/b6c/scripts/signal-watch.mts --poll 0.2` (started 2026-09-23 11:54, 33 h old). They were killed by hand. Their workspaces are gone, so they poll deleted files forever, and every leaked one is a process that outlives the run that made it: the BUG-001 class (a helper waiting forever on something that will never arrive). The first comes from `tests/mic-recovery` case mic-recovery-5 (BUG-150's own test), the second from `tests/baton-durability` #6c. **Cause, to be confirmed:** the scenario starts the watcher as a long-lived child and its teardown does not reap it on every exit path (a test that times out or fails before its own stop). The harness's `ProcessRegistry` already tracks scenario process groups (BUG-146 uses it for dumps), so the fix likely belongs there: kill every tracked group at scenario teardown, whatever the outcome. **Reproducer:** a fixture that starts a watcher and fails mid-test must leave no process behind. **Root cause confirmed:** `scenario()`'s teardown (`tests/harness/index.ts`) calls `registry.disposeAll()` only after `await body(s)` settles. `@vitest/runner`'s `withTimeout` does not cancel a test whose body outlives the test's own timeout — it rejects the outer promise and moves the runner straight to `afterEach`/`onFinished` while the body keeps executing unawaited, so the line that calls `disposeAll()` never runs for that exit path. A process started before that point stays registered but is never reaped. **No regression test — fixed:** `tests/harness/harness.spec.ts` describe block "harness — teardown survives an abandoned body (BUG-154)", two cases ("a scenario whose body outlives an abandoned await still starts its process (setup)" and "the process the previous case abandoned is not still running") — red on today's code (confirmed: the second case failed with the watcher's process group still signalable), reproducing the leak without a real 320s wait or a nested vitest process. **Re-open if** `pgrep -af signal-watch.mts` shows a watcher under `bp-harness-tmp` after the suite has finished. |
